# 协作顺滑度报告 · 第一百八十六份

窗口 `2026-09-24T04:35Z → 06:35Z`。上一份：`docs/collab/2026-09-24-0435.md`（`3cbc4b3`）。

## 一句话

**上一期我推断「找短间隔只会抓到恢复」，本期加了探针去验：它报了四次，四次全是我自己正常的重挂；而上一期那次真故障的值（`33s`）正落在这四次的范围里（`31`–`33s`）——两者在同一个位置上，分不开。**

## 指标

| | 本期 | 上期 |
|---|---|---|
| **节奏** | **3 条**（pm 2、服务 1） | 3 条 |
| **指令与确认** | 发 1（给 `pd`）、ack 0（收件人缺席） | 同 |
| **任务与验收** | **建 0、交 0、判 0——已连续 `121h01m`** | 已连续 `119h01m` |
| **摩擦** | 0 | 0 |
| **人的负担** | 卡 6 张，一张没点 | 同 |
| **在场** | **1 人**（只剩 pm） | 1 人 |
| **本期发现关于谁** | **向内** | 向内 |

## 与上一份的变化

```
任务停了多久      119h01m  →  121h01m      锚点 2026-09-19T05:34:01Z
除我之外的写入者   0  →  0                  自 dev 最后一次写起 73.7h、117 条：pm 79、服务 38、其他 0
看守（本窗）      119 轮/最大 62s  →  120 轮/最大 63s    本期无异常事件
不明缺口的率       2744 轮 1 次  →  2864 轮 1 次
痕迹合计          16205  →  16324            差值 119 = 按取数时刻计数 119 ✓
服务预警          连续相同 57  →  58 条       零漏跳；分段两边仍 0.255 / 0.247
短间隔探针        本期新加，报 4 次           全部是我自己的重挂，见下
```

## 结构性发现

### 那个探针的信噪比不是低，是零

上一期我从一次 `33` 秒的缺口推出：**「找短间隔」抓到的是恢复，不是故障。** **那是推断，本期我加了探针去验**（口径：相邻间隔 `< 50` 秒）。

**本期它报了四次**：

```
04:40:12 -> 04:40:45   33s
05:10:21 -> 05:10:54   33s
05:40:30 -> 05:41:01   31s
06:10:38 -> 06:11:09   31s
```

**逐条对上我自己的动作**：`04:40:42`、`05:10:49`、`05:40:58`、`06:11:06`——**本期四次到期重挂，一次不差。** **四次报警，四次都是「我刚把看守挂回去」。**

**而上一期那次真故障，缺口是 `33` 秒。** **它落在这四次的正中间。**

```
真故障（09-24T03:40 重启）      33s
例行重挂（本期四次）            33s  33s  31s  31s
```

> **第九十五条：当检测器报的事件与要找的事件落在同一个位置上，它的信噪比是零，不是低。** **信噪比低还可以靠提高阈值、多看几次来救**；**落在同一位置的，再怎么调都分不开——因为它们在这个量上本来就一样。** **此时要换的不是参数，是观测量。**

**所以这条路本期彻底封死了**：`>90s` 抓不到（方向不对，第九十四条）；`<50s` 抓到的全是自己（本期实测）。**痕迹这条通道对会话级重启没有可用的那一侧。**

**这也给第八十二条（合成测试）划了一条边**：**我上一期是可以合成一个 `33` 秒缺口来测这个探针的**，**而那样做只会告诉我「探针能抓到 33 秒」——它抓得到，问题从来不在这里。** **真正该问的是「它抓到的里面有几个是真的」，而这个问题只有真实数据回答得了。** **本期四次全假。**

## 与 `product.md` 的对照

| | 本期 |
|---|---|
| **T1 / T3 / T8 / T11** | 无往来、指令 1 ack 0（收件人缺席）、无可评、friction `0` |
| **T5** | 验收资格仍为空；**「无人能验」已持续 `121h01m`** |
| **O1** | 与人之间唯一的通道仍是那一页，**本期无变化** |
| **O2** | 服务预警**连续相同 58 条、零漏跳**；**分段：前 n=37 中位 `0.255`／最大 `2.559`；后 n=20 中位 `0.247`／最大 `0.767`** |
| **这条日志的用处本身** | **仍为 `0`**：73.7 小时、117 条，非 pm 非服务的事件 **0** |
| **这份报告在看谁** | 本期 1 条，**向内** |

## 分配预警

| 模式 | 本期 | 数字 |
|---|---|---|
| **重叠** | 无 | 接缝 0、任务事件 0 |
| **低效** | 无 | **假催办 0（来源已毁，非已修复）**；看守轮数：本窗 `120`，痕迹合计 `16324` |
| **无效上下文移交** | 无 | 73.7 小时内非 pm、非服务事件 0，**没有链可断** |
| **打破独立审核** | **结构性风险，已持续 `121h01m`** | `qa`、`dev` 均 missing；**在场 1 人**；**按第九十一条，也指没有人能验收我的判断** |
| **负载陷阱** | 无 | 队列深度全 0；pm 本期写入 2 条 |

## pm 自查

| | 本期 | 目标 | |
|---|---|---|---|
| **自我更正率** | **0%**（0 / 2） | ≤10% | 分母 2 |
| **判据被执行者回问** | **0** | ≤1 | 分母为 0 |

**本期那个探针加了就撤**——**按第八十六条（补），我这次事先写了停的判据**：**它要回答的是「短间隔里有没有真故障」，四次全假即答完。** **答完了，本期起不再挂它。** **这是我第一次在加一个数之前就把撤它的条件写下来**（前两次是第一百七十五份、第一百八十四份，都是用完才想起）。

**本期到期 4 次（`04:40:42`／`05:10:49`／`05:40:58`／`06:11:06`），4 次都在收到通知的当场重挂。**

## 给 pd 的问题

**Q12–Q18 均仍等**（`pd last_pull 2026-09-12T16:42:03Z`，距今 `277.9h`）。**本期不新增问题。**

那条老问题本期的材料：**这份报告本期加了一个探针、当期验完、当期撤掉**——**而撤它的条件是加它之前就写好的。**

**已发的九件仍等。**

## 口径（本期新增一条）

- **第九十五条：当检测器报的事件与要找的事件落在同一个位置上，它的信噪比是零，不是低。** **信噪比低还能靠调阈值、多看几次来救；落在同一位置的，再怎么调都分不开。** **此时要换的不是参数，是观测量。**

## 顺带检查

- **生产 `3e50e5b`**（`GET /health` 本期实测）、**整合支 `09256fd` 未变**，后者仍不含 `t-280`；**八件已验交付仍坐在 `claude/backend-development-gzqbjf` 上，等 human。**
- **焦点 `2026-09-22T02:41Z` 版未变**：本期是它的第二十六个整窗。
- **服务端无降级**：本窗 120 轮，中位 `61` 秒、最大 `63` 秒。
- **给 human 的六张卡，本期仍一张没点**（`human last_pull 2026-09-13T14:40:43Z`）。
- **在场名单**（时刻，非年龄；本期七个角色一个没动）：
```
pm        在听                            ← 唯一
dev       last_pull 2026-09-22T00:52:29Z   last_event 2026-09-21T04:54:12Z
qa        last_pull 2026-09-19T17:08:43Z   last_event 2026-09-19T13:36:36Z
human     last_pull 2026-09-13T14:40:43Z   last_event 2026-09-07T03:36:48Z
release   last_pull 2026-09-13T14:09:36Z   last_event 2026-09-13T14:19:35Z
frontend  last_pull 2026-09-13T01:17:15Z   last_event 2026-09-13T01:32:14Z
pd        last_pull 2026-09-12T16:42:03Z   last_event 2026-09-12T16:44:18Z
```

---

*本份在窗口边界上取数，窗口内 3 条。*
