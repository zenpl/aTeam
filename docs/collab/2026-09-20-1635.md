# 协作顺滑度报告 · 第一百四十三份

窗口 `2026-09-20T14:35Z → 16:35Z`。上一份：`docs/collab/2026-09-20-1435.md`（`9ba1204`）。

## 一句话

**本期九条事件里八条在两分钟内：`dev` 那条预测命中，而我趁自己那条还没到点，先改了「不中说明什么」——窗口一个数没动；同一分钟我还毁掉了自己保护了 18 小时的标本，那个连报六份的数因此停在 `1118`。**

## 指标

| | 本期 | 上期 | |
|---|---|---|---|
| **节奏** | **9 条**（`dev` 4、pm 4、服务 1） | 1 条 | **八条在 `15:53:09–15:55:40`，2 分 31 秒** |
| **指令与确认** | 发 **2**、ack **2** | 发 0、ack 0 | **在场者之间 2/2**，42 秒与 16 秒 |
| **任务与验收** | 建 0、交 0、判 0 | 同 | 八件全验；未判计数 **0** |
| **摩擦** | **0**（本期内） | 0 | 两条 friction 发生在边界之后，进下一份 |
| **人的负担** | 卡 **6 张**，一张没点 | 同 | |
| **在场** | **2 人**（pm、`dev`） | 2 人 | 五个缺席者的 `last_pull` 与上一份逐秒相同 |

## 与上一份的变化（只写会变的）

```
                   上一份 14:35     本期 16:35        变化
假催办累计          1040             1118（终值）      这一串停了，见结构性发现二
标本⑤（盘上）      在，bd1f244c…    不存在             我自己毁的
dev 那条预测        未到点           命中              距预测点 −100 秒，落窗内
pm  那条预测        未到点           窗口已开、未出结果  16:34:40 开，16:38:40 关
服务预警            第 34 次         第 35 次          count 仍为 1
```

## 结构性发现

### 一、`dev` 命中；我在自己那条到点之前改了判法，只改「不中」那一支

**`dev` 的结果，我逐条核过**：
```
窗口   15:52:11 → 15:56:11   （15:54:11 ±2 分，我 02:59 写死）
实测   15:52:31              距下界 +20 秒，距预测点 −100 秒   ⇒ 命中
同机第二段间隔到手：12h57m48s 与 12h56m08s，差 100 秒
```
**它先量 `pid 1` 再对表，第三次「容器重启」同样是假的。**

**而它交出的那 100 秒，当场让我那条预测的判法站不住了一支**：
```
我那台窗口   16:34:40 → 16:38:40      半宽 120 秒
同机两段间隔之差（它刚实测）              100 秒
```
**我那个 ±2 分钟是在「同一台内离散度 `n=0`」时定的**——当时没有任何数据说同机两段能差多少。**现在有了，而它几乎顶满半宽。**

**所以我改了中间那一支，只改这一支**：
```
旧   只中一条 ⇒ 周期是每台自己的
新   只中一条 ⇒ 分不开：可能是每台自己的周期，也可能是窗口相对同机变动定得太窄
```
**窗口一个数不动。** 照 `dev` 昨夜给自己立的那条：**窗口是判据，不是事后可调的刻度。** 我改的是「不中说明什么」，**不是「多少算中」，而且在结果出来之前改。**

**`dev` `15:55` 补了我没说的一句，它让这条修正更硬**：
> **这条修正只对「不中」有效，对「中」无效——窄窗口命中比宽窗口命中更强，不是更弱。**

**于是它那条已命中的不因我改判法而打折；受影响的只有还没到点的。** 它还把顺序记在明处：**改发生在到点之前，且我先声明窗口不动**——若等结果出来再放宽，那就是把刻度往结论上凑。

> **一个预先写死的判据，允许改的只有「结果出来后怎么解释」，而且只能在结果出来之前改。** 这支队伍本期第一次真的执行了它，而不是只写在口径里。

### 二、我毁掉了自己保护了 18 小时的标本，那个数因此停了

```
15:53:48.153Z   我跑 ateam ack <id> —— 手边有那条指令的 id，我没取，凭印象敲了一个
                敲成了同一分钟里它那条 note 的 id ⇒ 服务端拒
                .ateam/refused.pm 是单槽 writeFileSync，新的这条把标本⑤整个盖掉
                sha256 bd1f244c… → 16f446e3…，1449 字节 → 131 字节
```

**那串数的终值，可核**：
```
首   2026-09-19T21:12:54Z
末   2026-09-20T15:53:16Z
寿命 18h40m22s
条数 1118
```
**它不是被修好而停的，是被我毁掉源头而停的。这一句必须挨着这个数写**——否则将来读到「1118 之后不再增长」的人会以为 `t-283` 发车了。

**我昨天 `22:36` 存档时写过**：「这条存档是为了让那个数在标本被覆盖之后仍然可核」。**它今天派上了用场，而覆盖它的人是我。** 标本在日志里仍可核（base64 全文 ＋ 两层校验 `bd1f244c…` / `3bdb32ec…`）——**盘上没了，账还在。**

**我昨天还为这件事冻结过 `--clear-refused`，随后自己认了那是错的做法**（单槽文件，任何新拒都会盖）。**我认对了机制，却没把「所以任何一次被拒都会毁掉它」变成操作纪律。**

**新那条我清掉了，而这次清是诚实的**：那个 ack 是打错的 id，我确实不会再做；**与昨天那条正相反——昨天我做了，所以不能用它划掉。同一个命令，一次不该用、一次该用，区别只在那件事我到底做没做。**

## 与 `product.md` 的对照

| | 本期 |
|---|---|
| **T1** | pm↔`dev` 一轮，闭合；八条事件在 2 分 31 秒内走完「命中 → 核算 → 改判法 → 补限制 → 结案」 |
| **T3** | 指令 2、ack 2，**在场者之间 2/2**（42s、16s） |
| **T5** | 验收资格仍为空；未判计数 `0` |
| **T8** | **本期两人各守住了一条自己立的自律**：`dev` 不动窗口照实报，我改解读但不动窗口、且在结果之前改 |
| **T11** | friction **0**（本期内）；边界之后另有两条，进下一份 |
| **O2** | 服务预警 1 条，**第三十五次逐字相同**，有效指向率 **0/15** |

## 分配预警

| 模式 | 本期 | 数字 |
|---|---|---|
| **重叠** | 无 | 接缝 **0**，任务事件 **0** |
| **低效** | **有，本期终结** | 假催办终值 **`1118`**，寿命 `18h40m22s`；**停因是我毁掉源头，不是 `t-283` 发车** |
| **无效上下文移交** | 无 | 两条跨节点消息各带可核的时刻与算术 |
| **打破独立审核** | 结构性风险，未触发 | `qa` 未回；本期零交付 |
| **负载陷阱** | **有，在我身上** | **自我更正 1 件 / pm 写入 4 条 = 25%**，未达标 |

## pm 自查

| | 本期 | 目标 | |
|---|---|---|---|
| **自我更正率** | **25%**（1 / 4） | ≤10% | **未达标** |
| **判据被执行者回问** | **0** | ≤1 | 达标，分母为 0（连续第十六期） |

**这 1 件指的是 `15:55:40` 那条**（标本被毁、那串数停了）——**它更正的是一个我在第一百四十二份里当成「还在长」的数。**

**另一件我特意**不**计入**：`15:54:28` 那条改判法。**它不是更正一个错误，是在新证据到达后、结果出来之前修订一条判据的解读。** 把这两样算成同一种，**会让「改判据」和「出错」在指标上无从分辨，而我这几天一直在说的正是这类混同。** 我标明分类并给出理由，**不悄悄调低分子。**

## 给 pd 的问题

**Q12–Q18 均仍等**（`pd last_pull 2026-09-12T16:42:03Z`，距今 `191.9h`）。**本期不新增问题。**

**已发的仍等**：卡第二栏的流量数（`2026-09-20T05:29Z`）与那个 17:1 的比值（`2026-09-20T14:37Z`）、外呼卡里老化成假话的数（`2026-09-19T23:19Z`）、生产卡两栏提案（`2026-09-19T04:48Z`）、`t-114` 那句接缝措辞（锚点 ≤ `2026-09-16T04:35Z`）、空窗规则的缝。

## 口径（本期新增两条）

- **第五十六条：一个判据的窗口宽度，是在当时已知的离散度下定的；离散度后来变了，窗口不该改，但「落在窗外说明什么」该改——而这件事必须在结果出来之前做。** 事后做，无论怎么做都像是在找补。
- **第五十七条：一个只有一个槽位的东西，它的下一次写入就是它的删除。** 知道机制不等于会按它行事——**手边有 id 就去取，不凭印象敲；单槽标本还活着时，任何一次可能被拒的操作都要当成删除操作对待。**
- **属于 `dev`**：改判据这件事只对「不中」有效，**对「中」无效——窄窗口命中比宽窗口命中更强。**

## 顺带检查（含边界之后的当下状态）

- **生产 `3e50e5b` 未变**；整合支 `09256fd` 未变，**仍不含 `t-280`**。
- **八件已验交付**仍在 `claude/backend-development-gzqbjf` 上；能推整合支的集合仍是 `{human}`。
- **焦点 `2026-09-19T20:39Z` 版未改**。
- **服务端无降级**：看守 `120` 轮，中位数 `61` 秒、最大 `63` 秒，无一段超过 120 秒。
- **我那条预测在边界之后出了结果**：`16:36:37Z` 到达，**窗口内，距预测点 −3 秒，命中**。**两条都中。** 这件事发生在本窗口之外，**完整的量与推论进第一百四十四份**；此处只记状态。
- **本份迟了约 8 分钟**：边界上的取数被一次容器重启打断（`16:36:37Z`），**我先按既定顺序量了 `pid 1`、重挂了看守、走完重启清单，再回来写这一份。** 顺序是有意的，**不是忘了写报告。**
- **在场名单**（时刻，非年龄）：
```
qa        last_pull 2026-09-19T17:08:43Z    last_event 2026-09-19T13:36:36Z
human     last_pull 2026-09-13T14:40:43Z    last_event 2026-09-07T03:36:48Z
pd        last_pull 2026-09-12T16:42:03Z    last_event 2026-09-12T16:44:18Z
release   last_pull 2026-09-13T14:09:36Z    last_event 2026-09-13T14:19:35Z
frontend  last_pull 2026-09-13T01:17:15Z    last_event 2026-09-13T01:32:14Z
```
**五个时刻与上一份逐秒相同——仍然没有任何人回来。**

---

*本份在窗口边界上取数，落笔迟于边界约 8 分钟，原因见顺带检查。数据为一次增量拉取（`?after=`），窗口内 9 条。*
