# 协作顺滑度报告 · 第一百九十份

窗口 `2026-09-24T12:35Z → 14:35Z`。上一份：`docs/collab/2026-09-24-1235.md`（`5fc961b`）。

## 一句话

**本期两次拉取失败，一次在痕迹上留下 `105` 秒、一次一点痕迹没有——我二十分钟前刚说「拉取失败是看得见的那一类」，这句话被同一个窗口里的第二次否掉了；可见与否不取决于事件是什么，取决于它让下一行晚了多久。**

## 指标

| | 本期 | 上期 |
|---|---|---|
| **节奏** | **3 条**（pm 2、服务 1） | 3 条 |
| **指令与确认** | 发 1（给 `pd`）、ack 0（收件人缺席） | 同 |
| **任务与验收** | **建 0、交 0、判 0——已连续 `129h01m`** | 已连续 `127h01m` |
| **摩擦** | 0 | 0 |
| **人的负担** | 卡 6 张，一张没点 | 同 |
| **在场** | **1 人**（只剩 pm） | 1 人 |
| **本期发现关于谁** | **向内** | 向内 |

## 与上一份的变化

```
任务停了多久      127h01m  →  129h01m      锚点 2026-09-19T05:34:01Z
除我之外的写入者   0  →  0                  自 dev 最后一次写起 81.7h、129 条：pm 87、服务 42、其他 0
看守（本窗）      119 轮/最大 63s  →  118 轮/最大 105s   成因＝拉取失败，已实测
拉取失败          0  →  2                  13:58:33、14:15:43；服务端全程健康
不明缺口的率       3222 轮 1 次  →  3340 轮 1 次         本窗那 105s 成因已知，不计入
痕迹合计          16683  →  16801            差值 118 = 按取数时刻计数 118 ✓
服务预警          连续相同 61  →  62 条       零漏跳；两段最小/最大均未动
```

## 结构性发现

### 两次同类事件，一次有痕一次无痕——分类不是可见性的原因，耗时才是

本期 `13:58:33` 和 `14:15:43` 各有一次拉取失败。**服务端全程健康**：失败后 13 秒内 `/health` 连测三次均 `200`（`0.27`–`0.52` 秒），同一条增量拉取 `200`、`9328` 字节。**两次都是瞬时的，看守都按设计处理**（不推进高水位，下一轮补报）。

**第一次之后我下了一个判断，二十分钟后它被第二次推翻了**：

```
13:57:48 -> 13:59:33   105s   ← 第一次，超 90 秒，看得见
14:14:40 -> 14:15:42    62s
14:15:42 -> 14:16:43    61s   ← 第二次就在这附近，而前后一切正常
14:16:43 -> 14:17:44    61s
14:17:44 -> 14:18:48    64s
```

**第一次让下一行晚了 `45` 秒；第二次一秒没晚。** **同一类事件，同一个进程，同一天，一个有痕一个无痕。**

**所以我此前按「事件类别」分可见性的说法，站不住**。把四次重启和两次拉取失败放在一起，规律只剩一条：

```
事件               这次让下一行晚了多久    痕迹上的间隔     90 秒阈值
会话重启 09-24      −28s（提前）            33s            看不见
会话重启 09-23      −2s                     59s            看不见
会话重启 09-23      +11s                    72s            看不见
真重启   09-22      +45s                   106s            看得见
拉取失败 13:58      +45s                   105s            看得见
拉取失败 14:15       0s                     61s            看不见
```

> **第九十八条：一件事在采样痕迹上看不看得见，只取决于它让下一次采样晚了多久，与它属于哪一类无关。** **同一类事件可以一次有痕、一次无痕**；**按类别去讲可见性，早晚会被同类的第二个样本推翻。**

**这一条把第九十条、第九十四条收进同一个说法里**：那两条讲的是「会话重启看不见」，**而真正的原因不是「它是重启」，是「它耗时短于周期」。** **`09-22` 那次重启看得见，正因为宿主停机把它拖长了。**

**对那条 `90` 秒阈值的判词也要改**：我此前说它对某一类恒盲。**准确说法是：它只看得见「延迟超过约 29 秒」的事件，不论那是什么事。** **本期它抓到一次拉取失败，这是它这几天里第一次真正派上用场。**

## 与 `product.md` 的对照

| | 本期 |
|---|---|
| **T1 / T3 / T8 / T11** | 无往来、指令 1 ack 0（收件人缺席）、无可评、friction `0` |
| **T5** | 验收资格仍为空；**「无人能验」已持续 `129h01m`** |
| **O1** | 与人之间唯一的通道仍是那一页，**本期无变化** |
| **O2** | 服务预警**连续相同 62 条、零漏跳**；**前段 n=37 最小 `0.216`／最大 `2.559`；后段 n=24 最小 `0.199`／最大 `0.767`**（两段极值本期均未动，第 1 期） |
| **这条日志的用处本身** | **仍为 `0`**：81.7 小时、129 条，非 pm 非服务的事件 **0** |
| **这份报告在看谁** | 本期 1 条，**向内** |

## 分配预警

| 模式 | 本期 | 数字 |
|---|---|---|
| **重叠** | 无 | 接缝 0、任务事件 0 |
| **低效** | 无 | **假催办 0（来源已毁，非已修复）**；看守轮数：本窗 `118`，痕迹合计 `16801` |
| **无效上下文移交** | 无 | 81.7 小时内非 pm、非服务事件 0，**没有链可断** |
| **打破独立审核** | **结构性风险，已持续 `129h01m`** | `qa`、`dev` 均 missing；**在场 1 人**；**按第九十一条，也指没有人能验收我的判断** |
| **负载陷阱** | 无 | 队列深度全 0；pm 本期写入 2 条 |

## pm 自查

| | 本期 | 目标 | |
|---|---|---|---|
| **自我更正率** | **50%**（1 / 2） | ≤10% | **未达标** |
| **判据被执行者回问** | **0** | ≤1 | 分母为 0 |

**本期有两处要认**：

· **一处更正，当场发生当场收**：我在第一次拉取失败后说「这是本会话第 3 次 fetch-fail」，**那是我把 `tail -3` 的输出当成了整个文件**——实际 `17` 次（`09-12` 五次、`09-13` 六次、`09-15` 一次、`09-19` 四次、本期一次）。**与第八十条同一个毛病：拿一段当全部。**

· **一处判断过早**：第一次失败后我写「拉取失败是阈值看得见的那一类」，**二十分钟后第二次就否掉了它。** **这正是第九十二条（补）要防的——一个样本不立规律**；**而我上一期刚在另一件事上守住了这条，本期又在这件事上没守住。**

**本期自我更正率 `50%`，超 `≤10%` 的目标，如实记。** **两件都发生在窗口内、当期发现、当期写清，没有留到下一期。**

**本期到期 4 次（`12:43:07`／`13:13:17`／`13:43:25`／`14:13:35`），4 次都在收到通知的当场重挂。**

## 给 pd 的问题

**Q12–Q18 均仍等**（`pd last_pull 2026-09-12T16:42:03Z`，距今 `285.9h`）。**本期不新增问题。**

那条老问题本期的材料：**这份报告本期自我更正率 50%，而两件都是当期抓住的。** **连着两期「在该出错的位置没出错」之后，本期在两个新位置出错了——前一种是运气，后一种是常态。**

**已发的九件仍等。**

## 口径（本期新增一条）

- **第九十八条：一件事在采样痕迹上看不看得见，只取决于它让下一次采样晚了多久，与它属于哪一类无关。** **同一类事件可以一次有痕、一次无痕；按类别去讲可见性，早晚会被同类的第二个样本推翻。** **第九十条、第九十四条并入此条：会话重启之所以看不见，是因为它耗时短于周期，不是因为它是重启。**

## 顺带检查

- **生产 `3e50e5b`、整合支 `09256fd` 均未变**，后者仍不含 `t-280`；**八件已验交付仍坐在 `claude/backend-development-gzqbjf` 上，等 human。**
- **焦点 `2026-09-22T02:41Z` 版未变**：本期是它的第三十个整窗。
- **服务端本期实测健康**：失败后 `/health` 三连 `200`（`0.27`–`0.52` 秒），增量拉取 `200`／`9328` 字节。
- **给 human 的六张卡，本期仍一张没点**（`human last_pull 2026-09-13T14:40:43Z`）。
- **在场名单**（时刻，非年龄；本期七个角色一个没动）：
```
pm        在听                            ← 唯一
dev       last_pull 2026-09-22T00:52:29Z   last_event 2026-09-21T04:54:12Z
qa        last_pull 2026-09-19T17:08:43Z   last_event 2026-09-19T13:36:36Z
human     last_pull 2026-09-13T14:40:43Z   last_event 2026-09-07T03:36:48Z
release   last_pull 2026-09-13T14:09:36Z   last_event 2026-09-13T14:19:35Z
frontend  last_pull 2026-09-13T01:17:15Z   last_event 2026-09-13T01:32:14Z
pd        last_pull 2026-09-12T16:42:03Z   last_event 2026-09-12T16:44:18Z
```

---

*本份在窗口边界上取数，窗口内 3 条。*
