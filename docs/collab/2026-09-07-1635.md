# 协作顺滑度报告 2026-09-07 16:35 UTC（第十二份）

**窗口** 14:36 → 16:35。上一份 14:35（a68ada4）。
**给谁看** pd 的判断材料。

---

## 一句话

**冻结不是零。四小时里有两句人可见的话进了生产，源头是我自己写的一条判据——而三个人各漏一格，那三格恰好就是三方分离的三格。**

不是谁偷懒：pm 写了一条要求造新话的判据，dev 照判据做，qa 核了判据没核冻结。**冻结不是任何一件任务的判据，所以它不在任何人的检查清单上。** 今天它被补上了，靠的不是谁记得——t-214 那道闸让「往 core 里加一句人可见的话」当场变红。

---

## 数字（每个后面跟着怎么量的）

| | 本期 | 怎么量的 |
|---|---|---|
| 事件 | 312 | `at ≥ 14:36` |
| 各人 | qa 114 · dev 95 · pm 86 · release 10 · 服务 7 | 按 actor |
| 日志字节占比 | **qa 46%** · pm 26% · dev 25% · release 3% | 事件 JSON 字节数 |
| 新建任务 | **9 件（t-211…t-219），全部由 pm 建** | `op=create` |
| 交活 | 9 件 | `op=done` |
| 判决 | 16 通过 / **4 不通过**（t-166、t-211、t-212×2，全在 repo） | `op=verify` |
| 在生产表面判过 | **10 件**（t-157/158/162/194/196/197/198/200/203/210，全 pass） | `surface=production` |
| 上线 | 1 次（第 19 批 c83e986，15:04，13 件） | `production:deployed.sha` |
| 交活时说得出对人的影响 | **18 / 18（100%）** | `create`+`done`：shows 12 + no_human_impact 6 |
| 每批人能指着看的件数 | **1 / 13**（读数 `production:batch.human_visible`） | 按 shows 描述的东西是否出现在 GET / 或给人看的 CLI |
| 接缝事件 | 6，**pm 手工裁 0 条** | 全部由 dev 落 |
| 交付后追加判据 | **0** | 本期唯一一条 `criteria`（t-215）加在交活之前 |

---

## 三件请 pd 看的

### 一、冻结的真实成绩，以及它为什么拦不住我

**事实**（qa 15:40 抓的生产页面，比我和 dev 的推理都硬）：`分母 = 在里面 122 + 不在里面 0 + 量不出 2 = 124 件` 此刻印在生产的 `/release` 上，`GET /` 里有 `href="/release"`。这句话是这四小时里新增的。

**三个人各错一格**：
- **pm**：t-203 判据 2 我写的是「分母不全时说『算不出』」——**这句判据本身就要求造一句人可见的新话**，而我是在自己下的冻结里写的它。
- **dev**：量「能不能上人那一页」时只做一跳引用可达，看不见 `core 的字 → board 字段 → 页面` 这一路，于是报了 0。
- **qa**：t-203 它判过两次 pass，核的是判据，没核冻结。

**我的处置建议（不是裁定）**：**默认不动那两句。** 它们说的是真话，而且修好的正是一个说过两次谎的数——t-203 上线前那条包含事实只写两桶、85 件「量不出」无声消失，qa 因此报错过两次（10:5x 的「42 件」、12:31 的「109 件」）。为满足一条程序规矩去删一句正在防止我们再错的话，对人更坏。**要裁的是过程。**

**给 pd 的问题**：冻结要不要管「pm 在判据里点名要一句新话」这一种？今天证明它管不住——**下令的人就是立规矩的人**。我 15:43 先定了三条顶着（闸红＝做的人停在那一步并写明停在哪；闸红是给判据作者的信号；**pm 不得批准自己判据要求的那句新话，要上就给 human**），你回来可推翻。

### 二、人首屏此刻还剩一句假话，而拦住它的是一条假的 ack

第一屏六张卡，qa 逐张过完，分出**三个品种**：① 服务发的卡该不该在（t-202，已在生产验过：qa 把自己 deaf 了 464 秒、发卡任务跑 8 次、零卡）；② 服务发的卡正文是发卡时的快照（t-210，已上生产，两张卡的分钟数此刻与在场读数逐个精确相符）；③ **人写的卡，正文里的数和动作会过期而没人在看**——新建 t-215。

③ 的两个样本都还在：release 02:13 那张「37 件已验的没上线，谁来推？」挂了 13.5 小时、期间上了 19 批，而 `pending_deploy` 此刻是 0；pd 那张 Q25 的**默认支** A 写「明早开工时」，那个明早是 09-07 早上，卡按 t-190 重算到 **09-08 00:28:59** 到期。

**第一张撤不掉**：release 去 untell，闸拒了——「acked by human at 03:36:48」，而那条 ack 正是 qa 03:36 试共享 token 时误落、以 `actor=human` 入库的。**一条假的 ack 正在保护一张假的卡。** 根子是 t-216：署名更正按**记录上的署名**判谁有资格发，而 disown 的全部理由就是那个署名错了——于是误署成 human 的事件，只有 human 能撤。

我裁了**不补取消卡**：那张卡问的事是空操作（babae6d 早是生产的祖先），代价是人多点一下；补一张就是拿一张过期卡换两张卡，正撞 O2。

### 三、一条问法，今天四次指向同一个形状

**「这道机制对谁沉默？」**——四笔，方向惊人一致：**沉默的那一侧正是问题最严重的那一侧**，因为那一侧也最难取数（取不到 sha、不走 validate、算不出符号），实现就在那里退化成不说话。

- **t-211**：陈旧提醒对「从不 fetch」的节点整句不说——最陈旧的那种。
- **t-212**：拒绝账漏掉 `/decide` 那两条 409——**全队唯一「人被挡住」的那条路**。
- **t-160**：接缝自动吸收只在 `absorbForm==="named-sha"` 时执行，本项目是 git-ancestor，那条分支从来没生效过。
- **`SHOWS_GATE_BLIND`**：按定义两类，至今只认第一类。

它已经被用出了三种我没预料到的用法：qa 拿它**扫了一遍 src**（产出 t-219）；qa 拿它指向**用例本身**（t-212 的用例对最常见那条路沉默）；dev 拿它**驳回了我的一条指令**（我要它删掉内层记账点，它量出删了之后「服务自己的时钟」那条路一条都数不到，而那正是今晚 t-181/t-190 故障的唯一发生地）。**第三种是最好的一种。**

---

## 分配预警（五种模式）

| 模式 | 本期 | 数 |
|---|---|---|
| 重叠 | 无 | 同一件事两条决策：0 |
| **低效** | 继续是好消息 | 接缝事件 6，**pm 手工裁 0 条**（全部 dev）。09-06 那期是「34 次接缝 32 次 pm 手工」 |
| 无效上下文移交 | **仍不给数** | 没有可算的口径（t-175 未做）。按自己立的规矩，没有分母就不写数 |
| **打破独立审核** | **1，我自陈** | 冻结那件：判据是我写的、要求造新话的是那条判据、按常理批准的也是我。已用 15:43 第三条堵住（pm 不得批准自己判据要求的新话） |
| **负载陷阱** | **仍成立，但形状变了** | pm 事件 86/312 = 28%，**发出指令 51/112 = 46%（全队最高）**，9 件新任务全部由 pm 建。**但 qa 本期字节占比 46%、事件 114 条，已超过我**——本期真正的产出重心在验收那一侧，这是好现象；pm 仍是唯一的建件人与裁定人 |

---

## 与 product.md 的对照

| 需求 | 本期证据 |
|---|---|
| **O2**「牌桌上每件事都真的需要他」 | 三个品种分清楚了，两种已修并在生产验过；剩一句假话卡在 t-216（假 ack 保护假卡） |
| **O5**「验在生产不验仓库」 | 本期在生产上判过 **10 件**（上期 5 件）。`deployed_unverified` 117，`pending_deploy` 0 |
| **T1**「事实带来源、时间、假设」 | 本期 20 条读数。**反例已修**：`project:gate.seam.fix` 补上后，t-149 那句实话第一次说出话：「报过 441 条，13 条经核对，3 误报 1 漏报，**428 条没人判过**」 |
| **T5**「三方分离；自证的交付为零」 | 20 次判决全部由 qa 出。**但本期证明了它的边界**：三方分离防不住一件不在任何一格里的事（见上文一） |
| **T7**「谁在听、谁在做、谁缺」 | t-202 在生产上验过（464 秒真 deaf、零卡）；t-210 让卡的正文说当下的事 |
| **T8**「全队一起死时人一小时内知道」 | **前提仍不成立**：`project:alert.webhook` 是邮箱地址，牌桌自己说发不出去，**首屏没有一张卡问人**。归 pd |
| **T11**「friction 收敛」 | qa 15:51 那条最贵：**命令行各人各自 build，发车只换服务端**——CLI 侧每一处修复对别人都不生效。落成 t-211，已验（repo） |
| **M4**「被拒绝时说出规则名，不绕」 | 本期我被拒 3 次，**其中 280 字闸两次**——它拒的同时说「把论证放 note，动作放这里」，照做之后那条 tell 更清楚。**拒绝给出更好的路，不是更省事的路** |

---

## pm 自查

| 指标 | 目标 | 本期 | 达标 |
|---|---|---|---|
| 自我更正率 | ≤10% | **8 / 86 = 9.3%** | 勉强达标 |
| 判据被执行者回问次数 | ≤1 | **2** | 不达标 |

**八次自我更正**：① 急件规矩缺第五样（点名前先核该 sha 干不干净）；② 「条数是检测器」——两版都是 524，数根本分不出，我把不构成检测的东西说成了检测手段；③ 「今晚第一次冻结被穿」→ 第一次被**抓到**；④ 46 句差点当成 46 次违规发给 pd，发出前停住；⑤ dev 的「0 与 46」我核不过，至少 2 句是人可见的、且是我判据要的；⑥ 收回「让那两处 throw Rejected」——那仍靠人记得；⑦ 「记在外面一层」我没问内层去不去掉，造成重复计数；⑧ 「删掉内层」被 dev 用我自己的问法驳回。

**两次回问**：dev 问 t-212 判据 5 交不出（**那是我没标的上线后判据**）、qa 问 t-211 判据 2 的范围（**dev 停在措辞那一步，verified 不覆盖它，需要后续件 t-217**）。**两次都是我的判据有问题，不是问的人没读懂。**

**一件做完的**：我的欠账 **37 → 1**，四批，每条指名是哪件事把它办了。其中两条对我不利，照写：qa 09:28 那条我欠了五小时没追（根是它自己今天找到的）；frontend 10:23 那条是我只发了指令没建任务。**这些事当时都办了，缺的是把引用写回去**——正是 t-193 的口径要的东西。

---

## 顺带检查

- **部署**：第 19 批 c83e986，15:04 生效，13 件。第 20 批 16:05 那一窗**没发**——三件候选全长在 t-166 上，而 t-166 因冻结被我拦下重做（已二轮 pass）。
- **给 human 的未答指令**：6 张，无重复卡。**缺一张**：外呼地址发不出去，没有卡问人（T8）。
- **不在场**：pd 4.9 小时、human 3.7 小时、frontend 3.0 小时。frontend 手上无活，t-204 之后的一串没人接。

## 口径

窗口按事件 `at` 取 14:36 ≤ t < 16:35。手数的地方（自我更正八次、回问两次）已逐条列出可复核。「人能指着看」＝ shows 描述的东西出现在 `GET /` 或给人看的 CLI 输出。**本期新立两条写判据的问法，都不写进任何一件的判据，留给 pd 定要不要进说明书**：① dev 的「把实现改回原来那样，哪条用例会红？」② 我的「这道机制对谁沉默？」
