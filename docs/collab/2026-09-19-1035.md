# 协作顺滑度报告 · 第一百二十八份

窗口 `2026-09-19T08:35Z → 10:35Z`。上一份：`docs/collab/2026-09-19-0835.md`（`6358e2c`）。

## 一句话

**两小时 5 条事件：我 2 条交上一份报告，服务 3 条报同一件早已了结的事——而它报得一次比一次难看（`p90 30:4` → `35:0`），全程零新证据；`09:19` 之后连服务自己也停了，76 分钟无事件。**

## 指标

| | 本期 | 上期 | |
|---|---|---|---|
| **节奏** | **5 条**（服务 3／pm 2） | 5 条 | `dev` 0、`qa` 0，**而三者都在听**；**本期服务写得比我多** |
| **指令与确认** | 发 1、ack 0、未确认 1 | 发 1、ack 1 | 唯一那条是给 `pd` 的报告交接；`pd` 缺席约 **162 小时** |
| **任务与验收** | 建 0、交 0、判 0 | 同 | `t-282`…`t-289` 八件全验，待办清空 |
| **摩擦** | **0** | 0 | |
| **人的负担** | 卡 **6 张**，一张没点 | 同 | human 缺席约 **6 天** |

## 与上一份的变化

**总量不变（5→5），但构成翻转了**：上期我 4 条、服务 1 条；**本期我 2 条、服务 3 条**。
**而服务那 3 条全是同一件事的重复报告**（`09:05`／`09:12`／`09:19`，间隔 7 分钟）。**队伍安静之后，日志里最活跃的是一条假警报。**

## 结构性发现

### 一、一条会自己长大的假警报，本期长到了最难看的一格

**它报的事**：`qa` 的 ack 延迟 p90。**它指的那一条样本**：`qa` 办完 `dev` `03:57` 的指令才想起没 ack——**而 `qa` 自己 `04:28` 就自查、自陈、给出了成因**（「事做了，账没平」）。

**七小时里，零新证据，而它这样演变**：
```
07:05   p90 30 分钟   其他角色中位数 4      反差 30 : 4
09:05   p90 35 分钟   其他角色中位数 5
09:12   p90 35 分钟   其他角色中位数 4
09:19   p90 35 分钟   其他角色中位数 0      反差 35 : 0
```

**两头都在往坏里走，而两头的成因是同一个——没人写字**：
· **p90 从 30 涨到 35**：窗口滑走，旧样本在剩下的样本里占比越来越大。
· **对比基准从 4 塌到 0**：**别人的中位数掉到 0，不是因为别人变快了，是因为别人根本没有新的 ack 可量。**

> **一个统计量算的是「已经发生的事」，而它的分母随时间滑走。于是一件早已了结的旧事，在零新证据的情况下，会显得越来越严重；而当全队安静时，它的对照组会塌成 0，把反差推到最大。**

**这与今晚那一族同源，方向相反**：前面几例是**该响的没响**（看守漏了一类事件、`limit` 是装的、队列的笔停了），**这一例是不该响的越响越大**。
**共同点仍是那一条**：**一个数的名字与它真正数的东西不同**——它叫「`qa` 的 ack 延迟」，实际数的是「一件五小时前就办完的事，在一个正在缩小的样本里所占的份额」。

**我本期没有转发给 `qa`，也没有建任务**（按焦点第 ④ 节）。理由写明：**转发等于让它把已办过的事再读一遍**，那正是 `t-233` 那句错话的形状。**但下一个看牌桌的人读到的是「35 分钟」，不是「一件五小时前就办完的事」**——这一句留给 `pd`。

### 二、`09:19` 之后连服务自己也停了

本窗口最后一条事件是 `09:19:52`，**其后 76 分钟零事件**。看守 30 轮间隔全部 `60–64s`、无一异常——**仪器健康，安静是真的**（本期第四次做这个证伪，连续三个半小时每半小时一次）。

## 与 `product.md` 的对照

| | 本期 |
|---|---|
| **T1** | 无认知分歧可记 |
| **T3** | 无在场者之间的往来可量；唯一一条指令发给缺席的 `pd` |
| **T5** | 无判据、无验收 |
| **T8** | 三个节点在无活时都没有造活，**本期连我也只发了报告交接那两条** |
| **T11** | friction **0** |
| **O2** | 服务预警 3 条，**全是同一件已了结的事**，有效指向率 **0/3** |

## 分配预警

| 模式 | 本期 | 数字 |
|---|---|---|
| **重叠** | 无 | 接缝 **0**，任务事件 **0** |
| **低效** | **有，且本期它自己就是那个低效** | 服务 3 条预警全指同一件已了结的事，**有效指向率 0/3**；**而它占了本窗口事件总数的 60%（3/5）** |
| **无效上下文移交** | 无 | 点名给我的 **0** 条 |
| **打破独立审核** | 无 | 本期无判据、无验收 |
| **负载陷阱** | **无** | `dev`／`qa` 队列深度 **0**；pm 本期只写 2 条。**瓶颈 100% 在人那一侧** |

## pm 自查

| | 本期 | 目标 | |
|---|---|---|---|
| **自我更正率** | **0%**（2 条里 0 条） | ≤10% | 达标——**分母 2，与上期同理，这个数不含信息量** |
| **判据被执行者回问** | **0** | ≤1 | 达标（本期无判据） |

**本期我做的一个克制，单记**：`09:05` 之后那条预警每隔 7 分钟重复一次，**我在 `09:1x` 定下不再逐条去读它**，只在本报告里一次取全序列。
**理由**：**一条已知会重复的假警报，每次都去看，就成了它的第二重代价。**

## 给 pd 的问题

**Q12、Q13、Q14 均仍等**（缺席约 162 小时）。**本期新增一条，因为它已经从「记一笔」变成「会误导下一个看牌桌的人」**：

**Q15：`project:allocation` 的预警要不要有「已自查」这一档？**
事实：`qa` `04:28` 自查并了结了那件事；服务在其后七小时报了至少 4 次，**数字从 30 涨到 35，对比基准从 4 塌到 0，全程零新证据**。
**这不是要它别报**——是问**牌桌上一条预警要不要能区分「这件事没人管」与「这件事当事人已经自己了结了」**。此刻它只有前一种表达。
**归你，因为它是人会读到的那一格。** 事实那一半我已量全，序列在上面。

**另两条已发的仍等**：生产卡按两栏改的提案（`04:48`）、`t-114` 那句接缝措辞（`dev` `09-16` 提出，已 3 天）。
**空窗规则的缝本期第四次落进去**（只有 pm 与服务在写），按字面仍写全套，刻意写短。

## 口径（本期新增）

- **一个统计量的对照组会随安静塌成 0，把一件旧事的反差推到最大。** 看一条预警时，先问它的分母这段时间发生了什么。
- **一条已知会重复的假警报，每次都去看，就成了它的第二重代价。**

## 顺带检查

- **生产 `3e50e5b` 未变**；整合支 `09256fd` 未变，**且它连 `t-280` 都没有**。
- **八件已验交付**仍坐在 `claude/backend-development-gzqbjf` 上，能推整合支的集合仍是 `{human}`。
- **焦点 `06:39` 版仍成立**，本期未改——本期没有发生任何足以改动它的事。
- **服务端**：本期无降级，看守 120 轮（四个半小时）间隔全部 `60–64s`。

---

*本份在窗口边界上写，数据为 `10:36` 一次增量拉取。*
