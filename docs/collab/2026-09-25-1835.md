# 协作顺滑度报告 · 第二百零四份

窗口 `2026-09-25T16:35Z → 18:35Z`。上一份：`docs/collab/2026-09-25-1635.md`（`9c58039`）。

> 沿用 #197 的写法。Q21、Q22 均未答，`pd` 未否决。

## 一句话

**`18:31` 容器重启造成 47 秒断听，它进了离群表；而 `09-25T05:34` 那次同类事故的 54 秒没进——两次差 7 秒，却分属「表上有」与「表上没有」，这就是口径 108 要印落选值的全部理由。**

## 指标

| | 本期 | 上期 |
|---|---|---|
| **节奏** | 3 条（pm 2、服务 1） | 3 条 |
| **指令与确认** | 发 1（给 `pd`，即 Q22）、ack 0 | 同 |
| **任务与验收** | **建 0、交 0、判 0——已连续 `157h01m`** | `155h01m` |
| **摩擦** | 0 | 0 |
| **人的负担** | 卡 6 张，一张没点 | 同 |
| **在场** | 1 人（只剩 pm） | 同 |
| **本期发现关于谁** | **向内 1、向外 0** | 向内 2、向外 0 |

## 与上一份的变化

```
任务停了多久      155h01m  →  157h01m       锚点 2026-09-19T05:34:01Z
除我之外的写入者   0  →  0
看守（本窗）      118 轮/最大 63s(-27s)  →  119 轮/最大 63s(-27s)
重挂间隔平均      30.50s  →  31.00s         四次标准重挂 31/30/33/30
本窗真实故障       0 次  →  1 次             18:31 容器重启，断听 47s
重启通知的账      11 次里 1 次真  →  12 次里 1 次真   本次假报，btime 未动
拉取失败          09-25 至今 0  →  0        最后一次仍是 09-24T21:25:25，已静 1270 分钟
不明缺口的率       4875 轮 1 次  →  4994 轮 1 次
痕迹合计          18347  →  18466            差 119 = 窗内 119 ✓
服务预警          连续相同 75  →  76 条       跨 152 小时
```

## 结构性发现

### 一、同类事故差 7 秒，一个进表一个不进

```
离群表（<50s 或 >90s），五条
  16:52:56 → 16:53:27   31s   标准重挂
  17:23:05 → 17:23:35   30s   标准重挂
  17:53:12 → 17:53:45   33s   标准重挂
  18:23:23 → 18:23:53   30s   标准重挂
  18:31:01 → 18:31:48   47s   ← 容器重启造成的断听，不是重挂

两侧落选：下侧 61s（距 50s +11s）；上侧 63s（距 90s -27s）
```

把它和 `09-25T05:34` 那次并排：

```
09-25T05:34:25 → 05:35:19   54s   距 90s 门槛 -36s，距 50s 下限 +4s   两道筛子都不收
09-25T18:31:01 → 18:31:48   47s   距 90s 门槛 -43s，距 50s 下限 -3s   下侧筛子收了
```

**同一种事故（容器重启清掉后台任务、唯一在听的节点停摆），两次相差 7 秒，一次隐形、一次现形。** 而它现形的方式还是误导的——它混在四条重挂里，形状像第五次重挂，只有我知道 `18:31` 不在三十分钟的节拍上。

这一窗把口径 108 的价值钉死了：**落选值印出来，才知道盲带在哪、这次是不是又有东西掉进去。** 本窗盲带（`47s` 到 `61s`）里没有别的样本，所以除这一次之外没有第二次隐形事故。

顺带记一条该补的：离群表现在只印数值，不印**归因**。`47s` 那条与四条重挂并列，读者无从分辨。从本份起，离群表每条后面注明是「重挂」还是别的（本份已加）。

### 二、重启账目与反应时间

```
btime      2026-09-22T12:48:14Z   未动
uptime     77.72h（当时）          pid1 etimes 279808s 与之一致
自洽差     -1s
```

第 12 次通知，第 11 次假报。`/tmp` 未被清。处理：`18:31:43` 收到并完成分类 → `18:31:5x` 看守与边界双双重挂并验证。

按口径 107，这次断听 47 秒里有多少是我的反应时间：末次痕迹 `18:31:01`，我的新看守首写 `18:31:48`，而我在 `18:31:43` 才拿到通知——**47 秒里我占了约 5 秒，其余 42 秒是通知本身的延迟。** 这与 `05:34` 那次（我占约 20 秒）不同，说明口径 107 那句「量的是观测者的反应时间」还要再切一刀：**量的是「事故→通知」加「通知→我动手」，而前者不归我。**

## 与 product.md 的对照

与上期同。当期数字：

```
pd 313.9h · frontend 305.1h · release 292.3h · human 291.9h · qa 145.5h · dev 89.7h
服务预警连续第 76 次逐字相同 / 152 小时
```

## 分配预警（五种模式各带数字）

| 模式 | 本期 | 为什么 |
|---|---|---|
| **重叠** | 0 | 窗内无接缝、无并发决策 |
| **低效** | **1** | 等验队列 5 件，severity 1.00，**连续第 76 次逐字相同 / 152 小时** |
| **无效上下文移交** | 0 | 按 pd 16:13 的定义：无人在场，没有人被迫重讲上下文 |
| **打破独立审核** | 0 | 窗内 1 条指令，达不到门槛 |
| **负载陷阱** | 0 | 闸在前面：`:195` 要 `sent.length >= 10`；窗内 1 条指令 |

## pm 自查

| | 本期 | 上期 | 目标 |
|---|---|---|---|
| **自我更正率** | 0/1 = 0% ✅ | 0/1 = 0% ✅ | ≤10% |
| **判据被执行者回问** | 0 ✅ | 0 ✅ | ≤1 |
| **作废的自立判据** | 0 | 1 | —— |
| **被服务拒绝的写入** | 0 | 0 | —— |

**连续第十一期分母 ≤2。**

## 给 pd 的问题

1. **Q22**（`01M3CPVFRQMMJ6D7BWGBN4V3RR`）：什么算报告里的「一个发现」？未答。本份按我给的界记为 向内 1（口径 113）。
2. **Q20**、**Q21**、**Q19**：均未答。
3. Q12–Q18 七条仍未答。
4. `pd` 已缺席 **313.9h**；欠它的条目累计 **十九项**。

## 顺带

- **部署**：`production 3e50e5b`，`/health` 一致，`0.22s`。
- **焦点**：`87.9h` 前那版，未改。其 ⑤ 条（唯一在听的节点停摆）**本窗第三次实测发生**，47 秒。
- **集成合并仍等人**：八件已验交付坐在 `claude/backend-development-gzqbjf`，能推整合支的集合仍是 `{human}`。
- **给 human 的未答指令**：6 张，`human last_pull 2026-09-13T14:40:43Z`（291.9h）。

## 口径

- **口径 113**：离群表每条要注明归因（重挂／断听／其他）。只印数值时，一次真实事故会因为形状相近而混进例行项，读者与我自己都分辨不出。
