# 协作顺滑度报告 · 第一百七十九份

窗口 `2026-09-23T14:35Z → 16:35Z`。上一份：`docs/collab/2026-09-23-1435.md`（`98b99df`）。

## 一句话

**本期窗口里真的发生了一次故障——我聋了 `72` 秒；而它低于我那条 `90` 秒线，所以指标表上一个记号都没有。三个点凑齐之后，这不再是「有时看不见」，是「这一类按构造永远看不见」。**

## 指标

| | 本期 | 上期 |
|---|---|---|
| **节奏** | **3 条**（pm 2、服务 1） | 3 条 |
| **指令与确认** | 发 1（给 `pd`）、ack 0（收件人缺席） | 同 |
| **任务与验收** | **建 0、交 0、判 0——已连续 `107h01m`** | 已连续 `105h01m` |
| **摩擦** | 0 | 0 |
| **人的负担** | 卡 6 张，一张没点 | 同 |
| **在场** | **1 人**（只剩 pm） | 1 人 |
| **本期发现关于谁** | **向内**（事件是外来的，发现是关于我的阈值） | 0 条 |

## 与上一份的变化

```
任务停了多久      105h01m  →  107h01m      锚点 2026-09-19T05:34:01Z
除我之外的写入者   0  →  0                  自 dev 最后一次写起 59.7h、96 条：pm 65、服务 31、其他 0
看守（本窗）      119 轮/最大 64s  →  118 轮/最大 72s     那 72 秒就是本期那次重启
不明缺口的率       1910 轮 1 次  →  2028 轮 1 次          本期那次成因已知，不计入
痕迹合计          15370  →  15489            差值 119 = 按取数时刻计数 119 ✓
服务预警          连续相同 50  →  51 条       零漏跳；穿过本期那次重启没有漏跳
重启通知的账      7 次里 1 次真  →  8 次里 1 次真   本次为假
```

## 结构性发现

### 采样周期挡住了一整类故障，而它是更常见的那一类

`14:42:58` 收到重启通知。**按规矩先量 `pid 1`**：

```
btime         2026-09-22T12:48:14Z   没有动
uptime        93284.17s              btime + uptime ≈ 14:42，自洽
pid 1 etimes  93283
判决          宿主没重启。会话／容器重启，通知为假。第 8 次通知，真的仍只有 1 次。
```

**我确实聋了一段**：痕迹 `14:41:53 → 14:43:05`，**`72` 秒**。**而本窗最大间隔就是这 `72` 秒**——**低于我那条 `90` 秒线，所以「无 `>90s`」那一行照常是空的。**

**三次事件凑齐了，可以把话说死**：

```
09-22T12:48   真重启   缺口 106s   > 90s   在痕迹上可见
09-23T01:45   假通知   缺口  59s   < 90s   不可见
09-23T14:42   假通知   缺口  72s   < 90s   不可见（本期）
```

**成因分解一直是同一个**：**缺口 = 宿主停机 + 我的重挂延迟。**
**宿主停机只有真重启才有**；**我的重挂延迟单独一项，实测 `59`–`72` 秒。**
**而看守每 `61` 秒写一行。** **一个约等于一个周期的缺口，和一个正常周期在采样上长得一模一样。**

> **第九十条：当一类故障的持续时间小于或接近你的采样周期，任何基于采样的阈值都测不到它。** **「我的痕迹里没有异常」对这一类恒为真，与它是否发生无关。** **要测它，只能靠采样之外的通道。**

**这条比第八十四条硬**：那一条说的是「有些故障不留坑」，**本期说的是「只要我在通知当场重挂，会话级重启就按构造穿不过我的阈值」**——**而这一类占了通知的 `7/8`。**

**我不降低那条线。** 把 `90` 秒降到 `65` 秒，**会把大量正常的 `61`–`64` 秒周期变成告警**，**换来的是把一类我已经有别的办法知道的事重复告诉我一遍。**

**已经在做的那件事，本期证明它是唯一可行的那件**：`/tmp/pm-cli/restarts.log` 现在三条齐了，**每条带 `btime`、`uptime`、`pid 1`、缺口、仓库状态**。**采样测不到的，靠通知＋当场测量记下来**——**这不是补充手段，是这一类事件唯一的记录方式。**

## 与 `product.md` 的对照

| | 本期 |
|---|---|
| **T1 / T3 / T8 / T11** | 无往来、指令 1 ack 0（收件人缺席）、无可评、friction `0` |
| **T5** | 验收资格仍为空；**「无人能验」已持续 `107h01m`** |
| **O1** | 与人之间唯一的通道仍是那一页，**本期无变化** |
| **O2** | 服务预警**连续相同 51 条、零漏跳**（穿过本期那次重启）；**典型周期慢 `0.254` 秒；最近三个偏离 `0.560`／`0.242`／`0.274` 秒，至今最大 `2.559` 秒** |
| **这条日志的用处本身** | **仍为 `0`**：59.7 小时、96 条，非 pm 非服务的事件 **0** |
| **这份报告在看谁** | 本期 1 条，**向内**；近十份 `12 : 1` |

## 分配预警

| 模式 | 本期 | 数字 |
|---|---|---|
| **重叠** | 无 | 接缝 0、任务事件 0 |
| **低效** | 无 | **假催办 0（来源已毁，非已修复）**；看守轮数：本窗 `118`，痕迹合计 `15489` |
| **无效上下文移交** | 无 | 59.7 小时内非 pm、非服务事件 0，**没有链可断** |
| **打破独立审核** | **结构性风险，已持续 `107h01m`** | `qa`、`dev` 均 missing；**在场 1 人** |
| **负载陷阱** | 无 | 队列深度全 0；pm 本期写入 2 条 |

## pm 自查

| | 本期 | 目标 | |
|---|---|---|---|
| **自我更正率** | **0%**（0 / 2） | ≤10% | 分母 2 |
| **判据被执行者回问** | **0** | ≤1 | 分母为 0 |

**本期到期与异常合计 5 次（`14:42` 重启／`15:13:14`／`15:43:28`／`16:13:41`，以及重启前那次到期），5 次都在收到通知的当场处理。** **那 `72` 秒是重挂延迟本身，不是拖延**——**它是这条做法目前的下限。**

## 给 pd 的问题

**Q12–Q18 均仍等**（`pd last_pull 2026-09-12T16:42:03Z`，距今 `263.9h`）。**本期不新增问题。**

那条老问题本期的材料：**这份报告本期漏报了一次真实故障，而漏报是它的量具按构造决定的**；**我能写出来，只因为有一条采样之外的通知。** **一份报告该不该承认「我这套指标对某一类事恒为盲」，归你。**

**已发的九件仍等。**

## 口径（本期新增一条）

- **第九十条：当一类故障的持续时间小于或接近你的采样周期，任何基于采样的阈值都测不到它。** **「我的痕迹里没有异常」对这一类恒为真，与它是否发生无关。要测它，只能靠采样之外的通道。**

## 顺带检查

- **生产 `3e50e5b`、整合支 `09256fd` 均未变**，后者仍不含 `t-280`；**八件已验交付仍坐在 `claude/backend-development-gzqbjf` 上，等 human。**
- **焦点 `2026-09-22T02:41Z` 版未变**：本期是它的第十九个整窗。**其第 ⑥ 节的重启账本期更新为：通知 8 次，真 1 次、假 7 次；另有 1 次真重启没有通知。**
- **服务端无降级**：本窗 118 轮，中位 `61` 秒、最大 `72` 秒（成因＝本期那次重启，已实测）。
- **给 human 的六张卡，本期仍一张没点**（`human last_pull 2026-09-13T14:40:43Z`）。
- **在场名单**（时刻，非年龄；本期七个角色一个没动）：
```
pm        在听                            ← 唯一
dev       last_pull 2026-09-22T00:52:29Z   last_event 2026-09-21T04:54:12Z
qa        last_pull 2026-09-19T17:08:43Z   last_event 2026-09-19T13:36:36Z
human     last_pull 2026-09-13T14:40:43Z   last_event 2026-09-07T03:36:48Z
release   last_pull 2026-09-13T14:09:36Z   last_event 2026-09-13T14:19:35Z
frontend  last_pull 2026-09-13T01:17:15Z   last_event 2026-09-13T01:32:14Z
pd        last_pull 2026-09-12T16:42:03Z   last_event 2026-09-12T16:44:18Z
```

---

*本份在窗口边界上取数，窗口内 3 条。*
