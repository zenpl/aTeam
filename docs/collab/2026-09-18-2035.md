# 协作顺滑度报告 · 第一百二十一份

窗口 `2026-09-18T18:35Z → 20:35Z`。上一份：`docs/collab/2026-09-18-1835.md`（`9251483`）。

## 一句话

**两个节点按预测各自重启（dev 差 32 秒、qa 差 91 秒），而这一小时真正的产出不是那两个数——是三条关于「怎么量」的规矩，其中一条推翻了我，一条推翻了我对我自己的推翻。**

## 指标

| | 本期 | 上期 | |
|---|---|---|---|
| **节奏** | **18 条**（dev 7／pm 6／qa 4／服务 1） | 2 条 | pm 占比 **33%** |
| **指令与确认** | 发 3、ack 3 | 0 | 给 `dev` 三条：**24／23／23 秒** |
| **任务与验收** | 建 0、交 0、判 0 | 同 | 队列 5 件，**全部要生产表面，0 件可动** |
| **摩擦** | **0** | 0 | 本期来回全是量法上的收窄 |
| **人的负担** | 卡 6 张，一张没点 | 同 | human 缺席约 **6 天** |
| **pm 未动欠账** | **178 → 173** | 178 | 五条，**每条核过去处**（见发现四） |

## 结构性发现

### 一、两个预测都命中，而一个猜想在第一个数上就翻了两次

`dev` `19:38:27.144`（预测 `19:38:59` 前后，**差 32 秒**）、`qa` `19:50:18`（预测 `19:51:49`，**差 91 秒**）。

`dev` 照约定把 `date -u` 作为字面第一个动作读了（`T_wake`）。**我随即算出它这次的写入延迟 ＝ 32.4 秒，与 `qa` 的 49／33／41 同一档，于是我说「我那个猜想第一个数就不支持」。**

**`dev` 把这句也驳了，它对**：那 32.4 秒来自它六次重启里**做得最少的那一次**（它把六次「醒来到落库之间做了什么」逐条摆出，从「查两游标就写」到「读完一整轮想明白才写」，差出好几倍）。**一个点立得下这一列的下界，立不了它的离散度。**

**所以我从「猜想解释了它」一步跳到「猜想死了」，用的是一个两边都撑不住的点。** 这与我这周记过的那几次是同一族，**只是方向反了**：那几次是没核就断言，这次是没核就撤回。

### 二、按结论枚举天然漏格

`dev` 把判别式写在数据到齐之前（W＝`T_wake` 极差，N＝`note→note` 极差），三种结果各配一句结论。**我补了它没枚举的第四格**（`W 大而 N 小`）——预注册的价值全在穷尽，漏掉的那一格恰恰是「事后怎么解释都行」的那一格。

`dev` 补上后自己提炼出比结论更该留的一句：

> **那三条是顺着「猜想成立／不成立／都不需要」想的——那是按结论枚举，不是按数据枚举。按结论枚举天然漏格，因为它只列我想得到的解释；按数据枚举（两个量各两档，四格）才封得住。**

第一个能用的 `T_wake→T_wake` 间隔要等 `09-19 08:3x`，第二个还要再一轮。**这条线今晚收不了口，也不该收。**

### 三、一个数被更正之后，要回去看它此前喂过谁

`qa` 交了第三个点，并问：三段彼此差不到两分钟（自读间隔极差 **75 秒**），而 `dev` `09-17` 预的中心 `12.5` 与实测中心 **12.9534** 差 **27.2 分钟**——离散这么小、偏差这么大，不像噪声。

**它问得准，而答案是量纲**：那个 `12.5` 取自 `qa` 的**沉默间隔**，量的是「最后一条事件 → 再次写字」，不是「容器起来 → 容器起来」。**我 `09-17 05:01` 更正过这一点，`dev` 也认过——而那个数是在更正之前取的，一直没被换掉。**

> **一个数被更正之后，要回去看它此前喂过谁。** 那个 `12.5` 被两个人各自认过一次「量纲错了」，**而它照旧活在预言里 27 个小时**——因为更正改的是说法，没人回去换那个数。

### 四、`t-281` 第一次真实使用，以及我差点清错一整桶

`qa` 用它新加的那条出路一次了结十条（`191 → 181`），**每条逐字写明怎么了结**，并顺带在真路上证了 `t-197`（多 `--refs` 全存）在它那支是好的。**这十条此前没有任何其他办法能让它们消失**——`ack` 会被拒，写「不办」是假话。

我照做，清了五条（`178 → 173`）。**而做之前差点清错一整桶**：我第一次按 `didAct` 口径自己算得 **660**，列出的「最老十条」全是 `09-06` 的——**忘了 `t-193` 的起算点**。补上（`2026-09-07T09:03:08.880Z`，由 `batch.14` 那条含 `t-147` 的读数按 `ruleLiveAt` 的定义算出）之后得 **178**，与 `sync` 逐字相同，旁桶另有 **482** 条。

**只清五条而不是十条**：`qa` 那十条是纯回执；我这五条每条都要回去核任务此刻状态与读数是否被取代（查了 `t-219`／`t-222`／`t-223`／`t-070` 与一条读数的全部版本）。**五条核过的，比十条凭印象写的值钱。**

**顺带记一次只差一眼的事故**：我用 `curl` 打 `/owed` 想取全量，脚本写的是 `d.get('untouched') or []`，**打印出「untouched: 0」**。真实响应是 `{"error":"not found"}`——那条路在生产上没上线。**一个错误响应和一份空列表，在那段十行脚本里长得一模一样**，而这正是我在自己看守里修过的洞二。**只因为我多看了一眼原始 body 才没把「0」当成事实。**

## 与 product.md 的对照

| | 本期 |
|---|---|
| **T3** | 给 `dev` 三条：24／23／23 秒 |
| **T11** | friction **0**；本期的产出是量法规矩，不是工具缺陷 |
| **T10**（不被自己淹没） | `t-281` 那条出路第一次真实使用，`qa` −10、pm −5。**这一格此前没有任何可用动作** |
| **T8** | 两个节点按预测各自重启、各自自查恢复，**无人介入** |
| **O2** | 服务预警**第十四次逐字相同**；5 件里可动 **0** |

## 分配预警

| 模式 | 本期 | 数字 |
|---|---|---|
| **重叠** | 无 | 本期无任务、无接缝，人工裁决 **0** |
| **低效** | 有 | 服务预警十四次逐字相同，有效指向率 **0/5** |
| **无效上下文移交** | 无 | `qa` 与 `dev` 都直发，中间环节 **0** |
| **打破独立审核** | 无 | 本期无判据、无验收 |
| **负载陷阱** | **不在队里** | pm 占比 33%；`dev`／`qa` 队列各 **0**；瓶颈全在人那一侧 |

## pm 自查

| | 本期 | 目标 | |
|---|---|---|---|
| **自我更正率** | **33%**（6 条里 2 条） | ≤10% | **未达标** |
| **判据被执行者回问** | **0** | ≤1 | 达标（本期无判据，不值钱，照记） |

两条更正：撤回「猜想第一个数就不支持」（撤得太快）、更正自己算欠账时漏掉起算点。**第二条没有进日志之外的任何地方，因为它在动手之前就被自己拦住了——但它照记。**

## 给 pd 的问题

**Q12**、**Q13** 均仍等，本期无新增。

## 口径

- **撤回一个猜想，和提出它一样，需要够格的证据；一个点能立下界，立不了离散度。**
- **按结论枚举天然漏格；按数据枚举才封得住。**（`dev` 立的。）
- **一个数被更正之后，要回去看它此前喂过谁。**
- **清一笔账之前，先确认自己算的是哪一桶。**
