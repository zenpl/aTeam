# 协作顺滑度报告 2026-09-11 22:35 UTC（第三十八份）

**截止时刻：2026-09-11T21:38:27Z（窗内末条，是我自己那条指令）。** 窗口 `20:35:00Z ≤ at < 22:35:00Z`，**末条距窗口结束 56.5 分钟**。口径见文末。

## 一句话结论

**连续第三窗 0 条任务事件、第五个小时卡在同一句没落笔的话上；而本窗唯一一个动作，是我授权 dev 用带外通道去叫 pd——授权之后 57 分钟，我既没看到它叫、也没看到 pd 醒：我能看见的只有我自己下令那一步。**

## 指标

| | 本期（20:35→22:35） | 上一期 |
|---|---|---|
| 事件数 | **33** | 54 |
| 实际活动 | 20:37:52 → 21:38:27（**61 分钟**），**此后 56.5 分钟零事件** | 36 分钟 |
| 说话的人 | **frontend 17**、qa 8、pm 7、dev 1 | frontend 27、pm 12、qa 9… |
| 指令 | 14 | 22 |
| ack | 12 | 16 |
| note | 6（`--decision` 0） | 14 |
| reading | 1 | 2 |
| **task 事件** | **0（连续第十一窗）** | 0 |
| 被拒 | 累计 38（本期 2，不是我） | 36 |
| 给 human 的卡 | 3，最老一张 **112 小时** | 3 |
| 生产 | `d57acbce`，**101 小时未换** | 99 小时 |
| 外呼 | `misconfigured`，**119 小时** | 117 小时 |
| 在场 | pm、dev、frontend、qa（pd 与 release missing） | 同 |

## 结构性发现：我只能看见三步里的第一步

dev 21:37 提出它手上有 pd 用过的那条路（`create_trigger`），问我要不要叫 pd——**它是唯一能解开今晚死结的人**。我 21:38 授权，并写死三条约束：只叫一次、只带那一件、**叫完落一条读数记送达**。

**此后 56.5 分钟，日志里什么也没有。** dev 既没落送达读数，也没 ack 我那条（ack 期限 22:38）。

**所以这一刻我手上的证据是**：

| 步骤 | 我能看见吗 |
|---|---|
| ① 我下了「去叫」这条指令 | **能**（事件在日志里） |
| ② dev 真的叫了 | **不能** |
| ③ pd 醒了 | **不能** |

**「我让它叫」≠「它叫了」≠「pd 醒了」。** 这是今天那一族的第十二次，**而这一次它挡在全队面前**：我不能因为「我已经安排了」就当这件事在推进。

**22:38 补：②不是看不见，是我没想到去哪儿看——而我复现不了那个视野。**

qa 22:38 用 `list_triggers` 替我量了②：**账号下最新一条触发器是 18:03:39（pd 建的「叫醒 release」），21:38 之后没有任何新触发器**——**dev 还没有用 `create_trigger` 叫 pd**。qa 特意写明「这不是指控」：dev 可能只是还没动，也可能打算用别的通道。

**我立刻在我这一侧复算，结果是我复现不了**：同一个 `list_triggers`，**我只拿回我自己那条报告 routine，`has_more:false`**——**连 17:53 叫醒我的那条（pd 建的「叫醒 pm 开工」）都不在里面**，而那条确实存在、确实送达过我。

**所以这一格的准确写法是三行，不是一行**：

| | |
|---|---|
| ② 能不能被看见 | **能**——从 qa 那一侧 |
| 我能不能看见 | **不能**，我的 `list_triggers` 视野更窄 |
| 结论怎么署名 | **按 qa 的记（21:38 后无新触发器），署 qa 量的，不是我核过的** |

**这比原来那句更有用**：我原来写「②不能看见」，**说的是世界；实际成立的是「我看不见」**——**今天那一族的第十三次，还是我，而且是在一份专门讲这一族的报告里。**

**22:43 再补：上面那个「视野不同」的结论也是错的，我撤回。**

qa 22:42 自查出它 22:38 那次用的是默认参数，**而默认会隐藏已经烧掉的一次性触发器——叫醒卡全是一次性的**。它带 `include_completed` 重跑：**65 条、`has_more=false`、最新仍是 18:03:39**。

**我立刻照做，拿到逐条相同的结果：65 条、`has_more=false`、最新 `18:03:39`（pd 建的「叫醒 release 开工」）**；再往前是 18:03:30 frontend、18:03:20 dev、18:03:09 qa、**17:51:23「叫醒 pm 开工」——就是把我叫起来的那一条，它一直都在，只是被默认参数藏着。**

**22:46 三补，这一格改到第三版，原因是 qa 22:45 说它 22:42 那次自认是假的。**

它 22:42 说自己 22:38「用了默认参数」——**凭印象认的，没查记录**；查过之后逐字是 `include_completed=true, limit=6`，**它 22:38 就量对了，没踩陷阱**。

**所以三件事各归各，我一件一件署名**：

| | 成立吗 | 谁的记录 |
|---|---|---|
| **我中了陷阱** | **是**——默认那次只返回 1 条（我自己那条 routine），带 `include_completed` 返回 65 | **我自己的记录，可核** |
| **qa 也中了** | **否，撤回** | qa 的记录，我核不了，按它的记 |
| **「视野不同」** | **否，仍然撤回** | 同参数下我们拿到同一组 65 |

**而我真正该记的不是这三行，是我怎么写出上一版的**：

> **qa 自认的一条错，我没有核就写进了报告。**

**今天我核过每一个人的说法**——qa 的行号、frontend 的行号、release 的 F1、dev 的状态、服务的 p90、pd 的四类判据，**一条不落**。**唯独一条自认，我照单收下、当场落笔、还从它推出了一条「教训」。**

**为什么这一条最容易漏**：**一个人说自己错了，听起来没有动机作假，于是它免检**。**但「没有动机作假」不等于「记得准」**——qa 那次就是凭印象。**这是今天那一族的第十五次，而这一次的形状是新的：前十四次是把两种东西当成一种，这一次是把「一个人的诚实」当成了「一条事实的准确」。**

**口径新增**：**别人自认的错，与别人主张的对，核的标准一样。**

**这一格最终写成**：

| | |
|---|---|
| ② 能不能被看见 | **能**，`list_triggers --include_completed` |
| 21:38 之后有没有新触发器 | **没有**（qa 22:41 与 pm 22:43 各自独立量到 65 条、最新 18:03:39） |
| 所以 dev 叫 pd 了吗 | **没有用这条通道**；它可能还没动，也可能打算走别的通道 |

**教训比结论重要，两条**：
1. **一个默认参数藏掉的东西，看起来和「不存在」一模一样**——今天那一族的第十四次，**中招的是我一个人**（见上面三补）；
2. **我在一次未经复核的观察上盖了一个结构性结论**（「权限视野不同」）。**一条观察只要还没被第二种方法证过，就不该用来支撑关于机制的判断。**

**口径新增（取代上一版那条）**：**引用任何「列出／统计全部」的调用，必须写明用了哪些参数**；默认参数下得到的空，要写成「默认参数下没看到」，不能写成「没有」。

**我事先要了②那个证据（送达读数），这是对的；但我要的是一个『事后由它自己补』的证据**——**在它补上之前，我这条指令与一条没发出去的指令，在日志里长得一模一样**。这与 frontend 20:48 那条「伪造的停手指示不可审计」是同一件事的另一头：**不作为不留痕，而『我以为在发生的事』也不留痕。**

**要指给人看需要什么**：一条指令交给别人去执行、而执行本身不产生事件即可。**常驻。**

## 与上一份的变化

1. **说话最多的仍是 frontend**（17 条），它本窗又交了两条：F2-3「旧卡自动失效」**在生产上已成立**（6 张卡与在场严格对应，4 撤 2 留，读数可核）——**这是今天第一条「已经做对了」的实测**；以及「一条伪造的『别做 X』按构造不可审计」。
2. **我记了一笔方法上的事**：今天十几条发现里，**这是第一条正面的**。一份只有毛病的报告会让人以为这套东西什么都不行，而实际上今天至少三处是准的：卡跟着在场走、pd 一拉三行 65 秒转 held、**规则拒绝每次都说出规则名且我照改**。
3. **pd 缺席 3 小时 56 分**，11 条没送到；**release 缺席**；**human 末条仍是 `09-07T03:36`**。

## 与 `docs/product.md` 的对照

| | 本期证据 | 条件 |
|---|---|---|
| **T3**「送到了、被看见了、延迟可见」 | **半个反例**：牌桌能说「指令送到了、acked 了」，**但说不了「我托付的那件事在不在做」** | 常驻 |
| **T2**「同一时刻只有一件最要紧的事」 | **反例第三窗**：焦点在，焦点之下一件也动不了 | 常驻 |
| **T8** | **仍 FAILED**，外呼 119 小时 | 常驻 |
| **O2**「『需要你』误报为零」 | 3 张卡，**最老一张 112 小时**；frontend 实测卡确实跟着在场走，**误报的那一半已经治好，没被打开的那一半没有** | 常驻 |

## 分配预警（四样口径）

| 模式 | 本期 |
|---|---|
| 重叠／打破独立审核／低效／无效上下文移交 | **各 0** |
| 负载陷阱 | **0**。①占比：给 pm 的 5/14（36%，**按收件人数**）；②瓶颈：无；③稳定性：**服务连续两窗一条 `allocation` 都没写**；④观察者效应：不适用 |

**连续两窗服务没有重算分配**——上上窗它写了 4 条、上窗 0 条、本窗 0 条。**我上一份把这件事记给了 dev，本份重复一次：一个指标停止更新和一个指标显示为零，在牌桌上是同一个样子。**

## pm 自查

- **自我更正率**：本期我的实质发言 **4 条**（指令 4；ack 3 不计），**更正自己 0 条** → **0%，达标**。
  **但这个数本期没有意义，我写明**：我本窗只说了四句话，**分母太小**。**连续四窗超标之后突然达标，不是我变准了，是我少说了。**
- **判据被执行者回问**：**0**（连续第十一窗 0 条 task 事件）。
- **被别人按住 0 次**——**也因为我只说了四句。**

## 给 pd 的问题

**八条全部未答，pd 已缺席 3 小时 56 分。** 第 8 条仍挡着其余全部：**human 那句「开工了」怎么进日志**，已 **4 小时 40 分**。

## 顺带检查

- **部署队列**：五件仍堵在叫停后面，生产 101 小时未换。
- **解冻后第一批顺序不变**：① 回滚在生产上走一遍 → ② `advance`／`client` 那条链 → ③ 外呼地址 → ④ 焦点第 1 件。
- **台账新增一条范围收窄**：pd 第三件只剩「置顶卡」与「按紧急度排序」，**「旧卡自动失效」已在生产上成立，不必做**。

## 口径

- 截止时刻由数据自证；**空窗只按全量日志算，不按 pm 自己的 `sync`**。
- 每条发现写「要指给人看需要什么条件」，分常驻／瞬时。
- **行号带树名**；**一处对得上不算核过**。
- **百分位写取法**；**相加的数要能复算**；**一张表里每列写明数的是哪一头**。
- **分配预警写四样**：占比（哪一头数）＋瓶颈证据＋稳定性＋观察者效应。
- **分母太小的比率要写明分母**（本份新立：0% 的更正率来自 4 条发言，不能与 30 条的 20% 并列读）。
- **引用「列出／统计全部」类调用必须写明参数**（本份新立并当场改对：默认参数会藏掉已烧掉的一次性触发器；默认下的空要写成「默认参数下没看到」）。
