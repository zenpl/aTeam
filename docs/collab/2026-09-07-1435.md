# 协作顺滑度报告 2026-09-07 14:35 UTC（第十一份）

**窗口** 12:36 → 14:35。上一份 12:35（4ec88f5）。数据截至 14:15，两处「待补」补到 14:22。
**给谁看** pd 的判断材料：这段时间里协作在哪儿顺、在哪儿卡、代价落在谁身上。

---

## 一句话

**这是今晚最顺的一期——21 次判决全部通过，一次没红；四条队列各自见底过一次；十二件工具从提出到验完平均不到一小时。**

**然后 14:12 那次上线把尺子换了，我的欠账从 3 条跳到 37 条。**

顺，有一半是因为我们一直在用一把松的尺子量自己。t-193 把「办了」的口径从「我签收过」改成「有一条真引用它的事件」，上线那一分钟，全队的旧账一次现形。

---

## 数字（每个后面跟着怎么量的）

| | 本期 | 怎么量的 |
|---|---|---|
| 事件 | 311 | 日志里 `at ≥ 12:36` 的全部事件 |
| 各人事件数 | dev 93 · qa 82 · pm 80 · frontend 43 · release 12 | 同上按 actor 分 |
| 日志字节占比 | qa 35% · dev 29% · **pm 20%** · frontend 11% · release 4% | 事件 JSON 序列化后的字节数 |
| 新建任务 | **12 件，全部由 pm 建** | `op=create`，按 actor 计 |
| 交活 | 16 件 | `op=done` |
| 判决 | **21 通过 / 0 不通过** | `op=verify` |
| 其中在生产表面 | 5 件 | `surface=production` |
| 上线 | 1 次（第 18 批，14:12，39353bf，11 件） | `production:deployed.sha` |
| 交活时说得出对人的影响 | **28 / 28（100%）** | `create` 与 `done` 事件里 `shows` 17 条 + `no_human_impact` 11 条 |
| 放松一道闸的任务带「不许因此多漏」 | **3 / 3** | t-198 判据 2、t-201 判据 3、t-202 判据 4，逐条读出来的，名单在此可复核 |
| 交付后追加判据 | 1（t-196） | 三条 `op=criteria` 里，t-162 早于交活、t-207 在建件时、t-196 晚于交活 |
| 冻结的人可见句子 | 350 | `SECOND_HOME_FROZEN`，读 2a102e4 的 sayings.ts（14:09）。前值 351（f163113）、361（frontend 12:47） |

**两个数不许和上一期直接比**：冻结句数在 t-185 那次口径变更后从「3 个文件」扩到全量，旧的 173/169 与今天的 350 不是同一把尺子量的；「已验/在生产验过」现在是 187/70，而 12:35 那期我引用过的「42 件」分母已被 qa 收回（它来自一条漏写了一整类的事实，见 t-203）。

---

## 三件请 pd 看的

### 一、一句从来没说出过话的实话，今天说了第一句

t-149 那道「闸自己报出自己有多少毛病」的机制，上生产验过之后**一个字都没产出过**。qa 14:05 量出卡点：它要一条 `project:gate.seam.fix` 读数指出「修法在哪一件」，而**整份日志里这条读数一条都没有**。补上指针（14:06，指 t-160）之后，句子当场出现：

> 这道闸至今报过 441 条，13 条经核对，其中 3 条是误报、1 条还漏报了，另有 428 条没人判过；修法在 t-160，已验（repo），还没上生产。据它下的结论，先自己核一遍。

**428 / 441 从没有人判过。** 一道我们据以挡住合并的闸，被核对过的结论占 3%。

这句话的读者只有两处：牌桌页面的挖层，和这份报告——默认的 `ateam board` 不带它，只有 `--full` 有。那是 t-149 判据 3 的设计（不上首屏），不是缺陷；但它意味着**每回合跑 board 的人不会看见它**。

### 二、我用一条自己下的规矩，把 11 件已验的活挡了 80 分钟

第 18 次发车窗口空过。release 从最小到最大逐个测切点，全部含 `d995a24`：11 件已验的（最早 t-193，11:54 就验过）全被 t-196 一件挡着。它不肯 cherry-pick，理由对——摘出来就是谁也没验过的新 sha。

而 t-196 卡在判据 2 上，那条判据要一句只有 pd 能写的话。**我 11:17 冻结人可见的字，比 dev 11:33 交活早 16 分钟。** dev 做了数据那一半、明说另一半没做、停在措辞那一步，全对。是我让那条判据在交活的那一刻就已经做不完了。

解法是我加了一条判据 6 写清完成边界（后加的，责任写在我头上），把那句话移到 t-207，请 qa 判——并明说判 fail 我也认。qa 13:28 判了 pass，14:12 那批就发了车。

**给 pd 的问题**：冻结是我下的，不是你下的。它今天挡住的东西比它挡住的错误多。你回来时要不要给它一个例外形状（例如「实现者可以写一句临时的，牌桌标注『等 pd 定稿』」），还是维持「谁都不替它写」？

### 三、本期十二件新任务，只有一件是人看得见的

t-198…t-209，**全部由我一个人建**，其中只有 t-202（那张叫人去起一个正在交活的节点的卡）是人能指着看的。说明书那句「tooling 永不越过场景；一批全是 tooling 就说明哪里不对」此刻正指着我。

我 13:16 给自己定了一条：pd 回来之前不再建新的 tooling 件，除非是人首屏上的假话。**我随后破了三次例**（t-208 闸有洞、t-209 孤儿提交没人验、t-204 地基级），每次都在指令里写明是例外与理由。三次都是服务端或 qa 把路指到「建新件」这一条上的——**这说明那条自律的形状不对：它该限的是我自己起意的件，不是别人量出来的洞。**

---

## 那个数：问出来了，对上了

报告初稿写的是「牌桌说我欠 37 条，我自己算得 1 条，差 36，不猜」。dev 14:20 给了规则，重算之后**对上了，一条不差**：

| 算法 | 结果 |
|---|---|
| 我原来的（把光秃秃的 ack 算成办了） | 1 |
| t-193 的规则（**ack 不算办了**，只认 refs 或正文里出现它的 ULID，只扫 note/instruction 正文） | **520** |
| 再加上 dev 判据 7 的起算点（09:02，ack 退役上线那一刻） | **37**，旁桶 483 |

**37 = 520 − 483。** 牌桌那个数是对的，错的是我。而且错得正是这件事本身要修的那一点：**我把「我签收过」当成了「我办了」**——t-193 改的就是这个，我却用旧口径去核它的新数。

两件值得记：

- **qa 11:28 预测过 pm 518，实际 520。** 它当时是拿这个预测去判 t-193 fail 的（判据 7：树上没有旁桶也没有起算点），预测准到 2 条以内。
- **判据 7 是我在 dev 交活后一分钟加的**——今晚我为「后加判据」记了自己一整笔账，而**这一条是它救回来的一次**：没有它，每个人一睁眼看到的是五百多条的墙；有它，看到的是 37 条真正属于新口径的欠账，旧账在旁桶里等着，不假装不存在也不砸人。后加判据本身不是错，错的是我通常加得太晚、又不标注。

**仍待补（14:15 之后）**：qa 14:16 起把自己变成真 deaf 七分钟，在生产上验 t-202 那一半——唯一不靠模拟的验法，结果落在 t-202 的生产判决里。

---

## 我自己的账

| | 次数 | 说明 |
|---|---|---|
| 编造不存在的指令 id | **6** | 最后一次在我建完「修这个坑」那件任务（t-206）之后第 5 分钟。改法不是「记得」，是「先跑取 id 的命令再写 refs」 |
| 把缺了一类的名单当全集 | **3** | 接缝数说成 142（真数 441）、把 3.4 小时前的旧卡当成对当下的描述、引用了 qa 那个漏掉一整桶的分母 |
| 后加判据 | 1 | t-196，是我制造的死结的补救 |
| 自陈利益冲突 | 1 | t-158 的判据是我写的，让四条判据成立的那个动作（补指针）也是我做的；done 交给 dev、并明说 qa 判 fail 我认 |
| 白杀一个好好的监听 | 1 | 把「没收到通知」读成「监听坏了」，实际是事件排在我自己那一轮长回合后面 |

**三次「分母错」是同一个形状**：分母取自一份「有记录的」名单，而真正的全集是算出来的。qa 的 42 件、我的 142 条、release 那条写漏一桶的事实，都是这一种。t-203 与 t-209 就是修它的两件。

---

## 这一期真正变好的一件事

**「两套算法合成一套」成了会被主动用的手法，而不是事后补救。**

- t-180 把牌桌与命令行两套 `ago` 合成一把梯子放进 core；
- t-204 遇到两个抽字面量的扫描器（一个靠引号配对、会被模板串嵌引号错位；一个是逐字符词法），dev **没有去修有洞的那一份**，而是把没洞的那份抽成 `literals()` 让两边都调它——不是把洞挪个位置，是把没洞的那份变成唯一一份；
- t-199 我裁的是反面：「记法可以两种，算法只许一套」——紧凑记法给 agent 看，可以留 `30s/1.5h`，但分档取整必须来自同一段代码。

**qa 核这件的方法同样值得留**：dev 说「另一把扫描器从来没有这个洞」，qa 既没有信、也没有重新推理，而是把 `a134fcc~1` 的 sayings.ts 调出来读——**在改动之前的那一版上确认它本来就没洞**。要证伪「洞被挪了位置」，只能看改动前的那一版。

---

## pd 回来时的队列（谁都没替它写）

1. **t-207**：「那条不是我做的」怎么对人说——牌桌两条并排的措辞、印在哪一段、要不要带 reason；同一件里还有 HELP/docs/手册里 `disown` 的三句（做出来了没人找得到）。
2. **t-200**：「已经过去」那句新话——三把梯子对负数会自信地说反话。
3. **t-162 剩下的 21 处里的 B/C 分类**，以及那一对拼法冲突（同一状态 CLI 拼成一句、页面分成两句，一个出处给不出两种拼法）。
4. **t-132**：staging 与生产共用不共用一条日志（判据 2），以及 staging 上验过怎么和生产分得开（判据 4）。
5. **决策待定**：表面对 pass 独立、对 fail 不独立——任一表面一次 fail 就把整件打回 failed。默认保持现状（staging 没上线，没有强制期限），但 t-034 今天已经为它付过一次两小时的账。
6. **冻结本身**（见上文第二件）。
7. 三张卡回来后的措辞要在生产上真看一眼——你自己说的，我们只准备现场。

---

## 一句给人的

今晚这台机器跑得很顺，顺到我需要提醒自己：**它这两个小时里做的十二件事，十一件是给自己修工具。**
