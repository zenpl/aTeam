# 协作顺滑度报告 · 第一百二十九份

窗口 `2026-09-19T10:35Z → 12:35Z`。上一份：`docs/collab/2026-09-19-1035.md`（`0b91342`）。

## 一句话

**三条事件，今晚最少；而取数时发现一条针对我的预警被我自己漏掉了——我 `09:1x` 定下「不逐条读那条重复的假警报」，而这条新的装在同一条读数里。我定的降噪规矩，把同一个信封里的信号一起滤掉了。**

## 指标

| | 本期 | 上期 | |
|---|---|---|---|
| **节奏** | **3 条**（pm 2／服务 1） | 5 条 | `dev` 0、`qa` 0，**三者都在听**；今晚最少的一期 |
| **指令与确认** | 发 1、ack 0、未确认 1 | 发 1、ack 0 | 唯一那条是给 `pd` 的报告交接；`pd` 缺席约 **164 小时** |
| **任务与验收** | 建 0、交 0、判 0 | 同 | 八件全验，待办清空 |
| **摩擦** | **0** | 0 | |
| **人的负担** | 卡 **6 张**，一张没点 | 同 | human 缺席约 **6 天** |

## 结构性发现

### 一、我自己定的降噪规矩，滤掉了一条针对我的预警

**经过**：`09:05` 起，服务那条「`qa` ack 延迟」预警每 7 分钟重复一次。我 `09:1x` 定下**不再逐条去读它**，理由写在第一百二十八份里：「一条已知会重复的假警报，每次都去看，就成了它的第二重代价。」**这个理由此刻仍然成立。**

**代价**：`09:12:52` 那条读数里 `count: 2`，**第二条是一条我从没见过的新预警，而且是针对我的**：
```
[负载陷阱] pm 15% 的指令是自己写错后更正的（2/13）
           另有 0 条是情况变了才重发的、4 条说不好，都不计入
```
**我今天取序列才看见它，晚了三个半小时。**

**这是今晚那一族的又一例，而这次的过滤器是我自己当天现定的**：
```
qa 的看守     只认 INSTRUCTION，漏掉 task done          六天
dev 的看守    不认 REOPENED / criteria                  数天
我的 report.py 丢掉 pm 署名的事件                        一整晚
我 09:1x 的规矩 「不读那条重复的」→ 同一封里的新的一起漏  三个半小时
```
**共同点仍是那条：一个过滤器漏掉了什么，它自己不会说。** 而这次更薄一层——**我不是忘了检查，我是刚刚亲手把检查关掉的，并且为此写了一条听起来很对的理由。**

**修法，即刻生效**：**不逐条读「同一条」预警，改为逐条读「预警的条数」**。`count` 变了就去看全文；`count` 不变且文本相同才跳过。**代价几乎为零，而它正是我原先那条规矩缺的那一格。**

### 二、那条假警报自己停了，而它停的方式是 Q15 的一半答案

```
07:05   p90 30   其他中位 4
09:05   p90 35   其他中位 5
09:12   p90 35   其他中位 4
09:19   p90 35   其他中位 0      ← 最难看的一格
11:19   预警换回「等验队列 5 件」  ← 它停了
```
**没有人做任何事，它自己消失了**——那条旧样本终于滑出了统计窗口。

**所以 Q15 的答案有一半是「它会自己好」**，但那一半不推翻问题：
· **它用了约 7 小时**，期间报了至少 4 次；
· **它在消失之前先变得最难看**（对比基准塌到 0）；
· **而在那 7 小时里，任何一个看牌桌的人读到的都是「35 分钟」，不是「一件早上就办完的事」。**

**我把这个新事实补给 `pd`，它使 Q15 更具体，而不是取消它**：问的不再是「要不要有『已自查』这一档」，而是**「一条会自己过期的预警，在它过期之前的那几小时里，牌桌该怎么说它」**。

## 与 `product.md` 的对照

| | 本期 |
|---|---|
| **T1** | 无认知分歧；本期唯一的认知工作是发现我自己的过滤器漏了什么 |
| **T3** | 无在场者之间的往来可量 |
| **T5** | 无判据、无验收 |
| **T8** | 三节点均无活、均未造活 |
| **T11** | friction **0** |
| **O2** | 服务预警本期 1 条（「等验队列 5 件」，第二十一次逐字相同）；**而上期那条针对我的「负载陷阱」是我漏读的，本期补记** |

## 分配预警

| 模式 | 本期 | 数字 |
|---|---|---|
| **重叠** | 无 | 接缝 **0**，任务事件 **0** |
| **低效** | 有 | 「等验队列 5 件」第二十一次逐字相同，有效指向率 **0/5** |
| **无效上下文移交** | 无 | 点名给我的 **0** 条 |
| **打破独立审核** | 无 | 本期无判据、无验收 |
| **负载陷阱** | **有，服务报的是我，而我漏读了三个半小时** | **「pm 15% 的指令是自己写错后更正的（2/13）」**（`09:12`）。**这个数与我自己的自查是两把尺**：我算的是「更正事件 ÷ pm 全部事件」，它算的是「更正性指令 ÷ pm 全部指令」。**两把尺此刻方向一致**——我 `04:35` 那期自查 14.3% 未达标，它这里 15%。**一个外部的、我没参与构造的数，给了同一个结论。** |

## pm 自查

| | 本期 | 目标 | |
|---|---|---|---|
| **自我更正率** | **0%**（2 条里 0 条） | ≤10% | 达标——**分母 2，不含信息量，连续第三期如此** |
| **判据被执行者回问** | **0** | ≤1 | 达标（本期无判据） |

**但本期真正该记在这一节的不是那两个 0**，而是上面那条：**我漏读了一条针对我的预警三个半小时，成因是我自己当天定的降噪规矩。** 这不在两条软门槛里，**而它比那两个 0 说明更多**。

**另记一件本期做对的**：上一期我说「重挂看守会在轮次序列里留下一个短间隔（进程边界，不是服务抖动）」，本期两次重挂都**把首轮单标、不并进序列**——**同一条序列里混着两种事件，就会时不时冒出一个不该解释的数。**

## 给 pd 的问题

**Q12、Q13、Q14 仍等**（缺席约 164 小时）。

**Q15 有新事实，问法要改**（`10:37` 已发原版）：那条预警**自己停了，用了约 7 小时，且在消失前先变得最难看**。所以问的不再是「要不要有『已自查』这一档」，而是：
> **一条会自己过期的预警，在它过期之前的那几小时里，牌桌该怎么说它？**

**另两条已发的仍等**：生产卡按两栏改的提案（`04:48`）、`t-114` 那句接缝措辞（`dev` `09-16` 提出，已 3 天）。
**空窗规则的缝本期第五次落进去**（只有 pm 与服务在写），按字面仍写全套，刻意写短。

## 口径（本期新增）

- **一条为了降噪而定的规矩，会连同一个信封里的新信号一起滤掉。** 跳过「同一条」时，要读的是**条数**，不是内容。
- **一条会自己过期的假警报，在过期之前往往先走到最难看的一格**——因为使它过期的和使它变难看的，常常是同一件事。

## 顺带检查

- **生产 `3e50e5b` 未变**；整合支 `09256fd` 未变，**且它连 `t-280` 都没有**。
- **八件已验交付**仍坐在 `claude/backend-development-gzqbjf` 上，能推整合支的集合仍是 `{human}`。
- **焦点 `06:39` 版仍成立**，本期未改。
- **服务端**：本期无降级；看守四次重挂，每次首轮单标，其余间隔全部 `60–64s`。

---

*本份在窗口边界上写，数据为 `12:35` 一次增量拉取。*
