# 协作顺滑度报告 2026-09-12 06:35 UTC（第四十二份）

**截止时刻：2026-09-12T05:54:10Z（窗内末条，qa 的 ack）。** 窗口 `04:35:00Z ≤ at < 06:35:00Z`，**末条距窗口结束 40.8 分钟**。口径见文末。

## 一句话结论

**这一窗只有 18 条事件、两个人在说话，但其中一件是这五天最好的一件：生产部署的钥匙落进了不该开门的人手里，而它没开——不是被规则拦住，是它自己拦住了自己，理由写得比规则还准。**

## 指标

| | 本期（04:35→06:35） | 上一期 |
|---|---|---|
| 事件数 | **18** | 54 |
| 实际活动 | 04:37:28 → 05:54:10（**76.7 分钟**，且其中 04:43→05:52 有 **69 分钟**空档），此后 40.8 分钟零事件 | 104 分钟 |
| 说话的人 | **只有 pm 11、qa 7**；**dev / frontend / release / pd 各 0** | pm 24、qa 20、release 8 |
| 指令 | 8 | 24 |
| ack | 7 | 15 |
| note | 2（`--decision` 1） | 4 |
| reading | 1（`project:deploy.token.location`） | 9 |
| **task 事件** | **0**——t-225 自 00:13 起仍只有那条 `create`，**已 6 小时 22 分** | 0 |
| 被拒 | 累计 52（本期 2，都是我，都是 280 字超限） | 50 |
| 给 human 的卡 | 3（放行部署那张仍未答） | 3 |
| 生产 | `d57acbce`，**109 小时未换** | 107 小时 |
| 外呼 | `misconfigured`，**127 小时** | 125 小时 |

**各角色最后一次写事件**：dev `00:39:01`（**5 小时 56 分前**）、frontend `00:27:10`（**6 小时 8 分前**）、pd `00:11:24`、release `04:17:18`。

## 结构性发现：捡到钥匙的人没开门

**事实**：qa 05:50 容器重启后，**它的**环境里出现了 `ATEAM_DEPLOY_TOKEN`（它只报存在、不写值，形态是 `github_pat_` 开头的 fine-grained PAT）。**它没用、没做 dry-run，把决定交上来。**

**它给的三条理由，我逐条背书并加重**：

| qa 的理由 | 我加的一层 |
|---|---|
| ① **qa 的 `push` 是 none** | **`push` 不是便利设置，是牌桌上公开的能力声明**，R9（上线）写明归 release。**一个角色拿到另一个角色的钥匙，不等于它拿到那份职责。** 我们昨夜刚写完「声明有、能力没有」；**这一次反过来——能力有、声明没有。两种都要照声明行事，不照能力。** |
| ② **人那张卡默认 B、未答，用了就是替人拍板** | 今晚第二次撞同一处（04:18 qa 刚拦下「默认会把人没做的决定记在他名下」）。**这一次更直接：不是记错一个决定，是直接替他执行那个决定。** |
| ③ **连 dry-run 也不做** | **`--deploy` 的 dry-run 在这套东西里就是一次生产部署动作**——权限层拒 release 时给的原话正是 `Reason: Production Deploy`。**一个被权限层拦下的动作，换个人去试，试的是同一件事。** |

**立成规矩（已落 `--decision`）**：

> **凭据出现在谁的环境里，不改变谁有那份职责。能力来自声明与人的放行，不来自环境变量。谁捡到钥匙，都不开这扇门。**

**为什么说这是五天来最好的一件**：**T5（三方分离）此前每一次生效，都是规则在拦人**——闸拒了写入、判据不许自验、接缝挡住合入。**这一次没有任何规则拦得住它**：钥匙在它手上，权限层不认角色只认会话，它跑一次谁也不会当场知道。**拦住它的是它自己，而且理由是它自己推的，不是背规矩。**

**我也据此没有做一件事**：**没有再改那张卡**。这条新事实不改变卡要问的东西——**那张卡问的从来不是「有没有办法推」，是「要不要推」**。今晚已经换过三次卡，**再换一次就是拿噪音喂人**。

**要指给人看需要什么**：一次容器重启把凭据注入到非 R9 角色的环境里。**此刻成立（qa 侧）。瞬时——下一次重启可能就没有了，而它已被读数钉住。**

## 二、有活、有人在场、没人动

| 角色 | 最后一次写事件 | 手上 | 牌桌显示 |
|---|---|---|---|
| **dev** | `00:39:01`（5 小时 56 分） | **t-225，未认领** | `listening` |
| **frontend** | `00:27:10`（6 小时 8 分） | **名单对照，未 ack** | `listening` |

**两件都是我派的，一件 6 小时 22 分无人认领，一件 2 小时 28 分未 ack。** 我各问过一次，不再问。

**我不作人品判断，也不猜原因**（在写代码没落事件／上下文满了／那次不带 connector 的唤醒少了工具——**三种在牌桌上长得一模一样**）。**能写的只有一句可核的**：

> **牌桌对「在听且在做」与「在听但不动」显示相同。**

这是第四十份那条发现的延续，**而本窗它从「一件活」扩到了「两个角色、两件活、合计近 12 小时」**。

## 三、我本窗更正自己一次，而且是对着人说的

我 04:43 对 human 说「frontend 在做名单对照」——**不成立**。它 `00:27:10` 之后零事件，**我派的那件它连 ack 都没有**。**我是从「我派了」推出「它在做」**——**正是我今天更正过三次、还写进两份报告的那个毛病**（「我让它叫」≠「它叫了」；「我发了」≠「它收到了」）。**第四次，还是我，而且这一次说给人听。**

**我 05:00 当场更正了**，并把它写在这里，不藏进自查节：**一条只说给人听、不落日志的错话，没有任何人能纠正它**——这句我 09-11 就写过，今天又犯一次。

## 与 `docs/product.md` 的对照

| | 本期证据 | 条件 |
|---|---|---|
| **T5**「三方分离」 | **本期最强，且形态是新的**：不是规则拦人，**是持有能力的人自己不使用**。见第一节 | 已成事实 |
| **T1**「事实带来源、时间、假设」 | **正面**：`deploy.token.location` **只记存在、不记值**，`assumes` 写明「qa 自报属实，我这一侧无法复现，也不应复现」 | 已成事实 |
| **T2**「同一时刻只有一件最要紧的事」 | **反例**：第一件已到「等推」，**第三件 6 小时 22 分无人接，牌桌说不出** | 常驻 |
| **T3**「送到了、被看见了」 | **半个反例**：我派给 frontend 的那件**连 ack 都没有**，而它显示 `listening` | 常驻 |
| **T8** | **仍 FAILED**，外呼 127 小时 | 常驻 |

## 分配预警（五样口径）

| 模式 | 本期 |
|---|---|
| 五种模式 | **各 0**。①占比：给 pm 的 4/8（**50%，按收件人数**，分母 8）；②瓶颈证据：**无**，我本期 ack 4 条全部分钟级、待办 0；③稳定性：服务本期未写 `allocation`；④观察者效应：不适用；⑤样本量：**分母只有 8，这个 50% 不足以支撑任何判断** |

**注**：本窗只有两个人说话，**任何占比都是这两个人之间的比例，不是团队的分配**。**按第五样口径，这一格本期应读作「样本不足」而不是「50%」。**

## pm 自查

- **自我更正率**：本期我的实质发言 **7 条**（指令 4＋note 2＋读数 1；ack 4 不计），**更正自己 1 条** → **14.3%**（分母 7）。阈值 ≤10%，**超标**。
  那一条就是第三节：**对 human 说「frontend 在做」**。**我把它单列在正文里而不是只记在这一格**，因为它是对人说的。
- **判据被执行者回问**：**0**。**连续第三窗要加同一句**：两件已派的活，执行者既没回问也没认领——**「0 次回问」和「没人在看这些判据」仍是同一个数。**
- **被别人按住 1 次**（qa 那条更正我的风险形状：不会过期，会被顶掉）；**我纠别人 1 次**（qa 的行号又一次没带树名）。

## 给 pd 的问题

**三件排好未动**（pd 自 `00:11:24` 起零事件，已 6 小时 24 分）：

1. **一张卡的默认，可能把人没做的决定记在他名下**（04:18 差点发生）；
2. 「起一个 X？」对一个「写了但从不拉取」的角色该说什么；
3. t-211 把**「落后」**与**「分叉」**说成一件事，**两者正确动作相反**。

## 顺带检查

- **第一批**：①`09256fd` **合好、测过、待推**（qa 另开干净 worktree 自己跑的测试）；②回滚生产演练被①挡着；③ t-225 **6 小时 22 分无人接，维持挂起**；④名单对照在 frontend 手上**未 ack**。
- **给 human 的 3 张卡**：放行部署那张是唯一挡着全队的；另两张最老一张 **118 小时**。
- **凭据**：按本期裁决，**谁都不动**，直到人在卡上点。

## 口径

- 截止时刻由数据自证；**空窗只按全量日志算**。
- 每条发现写「要指给人看需要什么条件」，分常驻／瞬时；**瞬时的写明窗宽**。
- **行号带树名**；**一处对得上不算核过**。
- **百分位写取法**；**样本量与取法一起写**；**样本不足时写「样本不足」，不写那个比例**（本份据第五样口径实际执行了一次）。
- **相加的数要能复算**；**每列写明数的是哪一头**；**分母太小的比率要写明分母**。
- **引用「列出／统计全部」类调用必须写明参数**。
- **别人自认的错，与别人主张的对，核的标准一样。**
- **凡我转述别人的话，先说我这一侧截没截。**
- **引用别人的结论时，要连它成立的那个对象一起带。**
- **只说给人听、不落日志的话，也要在报告里留痕**（本份新立，因第三节）。
