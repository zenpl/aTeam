# 第六十四份：容器重启了一次，而这是本期唯一真正发生的事

窗口 `2026-09-14T00:35Z → 02:35Z`。**数据截至 `02:25Z`。**

**这一份按第六十三份的办法写短**：本期三条事件，**其中两条是我自己的杂务记录**（划掉一条被拒的写入、记下容器重启），**一条是服务的 `allocation` 读数**。**没有人做事、没有判决、没有交付。** 用八节常规结构描述这些，正是我连着四次记成 `T10` 反例的那件事。

**我第六次落进 pd 那条「在场但不动」没覆盖的情形，也是第六次自己临时决定怎么写。只记次数：第六次。**

---

## 本期唯一真正发生的事：容器重启，我有过一段没有看守的空窗

**实情**：harness 报容器重启，我两个后台任务一起没了——**看守**（`newtells.sh`）与**本报告的闹钟**。`sync.trail` 最后写到 `01:32`、`watch.lock` 停在 `01:10`，**所以没看守的那一段大致是 `01:10 → 01:33`，二十来分钟。**

**什么没丢，我逐项核了，不是假设**：
- 仓库 `claude/pm-role-s3ryk7` 在 `67b949a`（第六十三份那一版），**工作区干净**；
- **游标 `.ateam/cursor.pm` 完好**（`01M2ENB4QF1RT7NRRS7WVTT5HE`）；
- `bin/ateam` 跑得动；脚本、高水位、trail 都在。

**所以那二十分钟丢的不是消息，是及时性。** 游标是持久的，`sync` 一跑就补上缺口——**我跑了，`nothing new`。没有任何一条点名给我的指令落在那个缝里。**

**它与我今天立的一条口径是同一个洞的两头**：
- 今天那条是「**重挂看守之后要确认它真接上了**」，起因是**「我以为我挂上了」与「我真的挂上了」长得一样**。
- 容器重启是另一头：**「我挂上过」与「它此刻还活着」也长得一样**——而这一次连 Monitor 都没来得及报超时，**是 harness 替我报的**。

> **唯一真正兜底的，是那个持久游标加一次 `sync`。看守只决定我多快知道，不决定我知不知道。**

**已重挂并按 pid 核过**（单实例），闹钟已重建。

---

## 指标（数据截至 `02:25Z`）

| | 本期 `00:35→02:25` | 上期 `22:35→00:35` |
|---|---|---|
| 事件总数 | **3**（pm 2／ateam 1） | 1 |
| 指令／任务／交活／判决 | **0／0／0／0** | 0／0／0／0 |
| 最大静默 | **46.9 分钟** | — |
| 生产 | **`3e50e5b`**（`02:25:05Z` 实测，`14:11` 回滚后第十一次实测没动） | 同 |
| 人看得见的卡 | **6**，一张没被点，标题第五次复查仍干净 | 6 |

**在场（`02:25:05Z`）**：`pm`／`dev`／`qa` `listening`；`pd`／`frontend`／`release` `missing`。

---

## 谁在等谁

- **五件 `done` 等 `qa` 判**（`t-256`／`t-259`／`t-260`／`t-261`／`t-263`）。**上一次判决 `09-13T11:58`——距此刻 14 小时 27 分。**
- **`qa`** 最后一条事件 `09-13T20:44`，**5 小时 41 分**。队里八条未 ack。
- **`dev`** 最后一条 `00:33`（补完 57 条 ack 并交出那三条读数之后），**按停手令静止 112 分钟**。
- **`release`** missing **12 小时 5 分**，生产仍卡在它做了一半的演练上。
- **人** `last_pull` 停在 `09-12T22:41`，**约 27 小时 45 分**。
- **`t-267`** 没人认领（基线已定：`events.count = 9,959`）。

---

## 各节：空，以及为什么空

- **结构性发现**：**本期唯一的是上面那条容器重启。** 其余无。
- **与 `product.md` 的对照**：`T8`（少了谁都不停）**连续第七期是反例**。其余各格本期无数据。
- **分配预警**：五种模式**全部 0**；**「打破独立审核」与「负载陷阱」为 0 是因为没人做事**，不是守得好。服务那格仍报「等验队列 9 件」，**连续第六期逐字相同**。
- **pm 自查**：分母 **2**（两条 note），**更正 0 条**。**0/2，不构成趋势**——连续五期分母个位数或零。
- **给 pd 的问题**：**本期不新增**，前面六条仍挂着。

---

## 口径（本期新增一条）

1. **【新，全队】看守只决定你多快知道，不决定你知不知道。** 真正兜底的是持久游标加一次 `sync`。**所以任何一次「我可能断过」之后，第一个动作是 `sync`，不是重挂看守**——重挂解决的是下一次，`sync` 解决的是刚才。
2. **【不变，重申】** 重挂之后要按 pid 核它真接上了；引用别人量的数之前先核它旁边那句「所以」；跨表面的数不能相减；时刻不许手打；**先写死触发条件，再让事实来碰它**。
