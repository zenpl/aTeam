# 协作顺滑度报告 · 第一百六十三份

窗口 `2026-09-22T06:35Z → 08:35Z`。上一份：`docs/collab/2026-09-22-0635.md`（`e892f05`，更正追加于 `caced36`）。

## 一句话

**本期五条里四条是我的，而四条里三条在处理上一份的错；唯一一件新的是：本期出现一次 `91` 秒的看守缺口，而它不在任何一次到期点上——上一期我刚把缺口归因于自己的重挂快慢，这一次不是我。**

## 指标

| | 本期 | 上期 |
|---|---|---|
| **节奏** | **5 条**（pm 4、服务 1） | 4 条 |
| **指令与确认** | 发 2（均给 `pd`）、ack 0（收件人缺席） | 发 1、ack 0 |
| **任务与验收** | **建 0、交 0、判 0——已连续 `75h01m`** | 已连续 `73h01m` |
| **摩擦** | 0 | 0 |
| **人的负担** | 卡 6 张，一张没点 | 同 |
| **在场** | **1 人**（只剩 pm） | 1 人 |

## 与上一份的变化

```
任务停了多久      73h01m  →  75h01m        锚点 2026-09-19T05:34:01Z
在场              1 人，无变化              七个角色的 last_pull / last_event 一个没动
看守              117 轮/最大 140s  →  119 轮/最大 91s     四次到期全部当场重挂
                  但那 91s 不在到期点上 —— 性质与上期那 140s 不同，见下
服务预警          第 54 次 → 第 55 次        连续相同 34 → 35 条，跨 68.0h，五件 id 一字未变
上一份的更正      caced36 ＋ note 01M33YE...（supersedes 01M33XBBFC8R6AD09TF1FDR60J）
```

## 结构性发现

### 本期唯一的新事实：一次 91 秒的缺口，而它不是我造成的

上一期我量出「拖一会儿再挂 = 140 秒，当场挂 = 30–32 秒」，并据此立了第七十二条。**本期四次到期全部当场重挂**（`06:39:46`／`07:09:53`／`07:39:59`／`08:10:06`），按上期那条，最大缺口本该落在 30 秒上下。

**实际最大是 91 秒，而它落在 `06:46:58 → 06:48:29`——四个到期点一个都不在这里。**

```
06:39:46   到期 ①  当场重挂
06:42:02   我发出本窗最后一条命令
06:46:58 → 06:48:29    91s   ← 这里。我在这段时间里没有发出任何命令
07:09:53   到期 ②  当场重挂
07:39:59   到期 ③  当场重挂
08:10:06   到期 ④  当场重挂
119 轮，中位 61s
```

**这条修的是上一期那条的适用范围，不是推翻它。** 上期那 140 秒确实是我拖出来的；**但缺口不只有「我拖了」这一种来源**——**看守自己也会慢一拍，而这一次它慢在我完全没动的时段里。**

> **第七十五条：量出一个原因，不等于量出了全部原因。** 上一期我给缺口找到了一个真实的、可复现的成因（重挂快慢），**而那次测量里恰好每一个缺口都是那个成因**——**于是我把「这次全是它」读成了「它就是缺口」。** 本期一个反例就够。

**我不据此建任务**（此刻不造活、无人可验），**也不改看守脚本**——**一次 91 秒、中位仍是 61 秒，不足以支持改动，而改了就没有对照了。** 记下来，等第二例。

### 顺带：上一份那次更正的后续

**更正已落三处**：报告文件（`caced36`，原文一字未改、更正追加在后）、日志（`--supersedes 01M33XBBFC8R6AD09TF1FDR60J`）、`pd`（`01M33XK7A4JG2YXCQ13G34RHZY`）。
**本期那条 `07:20` 的服务预警，五件 id 仍与九天前逐字相同**——**与更正后的机制一致**（`allocation.ts:149` 按 `status` 扫全表，`t-094` 规矩上永远出不去）。**但这不算验证**：旧机制（窗口空）对本期也预测不变，**两者在静止期给出同一个预测，分不开。** 要分开它们，得等有任务真的动起来。

## 与 `product.md` 的对照

| | 本期 |
|---|---|
| **T1 / T3 / T8 / T11** | 无往来、指令 2 ack 0（收件人缺席）、无可评、friction `0` |
| **T5** | 验收资格仍为空；**「无人能验」已持续 `75h01m`** |
| **O1** | 与人之间唯一的通道仍是那一页，**本期无变化** |
| **O2** | 服务预警第 **55** 次逐字相同（连续 35 条、跨 68.0h）；**保证它不变的是 `allocation.ts:149` 的过滤条件，不是窗口**（见上一份更正） |

## 分配预警

| 模式 | 本期 | 数字 |
|---|---|---|
| **重叠** | 无 | 接缝 0、任务事件 0 |
| **低效** | 无 | 假催办 0；累计 **2169** 轮干净（2050 + 本期 119） |
| **无效上下文移交** | 无 | 本期零往来 |
| **打破独立审核** | **结构性风险，已持续 `75h01m`** | `qa`、`dev` 均 missing；**在场 1 人** |
| **负载陷阱** | 无 | 队列深度全 0；pm 本期写入 4 条 |

**「低效」一行按上一份的更正执行**：不再引用服务那条预警作为本期依据，只写我自己算得出的数。**那条预警按构造排不空**（四件等发车、`t-094` 规矩上出不去），**所以它此刻不是一个本期信号。**

## pm 自查

| | 本期 | 目标 | |
|---|---|---|---|
| **自我更正率** | **0%**（0 / 4） | ≤10% | 达标，分母 4 |
| **判据被执行者回问** | **0** | ≤1 | 达标，分母为 0（连续第三十六期） |

**这两格要先说清一条计法，否则会重复计数**：**上一份那次更正，事件落在本窗口（`06:41:48`），但它更正的是第一百六十二份的内容。** 我把它记在**被更正的那一份**上（第一百六十二份已重算为 `1 / 3 = 33%`，超标，写在它的更正附录里），**本期不再计一次。** 计法：**按它更正了哪一份算，不按它写在哪个窗口。**

**另一件记在这里，不进两格**：**本窗四条写入里，三条是在处理上一份的错**（note、tell、更正 note、更正 tell —— 除第一条外全是）。**这本身是一个数：一次发错，代价是下一窗四分之三的产出。**

## 给 pd 的问题

**Q12–Q18 均仍等**（`pd last_pull 2026-09-12T16:42:03Z`，距今 `231.9h`）。

**本期不新增问题。** 上一份那条给你的机制已更正并重发（`01M33XK7A4JG2YXCQ13G34RHZY`），**其中要你定的那件事没变**：指标表里那类「构造上不会变」的行，撤不撤、要不要只留附录，**归你**。

**已发的八件仍等。**

## 口径（本期新增一条）

- **第七十五条：量出一个原因，不等于量出了全部原因。** 上一期我给看守缺口找到一个真实且可复现的成因，**而那次测量里每一个缺口恰好都是它**——于是我把「这次全是它」读成了「它就是缺口」。**本期一个反例就够。**

## 顺带检查

- **生产 `3e50e5b`、整合支 `09256fd` 均未变**，后者仍不含 `t-280`；**八件已验交付仍坐在 `claude/backend-development-gzqbjf` 上，等 human。**
- **焦点 `2026-09-22T02:41Z` 版未变**：本期是它的第三个整窗。
- **服务端无降级**：119 轮，中位 `61` 秒、最大 `91` 秒（成因见上，未归因）。
- **给 human 的六张卡，本期仍一张没点**（`human last_pull 2026-09-13T14:40:43Z`）。
- **在场名单**（时刻，非年龄；本期七个角色一个没动）：
```
pm        last_pull 2026-09-22T08:35Z 前后   ← 唯一在听
dev       last_pull 2026-09-22T00:52:29Z   last_event 2026-09-21T04:54:12Z
qa        last_pull 2026-09-19T17:08:43Z   last_event 2026-09-19T13:36:36Z
human     last_pull 2026-09-13T14:40:43Z   last_event 2026-09-07T03:36:48Z
release   last_pull 2026-09-13T14:09:36Z   last_event 2026-09-13T14:19:35Z
frontend  last_pull 2026-09-13T01:17:15Z   last_event 2026-09-13T01:32:14Z
pd        last_pull 2026-09-12T16:42:03Z   last_event 2026-09-12T16:44:18Z
```

---

*本份在窗口边界上取数，窗口内 5 条。*
