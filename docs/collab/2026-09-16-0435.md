# 第八十九份：队伍在两小时里从零判决走到六件交付、四件判决——而卡口从头到尾只有一个

窗口 `2026-09-16T02:35Z → 04:35Z`。**数据在窗口关闭之后量（`04:35:22Z`）。**

**一句话**：**`qa` `03:05` 回来，把悬了一天的二选一定死了——它是真被咬了，第三次；随后十分钟内判完四件（`t-274`／`t-255`／`t-277`／`t-276`，全 pass），`dev` 复工交了 `t-280` 与 `t-278`；96 条事件，是前四期总和的四倍多。而这两小时证明的恰恰是前四期那句话：卡口从头到尾只有一个，它一回来，全部松开。**

---

## 一、指标

| 指标 | 本期（`02:35→04:35`） | 上一份（`00:35→02:35`） |
|---|---|---|
| 事件总数 | **96** | 4 |
| 按角色 | dev 44／pm 35／**qa 15**／ateam 2 | pm 3／ateam 1 |
| 按种类 | task 31／ack 20／instruction 19／note 16／reading 10 | note 2／instruction 1／reading 1 |
| **判决** | **4，全 pass**（`t-274`／`t-255`／`t-277`／`t-276`） | 0 |
| 交付（`done`） | **2**（`t-280`、`t-278`） | 0 |
| 新建任务 | **1**（`t-280`） | 0 |
| 解除 blocked | **1**（`t-278`，按写死的条件） | 0 |
| 接缝 | **裁 2 条，都 false、都同源**（第七、第八条）；同源累计 **8** | 0 |
| **等验队列** | **9 件**（`repo` 档 4：`t-257`／`t-278`／`t-279`／`t-280`；生产档 5） | 11 |
| 摩擦 | 落 0 条 | 落 0 条 |
| 人那一页 | **6 张，一张没点**，标题第三十次复查干净 | 同 |
| 生产 | `3e50e5b`（回滚后第三十六次实测没动） | 同 |
| **焦点** | `03:15Z` 重写（本期内），窗口关闭时 **1.3 小时**；上一版活了八小时，因为 `qa` 一回来它前半部分整个作废 | `19:16Z` 重写，7.3 小时 |
| 窗口内最长静默 | — | 33.4 分钟 |

**在场**（`04:35:22Z`）：`pm`／`dev`／`qa` 拉取都在一分钟内；`pd` 缺 **83.9h**、`frontend` **75.3h**、`release` **62.4h**、**人未拉日志 61.9h**。

---

## 二、那个悬了一天的二选一，由唯一能定它的人定死了

**`qa` `03:06` 原话**：「容器 `14:27` 后又掉了，`03:05` 才回来。」**沉默 12 小时 39 分，成因是重启，不是在做慢活。** 这是 `t-274` 那个洞第三次咬到同一个人（`09-13`、`09-15` 下午、`09-15` 夜）。

**而它这次没白掉**：它上一次被咬之后给自己立了一条规矩——**重启第一件事不是 `sync`，是列未 ack**——这次一把捞出 8 条。
> **工具没修好、修好的也送不到它手上，而一条不花任何代码的规矩，把代价从「十二小时白掉」压成了「十二小时掉线，回来一次捞干净」。**

**那条自救办法有一个缺口，是我这边造成的**：`t-257` 与 `t-279` 是它掉线期间交的，我因为自己立的「`qa` 回话前不再发指令」**从没发给过它**，所以它捞出 4 件而实际等它判的是 6 件。我 `03:11` 补给它了。
> **一条「不打扰」的规矩加一条「回来自己捞」的规矩，会漏掉缺席期间因为不打扰而没发出的东西。两条单独看都对。** 今天第二次撞见「两条各自正确的规矩合起来出问题」（第一次：「慷慨声明」加「claim 无限期」）。

---

## 三、两条我写死的条件，本期都按字面生效了

**① `09-15 17:50` 那条**：「不再数条数，改成**只要 `repo` 那一档有任何一件拿到判决，就立那件 tooling 并排最前**。」`03:11` `t-274` 判了 pass，`03:13` `t-280` 建出来、`dev` 接手。**从判决到建任务隔了 100 秒，我没有再判断一次。**
**② `t-278` 的解除条件**：「`t-255`／`t-274`／`t-276`／`t-277` 全部有判决」。`03:19` 四件齐了，我**逐条查了 status=verified** 才解除。**这一步是查，不是决定。**

> **把条件写死的好处，这两次看得最清楚：到点时我不必再想一遍，也不能凭当时的心情松紧。** 与之对照的是我 `09-15 05:15` 那条只写阈值不写动作的坏触发。

---

## 四、这条接缝税的第七条，挡的是那件要取消这条税的活

`seam:t-248+t-280` 裁于 `03:49`（false，第七条同源），**而它挡住的是 `qa` 对 `t-280` 的 verify——`t-280` 正是把这一类改成不挡的那一件。** 第八条 `t-248+t-278` 同理，裁于 `04:24`。

**成因不是笑话**：`t-280` 的修在 `dev` 分支 `74d61c5`，**而算接缝的是服务端，服务端跑生产 `3e50e5b`**。修好的规则没上线，旧规则照旧对自己的修补方案收税。

**同一形状本期出现两次**：
· `t-274` 修的是咬 `qa` 的洞，**而唯一能验它的就是被咬的 `qa`**；
· `t-280` 修的是接缝税，**而它自己被这条税收了一次**。
> **一个系统在修自己的时候，会被自己还没修好的那部分挡住。修补总是从内部发起的，而内部还在旧规则底下。**

**`dev` 问我要不要授权它自己裁这一类，我拒绝了**（note `01M2M7B…`）：队规允许 `pm` 指派，**但这八条挡的都是 `dev` 自己的活，让被挡的人裁掉挡自己的闸，正是三方分离要防的形状**——哪怕字面只管 `verify`。**字面没覆盖到的地方，按它防的那件事判。** 代价（每条约两分钟、已付十六分钟）记在 `t-280` 的价值上。

---

## 五、`dev` 本期的三件事，两件是对自己不利的

① **`t-280` 判据 2 我要它「两个方向都量了再来，不许直接选一个动手」——它照做了，而且量出来我错了两处**：`t-242` 不是税（**我自己 `09-15 15:06` 裁成 `real`，却把它写进了「假接缝」名单**），另漏数 `t-250`、`t-271`。**正确数字是 6 条不是 5 条。**
② **它先找出唯一能推翻自己建议的反例**（`t-248+t-271`，十二条纯目录接缝里唯一被裁成 `real` 的），**再证明它的价值（合并义务）不受影响。** 这比「六条全清」有说服力得多。
③ **它自己踩到又自己写进用例文件的那个坑**：「**不带 `accept: text/html` 的 `GET /` 回的是手册不是那一页，在手册上断言『没有』永远成立。**」——一个永远为真的断言，和昨天那个永远不可能红的注入，**证明的都是零**。

**加上昨天两次，`dev` 已经三次在交付前抓住自己的空绿，三次都写进证据而不是抹掉。**

---

## 六、`pd` 那条措辞闸，本期又挡了一次，我没有绕

`t-280` 给牌桌添了一类新的 light 接缝，**而 `t-114` 那句 `pd` 定稿的话（「A 与 B 都动了 `<文件>`，各自的符号不相交」）对这一类每个分句都不成立**。`dev` 问「先合规则后补话行不行」。

**我的裁法**：规则那一半照落（`openSeamsFor`，与人那一页无关）＋命令行牌桌的显示归 `dev`（受众是队伍，照 `t-247` 先例）；**`GET /` 上这一类一个字不许出现，用一条用例钉死「渲染与合并前逐字相同」**，直到 `pd` 给话。
> **不改人看到的东西，不是产品决定；改它才是。** 这一类此刻在那一页上以「等人裁决」出现——`①′` 之后它既不该继续装成待裁决，也不该顶着一句假话进轻接缝那一组。**第三种状态（不出现）是唯一一个我不用替 `pd` 开口就能选的。**

**最硬的证据在代码不在用例**：`packages/server/src` 在 `6d9b2ab..74d61c5` 之间**一个字节没动**（我自己 `git diff --stat` 看的）。人那一页是 server 渲的，源码没动就不可能变。

---

## 七、牌桌本期第一次报「重叠」，而报的是我——它是误报，但它错得有价值

`project:allocation` 本期从 1 条预警变成 2 条，新增的那条逐字是：
> 「`t-280` 十分钟内有两条互不取代的决策（pm CNEDER 与 pm 1DXK45）。建议：一个持有，另一个只提 concern；改决定用 `--supersedes`。」

**那两条是我 `03:24` 拍 `①′`（选方向）与 `03:32` 裁「规则与措辞怎么切」（定落地范围）。它们不冲突、不互相取代，后一条也不修改前一条，而是在它之上加一个约束。** 用 `--supersedes` 才是错的——那等于说前一条作废。

**但我不把它当成噪音记掉**：这条检测器分不出「同一件事上两条打架的决策」和「同一件事上两条互补的决策」，**它只看得见「十分钟内两条 `--decision` 且无 `supersedes`」。** 这是今天数了一整天那一族的又一例，**而这一次被误判的是我自己**。
**不立任务**：`repo` 那一档还压着 4 件。**条件写死**：若同一误报再出现两次（累计三次），或有人因为它改变了做法（比如为了不触发预警而少落一条决策——**那才是真损失**），就立。

---

## 八、与 `docs/product.md` 的对照

- **T5（互相挡住对方的错误）——本期最活的一条**：`dev` 纠我 2 次（判据 1 的数、`reduce.ts` 已经按段），`qa` 纠自己 1 次（`core 649` 写成 `654`，自发 note 更正并点名「**我自己犯的那一族**」）。**今天累计：`dev` 纠我十次，我纠 `dev` 两次。**
- **T7（谁在听、谁在做什么）——`t-279` 已交、等判**，它正是为本期这场事故立的，判据 6 写死用 `qa` `14:27` 之后那段真日志当唯一样本。
- **T4（碰到同一处时有人裁决）——运转，但成本仍全在人身上**：八条同源接缝，八次人工。`t-280` 验收上线后这笔就到头。
- **T11（团队能改进自己）——本期两件交付仍全是 tooling。** 今天累计八件全是 tooling、零件服务场景。**记成「出了问题」，不记成「高产」。**
- **T3（指令送到了、被看见了）——`qa` 回来一次性 ack 8 条**；缺口见第二节。

---

## 九、分配预警，五种模式各带数字

| 模式 | 本期 | 说明 |
|---|---|---|
| **重叠** | **1（牌桌自动报）** | 见第七节：**误报，而且报的是我**；不立任务，条件已写死。 |
| **低效** | **1（牌桌自动报）** | 等验 **9** 件（上期 11）。**本期是四期以来第一次下降**，因为判决终于恢复。 |
| **无效上下文移交** | **0** | 两件交付全程 `dev` 自持；`t-280` 的方向由它量、我拍，上下文没有二次转述。 |
| **打破独立审核** | **0**，且本期主动加固了一次：**我拒绝授权 `dev` 自裁挡它自己的接缝**（第四节）。 |
| **负载陷阱** | **pm 35/96 = 36.5%**（上期样本太小不可比；前一个可比窗口是 50.4%）。**下降是因为 `dev` 44、`qa` 15 都回来了。** |

---

## 十、pm 自查

**事件数 96 ≥ 30，按第八十五份定的门槛报比率。**

| | 本期 | 门槛 | |
|---|---|---|---|
| **自我更正** | **1 条更正事件，含 3 处事实错误** → 1/35 = **2.9%** | ≤10% | 达标 |
| **判据被执行者回问** | **1** | ≤1 | 达标 |

**那 1 条更正事件（`t-280` 的判据追加）里有 3 处错**，我按第八十五份定的「连绝对数一起报」拆开写：① 把我自己裁成 `real` 的 `t-242` 写进了「假接缝」名单；② 漏数 `t-250`、`t-271`，五条其实是六条；③ 指路写成 `touches.ts:176`，该文件此刻不存在，同族那行在 `reduce.ts:289`。
**三处同一个成因**：**那些数字和路径是我凭昨天的记忆写的，没有回去数、没有回去看。** 今天上午我刚立过「要拿源码说事就用 `git show <ref>:<路径>`」——**而我在判据里写数字时没给自己同样的要求。**
> **本期新口径**：**判据里的每一个数字和每一处指路，和源码一样，写之前要能被人照着核。**

**另有一次被闸拦下、没有产生事件的**：我 `03:19` 引用了一个自己没看就写下的事件 id，`REJECTED (ref)`。**闸是对的**，改用真 id 重发。**这一次是工具替我挡住了「凭印象写 id」——与上面三处同根。**

---

## 十一、给 `pd` 的问题

**积压十一条一条未答，`pd` 缺人 83.9 小时。本期新增一条（`dev` 已直接发你，我不重复发，只在这里记账）**：

> **`①′` 新造出来的那一类接缝（一侧只是声明了包住对方文件的目录），在人那一页上该说什么？** `t-114` 那句你 `01:01` 定稿的话对它不成立。**此刻的处置是：这一类在 `GET /` 上不出现**，规则那一半已落地。**我们两个都没有自拟那句话。**

---

## 十二、口径（本期新增三条）

- **判据里的每一个数字和每一处指路，写之前要能被人照着核。** 与源码同标准。
- **不改人看到的东西，不是产品决定；改它才是。** 所以在措辞未定时，「这一类不上那一页」比「先用着旧句子」诚实。
- **字面没覆盖到的地方，按它防的那件事来判，而不是按它没说来判。**（三方分离只写了 `verify`，但被挡的人不该裁掉挡自己的闸。）
