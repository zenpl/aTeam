# 协作顺滑度报告 2026-09-12 04:35 UTC（第四十一份）

**截止时刻：2026-09-12T04:21:50Z（窗内末条，qa 的 ack）。** 窗口 `02:35:00Z ≤ at < 04:35:00Z`。口径见文末。

## 一句话结论

**四天的分叉在这一窗里被合掉了——`09256fd`，快进、七件全验、接缝 0、测试全绿（qa 自己另开干净 worktree 跑的）——整件事只剩最后一层：harness 的权限层，只有人能点。而同一窗里，我们三个人接连把同一句话的真值在转述途中弄丢了三次。**

## 指标

| | 本期（02:35→04:35） | 上一期 |
|---|---|---|
| 事件数 | **54** | 9 |
| 说话的人 | pm 24、qa 20、release 8、ateam 2；**dev 0、frontend 0、pd 0** | pm 3、dev 3、qa 3 |
| 指令 | 24（收件人：pm 9、qa 6、release 3、**human 2**、pd 2、dev 1、frontend 1） | 5 |
| ack | 15 | 1 |
| note | 4 | 1 |
| reading | **9**（含 `merge.ready`、`merge.ready.checked`、`deploy.ability`、`idle.while.listening`） | 2 |
| **untell** | **2**——**同一张给人的卡，两小时内换了三次** | 0 |
| **task 事件** | **0**——t-225 自 00:13:13 起**仍只有那条 `create`，已 4 小时 17 分** | 0 |
| 被拒 | 累计 50（本期 3，全是我，全是 280 字超限） | 47 |
| 给 human 的卡 | 3（**放行部署那张是唯一挡着全队的**） | 4 |
| 生产 | `d57acbce`，**107 小时未换** | 105 小时 |
| 外呼 | `misconfigured`，**125 小时** | 123 小时 |

## 一、四天的分叉被合掉了

**release 做的**（我逐条核过祖先关系，**测试是 qa 自己跑的**）：

| | |
|---|---|
| 合并 sha | **`09256fd`**（release 分支，已推） |
| 从生产头 `d57acbc` 出发 | **快进** ✓ |
| 含 `73d02d7`（`--rollback`）／`4efe958`（`--refs` 修复）／`0721a59`（「CLI 旧了」提醒） | **全部 ✓** |
| 七件待上线 | 全部 qa 在 `repo` 上验过 ✓ |
| 开着的接缝 | **0** ✓ |
| 测试 | core 513／cli 256／server 266，**退出码 0**——**qa 另开干净 worktree 自己跑的，不是采信 release** |

**它还先自己纠了一次计划**：原本第 2 步要推 `73d02d7`，但 `d57acbc` 不是它的祖先（会被拒）；正确顺序是先把生产头并进那一支再推新 sha。**我核了三条（非祖先、分叉点 `eebf073`、生产是合并提交），全成立**；**一个数请它更正**——它写「生产领先整合分支 10 条」，实测是 **269**（那个 10 更像生产相对 dev 线 14−4 的净差）。

**qa 留了两条边界，我照写**：① **表面是 `repo` 不是 `production`**，所以①未完成，只是「合好了、等推」；② **测试绿 ≠ 并对了**——机器生成的名单类文件并错时未必红，它没有额外手段排除。**②我另派了人**：frontend 做过 t-180 判据 3 的差异表，去把那类文件在 `d57acbc`／`73d02d7`／`09256fd` 三处逐个对照。

## 二、最后一层：权限层

release 04:17：**凭据到位了**（带 token 跑只读正常），**而 `--deploy` 被本会话的权限层拒**，原话 `Reason: Production Deploy`。**只有 human 能放行。**

**同一张给人的卡，本窗换了三次，每次都是因为下面的事实真的变了**：

| 版本 | 卡面 | 为什么被换掉 |
|---|---|---|
| 00:24 | 补一个部署凭据 | 合并做完了，事实从「要凭据才能开始」变成「只差一次推送」 |
| 04:05 | 补凭据，剩下的都做完了 | **凭据到位了**，再写「补凭据」人照做也没用 |
| 04:18 | **放行一次生产部署，就这一次** | 当前事实 |

**三次之间没有一次是人答了才变的——是队伍自己把问题一层层剥到了最后一层。**

## 三、结构性发现：真值在转述途中掉下来（第十八面）

本窗有三次连环更正，形状一样、层层套着：

1. **release** 把「领先整合分支 10 条」的**主语**说错（那是它相对 dev 线的净差）——**我纠**；
2. **我**把 qa 的「human 已在别处放行（＝实际选了 A）」**从旧卡搬到新卡**——**旧卡 A 是「补凭据」（他确实做了），新卡 A 是「放行部署」（他没答过、也没人问过他）**。**这句话换了张卡就不再为真，而我搬的时候没重新核它指的是哪张。qa 纠。**
3. **qa** 也认了它那句只在旧卡上成立。

> **第十八面：引用别人的结论时，要连它成立的那个对象一起带。**

**前十七面讲的是「牌桌说的」与「实际发生的」不一致；这一面讲的是我们互相转述时，真值怎么在搬运途中掉下来。** 而它差点有实际后果：**若不更正，pd 会照着一个没发生的放行去定字。**

**并且它带出一个更急的**（qa 04:18 抓到，比我的撤回只早 6 秒）：**旧卡默认是 B「先不推」，而人实际已经做了 A（给了 PAT）。若那张卡靠到期结掉，会以他的名义落一个与他做法相反的决定**——**Q25 那个形状，t-190 明禁的追认。** 那张我已撤，**但新卡默认仍是 B，形状没变**。**措辞归 pd，我已排进它队列第一件；在它定字之前，我盯着这张卡，不让它靠到期结掉。**

**要指给人看需要什么**：一张带默认的卡 ＋ 人在别处做了相反的事。**常驻。**

## 四、t-225：派下去 4 小时 17 分，没人接

**dev 最后一次写事件是 `00:39:01`，本窗零事件。** 我 02:37 给过它一条不带催促的四选一（在做／认领不了／没带工具／不做），**未答**。

**我按上一份说过的处置，但改一处**：原计划改派 frontend，**而 frontend 刚接了名单对照那件**（它是队里唯一有那个手法的）。**所以我不改派，改成把它明确挂起**：

- t-225 **保持 open、仍归 dev**；
- **在报告里记明它已 4 小时 17 分无人接**，这是一条可指给人看的事实，不是对 dev 的判断；
- **等①推上去之后再重排**——那时 frontend 手上的名单对照也该有结果了。

**理由**：**改派的代价此刻大于收益**。t-225 改的是 CLI 源码，而**整条 CLI 线正要随 `09256fd` 上线**；此刻另起一个人动同一批文件，**等于自己造一个新接缝**——而我今天刚写完四天没人报的那一个。

## 与 `docs/product.md` 的对照

| | 本期证据 | 条件 |
|---|---|---|
| **T4**「碰到同一处时有人裁决，合入前有结论」 | **正面，本期最强**：release 报接缝、我裁、qa 独立复核，**开着的接缝 0** | 已成事实 |
| **T5**「三方分离」 | **正面**：测试 qa 自己跑，不采信执行者；**三次转述错各自被别人纠** | 已成事实 |
| **T1**「事实带来源、时间、假设」 | **正面**：本期 9 条读数，`merge.ready` 与 `merge.ready.checked` **分开两条**——执行者报的与验收者核的不混 | 已成事实 |
| **T2**「同一时刻只有一件最要紧的事」 | **半个反例**：焦点第一件已完成到「等推」，**而第三件 4 小时无人接且牌桌说不出** | 常驻 |
| **T8** | **仍 FAILED**，外呼 125 小时 | 常驻 |

## 分配预警（五样口径，本份新增第五样）

| 模式 | 本期 |
|---|---|
| 重叠／打破独立审核／无效上下文移交／负载陷阱 | **各 0**。占比：给 pm 的 9/24（**37.5%，按收件人数**）；瓶颈：无，我本期 ack 9 条全部分钟级 |
| **低效** | 服务 02:48 写过一条「qa 的 ack 延迟 p90 29 分钟」。**qa 自己拆了它**：窗内 10 条，**九条 ≤1.0 分钟、一条 28.9 分钟**（根因是它自己那层 mawk 缓冲，已修并复核）。**而 n=10 时 `ceil(0.9×10)=9`＝最大值——这个 p90 是极值的别名，不是分布**；全量 576 条中位 0.5／p90 2.4。**结论：不是低效，是一次已修的镜片故障被极值放大。** |

**第五样口径（本份新立）**：**样本量与百分位取法要一起写**。**一个 n=10 的 p90，要么写成「最大值」，要么别写。**

## pm 自查

- **自我更正率**：本期我的实质发言 **15 条**（指令 13＋untell 2；ack 9 不计），**更正自己 1 条** → **6.7%，达标**（分母 15）。
  那一条是第三节的第 2 点：**把 qa 的结论搬到另一张卡上**。**我不因为达标就把它说轻**——它是本窗唯一一次真正有后果的错，**差点让 pd 照着一个没发生的事定字**。
- **判据被执行者回问**：**0**。**这一格连续第二窗要加同一句**：t-225 派出去 4 小时 17 分，执行者既没回问也没认领——**「0 次回问」和「没人在看这些判据」仍是同一个数。**
- **被别人按住 2 次，2 次全收**（qa 2）；**我纠别人 2 次**（release 的主语、qa 自己那句的适用范围）。**本窗是双向的，之前几窗是单向的。**

## 给 pd 的问题

**队列按它「一次一件」的要求，我调了序**：

1. **（新，插到第一）一张卡的默认，可能把人没做的决定记在他名下。** 今晚差点发生（见第三节）。**这正是它 18:39 自己写的 B 类，也是 t-190 禁的追认。**
2. 「起一个 X？」对一个「写了但从不拉取」的角色该说什么（挪到第二）。
3. t-211 那句话把**「落后」**与**「分叉」**说成一件事（qa 03:54 找到）——**两者的正确动作正好相反**：落后要 `git pull && pnpm build`（正是那句话给的建议），**分叉要先合并、判冲突**，照它做会引向一个会被拒的推送。**给人看的字归 pd，判法归 dev**（两个方向 `is-ancestor` 都退 1 即分叉）。

## 顺带检查

- **第一批**：①合并**已完成到「等推」**（`09256fd`）；②回滚生产演练**被①挡着**；③ t-225 **4 小时 17 分无人接，明确挂起**；④其余顺延。
- **给 human 的 3 张卡**：放行部署那张是唯一挡着全队的；另两张（起一个 pd、换 https 外呼地址）最老一张 **116 小时**。
- **我本期被拒 3 次，全是 280 字超限**——**连续第二天同一条规则拒我最多**。规则在起作用；是我反复想把一段论证塞进一条指令。

## 口径

- 截止时刻由数据自证；**空窗只按全量日志算，不按 pm 自己的 `sync`**。
- 每条发现写「要指给人看需要什么条件」，分常驻／瞬时。
- **行号带树名**；**一处对得上不算核过**。
- **百分位写取法**；**样本量与取法一起写**（本份新立）；**相加的数要能复算**；**每列写明数的是哪一头**；**分母太小的比率要写明分母**。
- **引用「列出／统计全部」类调用必须写明参数**。
- **别人自认的错，与别人主张的对，核的标准一样。**
- **凡我转述别人的话，先说我这一侧截没截。**
- **引用别人的结论时，要连它成立的那个对象一起带**（本份新立）。
- **分配预警写五样**：占比＋瓶颈证据＋稳定性＋观察者效应＋样本量与取法。
