# 协作顺滑度报告 · 第一百六十八份

窗口 `2026-09-22T16:35Z → 18:35Z`。上一份：`docs/collab/2026-09-22-1635.md`（`9678cf7`）。

## 一句话

**上一份我立了规矩（每期加一的数要重算，算不出来就退役）并当场欠下一笔：把「累计干净轮」重算一次。本期还了这笔账——它算不出来，本期退役；而重算的过程顺带查出，我那份原始数据根本不是一条同质的序列。**

## 指标

| | 本期 | 上期 |
|---|---|---|
| **节奏** | **3 条**（pm 2、服务 1） | 3 条 |
| **指令与确认** | 发 1（给 `pd`）、ack 0（收件人缺席） | 同 |
| **任务与验收** | **建 0、交 0、判 0——已连续 `85h01m`** | 已连续 `83h01m` |
| **摩擦** | 0 | 0 |
| **人的负担** | 卡 6 张，一张没点 | 同 |
| **在场** | **1 人**（只剩 pm） | 1 人 |

## 与上一份的变化

```
任务停了多久      83h01m  →  85h01m        锚点 2026-09-19T05:34:01Z
除我之外的写入者   0  →  0                  自 dev 最后一次写起 37.7h、63 条：pm 43、服务 20、其他 0
看守（本窗）      119 轮/最大 63s  →  120 轮/最大 63s
不明缺口的率       596 轮 1 次  →  716 轮 1 次      本窗 120 轮无新例
服务预警连续相同   39 条  →  40 条，跨 78.0h        起点 2026-09-19T11:19:53.027Z，每期重算
累计干净轮        2646  →  退役                    重算不出来，见下
```

## 结构性发现

### 一、上一期欠的那笔账，还了，结果是退役

**上一份写**：「累计干净轮」也是一个每期加一的数（`2527 + 119`），**它有原始数据（`sync.trail` 的 `=== ` 行），下期要重算一次；算得出来就留，算不出来就退役。**

**重算，逐条给数**：

```
我上期印的「累计干净轮」                      2646
sync.trail 里 === 行的实际总数                14176
```

**差 5.4 倍。** `2646` 是从某个起点一路 `+119 / +120` 加出来的，**而原始数据里的轮数是 `14176`。** 我**拿不出那个起点**，也拿不出一个能把 `14176` 变成 `2646` 的过滤条件。

**按第七十九条：算不出来就退役。本期停印「累计干净轮」。**

**换上一个算得出来的**：**`sync.trail` 记录的看守轮数合计 `14176`**（口径：文件里 `^=== ` 行计数；本期实测）。**外加每期实测的本窗轮数（本期 `120`）。** 这两个我下期还能从同一个文件重算。

### 二、重算时撞见的：那份原始数据不是一条同质的序列

**数**：

```
=== 行总数                                     14176
  ISO 格式（=== 2026-09-22T18:34:57Z 这种）      7835
  只有时分秒（=== 04:59:13 这种）                6341
格式切换点   最后一条短格式  第 81040 行  === 04:59:13
            第一条 ISO      第 81052 行  === 2026-09-17T04:59:44Z
```

**这件事要紧，因为我每期量看守用的都是同一把尺子**：一段 `awk`，按 `=== ` 后面那个字段和 ISO 时间比大小。**短格式的 6341 行，这把尺子一条都匹配不上。**

**对近期的数没有影响**——本期、上期、以及我引用过的每一个「本窗 119 / 120 轮」，**窗口全部落在 `09-17T04:59:44Z` 之后，那里只有 ISO 格式。**

**有影响的是任何「整条痕迹」的说法。** 我此前做过一次跨全痕迹的扫描并报过一个轮数，**那个数与这里的 `6341` 很接近**；我手上没有那次计算的原件，**所以我只说我现在能证的**：**这条痕迹由两种格式组成，一次只匹配一种格式的扫描，最多覆盖其中一段**；**今天任何自称「全部」的轮数，必须等于 `14176`。**

> **第八十条：重算一个数之前，先问它的原始数据是不是一条同质的序列。** 我以为我在还一笔小账（把 `2646` 验一遍），**而真正的问题在更下面一层——那把每期都在用的尺子，量不到那份数据的一半。** **重算之所以值得做，不只是因为它能抓住算错的数，是因为它逼你回到原始数据，而原始数据会告诉你别的事。**

### 三、顺带把「假催办 0」的口径说清，不再裸报一个 0

**我每期在分配预警里印「假催办 0」。本期核了一下我在数什么**：

```
sync.trail 里「你读过还没动」出现次数        14211   ≈ 每一轮都有
```

**这一条不是假催办**，它是那条**合理的**「读过还没动」提醒（`207` 条，最久 `14570` 分钟）——**它每轮都响，本来就该每轮都响。**

**真正的那类假催办（`t-283` 那条）此刻确实是 `0`，但原因要写出来**：**不是它被修好了**（`t-283` 已修已验，**但没上线**），**是我在 `09-2x` 亲手把它的来源毁了**（往 `ateam ack` 里打了一个猜的 id，那次拒绝覆盖了单槽的 `.ateam/refused.pm`——第五十七条）。

**所以从本期起，这一格写成「假催办 0（来源已毁，非已修复）」**，**不再裸报一个 0。** 一个 `0` 如果不带上它为什么是 `0`，**下一个读它的人会以为问题解决了。**

## 与 `product.md` 的对照

| | 本期 |
|---|---|
| **T1 / T3 / T8 / T11** | 无往来、指令 1 ack 0（收件人缺席）、无可评、friction `0` |
| **T5** | 验收资格仍为空；**「无人能验」已持续 `85h01m`** |
| **O1** | 与人之间唯一的通道仍是那一页，**本期无变化** |
| **O2** | 服务预警**连续相同 40 条、跨 78.0h**（起点 `2026-09-19T11:19:53.027Z`，每期重算） |
| **这条日志的用处本身** | **仍为 `0`**：37.7 小时、63 条，非 pm 非服务的事件 **0** |

## 分配预警

| 模式 | 本期 | 数字 |
|---|---|---|
| **重叠** | 无 | 接缝 0、任务事件 0 |
| **低效** | 无 | **假催办 0（来源已毁，非已修复）**；看守轮数：本窗 `120`，痕迹合计 `14176` |
| **无效上下文移交** | 无 | 37.7 小时内非 pm、非服务事件 0，**没有链可断** |
| **打破独立审核** | **结构性风险，已持续 `85h01m`** | `qa`、`dev` 均 missing；**在场 1 人** |
| **负载陷阱** | 无 | 队列深度全 0；pm 本期写入 2 条 |

## pm 自查

| | 本期 | 目标 | |
|---|---|---|---|
| **自我更正率** | **0%**（0 / 2） | ≤10% | 分母 2 |
| **判据被执行者回问** | **0** | ≤1 | 分母为 0 |

**本期没有更正**：退役一个算不出来的数**不是更正**（它没有一个「正确值」可回退到），**是把一个不该印的东西停掉。** 我把它记在这里，**不计进自我更正率**——**否则「退役」会因为拉高这一格而变得不划算，而那正是它该被鼓励的时候。**

**本期到期 4 次（`16:50:08`／`17:20:17`／`17:50:27`／`18:20:36`），4 次都在收到通知的当场重挂。**

## 给 pd 的问题

**Q12–Q18 均仍等**（`pd last_pull 2026-09-12T16:42:03Z`，距今 `241.9h`）。**本期不新增问题。**

那条「在无人可对齐时这份报告还该是什么样」**本期又多一条材料**：**上一份点名的三个「每期加一」的数，本期查完第二个，也退役了。** 两期之内这份报告退掉了两个指标、给第三个加了口径。**它现在更短，也更算得出来。**

**已发的九件仍等。**

## 口径（本期新增一条）

- **第八十条：重算一个数之前，先问它的原始数据是不是一条同质的序列。** 本期我以为在还一笔小账，**真正的问题在更下面一层——那把每期都在用的尺子，量不到那份数据的一半。** **重算值钱不只因为它抓得住算错的数，是因为它逼你回到原始数据，而原始数据会告诉你别的事。**

## 顺带检查

- **生产 `3e50e5b`**（`GET /health` 本期实测）、**整合支 `09256fd` 未变**，后者仍不含 `t-280`；**八件已验交付仍坐在 `claude/backend-development-gzqbjf` 上，等 human。**
- **焦点 `2026-09-22T02:41Z` 版未变**：本期是它的第八个整窗。
- **服务端无降级**：本窗 120 轮，中位 `61` 秒、最大 `63` 秒。
- **给 human 的六张卡，本期仍一张没点**（`human last_pull 2026-09-13T14:40:43Z`）。
- **在场名单**（时刻，非年龄；本期七个角色一个没动）：
```
pm        在听                            ← 唯一
dev       last_pull 2026-09-22T00:52:29Z   last_event 2026-09-21T04:54:12Z
qa        last_pull 2026-09-19T17:08:43Z   last_event 2026-09-19T13:36:36Z
human     last_pull 2026-09-13T14:40:43Z   last_event 2026-09-07T03:36:48Z
release   last_pull 2026-09-13T14:09:36Z   last_event 2026-09-13T14:19:35Z
frontend  last_pull 2026-09-13T01:17:15Z   last_event 2026-09-13T01:32:14Z
pd        last_pull 2026-09-12T16:42:03Z   last_event 2026-09-12T16:44:18Z
```

---

*本份在窗口边界上取数，窗口内 3 条。*
