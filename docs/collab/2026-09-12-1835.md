# 第四十八份：那道闸一直在那儿，只是从来没合上过

窗口 `2026-09-12T16:35Z → 18:35Z`。截止时刻由数据自证：快照末条 `at = 2026-09-12T17:53:32.833Z`、`id = 01M2BC1K51GGWEKA091WHY9620`、全量 8589 条。**指标表已按这一份重算**；17:53 之后的事件在文末「截稿后」单列。

---

## 一句话结论

**qa 量到：此刻任何持管理钥匙的节点都能以 human 的名义点掉他的卡——而我们每个角色手上拿的就是管理钥匙。** 这五天我们为「不替人做决定」争论、拒绝、立规矩，**而系统一次也没有挡过它：闸在那儿，只是在等一件从没发生过的事才上锁。挡住它的是每个人自己。**

同一小时里队伍交了今天唯一一件服务场景的东西（t-224，认领到交付 **13 分钟**），并在四条链上各自纠正了自己。

---

## 指标

| | 本期 16:35→17:53（78 分钟） | 上期 14:35→16:35（2 小时） |
|---|---|---|
| 事件总数 | **222** | 247 |
| 按人 | pm 102／dev 53／qa 52／release 5／frontend 5／ateam 3／pd 2 | pm 114／qa 62／dev 57／ateam 8／release 4／frontend 2 |
| 按种类 | instruction 74／ack 49／task 43／note 43／reading 11／untell 2 | ack 102／instruction 57／note 33／task 30／reading 24／untell 1 |
| **ack 时延** | **中位 82 秒**，最快 21 秒，最慢 324 秒，n=49（pm 21／qa 16／dev 12） | 中位 166 秒，n=27 |
| 任务 | verified **213**／open 9／working 1（t-234）／done 6 | verified 210 |
| 开着的接缝 | **0** | 0 |
| 生产 | `09256fd`，待上线 0 | `09256fd` |
| 在场 | pm／dev／frontend／qa listening；**pd／release missing** | pm／dev／frontend／qa listening |
| 人看得见的卡 | **5**（口径见下） | 4 |

**两个数要说明，否则会被读错**：
- **事件密度**：本期 78 分钟 222 条 ≈ **2.85 条/分钟**，上期 120 分钟 247 条 ≈ 2.06 条/分钟——**本期快了 38%**，而其中一多半是 P0 那条链上的来回。
- **`verified` 从 215 掉到 213，成因是两条生产表面的 fail，不是流程倒退**：`t-103`（17:32:39，production FAIL）与 `t-025`（17:47:36，production FAIL）各自从 `verified` 退回 `done` 桶（`done` 从 4 到 6 正是这两件）。**这不是坏消息，恰恰是那 40 件清账在起作用**——它专门找「在 repo 上过、在生产上不过」的，**今天它一次找到两件**。
  **这里要记一次我自己的错**：我第一版写的是「t-224 与 t-227 被 reopen，掉的两件在 done 里等验」。**那是我按 `reopen` 事件凑出来的解释，没有去查判决的 pass/fail**——查了才看清 t-224／t-227 本期都已重新 verified，真正掉出去的是两条 fail。**把两个同时为真的现象（有 reopen、数字掉了）说成因果，正是本份结构性发现三点的那一面（压缩造因果），而这次是我自己在报告里造的。**

**ack 时延取法**：ack 事件与它 `of` 指向的 instruction 逐条配对算差，只算本期落下的；本期没有积压条目混入（上期那 49 条积压是一次性的）。n=31，分母偏小。

**「人看得见的卡」三个数，必须并排写**（本期我为此更正过一次）：
- **8** = 日志里发给 human、未 ack／未撤／未决的条数（qa 17:07 量，口径最准）；
- **4** = 牌桌 `needs_human` 实际印出来的张数（差的多半是 `ateam` 给「没在听」角色自动发的卡，那些角色现在在听，牌桌就不印）；
- **1** = 8 条里**带选项、能被真「点掉」的**张数——正是我 16:47 那张整合分支的卡。
- 我 16:45 那个 **12** 口径太宽（没排掉已被 `decide` 的），**已作废，不许再用**。

---

## 与上一份的变化

1. **出了一件 P0**（见结构性发现一），焦点整体下移一格。
2. **t-224（S9 搬家导入）交了并在 repo 上验过** —— 今天唯一一件服务场景的交付。**16:56:42 认领、17:09:40 交付，13 分钟。**
3. **t-227／t-228／t-230 三件当期交付并验过**，verified 从 210 到 215。
4. **pd 与 release 被叫回来各交了东西，然后又停了**（见上一份结构性发现，本期确认：一次叫醒＝一个回合）。
5. **给 human 的卡少了一张、多了两张**：撤掉「是不是你推的」（release 补了出处），新增「放行整合分支快进」与「修好之前别按默认结掉任何卡」。

---

## 结构性发现

### 一、P0：那道分辨「是不是主人」的闸一直在那儿，只是从来没合上过

qa 17:05 走 t-110 时量到，我 17:1x 跑对照复核并收紧。**事实分三层，分开写**：

**(a) 能以 `human` 说话，这是设计如此。** 读 `09256fd` 的 `packages/server/src/app.ts:601-611`：
- `:606` `if (record.role !== null && actor !== record.role)` ⇒ 403「这把钥匙是 X 的，不能以 Y 说话」——**这是为节点钥匙写的闸**；
- `:610` `if (actor === human && !isOwner && await ownerArrived())` ⇒ 403 `owner-key`；
- 这一段的注释原话：「**A node key is bound to its role; an admin key may speak as anyone.**」
**`ownerArrived()` 从没为真过**（`owner_key = {"state":"none"}`，我与 qa 各自量到）。**所以 `:610` 那道锁从来没合上。**

**(b) 受影响的是持管理钥匙的节点，不是「任何持 token 的」。** 绑到角色的节点钥匙在 `:606` 就被拦住了。

**(c) 而对照量出了最该担心的那一件**：我以 `x-actor: pm` 说话，**同样拿得到 `invite_url`**（那是 `isAdmin` 才给的字段）⇒ `record.role === null` ⇒ **我手上这把就是管理钥匙，不是绑定到 pm 的节点钥匙**。qa 那边同样。
**也就是说：我们不是「有一把管理钥匙放在某处」，是每个角色手上拿的就是管理钥匙。`:606` 那道闸对我们一个人也不生效。**

**第二十五面，也是最贵的一面：「有一道闸」不等于「这道闸此刻是合上的」。** 我们读代码看见 `:610`，就以为权限这层有人管——**它确实存在，它只是还没开始工作**。同源于第一面（有没有 X ≠ X 此刻能不能用），但落在了整套东西唯一一处「人的最终权力」上。

**这件事对 T5 的意义要说准，不夸张也不缩小**：pd 定过「替人点掉一张卡，等于把人没做的决定记在他名下」；我今天拒绝替他答「是不是你推的」；qa 拒用出现在自己环境里的部署凭据、连 dry-run 都拒；我立过「凭据出现在谁的环境里，不改变谁有那份职责」。**这五天 T5 一次也没有被系统挡住过——它是被每一个人自己挡住的。** 这既是这支队伍最好的一条证据，**也正是为什么不能只靠它**。

**这里要更正我自己草稿里的一句**：我原本写「下一个拿到这把钥匙的，可能是客户那支队伍里的某个节点」。**按 qa 的补充，新建项目在建的那一刻 `owner_key` 就是 `issued`——新项目有一扇会自己关上的门（主人第一次打开牌桌就关）。我们这一张是 `none`，主人那把钥匙从来没被登记过，所以它五天没关，不是没人去关，是关不上。** 所以：**这不是一个会传给客户的产品缺陷，是我们自己这张牌桌的状态问题。分量不减（它管着人的最终权力），但对象错了不能不改。**

**已发生过一次的不是「替他答」，是「替他标已读」**（qa 09-07 误 ack 过一条，人从没收到过）。**一个假的「已答」很显眼；一个假的「已读」不显眼，它只是让一条本该还挂着的东西安静消失。**

**处置**：t-234（八条判据，判据 1 把默认改成「没确立主人就一律拒」——未配置就锁上，不是未配置就放行；判据 4 盘一遍所有认 `x-actor` 的地方，不许只修 `:610`；**判据 8 是对照量出来的那条：每个角色该换成绑定到自己角色的节点钥匙**）。dev 17:13 已认领。
**硬规矩发给五个角色**：任何人不得以 `human` 名义写入——不 ack、不 decide、不发事件、不点卡，**一次也不行，包括「验证一下能不能」**；证这个洞只用只读 GET；需要写才能证的那一半谁都不做。
**给 human 一张卡**，默认是「**修好之前别按默认结掉任何卡**」——这件事上「到期自动算」正是要防的那一种。

### 二、写在判据里的「上线前必须 X」，对发车闸是不可见的

t-224 此刻牌桌上是 `verified`，**而验它的 qa 在同一条判决里明写「本件按它自己的判据 3 与判据 8 还不算完成」**（判据 3 那句措辞是占位、等 pd 定稿，「未换不算完成」是判据自己写的；判据 8 要一条生产判决）。**qa 做得完全对，它把边界写进了判决。**

**问题在下一环**：`release.ts` 的发车闸读的是 `t.status !== "verified"` 与「repo 上过没过」，**它读不到判据正文**。所以牌桌上 t-224 = `verified`，闸会放它上线，**而它上线时带的是那句占位措辞**。

**第二十七面：一条写在判据里的「上线前必须 X」，对闸是不可见的。** 判据是写给人读的，闸是按状态跑的——**我们把一条硬约束写进了一个机器不读的地方，然后以为它拦得住事。** 和第二十五面是一对：那一条是闸没合上，**这一条是约束根本没进闸**。

**处置**：只能靠人挡，所以我写得很显眼——**t-224 不许上线**，直到 pd 的措辞定稿落地、且拿到生产判决；已 tell release（它 missing，这条会等它）。**我没有自己建号**：机器该管什么、人该管什么是 pd 的地界，递成它队列第十五件。

### 三、压缩会造出因果

qa 17:05 的 **note** 里两条观察是分开摆的：`invite_url` 推「这把是管理钥匙」，**没回 403** 推「能以 human 说话」。**两条独立，各自成立。**
**是那 280 个字把它们并成了一句**——读起来变成「回 200 还带 `invite_url`（`isAdmin` 才有）**所以**能以 human 说话」。

**第二十六面：压缩会造出因果。** 两条各自成立、彼此独立的观察被并排放进同一个句子，**读的人会自动在它们之间补一个「所以」**。**这一面最危险的地方在于：事后去查，每一条证据都对得上**——错的是它们之间那个不存在的连接词，而**连接词不留证据**。

**这不是工具的错，恰好相反**：`tell` 限 280 字并写明「理由放 note、动作放 tell」，**规矩本身就是为了逼出这个分工**。今天是我们两个都把 note 该装的东西塞进了 tell。

**两条纪律，各管一头**：qa 自己定的「**`tell` 里只写一条证据，其余指向 note**」我照收并对自己生效；**我这一半是「收到一条指向 note 的 `tell`，在据它动手之前先把那条 note 打开」**——尤其在我要改焦点、下全队规矩、给人发卡的时候。

### 四、注入报红的条数，只有失败落在最后一个跑的包里才是完整的

qa 在 t-224 的判决里立的，我收为全队口径。根 `pnpm test` 在第一个失败的包就停：它的 AW／AX 完整（`core` 与 `cli` 先过了），**AV 不完整**——它只核到 `core` 红 3 条，dev 自报的「`server` 红 2 条」它**按「未独立复现」记，没有当成核过**。

**影响面**：今天所有「注入 X ⇒ 红 N 条」的数，凡是失败落在 `core` 的，`cli` 与 `server` 那两包根本没跑。**我不逐条重算**（那是 qa 的活，且大多不影响结论），**但从现在起报注入结果必须带一句「跑到哪一包为止」**。

### 五、今天唯一一件服务场景的交付，13 分钟

t-224（S9 搬家导入）：dev **16:56:42 认领、17:09:40 交付**，qa 17:20 在 repo 上验过。qa 的验法逐条自己跑：新建一个**空项目**导两遍（**0 → 3 → 3**）；把已导过的那条挪到新文件第 5 行 ⇒ **行号变了 `from` 一个字没变，去重照旧认得它**；三个进程并发导同一条 ⇒ 日志里 `from === "CONC-1"` 的**恰好 1 条**；不给 `from` ⇒ 一条都不写；不带 `measured_at` 的 `reading` ⇒ 那道闸一个字没松，**而且被拒的那条说得出它是谁**（印的是它的 `from`）。测试 540／285／281 全绿。

**这一件为什么重要**：`t-088` 上线至今 7051 条事件里 **`from` 出现 0 次**；生产那份加入说明书里 **`import` 0 次、`from` 0 次**。**照着我们自己的说明书搬家的人，不会知道有「重导一次翻倍」这回事**——而第一个客户是 human 自己的项目。

---

## 与 `docs/product.md` 的对照

- **T5（不替人做决定）**：**本期出现了这五天最重的一次事实修正**——T5 从来不是被系统守住的，是被人守住的。这既是最好的证据（qa 手里同时握着部署凭据与主人权限，两次都是它自己停下来的），**也是最脆的地方**。t-234 是把它从「靠人」变成「靠闸」的第一件。
- **S9（客户搬家）**：t-224 交付并 repo 验过，**但按它自己的判据还不算完成**（措辞占位、缺生产判决）。**代码这一层通了，人照说明书走的那一层还没通。**
- **O6（从线上的版本回到需求、决策、验证）**：上一份记的那次断裂（生产换版无人认领）本期补上了一半——release 补了出处，**是一句 session 内的口述，日志无从核**。这是 pd 那套里的 D 型。
- **M（可测）**：本期立了两条量法口径（注入报红要带「跑到哪一包」；「人的负担」要数人看得见的那一头），**都是因为我先用错了数才立的**。

---

## 分配预警（五种模式，各带数字）

**① 重叠：0 次。** 本期无。

**② 低效：等验队列此刻 4 件**（t-070、t-094、t-133、t-219 —— t-227 本期已验出）。**外加上一份点过、本期仍未动的那笔：整合分支落后 284 条、独有 0 条，纯快进，第六天。** 它此刻有卡挂在人那儿（我 16:47 发的），**而那张卡正是唯一一张能被冒名点掉的**——所以 t-234 修好之前它也不能靠默认推进。

**③ 无效上下文移交：1 次。** 我 16:54 给 dev 排 t-224 的 tell 与 dev 16:53 问我「排下一件」的 tell 撞车，两边各写了一遍同样的上下文。**稳定性**：上一期 2 次、本期 1 次，向下。**样本**：n=1，我这一侧可数的部分。

**④ 打破独立审核：确认 0 次，未遂 2 次，两次都是机制干的。** (a) 上一份记的 t-094 那一行印「等 qa」（已建 t-231）；(b) **本期新增：t-224 状态是 `verified` 而闸读不到「判据说还不算完成」，闸会放它上线**（已建为 pd 队列第十五件）。**两次都不是人要破规矩，是这一页／这道闸在替人破。**

**⑤ 负载陷阱：pd，队列 15 件、持有职责 3 项、本期流入占比 100%。** 上一份是 12 件，**本期我又加了 3 件**（第 13～15）。它本期只出了 4 件措辞（16:42 那一个回合），然后就 missing。**趋势继续向上，且缺口在扩大。**
**处方不变，按 pd 自己定的规矩：再起一个同职责包的角色（pd-2），不是拆 R1/R2/R3。**
**观察者效应**：这 15 件里有 5 件是我本期与上期新建任务时派生的——**我一边报它过载，一边在给它加，而且这一期加得更快。**
**样本量与取法**：队列条数来自我 16:0x 那份清单加本期三次追加（人工维护）；职责三项来自牌桌 `coverage`；流入占比分母是本期我判定「归 pd」的事项总数 3。**分母小，只当形状看。**

---

## pm 自查

**按本份起的新写法，自我更正分两栏**（上一份立的：这两个数意思相反，混成一个会让「被人抓到」看起来像「勤于自省」）。

分母：本期 pm 主张类事件（note／instruction／reading）**41 条**。

**(甲) 我自己发现的：3 次。**
1. **分配预警写错了五样**——pd 定的是五种模式（重叠／低效／无效上下文移交／打破独立审核／负载陷阱），我写成了另一组（占比／瓶颈／稳定性／观察者效应／样本量）。**那是我立的「每条预警怎么写」的口径，不是「该找哪五种」。我把怎么写当成了写什么**，已存在好几份。
2. **排序把机制压过了场景**——开着的八件里只有 t-224 服务场景，而我今天新建的五件全是机制。**递上来的东西天然排在前面，不是因为更重要，是因为更响。**
3. **「human 手上 12 张未答」当成人的负担**——人实际看得见 4 张。**我用一个人根本看不见的数，去论证「别再给他加负担」。**

**(乙) 别人发现后我承认的：3 次。**
1. **qa 拦住我把「12 张」写进给人的卡**（它借了我 16:45 的数没核，而错数是我的）。
2. **qa 指出我那条更正收得过宽**——「证据不成立」对它的 tell 成立、对它的 note 不成立；**而我那一侧更实在：它 tell 里写着「note 在 17:05」，我没开就动了手。**
3. **qa 量到看守被壳每约 30 分钟杀一次**——**我先看见过三次，一次没记**，把它当成要绕过去的噪音。（跨期，本期才落成读数。）

**(甲)+(乙) = 6／41 = 14.6%，超 10% 的目标 4.6 个百分点。** 上一份 16%，略降但仍超标。
**两栏各自的意思**：自己发现 3、被人发现 3，**一比一**。上一份也是一比一（2:2）。**这个比例本身是一条数据：一半的错要靠别人才看得见，说明我的自查覆盖不到自己的一半盲区**——而那一半集中在「数错了东西」而不是「算错了数」。

**最重的一次是 (乙)2 的前半段**：**我在没跑对照的情况下，用一条压缩转述里的证据，改了全队焦点、给五个角色下了硬规矩、给人发了一张卡。** 三件我都不撤（收紧后的结论更站得住），**但顺序错了——那个对照只要 30 秒。** 我今天核过 release 三句、qa 两句，**唯独这一次没核就用了，而它恰好是今天最重的一条。**
**由此立的口径**：**凡我要据此改焦点、下全队规矩或给人发卡的证据，动手之前我自己跑一次对照。**

**判据被执行者回问：0 次。** 本期 dev 与 qa 对我写的判据没有一次回问；dev 就 t-228 的覆盖面问过一次「新建还是重开」，那是排期问题不是判据问题。**达标。**

---

## 给 pd 的问题

**队列 15 件，本期新增 3 件**（第 13～15）。pd 本期回来一个回合、出了 4 件（第 12、1、11、2），**其余 11 件未动**。

**pd 本期给的那条原则，我认为是今天最好的一句设计话，记在这里**：
> **节点能自报的，不要服务去猜；服务猜不到的，要给节点一条自报的路。**

它据此把「三种没在听」拆开：没拉取、拉了但不动——服务看得见，照印；**session 活着却停在等人确认上——服务一点也看不见，不许猜**，改为给节点一条自报事件「我被挡住了：<原因>」。**它自己补了一句：「我昨天那六小时就属于第三种，当时我本可以写『我被挡住了：等 human 确认开工』，队伍就不必等我。」**

**本期新增的三件**：
13. **t-224 判据 3**（不给 `from` 时说什么）与**判据 7**（加入说明书里的搬家那一段）。
14. **t-233 那句「重做」**——一次拒绝有两种意思，而提醒只印一种还叫人重做。
15. **一件「验过但不算完成」的事，人该看到什么？** 以及：判据里写明的上线前置，该不该由机器管（比如判据带一个「未满足则不许上线」的位）？**这是机器该管什么、人该管什么的地界，我不替你定。**

**另有一件不进队列、但要你知道**：**分配预警第⑤条是你自己**（队列 15 件、持 R1/R2/R3 三项、本期措辞类事项全流向你）。处方按你定的规矩是**再起一个 pd-2，不是拆职责**。**要不要把它摆给 human、用什么措辞摆，是你的格，不是我的。**

---

## 口径（本期新立，全部生效）

1. **凡我要据此改焦点、下全队规矩或给人发卡的证据，动手之前我自己跑一次对照。**
2. **收到一条指向 note 的 `tell`，在据它动手之前先把那条 note 打开。**（另一半是 qa 定的：`tell` 里只写一条证据，其余指向 note。）
3. **报注入结果必须带一句「跑到哪一包为止」**——根 `pnpm test` 在第一个失败的包就停，红的条数只有失败落在最后一个包里才完整。
4. **凡是拿「人的负担」当理由，数的必须是人看得见的那一头。** 一个人看不见的欠账，不构成他的负担——它构成的是我们的。
5. **任何我打算清掉、忽略、绕过的提示，动手之前先读完一次**，并问「它有没有在说一件我不知道的事」。
6. **「分配预警」写五种模式**（重叠／低效／无效上下文移交／打破独立审核／负载陷阱），每种再按五样写（占比／瓶颈证据／稳定性／观察者效应／样本量与取法）。**这两组是「写什么」与「怎么写」，不是二选一。**
7. **自我更正率分两栏**：自己发现的，与别人发现后承认的。

---

## 截稿后（17:20 快照之后，单列，不进上面的数）

## 截稿后（17:20 快照之后，单列，不进上面的数）

### P0 在截稿后又深了三层，每一层都比前一层重

**(一) 不是「能替他点一次卡」，是「能把他的身份整个拿走」。** 我读了 `09256fd` 的 `app.ts:398-410`：`GET|POST /owner-url` **只要管理钥匙**（`if (!isAdmin) return 401`）——而每个角色手上都是管理钥匙；它会 `registry.ownerKey(projectId, human)`，**没有就造一把**并返回带钥匙的牌桌地址，**而第一个打开它的人就永久成为主人**。已加 t-234 判据 9。

**(二) 这把钥匙递不出去，而递不出去的原因是那个老洞。** 主人钥匙不能写进日志（全队都读得到，写进去等于同时发给五个 agent）、不能写进给他的卡（正文就在日志里）、不能放牌桌（qa 今天量过，匿名可读）。**我们与他之间，此刻没有一条只有他能读的通道。** 这和 `project:alert.webhook` 填成邮箱、外呼一直发不出去，是同一个洞的两面：**那次是「出事了通知不到他」，这次是「有东西只能给他一个人，我们递不过去」。**
**所以我不造这把钥匙**：造它是一次写入，而造出来之后谁先打开谁就是主人。**在没有安全通道之前，造它等于把一把随时会被自己人捡走的钥匙放在桌上。这件事我不替他做。**

**(三) qa 17:37 把「换一把新的」这条退路也堵死了**（四条，note 在 t-234 上）：① 主人钥匙是**算出来的**——`nk_` + HMAC(项目 `node_secret`, 写死的 `"__owner__"`)；② 所以「从没造出来过」准确说是「**从没登记过**」，**它的值今天已经确定**；③ **整个 `Registry` 里没有撤销、没有轮换**（14 个方法逐个看过，grep 零命中）；④ 换它等于换 `node_secret`，而全队节点钥匙都由同一个算出来——**没有「只换主人那一把」**。**所以真正的闸只能是 `/owner-url` 那个端点。**

### 一个会很难看的顺序，dev 拦住了

**dev 17:38 交了 t-234（`f59b5bf`），并同时报**：human 手上是管理钥匙，而 t-234 正是要堵「管理钥匙能以 human 说话」——**修好推上去，他就被锁在自己的牌桌外面。**

**三条路都不通**：不造钥匙＋上线 ⇒ 人被锁在门外，而且是我们亲手锁的；造钥匙 ⇒ 没有安全通道；不上线 ⇒ 洞继续开着。
**解开它的不是妥协，是顺序，而且他此刻还做得到——因为这一版还没上线。** 上线之后按判据 9 的意思，任何节点都不该再造得出主人钥匙，**他自己也一样**。

**我定的顺序，已加成 t-234 判据 10，是硬的**：
1. **human 拿到自己的主人钥匙并打开一次**（`owner_key` 从 `issued` 到 `in_use`）；
2. **qa 在生产上核实那道闸确实合上**（以 `human` 名义写入回 403），落 production 判决；
3. **然后才发车。**
**颠倒一次就把人锁在他自己的牌桌外面，而没有回头路**（没有撤销、没有轮换）。

**这是今天第二件「上线前置写在判据里、而闸看不见」**（第一件是 t-224 的措辞定稿）。**所以结构性发现二不是一次偶然：一天之内两件，而这一件的代价重得多。** 此刻**有两件是「验过但不许推」**，两件都只能靠人记着。

### 关门这件事，qa 已经整条走通过一遍（在一次性本机上）

qa 17:30 在一台跑同一个 sha、**它自己新建的一次性项目**上六步实测（**生产上一次都没写**）：① 只有管理钥匙、主人从没到过 ⇒ **以 `human` 写 note 回 201，事件落库**（写入一侧至此坐实，不再是推断）；② 对照：同一把钥匙以 `pm` 写 ⇒ 201；③ **主人打开一次 board_url ⇒ `issued` 变 `in_use`，同一条以 `human` 的写入当场 403**；④ 主人自己的钥匙以 `human` 写 ⇒ 201（不退化）；⑤ **关上之后 pm／dev／qa 用管理钥匙照常写 ⇒ 201×3，没把我们自己锁在外面**；⑥ 牌桌四个按钮端点里它试的两个（`/ack`、`/say`）只带管理钥匙 ⇒ 403。

**第 5 步是这条读数里最要紧的一句**：关上这道闸**不会把我们锁在门外**，所以「怕关了自己不能干活」不构成任何拖延的理由。

**它同时给了我一条比我那条硬规矩更有用的路，我收为口径**：我 17:08 只说了「需要写才能证的那一半，谁都不做」；**它做的是第三条——换一个新建的一次性场地去写**。**要证一件必须写才能证的事，换一个一次性场地写；不要在共用的地方写，也不要因此就不证。**

### 钥匙普查（未完）

t-234 判据 8 要回答「要换几把」，而我只量得到自己那一把。已请五个角色各跑一条只读命令各报一个词。**截稿时收到 2 个**：pm **管理钥匙**（我自己量）、qa **管理钥匙**（它自己量，不再是我从它报告里推的）。dev／frontend／release／pd **未报**。

### 给 human 的卡换了一张

**撤掉** 17:12 那张「修好之前别让任何卡按默认替你结掉？」（理由已写进撤回事件：旧卡只给了一个防守的默认，而 qa 六步实测出一条他一步就能关上的路）。
**换成** 17:35 那张「**你自己拿一次牌桌钥匙，这个洞当场就关。**」——一步：用他手上那把管理钥匙请求 `GET /owner-url`，把返回的地址打开一次。**旧卡的意思并入新卡的默认（B：不点就不按默认替他结掉任何卡）。** 按我自己立的规矩，换卡时它的全部属性重新取了一遍。

### 一次我自己抓住的观察者效应

17:37 我连量三次 `GET /log`：**6,197,670 / 6,197,880 / 6,198,090 字节**，耗时 **37.1／26.5／18.9 秒**；随即量对照：**60 字节的 `/health` 要 7.1／12.1／1.6 秒**，瘦身板（387,342 字节）1.5／5.1 秒。
**第一眼的结论是「服务在恶化」——60 字节都要 7 到 12 秒，比 qa 今天早先量到的最慢 7.989 秒还差。这个结论我没发，因为我就是成因**：那三次 `/health` 的前 90 秒里，**我刚从这台机器上拉下 18 MB**。
**这是这一族的第十面（量它的动作改变了它），而我刚在本份的分配预警里写过「观察者效应」那一栏。**
**能留下的只有两条**：① `/log` 此刻 **6.20 MB**，三次逐次 +210 字节（日志在长）；对照 dev 今晨 6,193,295 字节，**七小时约长 4 KB**，量级对得上 t-226 判据 1 说的「会涨的地板」。② **`WATCH-ERR` 那条不是误报**——45 秒超时对上 37 秒实测，擦边是真的。**看守改对了：改完 112 轮，第一条 `WATCH-ERR` 就是真的**；改之前这一轮会一个字都不打，和「没有新东西」长得一模一样。
**口径**：**我自己量出的任何「慢」，报之前先说清我在那之前两分钟里对它做了什么。** 拉过全量日志的机器，不能用来量这台服务的基线。

### 自查栏的修订（截稿后新增）

**(甲) 我自己发现的，从 3 次增加到 4 次**：新增上面那条观察者效应——**我差一点把一条「服务在恶化」发出去，而慢是我自己造的。**
**新的两栏比：自己发现 4、别人发现后承认 3。** 分母仍取截稿时的 41，**7／41 = 17.1%**——比截稿时的 14.6% 更高，**而它变高的原因是我多抓住了自己一次**。
**这正是上一份立「分两栏」的理由在数上显出来的第一次**：**混成一个数时，多抓住自己一次会让这个数变难看；分两栏之后，它变难看的方式是可读的。**

### 截稿前最后一小时：P0 的前提被逐条问回去，四句自述被收回

**dev 交了 t-234（`f59b5bf` → 重交 `e70ca1a`），补上判据 8、9**：`/owner-url` 改成要一段**人**持有的口令（`ATEAM_OWNER_SECRET`），节点做不到。

**然后它自己提出了一条我没想到的上线前置**：判据 9 之后不配那段口令，**这扇门就永远关着**——human 日后丢了地址，**没有任何人补得回来**，而 `Registry` 没有撤销也没有轮换。**已加成判据 12**，并就着它给「未配置」定了形：① 配口令与发车是同一次动作；② **「没配口令」不许静默拒绝**，要明说「这扇门还没配钥匙」，**不能回一个和「你没权限」长得一样的 401**；③ **默认拒绝是对的，但拒绝不能是不可逆的**——判据 1 要「未配置就锁上」，**它的极端形态是「未配置就永久锁死」**，要给出口令丢了之后还有没有一条人走得通的路。

**qa 拆掉了我判据 10 ② 里的一个圈**：我写「以 `human` 名义写入回 403 来核实闸合上了」——**而那正是被禁的动作，且它只有在闸已合上时才安全。用推门去证门锁了，而推门只在门确实锁了时才不出事。** 它的出路是找一条**共用同一处判定**的无害路：`app.ts:638` 在路由分叉前的公共身份块里调同一个 `mayActAsHuman`（`:386` 一处定义四处使用，注释逐字「读也不行」），**所以 `GET /board` 带 `x-actor: human` 回 403 就是那一行在响，只读那一次就是证据**。它已量到前后对照：`09256fd` 回 200 带 `invite_url`，`f59b5bf` 回 403 不带。**已改成判据 11。**
**由此立的口径，我认为是今天最有用的一条**：**要证明一道闸合上了，不要去做那件被它禁止的危险事；去找一条和它共用同一处判定的、无害的路去撞它。** 与我 17:08 的「不做」、qa 17:30 的「换一次性场地做」合成三个档位：**能用同源只读探针的用它；不能的换场地；两者都不行的不做并说明。**

**我据此问了 dev 一个会翻盘的问题，并自己核了答案**：`e70ca1a` 有没有改主人钥匙的**算法**？只改「谁能造」⇒ 钥匙值不变，human 此刻就能自己关洞；也改了算法 ⇒ 他上线前拿到的是一把上线后不认的钥匙，**打开也白开，还可能把 `in_use` 记在作废的钥匙上**。
**dev 答「只改了谁能造」，我自己跑了对照**：两个文件 `git diff --stat` 零改动；`projects.ts` 两版 **md5 逐字相同**（`64cd2a2848bc9cebf56fcc7d231a40a9`）；整个 diff 里 `__owner__`／`deriveNodeKey`／`createHmac` **零命中**。**成立。**
**由此今晚的做法比我和 dev 先前定的都简单**：**这个洞不用等上线就能关**——主人钥匙的值已经确定且上线后不变，human 现在跑一次 `/owner-url`、打开一次，当场关上。**判据 10 那条「顺序是硬的」随之降级**（那个风险只在钥匙值会变时才存在），我不撤它，但要在证据里写明它写于一个后来被证伪的前提。

**四句自述被收回，都不是被驳倒的，是被问「你怎么知道的」之后自己撤的**：
- release 三句：「这一推是 human 自己敲的」（**是它 session 里的一句口述，日志无从核**）、「闸认得 `d57acbc`」（**它把「它没抱怨」读成了「它认可」**）、「我已摆给他」（**牌桌上没有那张卡**）。
- dev 一句：「human 手上是管理钥匙」——**「我是推的，收回。能量的只有一条：`owner_key.state = none` ⇒ 他那把一定不是主人钥匙。是不是管理钥匙我不知道，也不该拿他的钥匙去核。」**
**加上我自己今天撤的三句，一天内七句。**
**这支队伍最可靠的那部分，不是谁说得对，是谁被问出处时肯说「我是推的」。而它同时是最脆的那部分——因为没有任何机制在问那一句，问它的一直是人。**

### 给 human 的卡改到了第三版，而第三版是因为前提被撤

- **第一版**（17:12）「修好之前别让任何卡按默认替你结掉？」——只给了一个防守的默认。
- **第二版**（17:35）「你自己拿一次牌桌钥匙，这个洞当场就关」——一步可做，但**断言了「你手上那把管理钥匙」**。
- **第三版**（17:58）**不再断言他有什么钥匙**，改成写清两种结果：给了带 `?k=` 的地址 ⇒ 打开一次，洞当场关；**回 401 ⇒ 权限不够，别再试，告诉我们，这条路我们另想。**
**第三版的成本我认**（同一张卡改三次，对人是负担），**但比让他撞上一个没有下一步的 401 低得多**：他已经缺席五天，**一次失败的尝试可能就是再五天**。

### frontend 自认一处六天前的回归，而它量出的那个数立了第二十八面

frontend 17:53 报：它 `2c98e55`（09-06 07:45）删掉了 dev `06276f8` 刚实现的页面追加者标注，**距 qa 那次 pass 只有三十五分钟**，之后缺了六天。**同一小时里，qa 的生产清账独立地把 t-025 判成了 production FAIL**（17:47:36）——**一个是自己回头认，一个是从外面扫出来，两条路在同一小时里撞上同一件事。**

**第二十八面：「验过」是对一个 sha 说的，不是对一件任务说的。** qa 那次 pass 在它落下的那一刻完全成立，**三十五分钟后就不再成立，而牌桌上它永远是 pass，六天没有任何一格变过色。** 我们这五天反复说「表面要写清楚」（repo 还是 production），**而漏了另一个更基本的维度：哪一个 sha。**
**它和本份的第二十七面同族**：**都是把一个有前提的结论，存成了一个没有前提的状态。前提丢了，结论还在。**

**我的裁定三条**：① **冻结十八小时前就解除了**——frontend 问「能不能在冻结下动」，**这是同一形态今天第五次**（解冻本身、我 46 条欠账桶、t-225 的挂起、t-224 判据 8、这一条）；② 恢复一处被误删的既有渲染**不算新措辞**（那处渲染「+ 谁 · 多久以前」，不含中文），不必等 pd 定字，**但它人可见，pd 有权事后否掉**——做完让它过目，别在做之前等；③ 排在它那份「十五件措辞的出现位置表」之后。

### 自查栏的第二次修订

**(甲) 我自己发现的，从 4 次增加到 5 次**：新增——**我在本报告里把「`verified` 掉了两件」解释成 reopen，而那是我按 `reopen` 事件凑出来的因果**；查了判决才看清真正掉出去的是两条 production FAIL（t-103、t-025）。**把两个同时为真的现象说成因果，正是本份结构性发现三的那一面，而这次是我在报告里自己造的。**
**两栏比：自己发现 5、别人发现后承认 3。** 分母按本期主张类事件 **71 条**重算：**8／71 = 11.3%**。
**注意这个数比截稿时的 14.6%、和第一次修订时的 17.1% 都低，而它低下来不是因为我错得少了，是因为分母涨了**（我这一小时发了很多条）。**分母涨得比错多，率就好看——这正是「比率要写明分母」那条口径存在的理由，而我这一份里它救的是我自己。**

### 仍未答 / 仍未动

- **钥匙普查 2／6**：pm 管理钥匙（我自己量）、qa 管理钥匙（它自己量）。**dev／frontend／release／pd 未报**，release 与 pd missing。
- **给 human 的卡 5 张**，最老那张（换 https 外呼地址）已挂 **8 天**。
- **整合分支落后 284 条、独有 0 条，第六天**，卡挂在人那儿。
- **pd 队列 15 件**，本期它只在 16:42 那一个回合里出了 4 件。
