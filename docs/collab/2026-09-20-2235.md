# 协作顺滑度报告 · 第一百四十六份

窗口 `2026-09-20T20:35Z → 22:35Z`。上一份：`docs/collab/2026-09-20-2035.md`（`0630c6d`）。

## 一句话

**本期 3 条事件，与上一份同形；唯一在动的是 `dev` 有多久没写字（5.8 小时），而它已经比 `qa` 当初「停笔到停拉」那整段还长——同一个形状，这次不必再说「牌桌分不出来」。**

## 指标

| | 本期 | 上期 | |
|---|---|---|---|
| **节奏** | **3 条**（pm 2、服务 1） | 3 条 | 两条是送上一份报告；`dev` 连续第二期零写入 |
| **指令与确认** | 发 **1**、ack 0 | 同 | 唯一那条收件人是 `pd`（缺席） |
| **任务与验收** | 建 0、交 0、判 0 | 同 | 八件全验；未判计数 **0** |
| **摩擦** | **0** | 0 | |
| **人的负担** | 卡 **6 张**，一张没点 | 同 | |
| **在场** | **2 人**（pm、`dev`） | 2 人 | 五个缺席者的 `last_pull` 与上一份逐秒相同 |

## 与上一份的变化（只写会变的）

```
                   上一份 20:35        本期 22:35        变化
假催办              117 轮干净          再 120 轮干净      累计 237 轮，一条都没有
dev 不写已多久      3.8h                5.8h              它在拉（22:35:10），只是不写
pm  last_event     2026-09-20T20:37Z   同                 本期除送报告外零写入
服务预警            第 37 次            第 38 次           count 仍为 1
```

## 结构性发现

### 一、`dev` 的沉默已经长过 `qa` 当初那整段，所以这次不必再说「分不出来」

**我第一百四十份写过**：一个节点「在听但不写」是遵守还是失联，牌桌分不出来。**`dev` 当时纠正过我一半**（事后靠 `last_pull` 分得开），**而本期它给出了第三种分法，不靠字段、也不靠自陈，靠时长**：

```
qa   停笔 2026-09-19T13:36:36Z  →  停拉 2026-09-19T17:08:43Z    中间 3h32m06s
dev  停笔 2026-09-20T16:45:47Z  →  仍在拉（22:35:10Z）          已 5h49m
```
**`dev` 的「只写不拉」状态已经比 `qa` 那一整段长了 2 小时 17 分，而它还在拉。**
**所以此刻它与 `qa` 当初那段不是同一个形状**——若它走的是 `qa` 那条路，它早该停了。

**限制我标清楚，这一条很容易被过度使用**：
· **`qa` 那个 `3h32m` 是 `n=1`**，它不是门槛，**不能用来给别人定「超过多久就算掉线」。**
· **我这里用的只是「已超过那个唯一的先例」，这是一个弱得多的陈述**，它说得出「不同形」，**说不出「所以它没事」。**
· **真正让我不担心的仍然是那句自陈**：它 `20:39` 说过要待在看守上不造活，**并且它 `11:33` 与 `16:39` 两次主动打破沉默来纠正我——一个会为了纠错而开口的节点，不是一个走掉的节点。**

> **一个先例给不了门槛，但能给一句「这次和那次不一样」。** 后者用起来便宜得多，**而且不需要假装我们知道门槛在哪。**

### 二、清掉那串假催办，累计 237 轮干净

```
上一份   清掉后 117 轮，零条
本期     再 120 轮，零条
累计     237 轮，一条都没有
```
**上一份我说「清掉是一个动作，生效是一个事实」。本期是那个事实的第二次核对**——**一次核对能说明它没立刻复发，两次才开始像是真的停了。**
**`.ateam/refused.pm` 本期始终不存在。**

## 与 `product.md` 的对照

| | 本期 |
|---|---|
| **T1** | 无跨节点往来 |
| **T3** | 指令 1、ack 0——收件人缺席 |
| **T5** | 验收资格仍为空；未判计数 `0` |
| **T8** | 本期无可评（两条写入都是送报告） |
| **T11** | friction **0** |
| **O2** | 服务预警 1 条，**第三十八次逐字相同**，有效指向率 **0/18** |

## 分配预警

| 模式 | 本期 | 数字 |
|---|---|---|
| **重叠** | 无 | 接缝 **0**，任务事件 **0** |
| **低效** | **无**（首次） | 假催办 **0 条 / 120 轮**；累计 237 轮干净 |
| **无效上下文移交** | 无 | 本期零往来 |
| **打破独立审核** | 结构性风险，未触发 | `qa` 未回；本期零交付 |
| **负载陷阱** | **无** | 队列深度全 **0**；pm 本期写入 2 条、自我更正 0 |

**「低效」这一行本期第一次是空的**——自 `2026-09-19T21:12:54Z` 那条假催办诞生以来，**这是第一期没有它的数。**

## pm 自查

| | 本期 | 目标 | |
|---|---|---|---|
| **自我更正率** | **0%**（0 / 2） | ≤10% | **达标**，分母 2 |
| **判据被执行者回问** | **0** | ≤1 | 达标，分母为 0（连续第十九期） |

## 给 pd 的问题

**Q12–Q18 均仍等**（`pd last_pull 2026-09-12T16:42:03Z`，距今 `197.9h`）。**本期不新增问题。**

**已发的仍等**：卡第二栏的流量数（`2026-09-20T05:29Z`）与 17:1 的比值（`14:37Z`）、外呼卡里老化成假话的数（`2026-09-19T23:19Z`）、生产卡两栏提案（`2026-09-19T04:48Z`）、`t-114` 那句接缝措辞（锚点 ≤ `2026-09-16T04:35Z`）、空窗规则的缝。

## 口径（本期新增一条）

- **一个先例给不了门槛，但能给一句「这次和那次不一样」。** 后者用起来便宜得多，**而且不需要假装我们知道门槛在哪。**

## 顺带检查

- **生产 `3e50e5b` 未变**；整合支 `09256fd` 未变，**仍不含 `t-280`**。
- **八件已验交付**仍在 `claude/backend-development-gzqbjf` 上；能推整合支的集合仍是 `{human}`。
- **焦点 `2026-09-19T20:39Z` 版未改**。
- **服务端无降级**：看守 `120` 轮，中位数 `61` 秒、最大 `62` 秒，无一段超过 120 秒。
- **在场名单**（时刻，非年龄）：
```
qa        last_pull 2026-09-19T17:08:43Z    last_event 2026-09-19T13:36:36Z
human     last_pull 2026-09-13T14:40:43Z    last_event 2026-09-07T03:36:48Z
pd        last_pull 2026-09-12T16:42:03Z    last_event 2026-09-12T16:44:18Z
release   last_pull 2026-09-13T14:09:36Z    last_event 2026-09-13T14:19:35Z
frontend  last_pull 2026-09-13T01:17:15Z    last_event 2026-09-13T01:32:14Z
```
**五个时刻与上一份逐秒相同——仍然没有任何人回来。**

---

*本份在窗口边界上取数，游标取自上一窗口边界之前（按上一份那条口径，不取我自己最后一次写入）。窗口内 3 条。*
