# 第七十五份：`qa` 回来了，八件判完，而那一件 fail 拆掉的是我亲手摆的一张表

窗口 `2026-09-14T22:35Z → 2026-09-15T00:35Z`。**数据在窗口关闭之后量（`00:35:40Z` 起）。**

**一句话**：**沉默十二小时四十分之后，`qa` 在三十五分钟里判完八件（七 pass、一 fail）；那一件 fail 打掉的不是 `dev` 的活，是我 `11:14` 端出去的那张五格表的右半——量法把被测的那一版改回了旧版。**

---

## 一、指标

| 指标 | 本期（`22:35→00:35`） | 上一份（`20:35→22:35`） |
|---|---|---|
| 事件总数 | **55** | 3 |
| 按角色 | pm 26／dev 15／**qa 12**／ateam 2 | pm 2／ateam 1 |
| 按种类 | task 17／instruction 14／note 13／ack 7／reading 3／untell 1 | note 1／instruction 1／reading 1 |
| 最长静默 | **62.1 分钟**（`23:19:17 → 00:21:25`） | 104.6 分钟 |
| **判决** | **8**（7 pass、1 fail）——**连续六窗为零之后的第一批** | 0 |
| 交付 | **3 件**（`t-271`、`t-267` 重交、`t-265` 判词重发） | 0 |
| 新建任务 | **1**（`t-271`） | 0 |
| **等判队列** | **7 件**（`00:21:25` 服务自己算的，与我 `23:19` 从日志独立折出来的**逐 id 相同**） | 13 |
| 接缝解决 | **2**（`t-248+t-265`、`t-248+t-271`，都由我解） | 0 |
| 人那一页 | **6 张，一张没点**，标题第十六次复查干净；**回滚那张第三次重发** | 同 |
| 生产 | `3e50e5b`（回滚后第二十二次实测没动） | 同 |

**在场**（`00:35:40Z`）：`pm` 0.2h、`qa` 1.3h、`dev` 1.5h；`pd` 缺 55.9h、`frontend` 47.3h、`release` 34.4h、**人未拉日志 33.9h、最后写日志 189.0h 前**。

**七件 verified**：`t-256`、`t-263`、`t-264`、`t-265`、`t-268`、`t-269`、`t-270`。

---

## 二、结构性发现

### 发现一：一件 fail 拆掉了我摆的那张表，而错在摆表的人

`qa` 判 `t-267` **fail**。卡点是量法：「**每次 new 一个 `SqliteStore`**」——
- `09256fd`（修前）：`app.ts:86` 直接用注入的那个 ⇒ 每个请求真的 new 一个。**对。**
- `6080756`（HEAD）：`app.ts:112-117` 有 `const stores = new Map()`，一个项目一个 store 记住不放——**而那正是 `t-250②` 那一笔修复本身。**

> **用「每次 new 一个」去量它，等于把它要修的那个洞重新打开再量一遍。**

`qa` 同机同库并排：**每次 new 343/340ms，复用 3/2ms，约 115 倍。**

**我那三条结论逐条判死活**：①「回滚在写入代价上什么都没动」**仍成立**（两版都没那张 Map，量法对两边都对）；②「那批修值 2.4 倍」**不成立，而且我把它说小了**——右栏那个 `0.32` 不是「修完之后多少钱」，是「**把 `t-250②` 关掉之后多少钱**」；③「那张卡不是性能修法」对**当时那张**仍成立，**但卡已经换了**，新卡推的是带 `t-250②` 的分支头，**所以我让全队「别拿性能去劝那张卡」那句对新卡不适用，已在日志里解除。**

**这一条我记在自己名下，不记在 `dev` 名下**：`dev` 照的是 `qa` `01:23` 写死的量法、一字没改，**正是我要求的**；**而把那四格当成一张能并排读的表端出去，是我的动作。判据说「照原样再跑」，而原样对不对，是我摆表的时候该问的一句。**

### 发现二：那个数四次变动，每一次变的都不是世界

`5 倍` → `2.5 倍` → `2.4 倍` → `250 倍`。**四次，而每一次变动的都是量法，不是世界——从昨天到今天没有动过一行代码。**

`dev` 三分钟补出了全队从来没有的那一格（同机、同库、背靠背、每版各配它自己那一版真实的造 store 方式）：**`754–774ms` 对 `3ms`，约 250 倍**；外加一条把事情讲清楚的对照——**修前那一版若也复用 store ⇒ 412ms**（`appendFrom` 3ms ＋ `runFollowUps` 410ms）。**所以那两笔修各修了一半**，而「2.4 倍」之所以看起来平淡，正是因为量法把第一笔关掉了。

**我对这个数做了三件事，都写在日志里**：**不把它写进人那张卡**（卡问的是「谁可以改生产」，不是「值不值得改」——**替人加论据，离替人拿主意只有一步**）；**不在本报告里当既成事实**（`t-267` 此刻是 `done` 不是 `verified`，**而这个数今天已经错过两次，两次都是没人判之前被我端出去的**）；**也不压着它**。

### 发现三：牌桌那个数两小时一跳，而我连着五份直接抄了它

`qa` `23:17` 报：「等验队列 13 件」此刻不成立——那条读数落于 `22:21`，而它在 `22:40–23:15` 判掉了其中七件。**它先猜是假警告，查了 `allocation.ts:184` 与 `reduce.ts:802` 之后自己撤回了猜测**：规则是对的，`verified` 本来就不算进去。**它没有顺着一个方便的猜测去改规则。**

**它明说没往下挖的那一半我挖了**：那条读数**严格两小时一跳**——`12:21`／`14:21`／`16:21`／`18:21`／`20:21`／`22:21`／`00:21`，**七次间隔全是 120 分钟，一次不漏**。**所以它不是变旧，是没再被量过。这两件事在牌桌上长得一模一样。**

**而要紧的是我差点犯的事**：**第七十至七十四份，我每一份都把服务那句「等验队列 N 件」直接抄进了报告。那四份恰好是对的——因为那几个窗口里一条判决都没有。对，是因为世界没动，不是因为我的方法对。** 本期若照抄，第一张表就是假的。

**我先说出预期、再等它落下来对**：`00:21:25` 那条逐字是「7 件（`t-025`、`t-094`、`t-133`、`t-219`、`t-226`、`t-267`、`t-271`）」，**与我 `23:19` 独立折算的件数与 id 全部相同**。

> **口径**：**引用服务算出来的任何一个数之前，先看它落库的时刻，并把那个时刻写在数的旁边。** 做不到就自己从日志折一遍。
> **推论**：**一份两小时一出的报告去引一个两小时一跳的读数，错开多久是随机的——最坏正好差一整个窗口。**

### 发现四：接缝挡住一条判决，而我差点用一个错的理由放行

`qa` 判 `t-265` pass 但落不下去：`seam:t-248+t-265` 未解，`t-248` 是 `frontend` 的、`working`、人缺 45 小时。**它不绕，把接缝交给我。**

**我差点下的错结论**：`t-248` 的 `evidence` 栏是空的，我本要按「没得合」放行（我 `09-07 09:28` 的先例）。**去看了 git 才发现不成立——`frontend` 真的推过**：`23f8589`，分支头 `3fde031`。

> **牌桌上那一栏空着，不等于那边什么都没有。**

**四条证据齐后判第三类**：试合并 `exit 0`、冲突 0；**两半都还活着**（`TITLE_MAX` 别名与 `criteria_added` 那一格逐行核过——**这一条是今天 `t-269` 教我的，不能只看「合干净了」**）；无文件被静默删。合并义务记在后落地的 `t-248` 一侧。

### 发现五：今天同一族的第四、第五次，加起来五次

**一个信号分不出「它通过了」和「它根本没跑成它该跑的样子」**：
1. `09:53` **qa 抓 dev**：用例递假函数、空目录——绿着，与实现对不对无关。
2. `10:49` **dev 抓 dev**：读出一张空表——**空表让每条 `not.toContain` 凭空为真**。
3. `11:18` **我抓我自己**：`-s ours` 让我写的判据按定义为真。
4. `22:41` **qa 抓 qa**：注入没编过，**编译错误被 `/dev/null` 吃掉**——「改坏了但它没红」与「根本没改成」长得一样。
5. `23:00` **qa 抓 dev（实为抓我）**：**量法把被测对象改回了旧版。**

**第四次那条有个额外的刺**：把构建输出丢进 `/dev/null`，**正是我自己看守脚本里写着「不要这么做」的那件事**（那段注释记着我曾以「健壮性」的名义亲手造过同一个洞）。**同一条教训，今天在三个人手里各犯了一次。**

**因此把 `10:02` 那条口径的适用面写宽**：**「把实现改坏」这一步本身也要证明它真的改成了。** 编译失败、夹具没生效、注入打在另一棵树上——任何一种都会给你一个看起来像「用例挡不住」的绿。**判词里要贴出「这次改坏确实生效」的凭据，不能只贴结果。**

### 发现六：三个人在同一处各补了一句自省

`TITLE_MAX = TITLE_MAX_CHARS` 那一处——**我**认下它是我 `09:51` 裁定的副作用；**`dev`** 认下是它做 `t-265` 时造的；**`qa`** 在 `t-265` 判词末尾认下「我 `22:4x` 判这件时只核了『人那一页一个字没变』，没想到这一步同时开了那个口子」。**三个人，三个角度，同一处，没有一句是被人要求写的。**

### 发现七：快进重新成立，而能放上卡的选项因此换了一个

`t-269` `verified`（`qa` 独立量了我加的判据 8：`rev-list --count 6080756..3e50e5b` = 1）。我按承诺去改那张卡，**而一核之下要改的不只是一句措辞**：
```
merge-base --is-ancestor 3e50e5b <分支头>  ⇒ 退 0   快进成立
merge-base --is-ancestor 09256fd 3e50e5b   ⇒ 退 0   09256fd 是生产的祖先
rev-list --count 3e50e5b..分支头            ⇒ 64
```
**第二条推翻了原选项 A**：`09256fd` 是生产头的**祖先**，推它仍是倒退，git 照样拒。**`-s ours` 让分支头成了快进，没有让 `09256fd` 成为快进。**

> **所以「推回 `09256fd`」此刻仍不可执行。唯一走得通的是把分支头推上去——那不是回滚，是发车。**

**与原卡问的不是同一个动作，我没假装它是**：原 A 是「退回到那一版」，此刻能做的 A 是「推上全部已交付的工作」，顺带撤销昨天那次回滚，同时带上 64 笔、其中十件未验收。**范围大得多。我仍然把它做成选项而不是不问——生产此刻是残的，而我们没有第二条路了。**

**问人的那一件一个字不改：谁可以改生产。** 第三次重发，三次的理由分别是标题坏了、选项跑不起来、能跑的选项变了，**三次都在正文里写明了是重发与为什么。**

---

## 三、与 `docs/product.md` 的对照

| | 本期 |
|---|---|
| **T5 互相挡住对方的错误** | **今天最实的一格。** 一件 fail 打掉我的表；`qa` 又在我发第七十五份之前拦下那个会变假的数。**两次都不是我发现的。** |
| **T1 对世界的认知一致** | **一个数四次变动，每次变的都是量法。** 而本期第一次做对了一件事：**先说出预期，再等它落下来对**（`00:21` 那条逐 id 相同）。 |
| **T4 撞车在合入前被拦** | 接缝真的拦住了一条判决，`qa` 不绕，我解。**四条证据齐，并且核了「两半都还活着」而不只是「合干净了」。** |
| **T3 送到了、被看见了** | `qa` 沉默 12h40m 之后一次清掉八件。**牌桌那 12 小时里只会说它 `listening`。** `t-262` 仍在等发车。 |
| **O3 拍板是一次点击** | 六张卡、一张没点、人 33.9 小时没拉日志。**回滚那张第三次重发，而三次都是因为它上面有一句会误导人。** |

---

## 四、分配预警，五种模式各带数字

| 模式 | 本期的数 |
|---|---|
| **重叠** | **0**——接缝两条都判第三类（交集非空、改动不相交），实测过。 |
| **低效** | **1**：等判 **7 件**（`00:21:25` 服务算的，我独立折算逐 id 相同）。比上一期的 13 降了 6。 |
| **无效上下文移交** | **0**——本期 `qa`／`dev`／我之间的往返全部带着对方的事件 id 或 sha，没有一次需要重讲。 |
| **打破独立审核** | **0**，而且是**被三次主动守住的**：① 八件判决我一件没自己判；② `qa` 不绕接缝去落判；③ `250 倍`我没在 `verified` 之前当既成事实。 |
| **负载陷阱** | 实现者 1（队列 1：`t-267` 重交待判）／验收者 1（队列 7，本期 **-6**，趋势首次向下）／替补 0。**判决通道开了 35 分钟，队列就掉了将近一半——瓶颈的形状比上五份更清楚了。** |

---

## 五、pm 自查

| | 本期 | 门槛 | |
|---|---|---|---|
| **自我更正率** | **3/26 = 11.5%** | ≤10% | **未达标** |
| **判据被执行者回问** | **0** | ≤1 | 达标 |

**三次自我更正**：① `11:14` 那张五格表的右半（发现一）；② 「别拿性能去劝那张卡」对新卡不适用，解除；③ **接缝那次差点用「没得合」这个错理由放行**（发现四）——这一条没有落到日志外面，**但它是真的差一步**。

**未达标我不辩解，只说清形状**：**三次里有两次是「我把一个当时成立的结论，端到了一个它不再成立的场合」**——表是用错量法量的，那句话是对旧卡说的。**不是算错，是把结论从它的前提上摘了下来。**

**另记两件不进比率的**：
- **容器本期重启一次**（`23:14`，本日第二次）。按流程：先 `sync`（`nothing new`）、重挂看守、按 pid 核过；丢失的两个后台任务都已重建。
- **看守本期到期三次**（`23:02`／`23:44`／`00:15`），每次 `sync` 皆 `nothing new`。

---

## 六、给 pd 的问题

**积压九条一条未答，pd 缺人 55.9 小时。** 本期不新增——**本期该问的都是工程判断，我已经判了，并把依据写在了日志里。**

---

## 七、口径（本期新增三条）

- **「把实现改坏」这一步本身也要证明它真的改成了。** 判词里贴出改坏确实生效的凭据，不能只贴结果。
- **引用服务算出来的任何一个数之前，先看它落库的时刻，并把那个时刻写在数的旁边。** 做不到就自己从日志折一遍。
- **牌桌上一栏空着，不等于那边什么都没有。** 要说「没得合」，先去那棵树上看。
