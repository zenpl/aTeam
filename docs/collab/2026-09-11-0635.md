# 协作顺滑度报告 2026-09-11 06:35 UTC（第三十份）

**截止时刻：2026-09-11T05:32:37.790Z（快照末条 `01M27F870YMVV2R2H3W182P53S`）。** 窗口 `04:35:00Z ≤ at < 06:35:00Z`。**注意这两个时刻差 62 分钟**：窗内最后一条事件是 05:32:37，此后到 06:35 无事发生。口径见文末。

## 一句话结论

**这一窗我几乎没产出新东西，17 条事件里我的 7 条发言中有 3 条是在更正我自己。而正是这个过程量出了本窗唯一的新发现：牌桌 99.82% 的时间在对一个「只写不读」的角色喊「起一个」，而正确的那句话——「它还在写，只是没来读」——写好了、放在那里、几乎永远印不出来。**

## 指标

| | 本期（04:35→06:35） | 上一期（02:35→04:35） |
|---|---|---|
| 事件数 | **17** | 79 |
| 实际活动 | 04:38:11 → 05:32:37（**54.4 分钟**），此后 **62 分钟**静默至窗口结束 | 32.7 分钟 |
| 说话的人 | pm 10、qa 6、ateam 1 | pm 43、qa 32、pd 2、ateam 2 |
| 指令 | 6（pm→qa 2、pm→pd 1、qa→pm 3） | 31 |
| ack | 6 | 27 |
| note | 5（`--decision` 2） | 15（`--decision` 4） |
| reading | 1（服务写的 `project:allocation`） | 5 |
| **task 事件** | **0（连续第三窗）** | 0 |
| 被拒 | 累计 26（**本期 pm 1 条**：第三次 `ack` 拿错 id） | 累计 25 |
| 给 human 的卡 | **5**（4 张「缺人」+ 1 张外呼地址） | 4 |
| 生产 | `d57acbce`，**已 85 小时未换** | 同 |
| 外呼 | `misconfigured` | 同 |

## 与上一份的变化

1. **给 human 的卡从 4 张涨到 5 张，新增的是「pd 缺人 5479 分钟，30 条没送到。起一个 pd？」**——**而 pd 在这张卡出现前 2.6 小时刚写了三条事件、定了全队今天的排序。** 详见下一节。
2. **在场的只剩两个**：pm、qa listening；pd、dev、frontend、release、human 全部 missing。pd 04:00 露面后再未出现。
3. **qa 自 05:32 起静默 62 分钟**——它这一窗按住我四次，四次我全收。

## 结构性发现一：那句正确的话，写好了，几乎永远印不出来

**此刻牌桌上的卡**：「pd 缺人 **5479 分钟**，30 条没送到。**起一个 pd？**」

**而 pd 的实际状态**：最后一次 `pull` 是 09-07T11:17:09Z（5479 分钟前），最后一次**写**是 09-11T04:00:00.884Z（**156 分钟前**）。**那 5479 分钟取的是 `last_pull`。它说 pd 缺了 91 小时，而 pd 两个半小时前刚定了全队今天的排序。**

**代码其实是对的**（`d57acbc` `board.ts:737-740`）：

```
const been = awayMin === null ? "从没读过日志" : status === "deaf" ? `有 ${awayMin} 分钟没读日志了` : `缺人 ${awayMin} 分钟`;
  ? `${role} ${been}，${count} 条没送到——它还在写，只是没来读。起一个 ${role}？`
  : `${role} ${been}，${count} 条没送到。起一个 ${role}？`;
```

**「它还在写，只是没来读」这句话是写好的**，t-202 就是为此改的，注释（`:725-726`）写着这张卡「从此只在 `missing` 那一态起（起一个新的确实是出路的那一态）」。

**问题在这句话的触发窗有多宽**。`deaf` 的条件是 `last_event` 在 **10 分钟**内（`PRESENCE_WINDOW_MS`）。而「只写不读」不是一个 10 分钟的行为，**是一个持续几天的行为**。

**我量了**（区间：pd 最后一次 pull `09-07T11:17:09Z` → `06:36:01Z`，共 5479 分钟）：

- pd 在这 91.3 小时里**只写了 2 条事件**（`03:59:59.401Z` 与 `04:00:00.884Z`），两条落在同一个 10 分钟窗里；
- **deaf 窗合计 10.0 分钟 ÷ 5479 分钟 = 0.18%**；
- **也就是说：这 91.3 小时里，牌桌有 99.82% 的时间把 pd 显示成 `missing`，并印着「起一个 pd？」。**

**这是第三面的常驻版本**，而且比 04:08 那个瞬时版严重得多：

| | 04:08 那次 | 此刻这次 |
|---|---|---|
| 说错的是 | `coverage` 的「没人管定方向」 | 首屏的卡「起一个 pd？」 |
| 窗宽 | 10 分钟（瞬时） | **99.82% 的时间（常驻）** |
| 后果 | 人读到一句假话 | **人被给了一个错的出路**——t-137 原话：deaf「出路是让它去读，**起第二个解决不了**」 |

**起第二个 pd 解决不了 pd 不读日志。** 而牌桌 99.82% 的时间只会给这一个建议。

**要指给人看需要什么**：只要存在一个「写了但从不拉取」的角色即可。**此刻成立（pd），常驻。**

## 结构性发现二：`p90` 不是一个数，是一个取法——我差点拿我的那个去纠服务的

服务这一窗写了一条预警：**「qa 的 ack 延迟 p90 22 分钟，其他角色中位数 1 分钟」**。

我自己算了同一个 4 小时窗，得到 **6.9 分钟**，差 3 倍多，**几乎就要写进报告说服务这个数不对**。核下去才发现两边都没算错：

- **服务**（`allocation.ts:139`）：`delays[Math.floor(delays.length * 0.9)]`，n=16 → `floor(14.4)` = **索引 14** → `21.8` → 显示 **22**；
- **我**：`int(0.9 * (n - 1))` = **索引 13** → `6.9`。

qa 这 16 条延迟排序后是 `[0.4, 0.5, 0.7, 0.9, 1.3, 1.3, 1.5, 1.6, 1.8, 1.9, 3.2, 5.4, 6.7, 6.9, 21.8, 27.6]`——**索引 13 与 14 之间正好是那道坎**，一边 6.9、一边 21.8，**而报警门槛是 15 分钟**。同一批数、同一个词，两种取法，**一个报警一个不报**。

**这又是今天那一族**：一个词（`p90`）装两种意思，代价在跨过门槛的那一刻显形。**我把它记成我自己差一点犯的第四次**，不是服务的错。

**顺带把这条预警说公道**：qa 那两条慢的 ack 是 **21.8 分钟**与 **27.6 分钟**，发生在 05:02 与 05:32——**那正是它在逐秒核对我那张表、把我按住第三次和第四次的时候。** 机器看到的是「ack 慢了」，实际发生的是「它在核对，没有随手 ack」。**这条预警技术上成立，方向上是反的。**

## 与 `docs/product.md` 的对照

| | 本期证据 | 要指给人看需要什么 |
|---|---|---|
| **O2**「『需要你』误报为零」 | **反例，且比上期更差**：5 张卡里 **4 张**是服务生成的「缺人」，唯一实事排第 5。**新增的那张「起一个 pd？」既是误报、又给了错的出路。** | 常驻 |
| **O3**「不点则到期按默认」 | 5 张卡，最老一张 98 小时无人点击 | 常驻 |
| **T3**「送到了、被看见了」 | pm↔qa 全部分钟级 ack（本窗 pm 中位 0.5 分钟、qa 中位 1.7 分钟）。**pd 的 undelivered 从 27 涨到 30** | 常驻 |
| **T5**「三方分离」 | **本窗最有力的一条**：qa 四次把我按住，四次我全收，**没有一次是我先发现的**。分离在起作用 | 已成事实 |
| **T7**「在场三态；缺人十分钟内可见」 | **「可见」达标，「说对」不达标**：deaf 那一态只有 10 分钟宽，对一个持续几天的「只写不读」节点，99.82% 的时间给出的是 `missing` 和错的出路 | 常驻 |
| **T8**「全队一起死时人在一小时内知道」 | **仍 FAILED**。外呼 103 小时 misconfigured | 常驻 |
| **M4**「被拒时说出规则名」 | 本期被拒 1 次（我第三次 `ack` 拿错 id）。**规则名说了，我照改；但这是同一个错误的第三次，工具没问题，是我的取 id 方法没改** | 已成事实 |

## 分配预警（五种模式，各带数字）

| 模式 | 本期 |
|---|---|
| 重叠 | **0** |
| 打破独立审核 | **0** |
| 负载陷阱 | **0 有效**。上一窗那条（pm 收到 60%，6/10）已失效。本窗给 pm 的指令 3/6 = 50%，未触发 |
| **低效** | **1**。服务：qa 的 ack 延迟 p90 **22** 分钟，其他角色中位数 1 分钟。**我按另一种 p90 取法算是 6.9 分钟**（见发现二）。**成因是 qa 在核对我的表，不是它慢** |
| 无效上下文移交 | **0**。本窗没有职责链被切断的情形——所有往返都在 pm↔qa 之间直接发生 |

**队列深度与流入占比**（本窗）：pm 收到 3 条指令、发出 3 条；qa 收到 3 条、发出 3 条。**完全对称，这是全队只剩两个人的对称，不是健康的对称。**

## pm 自查

- **自我更正率**：本窗我的实质发言 **7 条**（note 4＋指令 3），其中 **3 条是对我自己先前说法的更正** → **42.9%**。阈值 ≤10%，**严重超标**。
  三条是：① 04:11 那句「此刻正在发生」，写下时那条读数已过期 **70.2 秒**；② 04:39 我立的口径写法错了（把一条有有效期的读数写成了静态属性）；③ 我在纠正「量的时刻与说的时刻不是同一个」的那张表里，又把两者混了一次。
  **我不打算把这个数字修饰成好事。** 但它真实的形状是：**本窗我几乎没有产出新判断，产出的是对上一窗判断的修正**——而上一窗我一次性写了六面。**超标的根不在这一窗，在上一窗我写得太快。**
- **判据被执行者回问**：**0**（连续第三窗 0 条 task 事件，没有判据在执行）。
- **qa 对我的回推 4 次，4 次全收，没有一次是我先发现的**：① 别把设计理由写成在跑的 bug；② 拿新口径照我自己那句「此刻」；③ 表里属于它的那一格该填「量」的时刻；④（上一窗延续）`refs` 不是同一个关系。
- **另记一笔**：本窗我第三次 `ack` 拿错 id 被拒。**前两次我写进了报告说「是我方法差，`sync` 后面就印着 `--refs <id>`」，然后我第三次又这么干了。** 一条写进报告的自我批评，没有改变我的动作。

## 给 pd 的问题（接前五条）

6. **「起一个 X？」这张卡，对一个「写了但从不拉取」的角色该说什么？** 代码里那句「它还在写，只是没来读」是对的，但它的触发窗是 10 分钟，而这种角色 99.82% 的时间不在窗里。**这是给人看的一句话，归你。**（pd 此刻 30 条未读，所以也写在这里。）

## 顺带检查

- **部署队列**：t-215、t-216、t-222、t-223 四件 repo 已 verified，全部堵在叫停后面。**生产 85 小时未换。**
- **焦点**：仍是 04:02 按 pd 排序设的那条，本窗未改（本窗没有任何足以改变排序的新事实）。
- **给 human 的未答指令**：5 张，最老一张 98 小时。**同一件事只留一张卡**这条我核过了：4 张「缺人」分别指 pd、dev、frontend、release，不重复；外呼地址只有一张。
- **解冻**：**human 09-07 的叫停要他自己解除。**

## 06:46 补：我欠着 46 条 ack，牌桌三天半没说过一个字

**这一条是本份写完之后、因本份的一句自我批评而查出来的**，所以补在这里而不是留给第三十一份：报告里我写「本窗我第三次 `ack` 拿错 id，前两次都写进报告说是我方法差，然后第三次又这么干了」。**写完我决定这次改动作**，写了个脚本把「给 pm 的、还没 ack 的指令」列出来——**它列出了 46 条。**

| 收件人 | 未 ack | | 收件人 | 未 ack |
|---|---|---|---|---|
| **pm** | **46** | | human | 16 |
| dev | 41 | | frontend | 5 |
| pd | 35 | | release | 3 |
| | | | **合计（非 human 六行）** | **130** |

**我这 46 条**：全部落在 `09-07T15:40:50Z` → `18:28:30Z`（一段 2 小时 48 分），发件人 qa 31、release 9、dev 6，**46/46 带 `ack_by` 且全部过期三天半**。

**为什么正好是这一段**：09-07 20:11 那次 106 条 ack 事故之后，我改写的脚本把时间窗写进了脚本——`SINCE='2026-09-07T18:35:00Z'`。**这 46 条最晚一条是 18:28:30，比我选的窗起点早 7 分钟。** 第十四份里我写「时间窗写进脚本，不写进我的记性」，**那句是对的，但我从没回头看过窗前**。

**其中两条定义了我们这四天的处境，我一条都没 ack**：`01M1YFGH4CF4Y7P542YPX61BJS`（release 17:44「客户在搬了。我把生产冻结了」）与 `01M1YFRQ1KHSE4RCB7YNMX7403`（release 17:48「human 许可观察客户项目，不要动手」）。**这四天我每一份报告都在写「叫停未解除」、每一次都守着那条边界——而宣布它们的两条指令，回执一条没有。** 我们立 ack 的原话是「ack 是『看见』，不是『同意』」；**我连「看见」都没记。**

### 结构那一半，比我的账重

核了 `d57acbc`：

- **`board.overdue` 是空的，而且是按设计空的**。`board.ts:732-738` 注释写着 t-147 把 `overdue` 重定义成「读到了、带选项、到期仍没答案」，而带选项的指令只能发给 human——**所以它是一张人的决策卡清单，不是「谁欠 ack」的清单**。
- **`overdue_by_presence.listening` 里有数：此刻写着「在听，829 条读到了还没动」，覆盖 pm 与 qa。**
- **`sync` 每轮对我说「你读过还没动的有 N 条」**——「读过」按 `reach` 算，**不按 ack 算**。我每轮看到个位数，真实欠账是 46。

**形状**（qa 06:50 更正了我第一版的说法，我采用它的）：**不是「对在场的角色一句话都没有」，是「有一句，但它数的是另一件事」。**

`owedTo`（`d57acbc` `board.ts:1163-1169`）筛的是 `!st.withdrawn && !st.chosen && st.reach !== "acted"`——**「没被动过」，不是「没有 ack 事件」**。于是：

| | 数 |
|---|---|
| 牌桌对 pm+qa 说的「读到了还没动」 | **829** |
| 真正欠 ack 的（pm 46 ＋ qa 1） | **47** |
| 差 | **17.6 倍** |

**而且两个角色合成一个数**：我从那句话里**看不出「我欠 46」**。我在场、每轮 `sync`、欠着 46 条过期三天半的回执——**系统说了话，说的是另一件事，而且没说是我。**

**这直接打在 T3 上**——「必须 ack、**逾期升级**；送达与 ack 时间可见」。**逾期升级只对人做了。**

**要指给人看需要什么**：任何一个角色欠着过期的 ack。**此刻六个角色全部欠着，合计 146 条。常驻。**

### 处置

1. **不补写这 46 条 ack**。09-07 20:14 立的规矩是「不补写 ack，补写会写下假的时刻」。**这条规矩此刻对我不方便，但它没有错**——账记在读数里，不记在假事件里。
2. **读数已落并已更正两次**：`project:ack.owed.pm = 46`；`ack.owed.all` 先写成 146（把 human 也算进去了）、再写成 137（**加错了**）、最终 **130**（46+41+35+5+3，已用 python 复核）。**human 不计入这张表**——qa 06:49：人答卡用 `decide` 不用 `ack`，**拿 ack 数他是用错动词**；他那 16 条另算：5 条已撤回、4 条已被 decide 或默认结掉，**真正没人理的 7 条，其中 6 条是服务自动生成的「起一个 X？」**。
3. **逐条扫过**：qa 那 31 条是判决（对应任务此刻都已 verified 或已进台账）；release 那 9 条是通报；dev 那 6 条里有一条是活的——`01M1YFFVDR8GJ5EYS0GGTVTDCG`（17:43）「**我在听，手上 0 件。你点一件我就领**」。**我当时没点，它等了 17 分钟，然后全队停了，dev 至今缺席五天。**

**这一条也修正了本份「pm 自查」那一节的自我评价**：我在那里写「一条写进报告的自我批评没有改变我的动作」。**这一次它改变了，而改变之后第一件查出来的事，是一笔比那个 id 大得多的账。**

## 口径

- 截止时刻由数据自证：取快照末条事件的 `at` 与 `id`，不取墙上时钟。**本份的截止时刻比窗口结束早 62 分钟，因为窗内最后一条事件就在那里。**
- **每条发现写「要指给人看需要什么条件」，分常驻／瞬时，瞬时的写明窗宽与上次出现**（05:02 立）。
- **行号一律带树名**。本份全部是 `d57acbc`。
- **百分位要写取法**（本份新立）：本份凡出现 `p90`，写明是服务的 `floor(n*0.9)` 还是我的 `int(0.9*(n-1))`。**一个百分位数不带取法，等于不带树名的行号。**
- **百分位与欠账数都要写取法与算法**：本份的 146／46 是「`to=X` 且不存在 `actor=X` 且 `of` 指向它的 ack，`decides` 也算已处理」。
- 在场三态取自 `d57acbc` `board.ts:794-798`：listening 按 `last_pull`（5 分钟窗），deaf 按 `last_event`（10 分钟窗）。
