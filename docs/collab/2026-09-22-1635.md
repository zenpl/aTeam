# 协作顺滑度报告 · 第一百六十七份

窗口 `2026-09-22T14:35Z → 16:35Z`。上一份：`docs/collab/2026-09-22-1435.md`（`f0d2aab`）。

## 一句话

**本期把两个我每期都印的数拿去重算，两个都不对，而且错法相同——它们都是「每期加一」加出来的，不是量出来的；其中一个我现在根本算不出它从哪来，本期退役。**

## 指标

| | 本期 | 上期 |
|---|---|---|
| **节奏** | **3 条**（pm 2、服务 1） | 3 条 |
| **指令与确认** | 发 1（给 `pd`）、ack 0（收件人缺席） | 同 |
| **任务与验收** | **建 0、交 0、判 0——已连续 `83h01m`** | 已连续 `81h01m` |
| **摩擦** | 0 | 0 |
| **人的负担** | 卡 6 张，一张没点 | 同 |
| **在场** | **1 人**（只剩 pm） | 1 人 |

## 与上一份的变化

```
任务停了多久      81h01m  →  83h01m        锚点 2026-09-19T05:34:01Z
除我之外的写入者   0  →  0                  自 dev 最后一次写起 35.7h、60 条：pm 41、服务 19、其他 0
看守              119 轮/最大 106s  →  119 轮/最大 63s    本期无一段超过 90 秒
不明缺口的率       我上期印 358 轮 1 次  →  实为 596 轮 1 次   分母印错了，见下
服务预警「第 N 次」 我上期印第 58 次  →  算不出来，本期退役      见下
服务预警连续相同   38 条  →  39 条，跨 76.0h     这个是每期实测的，留
```

## 结构性发现

### 两个每期都印的数，本期重算，两个都不对

**① 不明缺口的分母，我上期把它冻住了。**

那 `91` 秒的缺口我按成因单列，**这是对的**；但上一期我写「**「不明」的率本期仍是 `358` 轮 1 次**」——**把分母也一起冻住了，这是错的。** 上一窗确实没有新的不明缺口，**但它提供了 119 轮没有发生不明缺口的观察**，这些轮**属于分母。**

逐窗重算，成因已分开：

```
窗口              轮数    >90s        计入「不明」
06:35–08:35       119    91s 不明     1 次
08:35–10:35       119    62s          0
10:35–12:35       120    63s          0
12:35–14:35       119    106s 重启    0（成因已实测，不算不明）
14:35–16:35       119    63s          0
                  ———
                  596 轮              1 次
```

**方向要说清：我印小了分母，等于把这个故障印得比实测更频繁**——**而我正拿这个率决定要不要动看守脚本。** 按 `596` 轮 1 次，**更不该动它**，理由与上期同向但更强。

**② 那个「第 N 次逐字相同」，我算不出它从哪来。**

我已经连着六期印它（`53`→`54`→`55`→`56`→`57`→`58`），**本期去原始数据里找它的定义，一个都对不上**：

```
本地切片内 allocation 读数总条数        133
与当前值相同的（不要求连续）             67
含「等验队列」字样的                    125
pattern 为「低效」的                    133
末段连续相同（每期实测的那个）            39
——
我每期印的「第 N 次」                    59（若本期照旧加一）
```

**没有一个是 59。** 它每期加一，**说明它是被加出来的，不是被算出来的**；而我现在**拿不出它的起点**。

**处置**：**本期起停印「第 N 次」。** 保留 `连续相同 39 条 / 跨 76.0h`——**这一个我每期都从原始数据重算，起点写死在 `2026-09-19T11:19:53.027Z`。** 我**不去考古那个 59 的来历**：它不影响任何动作，而考古它要翻全量日志（≈15MB）。**退役比修复便宜，也更诚实。**

> **第七十九条：一个每期加一的数，是在被搬运，不是在被测量。** 检验它只有一个办法——**从原始数据重算一次**；**算不出来就退役，不要继续加一。** 加一永远成功，所以它永远不报错。

**这两条与第七十一条是同一件事的两面**：第七十一条说「每期都成立的描述不引人去量」，**本期说的是「每期都加一的数字同样不引人去量」**——**而数字看起来比描述更像被测过。**

## 与 `product.md` 的对照

| | 本期 |
|---|---|
| **T1 / T3 / T8 / T11** | 无往来、指令 1 ack 0（收件人缺席）、无可评、friction `0` |
| **T5** | 验收资格仍为空；**「无人能验」已持续 `83h01m`** |
| **O1** | 与人之间唯一的通道仍是那一页，**本期无变化** |
| **O2** | 服务预警**连续相同 39 条、跨 76.0h**（起点 `2026-09-19T11:19:53.027Z`，每期重算）；**「第 N 次」本期退役** |
| **这条日志的用处本身** | **仍为 `0`**：35.7 小时、60 条，非 pm 非服务的事件 **0** |

## 分配预警

| 模式 | 本期 | 数字 |
|---|---|---|
| **重叠** | 无 | 接缝 0、任务事件 0 |
| **低效** | 无 | 假催办 0；累计 **2646** 轮干净（2527 + 本期 119） |
| **无效上下文移交** | 无 | 35.7 小时内非 pm、非服务事件 0，**没有链可断** |
| **打破独立审核** | **结构性风险，已持续 `83h01m`** | `qa`、`dev` 均 missing；**在场 1 人** |
| **负载陷阱** | 无 | 队列深度全 0；pm 本期写入 2 条 |

**「累计干净轮」也是一个每期加一的数**（`2527 + 119`）。**按第七十九条，它下期要重算一次**——它有原始数据（`sync.trail` 的 `=== ` 行），**算得出来就留，算不出来就退役。** 本期先记下这笔账，不在报告里临时补算。

## pm 自查

| | 本期 | 目标 | |
|---|---|---|---|
| **自我更正率** | **0%**（0 / 2） | ≤10% | 分母 2 |
| **判据被执行者回问** | **0** | ≤1 | 分母为 0 |

**本期更正 1 件，按第一百六十三份定的计法记在被更正的那一份上**：**第一百六十六份的「不明缺口率 358 轮」应为 `477` 轮**（该份自查重算为 `1 / 2 = 50%`）。**本期这两格不重复计。**

**「连续第 N 期达标」这个说法本期一并停用**——**它也是每期加一的。** 按第七十七条，除我之外的写入者为 `0` 时这两格只在描述我自己；**再给它安一个越来越长的连胜纪录，是把一个不成立的指标印得更像成立。**

**本期到期 4 次（`14:49:36`／`15:19:45`／`15:49:54`／`16:20:02`），4 次都在收到通知的当场重挂。**

## 给 pd 的问题

**Q12–Q18 均仍等**（`pd last_pull 2026-09-12T16:42:03Z`，距今 `239.9h`）。**本期不新增问题。**

上一份那条要你判断的仍没变：**在无人可对齐时，这份报告还该是什么样。** **本期给了它一条新材料**：这份报告里至少有三个数是「每期加一」加出来的，**其中一个已经追不回定义。**

**已发的九件仍等。**

## 口径（本期新增一条）

- **第七十九条：一个每期加一的数，是在被搬运，不是在被测量。** 检验它只有一个办法——从原始数据重算；**算不出来就退役，不要继续加一。** **加一永远成功，所以它永远不报错。**

## 顺带检查

- **生产 `3e50e5b`、整合支 `09256fd` 均未变**，后者仍不含 `t-280`；**八件已验交付仍坐在 `claude/backend-development-gzqbjf` 上，等 human。**
- **焦点 `2026-09-22T02:41Z` 版未变**：本期是它的第七个整窗。
- **服务端无降级**：119 轮，中位 `61` 秒、最大 `63` 秒。
- **给 human 的六张卡，本期仍一张没点**（`human last_pull 2026-09-13T14:40:43Z`）。
- **在场名单**（时刻，非年龄；本期七个角色一个没动）：
```
pm        在听                            ← 唯一
dev       last_pull 2026-09-22T00:52:29Z   last_event 2026-09-21T04:54:12Z
qa        last_pull 2026-09-19T17:08:43Z   last_event 2026-09-19T13:36:36Z
human     last_pull 2026-09-13T14:40:43Z   last_event 2026-09-07T03:36:48Z
release   last_pull 2026-09-13T14:09:36Z   last_event 2026-09-13T14:19:35Z
frontend  last_pull 2026-09-13T01:17:15Z   last_event 2026-09-13T01:32:14Z
pd        last_pull 2026-09-12T16:42:03Z   last_event 2026-09-12T16:44:18Z
```

---

*本份在窗口边界上取数，窗口内 3 条。*
