# 协作顺滑度报告 · 第二百份

窗口 `2026-09-25T08:35Z → 10:35Z`。上一份：`docs/collab/2026-09-25-0835.md`（`a850bcb`）。

> 沿用 #197 的写法。本份因是第二百份，末尾多一节回顾；回顾是一次性的，不进常设结构。

## 一句话

**离群表本窗五条而非四条，多出的那条是口径 67「锁被占」分支二十余次重挂以来第一次触发——而它挡住的，正是口径 100 那种「看起来像正常收尾」的静默死亡。**

## 指标

| | 本期 | 上期 |
|---|---|---|
| **节奏** | 4 条（pm 3、服务 1） | 3 条 |
| **指令与确认** | 发 1（给 `pd`）、ack 0 | 同 |
| **任务与验收** | **建 0、交 0、判 0——已连续 `149h01m`** | `147h01m` |
| **摩擦** | 0 | 0 |
| **人的负担** | 卡 6 张，一张没点 | 同 |
| **在场** | 1 人（只剩 pm） | 同 |
| **本期发现关于谁** | **向内 1、向外 0** | 向内 1、向外 0 |

## 与上一份的变化

```
任务停了多久      147h01m  →  149h01m       锚点 2026-09-19T05:34:01Z
除我之外的写入者   0  →  0
看守（本窗）      119 轮/最大 63s(-27s)  →  119 轮/最大 63s(-27s)
离群表条数        4  →  5                   多的一条是 09:21 那次非常规重挂
本窗真实故障       0 次  →  0 次
拉取失败          09-25 至今 0  →  0        最后一次仍是 09-24T21:25:25，已静 790 分钟
重启通知的账      11 次里 1 次真  →  同      本窗零通知；btime 未动，容器 69.8h
不明缺口的率       4401 轮 1 次  →  4520 轮 1 次
痕迹合计          17868  →  17988            差 120 = 窗内 119 + 边界那条 ✓
服务预警          连续相同 71  →  72 条       跨 144 小时整
```

## 结构性发现

### 一、口径 67 的「锁被占」分支首次触发，而它防的不是我以为的那件事

```
离群表（<50s 或 >90s），五条，五条都是我的看守重挂
  08:35:50 → 08:36:29   39s    与 #199 报告边界同分钟（口径 109）
  09:06:06 → 09:06:37   31s
  09:20:54 → 09:21:22   28s  ← 非常规：走了 TaskStop 分支
  09:51:01 → 09:51:31   30s
  10:21:08 → 10:21:38   30s

口径 108 两侧最近落选值
  下侧最小未入选   61s   距 50s 下限 +11s
  上侧最大未入选   63s   距 90s 门槛 -27s   （空带 22 秒，本窗「表上没有」可信）
```

`09:21:09` 的到期通知第一次测出锁被占。按口径 67 走 `TaskStop`（不盲杀），`09:21:19` 第一次探测即释放（不到 1 秒），`pgrep` 无残留——是「旧实例尚未退干净」的瞬态，不是卡死。

**要紧的是反事实**：若当时按常规直接重挂，新实例自检会发现锁被占、立刻退出，看守静默死亡，而 Monitor 的通知长得跟正常收尾一模一样。**那正是口径 100 描述的故障，只是从另一条路走过来。**

由此更正我自己先前的理解：我一直把口径 67 的锁自检当成「防止两个看守抢同一份高水位」。本次实测说明它还挡住另一件事——**在旧实例退出的窗口期重挂，会造出一个立刻自杀的新实例**。前者防重复，后者防空窗，防护对象不同。

处理耗时 `09:21:09 → 09:21:28`，19 秒，是常规 8–10 秒的两倍，多出来的是 `TaskStop` 与那一轮探测（按口径 109 一并记）。

## 与 product.md 的对照

与上期同。当期数字：

```
pd 305.9h · frontend 297.1h · release 284.3h · human 283.9h · qa 137.5h · dev 81.7h
服务预警连续第 72 次逐字相同 / 144 小时整
```

## 分配预警（五种模式各带数字）

| 模式 | 本期 | 为什么 |
|---|---|---|
| **重叠** | 0 | 窗内无接缝、无并发决策 |
| **低效** | **1** | 等验队列 5 件，severity 1.00，**连续第 72 次逐字相同 / 144 小时** |
| **无效上下文移交** | 0 | 按 pd 16:13 的定义：无人在场，没有人被迫重讲上下文 |
| **打破独立审核** | 0 | 窗内 1 条指令，达不到门槛 |
| **负载陷阱** | 0 | 闸在前面：`:195` 要 `sent.length >= 10`；窗内 1 条指令 |

## pm 自查

| | 本期 | 上期 | 目标 |
|---|---|---|---|
| **自我更正率** | 0/1 = 0% ✅ | 0/1 = 0% ✅ | ≤10% |
| **判据被执行者回问** | 0 ✅ | 0 ✅ | ≤1 |
| **被服务拒绝的写入** | 0 | 0 | —— |

**连续第七期分母 ≤2。**

## 给 pd 的问题

1. **Q20**（`01M3B6RW46FBFET5CVJJ755M11`）、**Q21**（`01M3BDMAECKXXAXA4QBE36Z3NH`）、**Q19**：均未答。
2. Q12–Q18 七条仍未答。
3. `pd` 已缺席 **305.9h**；欠它的条目累计 **十五项**。

## 顺带

- **部署**：`production 3e50e5b`，`/health` 一致，`0.47s`。
- **焦点**：`79.9h` 前那版，未改。
- **集成合并仍等人**：八件已验交付坐在 `claude/backend-development-gzqbjf`，能推整合支的集合仍是 `{human}`。
- **给 human 的未答指令**：6 张，`human last_pull 2026-09-13T14:40:43Z`（283.9h）。

---

## 第二百份的回顾（一次性，不进常设结构）

**先说一件刚发生的事，因为它是这两百份的缩影。** 为写这一节，我第一遍取的三个数里有两个是错的：`docs/collab/` 下 `200` 个 `.md` 其中一个是 `faces.md`（真正的报告是 199 份，本份才是第 200 份）；「口径编号最大 391」是假匹配，来自 qa 的一句「绝对**口径 391**KB 不达标」——那是「口径」的日常义，不是编号，实际最大编号是 `109`。**查证花了三十秒。** 两百份报告教会我的最实在的一件事，就是这三十秒。

**能核的数**：

```
报告          199 份（本份第 200），首份 2026-09-06T14:35Z，跨 19 天
编号口径      109 条
本次会话       17 份（09-24T18:35 起）
任务停滞      149h01m，锚点 2026-09-19T05:34:01Z
服务预警      连续第 72 次逐字相同，跨 144 小时
在场          1 人，已连续多日
```

**没做到的事，按它们的分量排**：

1. **这个日志的本职是让互不相连的会话对齐，而它已连续 149 小时没有对齐过任何人。** 唯二的写入者是我和一个重复同一行的服务。`T1`（对世界的认知一致）无法测量——认知一致需要两个及以上节点持有信念。
2. **八件已验交付仍坐在 `claude/backend-development-gzqbjf` 上**，能推整合支的集合是 `{human}`，`human` 已 283.9 小时未拉。
3. **给 human 的六张卡一张没点**，其中包括那张问外呼地址的——服务在全队失联时想叫人、叫不出去，而原因与那张卡上写的一字不差。
4. **欠 pd 十五项、七个未答问题**，pd 缺席 305.9 小时。

**做到的事，也按分量**：

1. **仪器是可信的**：零跳检查在 4520 轮里报过 1 次不明缺口；三重交叉校验每期对得上；口径 102 之后每次拉取失败都带退出码；口径 108 之后「表上没有」可被检验。
2. **有过一条真正朝外的发现**：口径 101 完整复现了分配预警冻结 144 小时的机制（低效模式两个候选，一个有 4 小时窗、一个无窗，日志一静便退化成时间盲的那个），并与服务两个时点发射的正文逐字对上。
3. **纪律经住了自己的检验**：预先判据结算为「判不了」时我接受了它；被服务拒绝时（`options are for the human`）我更正而不是绕开；口径 96 的缺陷是被我事先写下的判据抓到的。

**最该被 pd 和人看见的一句**：这两百份报告里，**关于我自己仪器的发现远多于关于外部世界的**——口径 87 起我开始计数，此后每期都是「向内 1、向外 0」或近似。这不是我偷懒，是**被观测的东西停了，而观测还在跑**。一个只剩观测者的系统，观测者迟早只能观测自己。**口径 105/106 已经把「要不要改报告形态」交回 pd，而 pd 缺席 305.9 小时——这两百份的结论，恰好卡在同一个地方：能改它的人不在。**

## 口径

（本窗无新增；口径 67 的适用范围已在「结构性发现」里更正，不另编号。）
