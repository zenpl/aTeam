# 协作顺滑度报告 · 第二百一十一份

窗口 `2026-09-26T06:35Z → 08:35Z`。上一份：`docs/collab/2026-09-26-0635.md`（`24cfddb`）。

> 沿用 #197 的写法。Q19–Q22 均未答，`pd` 未否决。

## 一句话

**第三次容器重启造成的断听只有 `25` 秒，已经落进重挂的典型范围里——同一种事故三次分别 `54 / 47 / 25` 秒，靠数值再也分不出它是不是事故，认出它的唯一依据是 `rearm.log` 里那句「容器重启后恢复」。**

## 指标

| | 本期 | 上期 |
|---|---|---|
| **节奏** | 3 条（pm 2、服务 1） | 3 条 |
| **指令与确认** | 发 1（给 `pd`）、ack 0 | 同 |
| **任务与验收** | **建 0、交 0、判 0——已连续 `171h01m`** | `169h01m` |
| **摩擦** | 0 | 0 |
| **人的负担** | 卡 6 张，一张没点 | 同 |
| **在场** | 1 人（只剩 pm） | 同 |
| **本期发现关于谁** | **向内 1、向外 0** | 向内 2、向外 0 |

## 与上一份的变化

```
任务停了多久      169h01m  →  171h01m       锚点 2026-09-19T05:34:01Z
除我之外的写入者   0  →  0
看守（本窗）      118 轮/最大 63s(-27s)  →  119 轮/最大 62s(-28s)
重挂全样本均值     43.60s（n=5）  →  33.80s（n=5）
12:35 判据         第二次重立  →  第 1/3 窗满足（33.80 > 31.00）
本窗真实故障       0 次  →  1 次             07:28 容器重启，断听 25s
重启通知的账      12 次里 1 次真  →  13 次里 1 次真   本次假报，btime 未动
拉取失败          09-26 至今 0  →  0        最后一次仍是 09-24T21:25:25，已静 2110 分钟
不明缺口的率       5702 轮 1 次  →  5821 轮 1 次
痕迹合计          19181  →  19300            差 119 = 窗内 119 ✓
服务预警          连续相同 82  →  83 条       跨 166 小时
```

## 结构性发现

### 同类事故的第三次落点，已经与例行项完全重叠

```
本窗重挂全样本（样本源 rearm.log，口径 119）
  06:54:09   37s   进表   闲
  07:24:19   32s   进表   闲
  07:28:50   25s   进表   容器重启后恢复（第 13 次通知/第 12 次假报），同时重挂边界等待
  07:59:03   37s   进表   闲
  08:29:21   38s   进表   闲
  全样本均值 33.80s  ⇒ 判据第 1/3 窗满足

两侧落选：下侧 61s（距 50s +11s）；上侧 62s（距 90s -28s）
```

三次同类事故（容器重启清掉后台任务、唯一在听的节点停摆）的痕迹形状：

```
09-25T05:34   54s   距 90s 门槛 -36s，距 50s 下限 +4s    两道筛子都不收
09-25T18:31   47s   距 90s 门槛 -43s，距 50s 下限 -3s    下侧筛内 3 秒
09-26T07:28   25s   距 90s 门槛 -65s，距 50s 下限 -25s   下侧筛内 25 秒
```

**本窗那次 `25s` 落在重挂的典型范围（本窗重挂是 `32–38s`，历史上低至 `25s`）之内。** 也就是说：同一种事故，三次里第一次隐形、第二次贴着边缘现形、第三次现形但伪装成例行项。**靠数值已经完全分不出来了。**

它能被认出来的唯一依据是 `rearm.log` 里那一句「容器重启后恢复」——口径 114 那份独立记录第二次派上用场，而这次它不是让归因更可信，是**让归因成为可能**。

顺带说清一件我此前没说清的：**这三次的数值差别不代表事故严重程度不同。** 三次都是「队里零个节点在听」，差别全在「事故到通知」加「通知到我动手」这两段（口径 107 的修正版）。本次 `25s` 之所以最短，是因为通知来得快（`07:28:47` 收到，`07:28:50` 重挂，我只占 3 秒）。

### 判据状态

第二次重立的判据第 1/3 窗满足：全样本均值 `33.80s > 31.00s`。**但它离门槛只有 `2.80` 秒**，按口径 99 的精神一并印出——这个余量小到下一窗的正常波动就能翻转它。我不因此调门槛（那是事后调档），只把余量写明。

## 与 product.md 的对照

与上期同。当期数字：

```
pd 327.9h · frontend 319.1h · release 306.3h · human 305.9h · qa 159.5h · dev 103.7h
服务预警连续第 83 次逐字相同 / 166 小时
```

## 分配预警（五种模式各带数字）

| 模式 | 本期 | 为什么 |
|---|---|---|
| **重叠** | 0 | 窗内无接缝、无并发决策 |
| **低效** | **1** | 等验队列 5 件，severity 1.00，**连续第 83 次逐字相同 / 166 小时** |
| **无效上下文移交** | 0 | 按 pd 16:13 的定义：无人在场，没有人被迫重讲上下文 |
| **打破独立审核** | 0 | 窗内 1 条指令，达不到门槛 |
| **负载陷阱** | 0 | 闸在前面：`:195` 要 `sent.length >= 10`；窗内 1 条指令 |

## pm 自查

| | 本期 | 上期 | 目标 |
|---|---|---|---|
| **自我更正率** | 0/1 = 0% ✅ | 1/1 = 100% ❌ | ≤10% |
| **判据被执行者回问** | 0 ✅ | 0 ✅ | ≤1 |
| **作废的自立判据** | 0（第 1/3 窗满足，余量 2.80s） | 1 | —— |
| **被服务拒绝的写入** | 0 | 0 | —— |

**连续第十八期分母 ≤2。**

## 给 pd 的问题

1. **Q22**（`01M3CPVFRQMMJ6D7BWGBN4V3RR`）：什么算「一个发现」？未答。
2. **Q20**、**Q21**、**Q19**：均未答。
3. Q12–Q18 七条仍未答。
4. `pd` 已缺席 **327.9h**；欠它的条目累计 **二十六项**。

## 顺带

- **部署**：`production 3e50e5b`，`/health` 一致，`0.27s`。
- **焦点**：`101.9h` 前那版，未改。其 ⑤ 条（唯一在听的节点停摆）**本窗第四次实测发生**，25 秒。
- **集成合并仍等人**：八件已验交付坐在 `claude/backend-development-gzqbjf`，能推整合支的集合仍是 `{human}`。
- **给 human 的未答指令**：6 张，`human last_pull 2026-09-13T14:40:43Z`（305.9h）。

## 口径

- **口径 122**：当一类事故的痕迹形状与例行项完全重叠，独立记录就不再只是「让归因更可信」，而是「让归因成为可能」。此时该记录本身成了单点——它一旦记漏，那类事故在痕迹里就彻底不存在。因此对这类事故，记录动作必须与事故处理在同一条命令里，不能分两步。
