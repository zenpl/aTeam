# 协作顺滑度报告 · 第一百七十四份

窗口 `2026-09-23T04:35Z → 06:35Z`。上一份：`docs/collab/2026-09-23-0435.md`（`e315b4d`）。

## 一句话

**上一期我换掉的那个统计量，本期第一次有了对照：旧的又跌了 `0.009`，新的一个数字没动——而这正是我换它的理由，不是它的功劳。**

## 指标

| | 本期 | 上期 |
|---|---|---|
| **节奏** | **3 条**（pm 2、服务 1） | 3 条 |
| **指令与确认** | 发 1（给 `pd`）、ack 0（收件人缺席） | 同 |
| **任务与验收** | **建 0、交 0、判 0——已连续 `97h01m`** | 已连续 `95h01m` |
| **摩擦** | 0 | 0 |
| **人的负担** | 卡 6 张，一张没点 | 同 |
| **在场** | **1 人**（只剩 pm） | 1 人 |

## 与上一份的变化

```
任务停了多久      95h01m  →  97h01m        锚点 2026-09-19T05:34:01Z
除我之外的写入者   0  →  0                  自 dev 最后一次写起 49.7h、81 条：pm 55、服务 26、其他 0
看守（本窗）      120 轮/最大 63s  →  120 轮/最大 62s
不明缺口的率       1313 轮 1 次  →  1433 轮 1 次
痕迹合计          14773  →  14893            差值 120 = 按取数时刻计数 120 ✓
服务预警          连续相同 45  →  46 条       零漏跳（相邻间隔最大 7202.559s < 10800）
典型周期漂移       0.254 秒  →  0.254 秒       一个数字没动，见下
```

## 结构性发现

### 换统计量这件事，本期拿到了它的对照组

上一期我把「每周期漂移」从**均值**改成**中位**，理由是：**均值在右偏分布里报的是尾巴，而且会随样本增加持续移动，于是这个数每期都在变，变的原因与服务无关。** 上一期我还写了一句预测：**它会继续跌，一直跌到贴近 `0.254`。**

**本期把两个口径并排算了一遍**：

```
样本数 n   均值漂移（旧口径）   中位漂移（新口径）   尾巴 >0.5s
  42            0.606                0.254            9/42
  43            0.605                0.254           10/43
  44            0.596                0.254           10/44
  45            0.587                0.254           10/45
```

**旧口径从 `0.606` 走到 `0.587`，四个样本点跌了 `0.019`，而且还在跌。**
**新口径四个样本点上一个数字没动。**
**本期新增的那个间隔是 `7200.199` 秒**——**比中位还典型，所以它把均值又往下拽了一点，而中位纹丝不动。**

**这里要说清一件事，免得把它当成战绩**：**中位不动不是它「测得准」，是它按构造就不容易动**——它只在中间那个元素换人时才跳，而典型值扎堆的时候中间那个元素不会换。**我换它，要的就是这个性质**：**让这个数只在服务真的变了的时候才变。** **本期证明的是它做到了这一点，不是它比均值更接近某个真相。**

> **第八十六条：换一个统计量之后，把新旧两个并排算几期。** 只报新的，**你说不出换对了没有**；并排之后，**「旧的还在动、新的没动」本身就是一条证据**——**而它证的是「这个数不再因为我又多看了一眼而改变」，不是「这个数更真」。**

**顺带，尾巴本期没有新成员**（`10/44 → 10/45`，最慢仍是 `2.559` 秒）。**那十个大间隔全部发生在更早的时段**，**近期的周期都典型。** 我不据此说「服务变稳了」——**十个点分不出趋势，而我也找不出一个会因此改变的动作**（第六十二条）。

## 与 `product.md` 的对照

| | 本期 |
|---|---|
| **T1 / T3 / T8 / T11** | 无往来、指令 1 ack 0（收件人缺席）、无可评、friction `0` |
| **T5** | 验收资格仍为空；**「无人能验」已持续 `97h01m`** |
| **O1** | 与人之间唯一的通道仍是那一页，**本期无变化** |
| **O2** | 服务预警**连续相同 46 条、零漏跳**；**典型周期慢 `0.254` 秒，10/45 超过 `0.5` 秒，最慢 `2.559` 秒** |
| **这条日志的用处本身** | **仍为 `0`**：49.7 小时、81 条，非 pm 非服务的事件 **0** |

## 分配预警

| 模式 | 本期 | 数字 |
|---|---|---|
| **重叠** | 无 | 接缝 0、任务事件 0 |
| **低效** | 无 | **假催办 0（来源已毁，非已修复）**；看守轮数：本窗 `120`，痕迹合计 `14893` |
| **无效上下文移交** | 无 | 49.7 小时内非 pm、非服务事件 0，**没有链可断** |
| **打破独立审核** | **结构性风险，已持续 `97h01m`** | `qa`、`dev` 均 missing；**在场 1 人** |
| **负载陷阱** | 无 | 队列深度全 0；pm 本期写入 2 条 |

## pm 自查

| | 本期 | 目标 | |
|---|---|---|---|
| **自我更正率** | **0%**（0 / 2） | ≤10% | 分母 2 |
| **判据被执行者回问** | **0** | ≤1 | 分母为 0 |

**按第七十七条，这两格在「除我之外的写入者为 `0`」时只在描述我自己，照记不当协作指标。**

**本期到期 4 次（`04:46:13`／`05:16:19`／`05:46:26`／`06:16:33`），4 次都在收到通知的当场重挂。**

## 给 pd 的问题

**Q12–Q18 均仍等**（`pd last_pull 2026-09-12T16:42:03Z`，距今 `253.9h`）。**本期不新增问题。**

那条「在无人可对齐时这份报告还该是什么样」，**本期的材料**：**这份报告上一期换了一个统计量，本期把新旧并排算了一遍才敢说换对了。** **并排是可以做到的——只要还留着原始数据。**

**已发的九件仍等。**

## 口径（本期新增一条）

- **第八十六条：换一个统计量之后，把新旧两个并排算几期。** 只报新的，**你说不出换对了没有**；并排之后，**「旧的还在动、新的没动」本身就是一条证据**——**而它证的是「这个数不再因为我又多看了一眼而改变」，不是「这个数更真」。**

## 顺带检查

- **生产 `3e50e5b`、整合支 `09256fd` 均未变**，后者仍不含 `t-280`；**八件已验交付仍坐在 `claude/backend-development-gzqbjf` 上，等 human。**
- **焦点 `2026-09-22T02:41Z` 版未变**：本期是它的第十四个整窗。
- **服务端无降级**：本窗 120 轮，中位 `61` 秒、最大 `62` 秒。
- **给 human 的六张卡，本期仍一张没点**（`human last_pull 2026-09-13T14:40:43Z`）。
- **在场名单**（时刻，非年龄；本期七个角色一个没动）：
```
pm        在听                            ← 唯一
dev       last_pull 2026-09-22T00:52:29Z   last_event 2026-09-21T04:54:12Z
qa        last_pull 2026-09-19T17:08:43Z   last_event 2026-09-19T13:36:36Z
human     last_pull 2026-09-13T14:40:43Z   last_event 2026-09-07T03:36:48Z
release   last_pull 2026-09-13T14:09:36Z   last_event 2026-09-13T14:19:35Z
frontend  last_pull 2026-09-13T01:17:15Z   last_event 2026-09-13T01:32:14Z
pd        last_pull 2026-09-12T16:42:03Z   last_event 2026-09-12T16:44:18Z
```

---

*本份在窗口边界上取数，窗口内 3 条。*
