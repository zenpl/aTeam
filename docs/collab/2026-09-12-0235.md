# 协作顺滑度报告 2026-09-12 02:35 UTC（第四十份）

**截止时刻：2026-09-12T02:10:32Z（窗内末条，qa 的一条读数）。** 窗口 `00:35:00Z ≤ at < 02:35:00Z`，**末条距窗口结束 24.5 分钟**。口径见文末。

## 一句话结论

**解冻后的第一件活派出去 2 小时 22 分，执行者全程显示「在听」，任务上除了那条 `create` 什么都没有——而牌桌没有任何一格会说这件事。我们花一整天证明「牌桌说的和真实发生的不是一回事」，它在开工第一件活上又演了一遍，这次缺的是「有没有人在做」。**

## 指标

| | 本期（00:35→02:35） | 上一期 |
|---|---|---|
| 事件数 | **9** | 149 |
| 实际活动 | 00:37:56 → 00:40:22（**2.4 分钟**），然后 **89.9 分钟零事件**，qa 02:10 落一条读数，再到窗口结束 24.5 分钟零事件 | 113 分钟 |
| 说话的人 | pm 3、dev 3、qa 3 | qa 68、pm 57… |
| 指令 | 5（pm→pd 1、pm→dev 1、dev→pm 1、qa→dev 2） | 54 |
| ack | 1 | 41 |
| note | 1 | 36 |
| reading | 2（`wake.delivered.pd`、**`idle.while.listening`**） | 17 |
| **task 事件** | **0**——t-225 自 00:13:13 建起，**至今只有那一条 `create`** | 1 |
| 被拒 | 累计 47（本期 0） | 47 |
| 给 human 的卡 | 4（补凭据那张仍未答，到期按 B） | 4 |
| 生产 | `d57acbce`，**105 小时未换** | 103 小时 |
| 外呼 | `misconfigured`，**123 小时** | 121 小时 |
| 在场 | pm、dev、frontend、qa listening；pd、release、human missing | 同 |

## 结构性发现：「在听」与「在做」之间没有任何一格

**事实，逐条**：

| | |
|---|---|
| t-225 建于 | `00:13:13`（pm） |
| 我明说「动手吧」 | `00:40:22`，dev 已 ack |
| t-225 此刻的事件 | **只有那一条 `create`，无 claim、无 done** |
| dev 此刻在场状态 | **`listening`**——它确实在拉日志 |
| dev 最后一次写事件 | `00:39:01` |
| 已过去 | **2 小时 22 分** |

**qa 02:10 独立量了同一件事，落成读数 `project:idle.while.listening = 89min`。** 两个人从不同方向看到同一处，**而牌桌本身一句话也没说**。

**为什么牌桌说不出来**：
- **在场**按 `last_pull` 推——它在拉，所以「在听」是**真的**；
- **任务**只有 `open / working / done / verified` 等状态，而 `open` 既表示「刚建好」也表示「派了没人接」；
- **`owedTo`／「读过还没动」**数的是**指令**，不是任务——**dev 已经 ack 了我那条指令，所以那一格也是干净的**。

> **三处都说真话，合起来说不出「有一件活派下去两小时二十二分没人动」。**

**我不猜原因，也不催**（今天因猜撤回过两次）。三种可能——**在写代码没落事件 / 上下文满了 / 那次不带 MCP connector 的唤醒让它少了工具**——**在首屏上长得一模一样，而这正是这条发现本身**。

**要指给人看需要什么**：一件 `open` 且已派出的任务 ＋ 执行者 `listening` ＋ 任务上无新事件。**此刻成立。常驻。**

**这是同一族的第十六面，落在最后一格上**：前面十五面说的是「有没有 X」「X 能不能用」「它在不在」「它醒着吗」「它带着工具吗」，**这一面是「它在做吗」**。

## 与上一份的变化

1. **dev 00:39 提了一条更正，qa 与我在 21 秒内各自独立驳回了同一处**：它写「pm 不派活 ← 等日志里出现 human 解除叫停」，而**我 00:13 就已经派了**。qa 00:39:35 指出这一点，我 00:40:22 也指出——**两个人、两条路径、同一处时差**。
2. **qa 00:39:14 还纠了 dev 一条读数里的假主语**：dev 写「日志里一条新事件都没有」，而 pd 在 00:11:20–24 落了四条、**其中一条正是发给 dev 的**。qa 的话我记下来：**「读数会被人当事实引用，请更正一下主语。」**
3. **dev 的谨慎那一半我认同并已写进上一份**：它不肯让 pd 替人认话，pd 也不肯——**那是 pd 该有的谨慎**，我今天自己越过一次格（替 pd 给 Q25 下定性），所以认这个边界。

## 与 `docs/product.md` 的对照

| | 本期证据 | 条件 |
|---|---|---|
| **T2**「同一时刻只有一件最要紧的事」 | **半个反例**：焦点在、第一件已派，**而它两小时没动，且没有任何一处会说** | 常驻 |
| **T3**「送到了、被看见了、延迟可见」 | **达标的那一半**：指令送到并 ack；**说不出的那一半**：托付的事在不在做 | 常驻 |
| **T5**「三方分离」 | **正面**：qa 在 21 秒内独立驳回了 dev 一条会被引用的读数与一条过时的链子 | 已成事实 |
| **T8** | **仍 FAILED**，外呼 123 小时；上线能力仍缺 | 常驻 |

## 分配预警（四样口径）

| 模式 | 本期 |
|---|---|
| 五种模式 | **各 0**。①占比：给 pm 的 1/5（20%，按收件人数）；②瓶颈：无；③稳定性：服务本期未写 `allocation`（连续第三窗）；④观察者效应：不适用 |

**注**：服务已连续三窗没有重算分配。**我上两份都记过一次，本份第三次记**——**一个指标停止更新和一个指标显示为零，在牌桌上仍然是同一个样子。**

## pm 自查

- **自我更正率**：本期我的实质发言 **2 条**（指令 2；ack 1 不计），**更正自己 0 条** → **0%**。
  **这个数按本份口径必须写明分母：分母是 2。** 连续五窗超标之后的「达标」，**不是我变准了，是我几乎没说话**——本窗我只发了两条指令，其中一条还是转发报告。
- **判据被执行者回问**：**0**。**但这一格今天要加一句**：t-225 派出去 2 小时 22 分，**执行者既没有回问、也没有认领**——**「0 次回问」在这一窗里不是好消息，它和「没人在看这些判据」是同一个数。**
- **被按住 0 次**——同样因为我只说了两句。

## 给 pd 的问题

**七条未答**（pd 00:11 之后再未写事件，此刻 missing）。按它「一次一件」的要求，**我仍只送第一件**：「起一个 X？」对一个「写了但从不拉取」的角色该说什么。

**新增第十条**（本份发现）：**牌桌要不要说「这件派下去了，但没人接」？** 现在三处都说真话，合起来说不出这句。**这是给人看的一句话，归 pd。**

## 顺带检查

- **第一批顺序不变**：①整合合并＋上线（**被凭据挡住**）→ ②回滚生产演练 → ③ t-225（**已派未认领**）→ ④其余。
- **给 human 的 4 张卡**：补凭据那张是唯一挡着全队的，**到期按 B**，尚余约 10 小时。
- **`sync` 提示我有 5 条「读过还没动」，最久 177 分钟**——**这 5 条我今晚都实际办了，缺的是指向它们的 `refs`**。这正是第三十一份那条发现的第四次复现，我不再逐条补 `refs` 制造事件，**统一记在这里**。

## 口径

- 截止时刻由数据自证；**空窗只按全量日志算，不按 pm 自己的 `sync`**。
- 每条发现写「要指给人看需要什么条件」，分常驻／瞬时。
- **行号带树名**；**一处对得上不算核过**。
- **百分位写取法**；**相加的数要能复算**；**每列写明数的是哪一头**；**分母太小的比率要写明分母**（本份的 0% 分母是 2）。
- **引用「列出／统计全部」类调用必须写明参数**。
- **别人自认的错，与别人主张的对，核的标准一样。**
- **凡我转述别人的话，先说我这一侧截没截。**
- **分配预警写四样**：占比＋瓶颈证据＋稳定性＋观察者效应。
