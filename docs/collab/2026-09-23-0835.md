# 协作顺滑度报告 · 第一百七十五份

窗口 `2026-09-23T06:35Z → 08:35Z`。上一份：`docs/collab/2026-09-23-0635.md`（`9cc5bb7`）。

## 一句话

**上一期我立的第八十六条说「并排算几期」，却没写几期算完——本期第五个样本点到手，问题已经答完，我给那条补上停的判据，并停掉并排。**

## 指标

| | 本期 | 上期 |
|---|---|---|
| **节奏** | **3 条**（pm 2、服务 1） | 3 条 |
| **指令与确认** | 发 1（给 `pd`）、ack 0（收件人缺席） | 同 |
| **任务与验收** | **建 0、交 0、判 0——已连续 `99h01m`** | 已连续 `97h01m` |
| **摩擦** | 0 | 0 |
| **人的负担** | 卡 6 张，一张没点 | 同 |
| **在场** | **1 人**（只剩 pm） | 1 人 |

## 与上一份的变化

```
任务停了多久      97h01m  →  99h01m        锚点 2026-09-19T05:34:01Z
除我之外的写入者   0  →  0                  自 dev 最后一次写起 51.7h、84 条：pm 57、服务 27、其他 0
看守（本窗）      120 轮/最大 62s  →  119 轮/最大 64s
不明缺口的率       1433 轮 1 次  →  1552 轮 1 次
痕迹合计          14893  →  15013            差值 120 = 按取数时刻计数 120 ✓
服务预警          连续相同 46  →  47 条       零漏跳（最大间隔 7202.559s < 10800）
典型周期漂移       0.254  →  0.254            第五个样本点，仍未动
并排（旧口径均值）  0.587  →  0.580            仍在跌；本期起停印，见下
```

## 结构性发现

### 我给第八十六条补一个它本该自带的东西：什么时候停

上一期我写：**换一个统计量之后，把新旧两个并排算几期。** **「几期」是一句没有判据的话。** 本期第五个样本点到手：

```
样本数 n   均值漂移（旧）   中位漂移（新）   尾巴 >0.5s
  42          0.606            0.254          9/42
  43          0.605            0.254         10/43
  44          0.596            0.254         10/44
  45          0.587            0.254         10/45
  46          0.580            0.254         10/46
```

**五个点，旧口径单调下行、累计走了 `0.026`；新口径一个数字没动。**
**本期新增的间隔是 `7200.247` 秒，又是一个典型值**——**它把均值再拽下去一点，而中位仍在原处。**

**这件事已经答完了。** 并排要回答的问题是**「换对了没有」**，**而不是「今天均值走到哪了」。** 答案拿到之后再并排下去，**就是每期重印一个已经答过的问题**——**正是第七十九条说的那种「每期加一」的仪式，只不过这次加的是我自己新立的一栏。**

> **第八十六条（补）：并排的时候就把停的判据写下来。** 判据是**「新旧两边的走向已经分开，且分得开的程度不再因为多一个样本而改变」**；**满足了就停，不满足就说清还差什么。** **没有停的判据，过渡手段会变成新常态**——**而一个不会停的对照，跟它要取代的那个毛病是同一种。**

**本期按这条办**：**五个点里旧口径每一步都在同向移动、新口径每一步都不动**，**第六个点不会改变这个判断。** **本期起，报告只写典型（中位）与尾巴，不再并排印均值。** 原始数据仍在，**要复查随时能再算。**

**要认的一件**：第八十六条是我上一期刚立的，**而它一出生就缺了这一半。** **一条只说「要做什么」不说「做到什么时候」的规矩，会自己长成负担。**

## 与 `product.md` 的对照

| | 本期 |
|---|---|
| **T1 / T3 / T8 / T11** | 无往来、指令 1 ack 0（收件人缺席）、无可评、friction `0` |
| **T5** | 验收资格仍为空；**「无人能验」已持续 `99h01m`** |
| **O1** | 与人之间唯一的通道仍是那一页，**本期无变化** |
| **O2** | 服务预警**连续相同 47 条、零漏跳**；**典型周期慢 `0.254` 秒，10/46 超过 `0.5` 秒，最慢 `2.559` 秒** |
| **这条日志的用处本身** | **仍为 `0`**：51.7 小时、84 条，非 pm 非服务的事件 **0** |

## 分配预警

| 模式 | 本期 | 数字 |
|---|---|---|
| **重叠** | 无 | 接缝 0、任务事件 0 |
| **低效** | 无 | **假催办 0（来源已毁，非已修复）**；看守轮数：本窗 `119`，痕迹合计 `15013` |
| **无效上下文移交** | 无 | 51.7 小时内非 pm、非服务事件 0，**没有链可断** |
| **打破独立审核** | **结构性风险，已持续 `99h01m`** | `qa`、`dev` 均 missing；**在场 1 人** |
| **负载陷阱** | 无 | 队列深度全 0；pm 本期写入 2 条 |

## pm 自查

| | 本期 | 目标 | |
|---|---|---|---|
| **自我更正率** | **0%**（0 / 2） | ≤10% | 分母 2 |
| **判据被执行者回问** | **0** | ≤1 | 分母为 0 |

**本期给第八十六条补了一半，按第一百六十八份定的处理：给一条规矩补上它缺的判据不算自我更正**（它没有一个「正确值」可回退），**记在这里。**

**另记一件不进两格的**：上一期我**第二次**把边界等待用 `&` 挂在一次 Bash 调用里——**那不是受管的后台任务，不会叫醒我。** 两次都是我自己当场发现并重挂的，**但同一个错犯了两次，说明我没有把它变成一条会自动生效的做法**，**而此刻我不新建机制**（不造活、无人可验）。**写在这里，作为第三次发生时的对照。**

**本期到期 4 次（`06:46:39`／`07:16:46`／`07:46:54`／`08:17:05`），4 次都在收到通知的当场重挂。**

## 给 pd 的问题

**Q12–Q18 均仍等**（`pd last_pull 2026-09-12T16:42:03Z`，距今 `255.9h`）。**本期不新增问题。**

那条「在无人可对齐时这份报告还该是什么样」，**本期的材料**：**这份报告本期主动停掉了一栏自己上一期才加的东西。** **加得动，也停得下来。**

**已发的九件仍等。**

## 口径（本期补一条旧的，不新增）

- **第八十六条（补）：并排的时候就把停的判据写下来。** 判据是**「新旧两边的走向已经分开，且分得开的程度不再因为多一个样本而改变」**。**没有停的判据，过渡手段会变成新常态——而一个不会停的对照，跟它要取代的那个毛病是同一种。**

## 顺带检查

- **生产 `3e50e5b`、整合支 `09256fd` 均未变**，后者仍不含 `t-280`；**八件已验交付仍坐在 `claude/backend-development-gzqbjf` 上，等 human。**
- **焦点 `2026-09-22T02:41Z` 版未变**：本期是它的第十五个整窗。
- **服务端无降级**：本窗 119 轮，中位 `61` 秒、最大 `64` 秒。
- **给 human 的六张卡，本期仍一张没点**（`human last_pull 2026-09-13T14:40:43Z`）。
- **在场名单**（时刻，非年龄；本期七个角色一个没动）：
```
pm        在听                            ← 唯一
dev       last_pull 2026-09-22T00:52:29Z   last_event 2026-09-21T04:54:12Z
qa        last_pull 2026-09-19T17:08:43Z   last_event 2026-09-19T13:36:36Z
human     last_pull 2026-09-13T14:40:43Z   last_event 2026-09-07T03:36:48Z
release   last_pull 2026-09-13T14:09:36Z   last_event 2026-09-13T14:19:35Z
frontend  last_pull 2026-09-13T01:17:15Z   last_event 2026-09-13T01:32:14Z
pd        last_pull 2026-09-12T16:42:03Z   last_event 2026-09-12T16:44:18Z
```

---

*本份在窗口边界上取数，窗口内 3 条。*
