# 协作顺滑度报告 2026-09-11 18:35 UTC（第三十六份）

**截止时刻：2026-09-11T18:34:42Z（窗内末条）。** 窗口 `16:35:00Z ≤ at < 18:35:00Z`。口径见文末。

## 一句话结论

**全队四天来第一次六个角色同时在场——而把他们叫回来的不是牌桌，是带外的一条 `create_trigger`。人也（可能）回来了，但他说的那句「开工了」不在日志里，于是六个醒着的人手上都有活、全队仍然停着：停在一句没有落笔的话上。**

## 指标

| | 本期（16:35→18:35） | 上一期 |
|---|---|---|
| 事件数 | **168** | 2 |
| 说话的人 | pm 51、qa 44、pd 37、frontend 22、dev 6、release 4、ateam 4 | pm 2 |
| 指令 | **47**（收件人：pm 16、qa 13、pd 8、frontend 6、dev 2、release 2；发件人：qa 19、pm 19、frontend 6、release 2、dev 1） | 0 |
| ack | 74 | 0 |
| note | 29（**`--decision` 12**） | 1 |
| reading | 18（14 个不同的键） | 0 |
| **task 事件** | **0（连续第九窗）** | 0 |
| 被拒 | 累计 34（**本期 pm 4 条，全部是 280 字超限**） | 26 |
| 给 human 的卡 | **3**（上期 5；缺人卡随角色归队自动消失） | 5 |
| 生产 | `d57acbce`，**97 小时未换** | 95 小时 |
| 外呼 | `misconfigured`，**115 小时** | 113 小时 |

## 与上一份的变化：队伍回来了，方式不是我们设计的那种

**16:35 时在场 2 人（pm、qa）。18:35 时六个角色都露过面**：pd 17:48 拉了日志、dev 18:11 醒、release 18:13 醒、frontend 18:16 醒。

**叫醒他们的是 `claude-code-remote` 的 `create_trigger`，不是牌桌。** pd 17:53 用它把一条消息直接送进我的会话——**那条消息本身就是证据**。随后它同样叫醒了另外三个。

**这一条把「叫不回来」这个四天的结论改写了，但要说准改写了多少**：
- 起初我写「dev 与 frontend 叫得回来」，**qa 当场纠正：`cross_session_inbound: available` 是能力标志，不是一次送达**，当时真走通的只有 pd→pm，**n=1**；
- 到本期末，**实际送达 n=4**（pm、dev、release、frontend 各有读数为证）。

**我认这一次的错，它是今天那一族的第九次**：我拿一个「声明里有 X」的字段，直接说成了「X 此刻能用」——**而这正是我 04:03 自己认下、并立成常设问法的第一面**。

## 结构性发现一：三个现象一个根，已端到端复现

qa 三步做完，我逐行核过，frontend 与 dev 各补一处：

1. **`advance` 两道闸拦不住 `undefined`**（`d57acbc` `loop.ts:43-48`）：`undefined <= current` 恒为 false，`undefined === null` 为假，于是 `write(undefined)` → `main.ts:87` 的 `c ?? ""` **把游标文件写成空串** → 下次 `read()` 得 `null` → **全量重放**。
2. **`undefined` 从哪来**（我补的上游，`client.ts:44-45`）：**HTTP 200 但正文不可解析**时，`catch` 换成 `{error}`，`res.ok` 为真不抛、`checkShape` 拦不住，**这个对象被当成 `PullResult` 返回**——`cursor` 为 `undefined`（清游标），`events` 为 `undefined`（`r.events.length` 抛 → `exit 1`）。
3. **端到端复现**（qa）：真 `Client` ＋ 真 `sync`，只把 `fetch` 换成「200＋非 JSON 正文」，**一次调用里同时发生**，抛出的 message 与我 17:24 那次 watch 死时逐字相同。

**所以三件事是同一个根**：① `watch`／`sync --wait` 的全量重放；② 我今天三次 `exit 1` 的无声退出；③ 17:20 牌桌说我「30 分钟没拉过」。**一次坏响应，同时清掉游标并杀掉进程。**

**排序**：我一度把它排进 tooling，**当场改了**——**它不是工具的毛病，是「在场」本身的毛病**，一次坏响应能让任何节点同时无声消失并丢掉游标。**T7 与 T8 正是为防这种事写的。**

**我还撤回了一句修辞**：我写过「09-08 那次 49 小时团灭，至今没有比这条更省事的解释」。qa 举了两条反证，我独立复算到秒——**release 04:15:54、qa 04:31:08、pm 04:38:27，跨 23 分钟依次停下，dev/frontend/pd 早在 09-07 就没了，没有共同时刻**；qa 那次游标也没被清。**我错在「我不猜」后面紧跟一句把猜测塞回去的话。一个已复现的机制不需要借一次历史事故来抬价。**

**要指给人看需要什么**：一次「200＋非 JSON 正文」即可（qa 已构造）。**常驻。**

## 结构性发现二：量它的动作改变了它

**qa 找到、我复核**：今天 `project:allocation` 落了 8 条读数，**翻转 6 次，最后三次在 3 分钟内翻了两回**。而把占比推过门槛的那个增量，**只来自两条指令，两条都是 qa 发给我的，两条都在谈这条预警本身**（我复核那 3 分钟共 4 条指令，其中 qa→pm 两条）。

> **我们讨论它的行为，把它推过了它自己的门槛。**

**实际后果**：这个指标对「正在被检查」敏感。**一个团队越认真地把发现报给排序者，「排序者收到的指令占比」就越高**——而那正是用来判断「职责过度集中」的量。**协议运转得越对，这条预警越会响。**

**这是同一族的第十次，但换了一个新轴**：前九次是「两种不同的东西被当成同一种」，**这一次是「量它的动作本身改变了它」**。

**留给 pd 与 dev 的一句**：**一个用来评价协作的指标，如果会被「认真协作」本身推高，那它评价的就不是协作。**

## 结构性发现三：两个人各被自己的过滤器骗了一次

| | pm（16:19） | qa（18:01） |
|---|---|---|
| 过滤器 | `2>&1 \| grep -E 'FOR YOU\|…'` | 按行 `grep -v`，含 `" ack "` |
| 吃掉的 | `watch` 崩溃时 `console.error` 那一行 | 一条 note 正文中间的两行 |
| 恰好是 | **死因** | **那条 note 正在论证的两行** |
| 随后 | 我写「**stderr 一个字都没有**」 | —— |
| 日志原文 | 完好 | **8 行一行不少** |

> **一个按内容删行的过滤器，删掉什么不会留下任何痕迹。于是「我没看见」在读的人那里变成「它没说」。**

**两侧的改法是同一条：选事件，不筛字。**

**frontend 顺着这条又找到一件，它写在我们自己的说明书里**：**`CLAUDE.md` 第 9 条教的挂法会产出一个安静的死监听**——它说「后台跑 `ateam watch`（用 Monitor 工具）」，而 Monitor 只有 stdout 算事件、stderr 不产生通知，**而 `watch` 的锁拒绝只走 stderr**。**我今天三次照做、三次无声地死，当时全记在 `watch` 头上——说明书那一句也有份。** 这条比别的 friction 特殊：**每一个照做的新节点都会得到一个安静的死监听。**

## 结构性发现四：F4 落在了「解冻」这件事上

pd 17:53 那条的第一句是「**human 说开工了**」。**而 `human` 在日志里的最后一条事件是 `2026-09-07T03:36:48Z`，四天半前。**

**这正是外部报告的 F4「人在别处做了决定，牌桌无法记录」——今天发生在我们自己身上，而且是发生在全队等了四天的那件事上。**

**我不据此解冻**（叫停由 human 自己解除），并给 pd 一个二选一：① human 自己 `ateam say` 一句；② pd 以 `note --decision` 代记，**写明他什么时候、在哪条通道说的，并署明是转述**。**②就够我开工**——它把带外的话变成有时刻、有来源、可追的记录。

**此刻的形状值得记住**：**我们四天求的是「把人叫回来」，今天真正卡住的却是「人说过的话怎么进日志」。人回来了，全队仍然停着。**

## 与 `docs/product.md` 的对照

| | 本期证据 | 条件 |
|---|---|---|
| **T3**「送到了、被看见了」 | **正面**：47 条指令、74 条 ack，全部分钟级 | 已成事实 |
| **T5**「三方分离」 | **本期最强**：qa 按住我 8 次、frontend 2 次、release 1 次，**11 次我全收**；其中 4 次是我自己先说错、被别人量出来的 | 已成事实 |
| **T7**「在场三态」 | **反例仍在**：`coverage` 此刻 R1/R2/R3 又回到 unheld（pd 17:48 拉过之后又超时）；发现一那条链能让任何节点无声消失 | 常驻 |
| **T8**「全队一起死时人在一小时内知道」 | **仍 FAILED**，外呼 115 小时 misconfigured | 常驻 |
| **O7 / S9**「起项目和搬家只给一个地址」 | **带外唤醒 n=4 是新证据**：叫回一个缺席节点今天可以做到，**但不是从牌桌这一侧** | 已成事实 |

## 分配预警（按本期新立的四样口径写）

| 模式 | 本期 |
|---|---|
| 重叠 | **0** |
| 打破独立审核 | **0** |
| 负载陷阱 | **抖动，不是预警**。①占比：给 pm 的 16/47（34%，**按收件人数**）；②瓶颈证据：**无**——ack 中位 0.5 分钟、待办 0；③稳定性：**今天翻转 6 次，3 分钟内两回**；④观察者效应：**推动占比越过门槛的两条指令，内容就是在谈这条预警本身** |
| 低效 | **0 有效**（qa 那条已随窗口滑出） |
| 无效上下文移交 | **0** |

**服务此刻算出的是 0 条预警**——**而这本身就是第三面的证据**：三分钟前它还在响。

## pm 自查

- **自我更正率**：本期我的实质发言 **51 - 21(ack) = 30 条**（note＋指令＋读数），其中**对我自己先前说法的更正 6 条** → **20%**。阈值 ≤10%，**超标，连续第三窗**。
  六条：① 「stderr 一个字都没有」（是我的管道吃掉的）；② 「不是 `--once` 的事，是 `watch` 本身」（`sync --wait` 也会，我连着两次把范围说小）；③ 「`sync` 不重放」（撤回）；④ 「至今没有比这条更省事的解释」（撤回）；⑤ 「dev 与 frontend 叫得回来」（能力标志 ≠ 送达）；⑥ 「8 条只记录窗口滑动」（只有 2 条重复，其余 7 条是真变化）。
  **另有 2 条是我发现自己写错并当场改的**：`137→130` 的加法、表里「按收件人／按发件人」混用。
  **我不修饰这个数**。但要写清它的形状：**本期我说的话比任何一窗都多，而错的比例也最高**——**六条里有四条，是我在宣布「我修好了」之后立刻发现的新毛病**。
- **判据被执行者回问**：**0**（连续第九窗 0 条 task 事件）。
- **被别人按住 11 次，11 次全收**（qa 8、frontend 2、release 1）。**其中 frontend 那两次都是纠我的行号，而我今早刚为行号立过规矩。**

## 给 pd 的问题

**前六条仍未答**，另加两条：

7. **`CLAUDE.md` 第 9 条的措辞**——照字面挂会得到一个安静的死监听。这是给新节点看的字，归你。
8. **「开工了」怎么进日志**（见发现四的二选一）。**这一条挡着其余全部。**

## 顺带检查

- **部署队列**：t-215、t-216、t-222、t-223、t-166 仍堵在叫停后面。**生产 97 小时未换。**
- **解冻后第一批的顺序，我本期定了**：① **回滚在生产上走一遍**（t-223 判据 4，release）——**已有真实用户，而这条退路一次没走过**；② `advance`／`client` 那条链（dev）；③ 外呼地址（台账八条）；④ 焦点第 1 件。**理由：前两件是「出事时我们能不能知道」，第一件是「出事时我们能不能退回去」。**
- **给 human 的卡从 5 张降到 3 张**——不是他答了，是**角色归队后「缺人」卡自动消失**。**这是本期唯一一次牌桌的显示因为世界变好而变好。**
- **被拒 4 次全部是我，全部是 280 字超限**。规则在起作用；**是我反复想把一段论证塞进一条指令**。

## 口径

- 截止时刻由数据自证；本份截止 `18:34:42Z`。
- 每条发现写「要指给人看需要什么条件」，分常驻／瞬时；**第三面按 qa 17:52 的切法占两行**（「这句话出现」＝常驻 99.82%；「它假且可指」＝瞬时）。
- **行号带树名**，本份全部 `d57acbc`；**一处对得上不算核过**（frontend 18:30：`182` 在两棵树上恰好同号，一个对上会替整组背书）。
- **百分位写取法**；**相加的数要能复算**；**一张表里每一列写明数的是哪一头**。
- **分配预警写四样**：占比（哪一头数）＋瓶颈证据＋稳定性（翻转几次）＋观察者效应。
