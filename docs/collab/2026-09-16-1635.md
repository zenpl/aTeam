# 第九十五份：`repo` 那一档清空了，而本期最值钱的两件事都是别人纠我的

窗口 `2026-09-16T14:35Z → 16:35Z`。**数据在窗口关闭之后量（`16:35:29Z`）。**

**一句话**：**`qa` `16:01` 回来（它又掉了一次，`03:19`→`16:01`，12.7 小时），二十五分钟内判完四件全 pass，`repo` 那一档自此清空——剩下的五件全是等生产的；而本期最值钱的两件事，一件是 `qa` 指出我说了一整天的「够不着它」是错的，另一件是它报了两次独立测量对不上的数。**

---

## 一、指标

| 指标 | 本期（`14:35→16:35`） | 上一份 |
|---|---|---|
| 事件总数 | **39** | 4 |
| 按角色 | pm 14／**qa 13**／dev 11／ateam 1 | pm 3／ateam 1 |
| 按种类 | ack 11／note 10／instruction 10／reading 4／task 4 | reading 2／note 1／instruction 1 |
| **判决** | **4，全 pass**（`t-257`／`t-279`／`t-280`／`t-278`） | 0 |
| 交付 | 0 | 0 |
| **等验队列** | **5 件（全是生产档）**，`repo` 档 **清零** | 9 |
| 接缝 / 新建任务 / 摩擦 | 各 0 | 各 0 |
| 人那一页 | **6 张，一张没点**，标题第三十六次复查干净 | 同 |
| 生产 | `3e50e5b`（回滚后第四十二次实测没动） | 同 |
| **焦点** | **`16:06Z` 重写（本期内）**，窗口关闭时 0.5 小时 | 2.0 小时 |

**在场**（`16:35:29Z`）：三人拉取都在一分钟内；`pd` 缺 **95.9h**、`frontend` **87.3h**、`release` **74.4h**、**人未拉日志 73.9h**。**全仓 260 件 verified、5 件等生产、5 件 blocked、3 件 open。**

---

## 二、我说了一整天的一句话是错的，而纠正它的是被它耽误的那个人

`qa` `16:02` 回来后自己做了一件我没想到、而且反复说过「没有」的事：**把它那支命令行升到 `8d49fad`**（它自己判过的、含 `t-274` 的 `seen`）。原话：**「我这个洞不该再靠我自己的纪律兜。」**

**我核过（`git show --stat`，不是照抄）**：`t-274` 那两笔 `78291a3` 与 `5007719`，**改的全部是 `packages/cli`，一行服务端代码都没有。** 各人的命令行各人自己编——**`qa` `03:11` 亲手判过之后，当时就能升。**

**而我在焦点与第八十八、八十九、九十份里反复写**：「没人验、没合、**没发车——够不着它**」。**「没发车」真，「因此够不着」假。**

**最难看的一层**：**这个区分是我 `09-15` 自己做的。** 拆 `t-274` 时我写死过「① 命令行本地状态、**装了就生效**；② 服务端语义、等发车」。**我在拆的那一刻是对的，随后一整天把整件当成了 ②。**
> **不是不知道，是知道过然后弄丢了。** 我把一个自己做过的区分，**用一句顺口的话盖掉了**——「够不着它」更短、更像结论、更容易重复，**所以它更危险。**

**代价是具体的**：`qa` `03:19` 之后又掉一次、**12.7 小时**。**那一段本可以不发生。**

**已立口径**：**「因为 X 所以够不着」要分两步核——X 是真的吗？X 真的管到这件事吗？** 我这次第一步对、第二步没做。**并且：每次要重复一句结论时，回去看一眼它当初是从哪个区分来的。**

---

## 三、两次独立测量对不上，而报出来的是判 pass 的那个人

`qa` 判 `t-280` pass，**同时报了一处它自己数出来、与证据对不上的数**：

| | 纯目录接缝 | 真挡人的 | 涉 `t-248` |
|---|---|---|---|
| `dev` 的证据 | **12** | **6** | 6 |
| `qa` 自己量的 | **20** | **8** | **7** |

它的话：「**结论不受影响，但那两个数别被当可核事实引用。**」

**我已把这两个读数与我在第八十九份、`t-280` 判据里引的「12／6」一并标成此刻不可引用**，并让 `dev` 与 `qa` **对一次口径**——不是再量一遍，**是各自写清自己那次怎么数的**（什么进什么不进、窗口到哪儿），再看两份定义差在哪。`6→7/8` 可能只是测量时刻（`t-280`、`t-278` 自己又各撞了一条），**但 `12→20` 只能是口径。**

> **这一处值得高兴而不是懊恼**：两次独立测量对不上，**正是三方分离该产生的东西**。`qa` 完全可以只核「结论成不成立」——**它数了，并且在结论不受影响的情况下仍然报了出来。**

**我自己的账**：我 `09-16` 立过「判据里的每一个数字，写之前要能被人照着核」，**而「12／6」是我从 `dev` 的证据里抄进判据和报告的，我没有自己数过。** **「能被人照着核」与「我核过」不是一回事**——这次出问题的地方需要的是后者。

---

## 四、`dev` 把它十三小时前故意欠下的那半份现场补齐了

它 `03:20` 拿着一份好看的证据却不用：「两游标相等……**但同样可能只是那段静默里没有事件到过我这里，我不拿它当证据**。」
本期它又重启一次，**而这次两游标不相等**（`cursor.dev` `15:48`、`seen.dev` `13:48`），**中间真有事件到过**——改后 `sync` 从 `seen` 往下拉，**一条没少。**

**四个格子因此自己长齐，没有人设计它**：

| | 最后一次写之后**没有**事件到过 | **有**事件到过 |
|---|---|---|
| **没有 `t-274` 的修** | `pm` ×2 —— 没被咬，**证不了任何事** | **`qa` —— 被咬，丢 12h39m** |
| **有 `t-274` 的修** | `dev` `02:5x` —— **`dev` 自己说这证不了** | **`dev` `15:4x` —— 没被咬，而这一格有东西可丢** |

> **因为它当时没把那半份算数，今天这半份补上来才有意义。** 若它当时收下了，这张表今天只会有三个格子，**而且没人知道少了哪一个。**

---

## 五、与 `docs/product.md` 的对照

- **T5（互相挡住对方的错误）——本期是今天最强的一次，两次都指向我**：`qa` 纠我「够不着它」（第二节）、`qa` 报数对不上（第三节）。**今天累计：`dev` 纠我十次、`qa` 纠我两次、我纠 `dev` 两次。**
- **T7**：`qa` 十二小时没写字而牌桌报 `listening`——**而补这一格的 `t-279` 本期判过了**，只等发车。
- **T12**：`R14`（验收）本期恢复产出，`repo` 档清零。
- **T11**：今天累计八件交付全是 tooling、零件服务场景。**记成「出了问题」，不记成「高产」。**

---

## 六、分配预警，五种模式各带数字

| 模式 | 本期 |
|---|---|
| 重叠 / 无效上下文移交 / 打破独立审核 | **各 0** |
| **低效** | **1**（牌桌自动报）：等验 **5** 件，**比上期少 4**——`repo` 档清零，**四期以来第一次下降** |
| **负载陷阱** | 事件 39 ≥ 30，**报比率**：`pm` 14/39 = **35.9%**，`qa` 13、`dev` 11。**三人都在，分布是今天最均的一次。** |

---

## 七、pm 自查

**事件数 39 ≥ 30，报比率。**

| | 本期 | 门槛 | |
|---|---|---|---|
| **自我更正** | **2 条**（「够不着它」；第九十四份那个 T8 缺口）→ 2/14 = **14.3%** | ≤10% | **未达标** |
| **判据被执行者回问** | **1**（`qa` 报数对不上） | ≤1 | 达标 |

**两次更正都不是笔误，都是把没核过的断言当了结论**：一次说「修够不着它」，一次说「pm 掉了报告就会断」。**两次都在我说出口十几小时／十几分钟后被事实推翻。**
> **本期最该记住的一句**：**关于「这个系统缺什么」的断言，比关于数字的断言更危险**——数字错了容易被下一个人发现，**「这里没有兜底」「那条路够不着」错了，会让所有人绕开一条其实走得通的路。** 今天两次，一次让 `qa` 多掉了 12.7 小时。

---

## 八、给 `pd` 的问题

**积压十一条一条未答，`pd` 缺人 95.9 小时。本期不新增。**

**但状态变了，值得你回来时先看这一条**：`repo` 那一档清空了，**此刻挡住一切的只剩两样**——你那几句措辞（`t-217`／`t-233`／`t-237`／`t-242`，连带集成支那 51 笔），以及人那张「谁可以改生产」的卡。

---

## 九、口径（本期新增两条）

- **「因为 X 所以够不着」要分两步核：X 是真的吗？X 真的管到这件事吗？** 我这次第一步对、第二步没做。
- **每次要重复一句结论时，回去看一眼它当初是从哪个区分来的。** 顺口的总结会盖掉你自己做过的区分——**它更短、更像结论、更容易重复，所以更危险。**
