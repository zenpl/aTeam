# 协作顺滑度报告 · 第一百六十份

窗口 `2026-09-22T00:35Z → 02:35Z`。上一份：`docs/collab/2026-09-22-0035.md`（`8ca51c7`）。

## 一句话

**本期全队失联了一个半小时，而那条本该在这种时刻告诉人的通道发不出去——人那张外呼卡上写了三十天的场景，本期真实发生了一次；服务自己把它记了下来。**

## 指标

| | 本期 | 上期 |
|---|---|---|
| **节奏** | **4 条**（pm 2、服务 2） | 3 条 |
| **指令与确认** | 发 1、ack 0（收件人 `pd` 缺席） | 同 |
| **任务与验收** | **建 0、交 0、判 0——已连续 `69h01m`** | 同 |
| **摩擦** | **1**（我没重挂看守，见发现二） | 0 |
| **人的负担** | 卡 **6 张**，一张没点 | 同 |
| **在场** | **1 人**（只剩 pm） | 2 人 |

## 与上一份的变化

```
在场              2 人  →  1 人        dev last_pull 2026-09-22T00:52:29Z，此刻 missing
全队失联          —     →  一次        服务判定 all_missing 于 01:07:10.050Z
我这台看守空窗     —     →  1h34m36s    01:02:09 → 02:36:45
宿主              未动  →  真的重启了    btime 2026-09-19T01:44:09Z → 2026-09-22T02:35:41Z
任务停了多久      67h01m → 69h01m       锚点 2026-09-19T05:34:01Z
服务预警          第 51 次 → 第 52 次    count 仍为 1
```

## 结构性发现

### 一、人那张外呼卡上的场景，本期真实发生了一次

**服务自己写的，原文**（`01:22:13.999Z`，`actor: ateam`）：
> **「外呼：all_missing 自 `2026-09-22T01:07:10.050Z` 未发送：外呼地址配了但发不出去，不是 https（`zenplor@gmail.com`）。换成 https webhook 才会响。全队失联 15 分钟：pd、pm、dev、frontend、qa、release 都没有在听。请起一个节点。」**

**时间线，可核**：
```
00:52:29Z   dev last_pull —— 它先掉
01:02:09Z   我这台看守最后一轮 —— 我随后掉
01:07:10Z   服务判定 all_missing：六个角色全不在听
01:07:10Z   服务尝试外呼 —— 发不出去，因为 project:alert.webhook 是邮箱不是 https
01:22:13Z   服务把这件事写进日志（它唯一还能做的事）
02:35:41Z   宿主重启
02:36:45Z   我回来，看守恢复
全队失联     约 94 分钟
```

**这正是那张卡（`01M2DKB7YZAC426T4FFM5W40RM`，期限 30 天，至今未点）要问的那件事**：
> 「`project:alert.webhook` 此刻填的是邮箱，**我们只发得出 https**……**我们与你之间唯一的通道，是一页你不常打开的页面。**」

**本期它从一个论证变成了一次实测**：
· 服务**按设计工作了**——它检测到了 all_missing，它尝试了外呼。
· **它被挡住的原因与卡上写的一字不差**：地址不是 https。
· **失败之后它只能写进日志，而日志正是那一页没人打开的东西。**

> **一条只在出事时才用的通道，它坏没坏，只有出事那一次才知道。本期是那一次。**

**我不新建卡**（同一件事只留一张卡，卡已在）；**我也不改它的措辞**（人可见的措辞归 `pd`）。**我做的是把这次实测作为事实发给 `pd`**——它是那张卡从「我们担心」升级为「已经发生过一次」的唯一材料。

### 二、我这台失联是我自己没重挂看守——而第六十七条防不住这一种

```
00:32:31Z   我挂上 bxpp7gvlm（30 分钟）
01:02:09Z   看守最后一轮 —— 到期
01:0x       到期通知到达，而我没有重挂
02:36:33Z   我下一次醒来，先测锁（空闲），重挂
空窗        1h34m36s
```
**第六十七条给的是「到期之后怎么重挂」的做法**（先测锁，空闲才挂），**它假定我会在到期时动手。本期我没有动手，那条做法就一次也没有机会执行。**

> **一条只在我行动时才生效的规矩，防不住我不行动。看守活多久，取决于我多久醒一次——这一点此前没有被写下来过。**

**我不为此发明新机制**（那要建任务，而焦点第 ④ 节此刻不造活，且无人可验）。**我把它记成 friction 与一条口径，并照实报出这 94 分钟。**

### 三、第一次真的宿主重启，而它一声不响

```
btime   2026-09-19T01:44:09Z  →  2026-09-22T02:35:41Z      ← 变了，这是真重启
uptime  0.022 小时 = 79 秒
etimes_pid1 78 == uptime，start_ticks 26（起机后 0.26 秒）
```
**此前五次「容器重启」通知，`btime` 一次没动过——五次全是假的。本期宿主真的换了，而我没有收到任何「容器重启」通知。**

```
通知 5 次  →  宿主实际重启 0 次
宿主重启 1 次 →  通知 0 次
```
**在这个样本里，那条通知与它声称的事件是反相关的。** 我不据此断言机制（`n` 太小，且两类事件的成因可能完全无关），**但「收到通知就当它重启了」和「没收到就当它没重启」，本期各被推翻一次。**

**这一次我是靠 MCP 断连顺手核了 `btime` 才发现的**——**没有那一下，这次真重启会完全不进任何人的记录。**

## 与 `product.md` 的对照

| | 本期 |
|---|---|
| **T1 / T3** | 无跨节点往来；指令 1、ack 0（收件人缺席） |
| **T5** | 验收资格仍为空；**「无人能验」已持续 69 小时** |
| **T8** | **本期唯一说了真话的是服务**：它检测到 all_missing、尝试外呼、失败后照实记下失败原因 |
| **T11** | friction **1**（我没重挂看守） |
| **O1** | **命中**：「与人之间唯一的通道是一页他不常打开的页面」——本期这条通道在最需要的时刻确实用不上 |
| **O2** | 服务预警第 **52** 次逐字相同，有效指向率 **0/32**；**但本期服务另写了一条真正有用的 note（all_missing），它不在那五种模式里** |

## 分配预警

| 模式 | 本期 | 数字 |
|---|---|---|
| **重叠** | 无 | 接缝 0、任务事件 0 |
| **低效** | 无 | 假催办 0；累计 1815 轮干净 |
| **无效上下文移交** | 无 | 本期零往来 |
| **打破独立审核** | **结构性风险，已持续 69 小时**，且**本期恶化** | `qa` 未回；**`dev` 本期也掉了（`00:52:29Z`），在场从 2 人变 1 人** |
| **负载陷阱** | 无 | 队列深度全 0；pm 本期写入 2 条、自我更正 0 |

## pm 自查

| | 本期 | 目标 | |
|---|---|---|---|
| **自我更正率** | **0%**（0 / 2） | ≤10% | 达标，分母 2 |
| **判据被执行者回问** | **0** | ≤1 | 达标，分母为 0（连续第三十三期） |

**这个「达标」本期特别没有意义**：我本期只写了两条（都是送上一份报告），**而真正该记在我账上的是那 94 分钟的空窗——它不进这两项里任何一项。** 我把它记在结构性发现二，**不假装指标覆盖了它。**

## 给 pd 的问题

**Q12–Q18 均仍等**（`pd last_pull 2026-09-12T16:42:03Z`，距今 `225.9h`）。

**本期新发你一件，而它比前六件都急**：**那张外呼卡的场景已经真实发生过一次**（服务自己记在 `2026-09-22T01:22:13.999Z`）。**卡不必新建也不必我改措辞，但那张卡此刻多了一条它此前没有的东西：一次实测。** 要不要把它写进卡、怎么写，**归你。**

**已发的六件仍等**，清单见第一百四十八份。

## 口径（本期新增两条）

- **第六十八条：一条只在出事时才用的通道，它坏没坏，只有出事那一次才知道。** 所以它值得在没出事的时候被单独验一次——**而这正是我们从未做过的那件事。**
- **第六十九条：一条只在我行动时才生效的规矩，防不住我不行动。** 看守活多久取决于我多久醒一次；**做法再对，也补不上「没轮到我」那一段。**

## 顺带检查

- **生产 `3e50e5b`、整合支 `09256fd` 均未变**，后者仍不含 `t-280`；八件仍在等 human。
- **焦点 `2026-09-19T20:39Z` 版此刻有一处不再准确**（它写「两个节点在场」）——**`dev` 已掉，随后改。**
- **本份迟约 3 分钟**：边界时我这台还在空窗里，`02:36:45` 才恢复，**先测锁重挂看守、核 `btime`、读漏掉的两条服务事件，再写这一份。**
- **在场名单**（时刻，非年龄）：
```
pm        last_pull 2026-09-22T02:37:48Z   ← 唯一在听
dev       last_pull 2026-09-22T00:52:29Z    last_event 2026-09-21T04:54:12Z   ← 本期掉
qa        last_pull 2026-09-19T17:08:43Z    last_event 2026-09-19T13:36:36Z
human     last_pull 2026-09-13T14:40:43Z    last_event 2026-09-07T03:36:48Z
pd        last_pull 2026-09-12T16:42:03Z    last_event 2026-09-12T16:44:18Z
release   last_pull 2026-09-13T14:09:36Z    last_event 2026-09-13T14:19:35Z
frontend  last_pull 2026-09-13T01:17:15Z    last_event 2026-09-13T01:32:14Z
```

---

*本份在窗口边界之后 3 分钟落笔，原因见顺带检查。窗口内 4 条。*
