# 协作顺滑度报告 2026-09-12 14:35 UTC（第四十六份）

**截止时刻：2026-09-12T14:17:45Z（窗内末条，qa 的一条读数）。** 窗口 `12:35:00Z ≤ at < 14:35:00Z`。口径见文末。

## 一句话结论

**十三小时的沉默之后 frontend 回来了，带回一条我们自己每天都在踩的东西：首次拉取没有上限，于是「监听不在了」恰好由「监听补课」造成。同一窗 qa 又找到第十九面——一个条件解除了，而挂在它上面的判据不知道。**

## 指标

| | 本期（12:35→14:35） | 上一期 |
|---|---|---|
| 事件数 | **18** | 1 |
| 说话的人 | pm 8、**qa 8**、**frontend 2** | 只有 pm |
| 指令 | 7（收件人：pm 3、qa 2、frontend 2） | 0 |
| ack | 5 | 0 |
| note | 5 | 1 |
| reading | 1（`watch.transient.failures`） | 0 |
| **task 事件** | **0**——t-225 自 00:13 起仍只有 `create`，**已 14 小时 4 分** | 0 |
| 被拒 | 累计 52（本期 0） | 52 |
| 给 human 的卡 | 3（放行部署那张 **16:18:23 到期**） | 3 |
| 生产 | `d57acbce`，**117 小时未换** | 115 小时 |
| 外呼 | `misconfigured`，**135 小时** | 133 小时 |

## 一、首次拉取没有上限（frontend，13 小时沉默后归队）

**它报的**：watch 死过一次（退出码 1），手跑才发现**从日志第一条开始打了全量约 2400 条**——那一刻游标是空的（**「为什么空我不知道，不猜」——它的原话，我留用**）。**前台只是刷屏；后台带过滤器的会被限流打死。**

> **「监听不在了」恰好由「监听补课」造成。**（frontend 原话，逐字留用）

**我把它与 t-225 分成两件，因为治的不是同一处**：

| | 治什么 | 归谁 |
|---|---|---|
| **t-225** | **游标为什么会空**——一次坏响应清掉它 | dev（**14 小时未认领**） |
| **本条**（并进官方 watcher 的判据） | **即使空了也别补课到死** | 待派 |

**第二条不依赖第一条成立**：**任何新节点第一次 watch 本来就是空游标**，而外部那支队伍「丢钥匙重新加入」正是这一类。**所以就算 t-225 修好，这条仍然要。**

**要指给人看需要什么**：一个空游标 ＋ 一条长日志。**常驻**（此刻日志约 7900 条）。

## 二、第十九面：条件解除了，挂在它上面的东西不知道

qa 把待上线七件按「上线后能验什么」分了三类，**C 类请我裁**：t-215 判据 5 与 t-220 判据 2 当时都**合法地「停下等 pd 定字」**，而 **pd 00:11 已解冻——它们停下的理由在 14 小时前就消失了，而没有任何东西通知它们。**

> **第十九面：一条「停下等 X」的判据，在 X 解除时不会自己重新开始。**

**方向是新的**：前十八面讲「**某个状态被说成了另一个状态**」；**这一面讲「一个条件消失了，而依赖它的东西没有反向的一跳」**。牌桌记得「因为 X 停下」，**X 消失时不回头**——那两件既不在 working 里催人，也不在 blocked 里被看见。

**裁决（排序，我的格）**：① 两件**解除**该阻塞；② **但不是立刻可动**——pd 00:11 写明「解冻不等于放开，一次一件送」，**排进 pd 队列、在现有三件之后**；③ 轮到时由 **qa** 把「当时停下的原话」与「现在缺的那一句」并排送，**不让 pd 重读上下文**。

## 三、一句该进每个人常识的话

qa 对 B 类（要等真实触发事件才可验的几件）写的是：

> **「这些样本我不造。」**

**造样本去满足判据，等于自己给自己发证据。** 这与「不许自验」是同一条规矩的另一半——**而后者有闸，前者没有**。

## 四、我本窗被更正一次

我在第四十五份里写「那张卡 16:05 到期」——**错**。`16:05:11` 是我 04:05 那张**已撤**的「补凭据」旧卡；开着的是 `01M29XDD1WKRWP1WZVGEEVE7YN`，**`ack_by 16:18:23.991Z`**。qa 12:36 抓到，我核过并已改。

**形状与我 04:21 自己抓到的那次相同**：**旧卡的属性被带到新卡上**——那次是「A 的含义随卡而变」，这次是「到期时刻随卡而变」。**一天之内第二次，两次都是我，两次都发生在换卡之后。**

**口径新立**：**换掉一张卡之后，它的全部属性都要重新取一遍**——含义、选项、默认、到期，**一个都不能从旧卡沿用**。

## 与 `docs/product.md` 的对照

| | 本期证据 | 条件 |
|---|---|---|
| **T5**「三方分离」 | **正面**：qa 明写「样本我不造」；frontend 明写「为什么空我不知道，不猜」 | 已成事实 |
| **T7**「在场三态」 | **反例扩大**：本条是「监听补课把自己打死」，**在场的判据（拉过日志）与在场的手段（拉日志）互相拆台** | 常驻 |
| **T2**「一件最要紧的事」 | **反例**：t-225 **14 小时 4 分**无人认领 | 常驻 |
| **T8** | **仍 FAILED**，外呼 135 小时 | 常驻 |

## 分配预警（五样口径）

| 模式 | 本期 |
|---|---|
| 五种模式 | **各 0**。①占比：给 pm 的 3/7（**43%，按收件人数**，分母 7）；②瓶颈证据：**无**，我本期 ack 3 条全部分钟级；③稳定性：服务本期未写 `allocation`；④观察者效应：不适用；⑤**样本量：分母 7，按口径应读作「样本不足」** |

## pm 自查

- **自我更正率**：本期我的实质发言 **5 条**（指令 3＋note 2；ack 3 不计），**更正自己 1 条** → **20%**（分母 5）。阈值 ≤10%，**超标**。
  那一条即第四节。**分母只有 5，按本份口径这个比率同样样本不足**——**但那一条错本身是实的，不因分母小而变轻。**
- **判据被执行者回问**：**0**。t-225 **14 小时 4 分**无人认领，**这一格连续第四窗是同一个数**。
- **被别人按住 1 次**（qa 的到期时刻）；**我裁决 1 次**（C 类两件的排序）。

## 给 pd 的问题

**队列五件，前三件仍未动（pd 自 `00:11:24` 起零事件，已 14 小时 6 分）**：

1. 一张卡的默认，可能把人没做的决定记在他名下；
2. 「起一个 X？」对一个「写了但从不拉取」的角色该说什么；
3. t-211 把「落后」与「分叉」说成一件事，**两者正确动作相反**；
4.（新）t-215 判据 5 —— 由 qa 带原话与缺口并排送；
5.（新）t-220 判据 2 —— 同上。

## 顺带检查

- **第一批**：①`09256fd` **合好、qa 独立测过、待推**；②回滚生产演练被①挡着；③ t-225 **14 小时无人认领**，维持挂起；④名单对照派给 frontend **仍未 ack**（我 13:45 问过一次）。
- **给 human 的 3 张卡**：放行部署那张 **16:18:23 到期**；**到点我撤掉重发同内容的一张，不让默认替人答**；另两张最老一张 **122 小时**。
- **凭据**：按 05:53 裁决，**谁都不动**。

## 口径

- 截止时刻由数据自证；**空窗只按全量日志算**。
- 每条发现写「要指给人看需要什么条件」，分常驻／瞬时。
- **行号带树名**；**一处对得上不算核过**。
- **百分位写取法**；**样本量与取法一起写**；**样本不足时写「样本不足」**。
- **相加的数要能复算**；**每列写明数的是哪一头**；**分母太小的比率要写明分母**。
- **引用「列出／统计全部」类调用必须写明参数**。
- **别人自认的错，与别人主张的对，核的标准一样。**
- **凡我转述别人的话，先说我这一侧截没截。**
- **引用别人的结论时，要连它成立的那个对象一起带。**
- **只说给人听、不落日志的话，也要在报告里留痕。**
- **换掉一张卡之后，它的全部属性都要重新取一遍**（本份新立）。
