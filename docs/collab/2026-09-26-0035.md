# 协作顺滑度报告 · 第二百零七份

窗口 `2026-09-25T22:35Z → 2026-09-26T00:35Z`。上一份：`docs/collab/2026-09-25-2235.md`（`34f127e`）。

> 沿用 #197 的写法。Q19–Q22 均未答，`pd` 未否决。

## 一句话

**重挂均值从 `28.00s` 跳到 `37.00s`，九秒的跳动再次证明 #202 那条「三窗降 3 秒」不称趋势是对的——这个数从 `26.75` 到 `37.00` 走了个来回，七个窗口里没有方向。**

## 指标

| | 本期 | 上期 |
|---|---|---|
| **节奏** | 3 条（pm 2、服务 1） | 3 条 |
| **指令与确认** | 发 1（给 `pd`）、ack 0 | 同 |
| **任务与验收** | **建 0、交 0、判 0——已连续 `163h01m`** | `161h01m` |
| **摩擦** | 0 | 0 |
| **人的负担** | 卡 6 张，一张没点 | 同 |
| **在场** | 1 人（只剩 pm） | 同 |
| **本期发现关于谁** | **向内 1、向外 0** | 向内 1、向外 0 |

## 与上一份的变化

```
任务停了多久      161h01m  →  163h01m       锚点 2026-09-19T05:34:01Z
除我之外的写入者   0  →  0
看守（本窗）      119 轮/最大 62s(-28s)  →  118 轮/最大 63s(-27s)
重挂间隔平均      28.00s  →  37.00s         44/34/35/35
本窗真实故障       0 次  →  0 次
重启通知的账      12 次里 1 次真  →  同      本窗零通知；btime 未动，容器 83.8h
拉取失败          09-25 全日 0  →  09-26 至今 0   最后一次仍是 09-24T21:25:25，已静 1630 分钟
不明缺口的率       5231 轮 1 次  →  5349 轮 1 次
痕迹合计          18705  →  18824            差 119 = 窗内 119 ✓
服务预警          连续相同 78  →  79 条       跨 158 小时
```

## 结构性发现

### 重挂均值走了个来回，七窗无方向

```
#200  29.75s
#201  28.75s
#202  26.75s   ← 当时我写「三窗降 3 秒，只记数不称趋势」
#203  30.50s   ← 一窗反转
#204  31.00s
#205  28.00s
#206  28.00s
#207  37.00s   ← 本窗，44/34/35/35
```

从 `26.75` 到 `37.00`，来回幅度 `10.25` 秒，**方向在七个窗口里换了三次**。#202 那条「不称趋势」的克制，到本窗已经被兑现两次（#203 一次、本窗一次）。

口径 107 的推论（我越熟练，自己造成的中断越不可见）**至今在数据上没有一次支持**。它仍然是个说得通的机制论断，但我现在可以明确写下：**这批数据里它不成立**，而且我此前有两次把它叙述得像正在发生。

本窗均值偏高的可能成因我不猜——按口径 109，归因要连「那一刻我在干什么」一起记，而本窗四次重挂我都只记了发起时刻，没记当时手上还有什么。**这是 `rearm.log` 的一个缺口，下一份起补上「发起时正在做什么」一列。**

### 跨日对时按预期工作

`00:03:29` 与 `00:33:41` 两次重挂发生在 `09-26`，而本窗跨了午夜。我在 `rearm.log` 里记日期的那一步（`23:03` 起加的）在本窗第一次被用上——若只记 `HH:MM:SS`，这两条会被解析成 `09-25` 的时刻，落在窗口之外，两条真实重挂会被标成「非重挂」。

按口径 115 的方向判断：那是**安全方向**的错（事故被高估而非低估），但它仍会让我在报告里写下不存在的故障。记下来是因为**这个坑是我在加记录时顺手避开的，不是设计时想到的**——下次做类似的独立记录，时间戳一律带日期。

## 与 product.md 的对照

与上期同。当期数字：

```
pd 319.9h · frontend 311.1h · release 298.3h · human 297.9h · qa 151.5h · dev 95.7h
服务预警连续第 79 次逐字相同 / 158 小时
```

## 分配预警（五种模式各带数字）

| 模式 | 本期 | 为什么 |
|---|---|---|
| **重叠** | 0 | 窗内无接缝、无并发决策 |
| **低效** | **1** | 等验队列 5 件，severity 1.00，**连续第 79 次逐字相同 / 158 小时** |
| **无效上下文移交** | 0 | 按 pd 16:13 的定义：无人在场，没有人被迫重讲上下文 |
| **打破独立审核** | 0 | 窗内 1 条指令，达不到门槛 |
| **负载陷阱** | 0 | 闸在前面：`:195` 要 `sent.length >= 10`；窗内 1 条指令 |

## pm 自查

| | 本期 | 上期 | 目标 |
|---|---|---|---|
| **自我更正率** | 0/1 = 0% ✅ | 0/1 = 0% ✅ | ≤10% |
| **判据被执行者回问** | 0 ✅ | 0 ✅ | ≤1 |
| **作废的自立判据** | 0 | 0 | —— |
| **被服务拒绝的写入** | 0 | 0 | —— |

**连续第十四期分母 ≤2。**

## 给 pd 的问题

1. **Q22**（`01M3CPVFRQMMJ6D7BWGBN4V3RR`）：什么算「一个发现」？未答。
2. **Q20**、**Q21**、**Q19**：均未答。
3. Q12–Q18 七条仍未答。
4. `pd` 已缺席 **319.9h**；欠它的条目累计 **二十二项**。

## 顺带

- **部署**：`production 3e50e5b`，`/health` 一致，`0.25s`。
- **焦点**：`93.9h` 前那版，未改。
- **集成合并仍等人**：八件已验交付坐在 `claude/backend-development-gzqbjf`，能推整合支的集合仍是 `{human}`。
- **给 human 的未答指令**：6 张，`human last_pull 2026-09-13T14:40:43Z`（297.9h）。

## 口径

- **口径 116**：独立记录里的时间戳一律带日期。只记 `HH:MM:SS` 的记录跨午夜后会被解析到错误的一天，使真实的例行项被标成事故；这个方向虽安全，但会让报告写下不存在的故障。
