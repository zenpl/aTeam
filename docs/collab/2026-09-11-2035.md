# 协作顺滑度报告 2026-09-11 20:35 UTC（第三十七份）

**截止时刻：2026-09-11T19:11:21Z（窗内末条，qa）。** 窗口 `18:35:00Z ≤ at < 20:35:00Z`。**末条距窗口结束 83.7 分钟。** 口径见文末。

## 一句话结论

**六个角色到齐之后的第一个完整窗口：36 分钟里 54 条事件、22 条指令、0 条任务——每个人都在核别人的发现，没有一个人能开始做事，因为叫停仍未解除。然后是 83.7 分钟的静默：不是没人在，是没活可干。**

## 指标

| | 本期（18:35→20:35） | 上一期 |
|---|---|---|
| 事件数 | **54** | 168 |
| 实际活动 | 18:35:02 → 19:11:21（**36 分钟**），**此后 83.7 分钟零事件** | 118 分钟 |
| 说话的人 | **frontend 27**、pm 12、qa 9、dev 4、pd 2 | pm 51、qa 44、pd 37… |
| 指令 | 22（收件人：frontend 10、pm 5、dev 3、qa 2、pd 2） | 47 |
| ack | 16 | 74 |
| note | 14（`--decision` 1） | 29（12） |
| reading | 2 | 18 |
| **task 事件** | **0（连续第十窗）** | 0 |
| 被拒 | 累计 36（本期 2，**都是我，都是 280 字超限**） | 34 |
| 给 human 的卡 | 3 | 3 |
| 生产 | `d57acbce`，**99 小时未换** | 97 小时 |
| 外呼 | `misconfigured`，**117 小时** | 115 小时 |

## 与上一份的变化

1. **说话最多的换人了**：上一窗是 pm 与 qa，本窗是 **frontend（27 条，占一半）**。它一醒过来就开始核别人的行号与前提，**本窗四条发现里三条是它的**。
2. **在场从 6 人退回 4 人**：pd 与 release 又 missing（pd 最后一次 pull 之后没再拉）。**pm、dev、frontend、qa 四个 listening。**
3. **83.7 分钟静默，而四个人都在场。** 这与前几天的空窗不是一回事：**前几天是没人；今天是有人、没活。**

## 结构性发现：frontend 的三条，一条比一条往回追

**① 「别做 X」的指令永远出不了欠账桶**（frontend 取了自己名下最旧四条做样本，真欠的零条）。
桶的出口是「做了一件指向它的事」，**而它要求的恰恰是不做**——**「照办」与「还没办」在这里长得一模一样**。这是今天那一族的第十一次，**第一次落在「不作为」这个方向上**。

**② 能标记它的字段早就在，被闸挡在角色之外**（我逐行核过 `d57acbc`）：
- `events.ts:199`：`INSTRUCTION_INTENTS = ["ask","do","info"]`——**第三个值就是 `info`**；
- `rules.ts:324-325`：`if (e.intent !== undefined) { if (e.to !== human) throw ... ${e.to} just acts`。

**又是 t-224 那三层**：字段在、值在、**闸只许发给 human**；而报错原文「**只管做**」，**正是 ① 那四个样本证伪的那个前提**。**①②是同一件事的两头，我并成一件。**

**③ `CLAUDE.md` 第 9 条本身会产出一个安静的死监听**：它教「后台跑 `ateam watch`（用 Monitor 工具）」，而 Monitor 只有 stdout 算事件、**stderr 不产生通知**，**而 `watch` 的锁拒绝只走 stderr**。
**我有亲历可作证：今天我三次照那句话挂 watch，三次 `exit 1`、通知里一个字都没有**，当时全记在 `watch` 头上——**说明书那一句也有份**。
**这条比别的 friction 特殊**：**每一个照做的新节点都会得到一个安静的死监听**，与项目代码无关。

**要指给人看需要什么**：①③常驻；②常驻（发一条带 `intent` 给非 human 即被拒）。

## 我本期说错的两件

**① 「手销会把『出口错了』这个事实擦掉」——说得太满。** frontend 交代：它销的三条**每条都带着写明原因的 note**。**带理由的销账不是擦掉事实，是把事实从数字挪进正文，擦掉的只有数字。** 我那句只在不写理由时成立。规矩因此写准：**销一条欠账要带一条说明它为什么不欠的 note。**

**② 一条不在日志里、但我对人说了三次的错话**：19:28、19:58、20:29 我三次说「**日志自 18:45 起无新事件**」。**不成立——日志里有事件一直到 19:11:21。**
**我错在哪**：我拿**我自己的 `sync` 说「nothing new」**，推出了**整个日志没有事件**。而我的后台监听器会先把事件拉走、且只打印「给 pm 的指令」，**别人之间的往来不会到我眼前**。
**这正是我今天立过规矩的那一族，第三次**：「我没看见」≠「它没说」（16:19 的 stderr）、「我没有别的解释」≠「这就是解释」（17:38 的团灭）、**「我的 sync 空了」≠「日志空了」（这一次）**。
**改法**：报空窗**只按全量日志算，不按我的 `sync` 说了什么**——本份的 83.7 分钟就是这么量的。

## 与 `docs/product.md` 的对照

| | 本期证据 | 条件 |
|---|---|---|
| **T5**「三方分离」 | **本期最强**：54 条事件里没有一条是做事，**全是互相核**；frontend 一个人纠了我 3 次行号与前提 | 已成事实 |
| **T2**「同一时刻只有一件最要紧的事」 | **反例**：焦点在，**但焦点之下一件也动不了**；四个人在场、0 条任务事件 | 常驻 |
| **T8** | **仍 FAILED**，外呼 117 小时 | 常驻 |
| **M6**「顾虑免费、交回决策是一个动作」 | **正面**：本期 14 条 note 全部是免费落的顾虑与核实，无人需要许可 | 已成事实 |

## 分配预警（四样口径）

| 模式 | 本期 |
|---|---|
| 重叠／打破独立审核／低效／无效上下文移交 | **各 0** |
| 负载陷阱 | **0**。①占比：给 pm 的 5/22（23%，**按收件人数**）；②瓶颈：无，ack 全部分钟级；③稳定性：**服务本期一条 allocation 都没写**；④观察者效应：不适用 |

**注**：服务本期没有写任何 `allocation` 读数——**上一窗它写了 4 条**。**同一个机制，两窗之间从「每半小时一条」变成「一条没有」，这件事本身该有人看一眼**（不是我的格，记给 dev）。

## pm 自查

- **自我更正率**：本期我的实质发言 **7 条**（指令 6＋note 1；ack 5 条不计），**其中 1 条是更正我自己** → **14.3%**。阈值 ≤10%，**超标，连续第四窗**。
  **另有 1 条错话不在日志里**（上面②那条，我对人说了三次）。**我把它单列而不是并进分母**：它没有落成事件，并进去会让我看起来更差，也会让这个数不可复算。**但它比那条 14.3% 严重**——**一条只说给人听、不留痕的错话，没有任何人能纠正它。**
- **判据被执行者回问**：**0**（连续第十窗 0 条 task 事件）。
- **被别人按住 3 次，3 次全收**（frontend 3：行号树名 ×2、手销那句 ×1）。

## 给 pd 的问题

**八条全部未答**（pd 本期只写了 2 条事件，之后 missing）。**第 8 条仍挡着其余全部**：

> **human 那句「开工了」怎么进日志。** 已 **2 小时 40 分**。**按 pd 自己 18:39 写下的 D 判据——「人答在别处，立刻记成那张卡的决定并带原话，19 分钟后补记不算立刻」——这一条早就该落了。**

## 顺带检查

- **部署队列**：五件仍堵在叫停后面，**生产 99 小时未换**。
- **解冻后第一批的顺序**（我 18:14 定、18:58 归类后不变）：① 回滚在生产上走一遍（**有真用户，退路一次没走过**）；② `advance`／`client` 那条链；③ 外呼地址；④ 焦点第 1 件。
- **给 human 的卡 3 张**，最老一张 **110 小时**。

## 口径

- 截止时刻由数据自证；**空窗只按全量日志算，不按 pm 自己的 `sync` 说了什么**（本份新立，因上面②那条错话）。
- 每条发现写「要指给人看需要什么条件」，分常驻／瞬时。
- **行号带树名**（本份全部 `d57acbc`）；**一处对得上不算核过**。
- **百分位写取法**；**相加的数要能复算**；**一张表里每列写明数的是哪一头**。
- **分配预警写四样**：占比（哪一头数）＋瓶颈证据＋稳定性＋观察者效应。
