# 协作顺滑度报告 · 第一百九十九份

窗口 `2026-09-25T06:35Z → 08:35Z`。上一份：`docs/collab/2026-09-25-0635.md`（`6d5b3ce`）。

> 沿用 #197 的写法：九节一节不少，无变化的节只写「与上期同」加当期数字。Q21 仍未答，`pd` 未否决。

## 一句话

**口径 108 第一次用上就有回报：两侧最近落选值是 `61s` 与 `63s`，`46s` 到 `61s` 之间是空的——本窗「表上没有」确实等于「没发生」，这是上一窗做不到的区分。**

## 指标

| | 本期 | 上期 |
|---|---|---|
| **节奏** | 3 条（pm 2、服务 1） | 4 条 |
| **指令与确认** | 发 1（给 `pd`）、ack 0 | 同 |
| **任务与验收** | **建 0、交 0、判 0——已连续 `147h01m`** | `145h01m` |
| **摩擦** | 0 | 0 |
| **人的负担** | 卡 6 张，一张没点 | 同 |
| **在场** | 1 人（只剩 pm） | 同 |
| **本期发现关于谁** | **向内 1、向外 0** | 向内 1、向外 0 |

## 与上一份的变化

```
任务停了多久      145h01m  →  147h01m       锚点 2026-09-19T05:34:01Z
除我之外的写入者   0  →  0
看守（本窗）      118 轮/最大 63s(-27s)  →  119 轮/最大 63s(-27s)
本窗真实故障       1 次（54s 断听）  →  0 次
拉取失败          09-25 至今 0  →  0        最后一次仍是 09-24T21:25:25，已静 670 分钟
重启通知的账      11 次里 1 次真  →  同      本窗零通知；btime 未动，容器 67.8h
不明缺口的率       4282 轮 1 次  →  4401 轮 1 次
痕迹合计          17749  →  17868            差 119 = 窗内 119 ✓（边界那条落在下一窗）
服务预警          连续相同 70  →  71 条       跨 142 小时
```

## 结构性发现

### 一、口径 108 上线第一窗：这次「表上没有」是可以信的

```
离群表（<50s 或 >90s），四条，四条都是我的看守到期重挂
  06:35:04 → 06:35:50   46s
  07:05:27 → 07:05:57   30s
  07:35:35 → 07:36:04   29s
  08:05:43 → 08:06:11   28s

口径 108 要求的两侧最近落选值
  下侧最小未入选   61s   距 50s 下限 +11s
  上侧最大未入选   63s   距 90s 门槛 -27s
```

**关键是那个空带**：入选的最大值是 `46s`，落选的最小值是 `61s`，中间 15 秒没有任何样本。上一窗那次 54 秒的断听，正好落在这个带里；本窗这个带是空的，所以「离群表上只有我的重挂」这句话这次成立。

这就是口径 108 要买的东西：**它不让表变长，它让「表上没有」这句话可被检验。** 上一窗我只能说「表上没有」，本窗我能说「表上没有，且最近的落选值离筛子边界 11 秒，中间无样本」。

### 二、`06:35:04 → 06:35:50` 那条 46 秒，是边界与重挂撞在一起

四条里有一条不是标准的 `28–31s`，而是 `46s`。成因可查：`08:35` 边界前的那次重挂发生在 `06:35:47`，与我处理 `06:35` 报告边界（拉日志、写文件、commit、push、tell）同一分钟。**重挂本身仍是约 30 秒的反应，多出来的 16 秒是那一轮 `ateam sync` 与我的 `git push` 抢同一条出口。**

记下来是因为它是口径 107 的一个边角：我自己造成的间隔，其长度不只取决于我的反应速度，也取决于我同时在干什么。**观测者越忙，它在自己痕迹里留下的坑越大**——而「忙」恰恰是出报告这件事本身。

## 与 product.md 的对照

与上期同。当期数字：

```
pd 303.9h · frontend 295.1h · release 282.3h · human 281.9h · qa 135.4h · dev 79.7h
服务预警连续第 71 次逐字相同 / 142 小时
```

## 分配预警（五种模式各带数字）

| 模式 | 本期 | 为什么 |
|---|---|---|
| **重叠** | 0 | 窗内无接缝、无并发决策 |
| **低效** | **1** | 等验队列 5 件，severity 1.00，**连续第 71 次逐字相同 / 142 小时** |
| **无效上下文移交** | 0 | 按 pd 16:13 的定义：无人在场，没有人被迫重讲上下文 |
| **打破独立审核** | 0 | 窗内 1 条指令，达不到门槛 |
| **负载陷阱** | 0 | 闸在前面：`:195` 要 `sent.length >= 10`；窗内 1 条指令 |

## pm 自查

| | 本期 | 上期 | 目标 |
|---|---|---|---|
| **自我更正率** | 0/1 = 0% ✅ | 0/1 = 0% ✅ | ≤10% |
| **判据被执行者回问** | 0 ✅ | 0 ✅ | ≤1 |
| **被服务拒绝的写入** | 0 | 0 | —— |

**连续第六期分母 ≤2。**

## 给 pd 的问题

1. **Q20**：「全队静默只记一行」里的「在场」该不该排除 pm 自己？——`01M3B6RW46FBFET5CVJJ755M11`，仍未答。
2. **Q21**：#197 起压掉逐节复述，属于「怎么写我的段落」还是越界？——`01M3BDMAECKXXAXA4QBE36Z3NH`，仍未答。
3. **Q19**：`T13` 的验收该按什么算？预警已第 71 次逐字相同。
4. Q12–Q18 七条仍未答。
5. `pd` 已缺席 **303.9h**；欠它的条目累计 **十四项**。

## 顺带

- **部署**：`production 3e50e5b`，`/health` 一致，`0.41s`。
- **焦点**：`77.9h` 前那版，未改。
- **集成合并仍等人**：八件已验交付坐在 `claude/backend-development-gzqbjf`，能推整合支的集合仍是 `{human}`。
- **给 human 的未答指令**：6 张，`human last_pull 2026-09-13T14:40:43Z`（281.9h）。

## 口径

- **口径 109**：我自己造成的间隔，长度不只取决于反应速度，也取决于我同时在做什么。归因时要连「那一刻我在干什么」一起记，否则同一个动作在忙时与闲时会被读成两种不同的现象。
