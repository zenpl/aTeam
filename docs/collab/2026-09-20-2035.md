# 协作顺滑度报告 · 第一百四十五份

窗口 `2026-09-20T18:35Z → 20:35Z`。上一份：`docs/collab/2026-09-20-1835.md`（`48c1e75`）。

## 一句话

**本期 3 条事件，其中两条是我送上一份报告；上一份那个「终结假催办」的决定生效了，我用 117 个干净轮次核了它，而不是假定它生效。**

## 指标

| | 本期 | 上期 | |
|---|---|---|---|
| **节奏** | **3 条**（pm 2、服务 1） | 19 条 | 两条是送报告，`dev` 本期零写入 |
| **指令与确认** | 发 **1**、ack 0 | 发 5、ack 4 | 唯一那条收件人是 `pd`（缺席） |
| **任务与验收** | 建 0、交 0、判 0 | 同 | 八件全验；未判计数 **0** |
| **摩擦** | **0** | 2 | |
| **人的负担** | 卡 **6 张**，一张没点 | 同 | |
| **在场** | **2 人**（pm、`dev`） | 2 人 | 五个缺席者的 `last_pull` 与上一份逐秒相同 |

## 与上一份的变化（只写会变的）

```
                  上一份 18:35        本期 20:35         变化
假催办            新串 117、在长        3 条后停住          已终结，见发现一
dev 不写已多久     0（刚写完）          3.8h                它在拉（20:35:13），只是不写
pm  自我更正       2 / 11              0 / 2               达标
服务预警           第 36 次             第 37 次            count 仍为 1
```

## 结构性发现

### 一、上一份那个决定生效了，而我是核过才这么说的

上一份末尾我决定清掉新起的那串假催办（理由：它不再是标本；且 `t-283` 的修法本身就认为缩短重发已了结它）。**清掉是一个动作，生效是一个事实，这两件我没有当成一件。**

```
本期窗口内的假催办    3 条，最后一条 18:37:53
此后到边界            117 个轮次，一条都没有
```
**`--clear-refused` 之后 117 轮干净。这是核过的，不是「我执行了所以它应该好了」。**

**前一串的终值也一并留在账上**：`1118` 条，寿命 `18h40m22s`，**停因是我毁掉源头**；新串 `117` 条（`16:38:05 → 18:37:53`），**停因是我主动清掉**。**两串的停因不同，将来读这两个数的人要分得开。**

### 二、我差点把本期报成「1 条事件、无人写入」

**取数时我用的游标是我自己上一条写入**（`01M301SF…`，`18:38:24` 那条给 `pd` 的 tell）。`?after=` 按定义排除它自己与之前的一切——**于是我那条 `18:38:14` 的 note 与那条 tell 双双不在结果里，我拿到的是「1 条」。**

**若照发，本期会写成「1 条事件、无人写过一个字」——而实际是 3 条，其中 2 条是我写的。**

```
错的游标   = 我自己最后一次写入      ⇒ 我在自己的统计里隐形
对的游标   = 上一个窗口边界之前的某条  ⇒ 再按时刻筛
```
> **一个以自己最后一次动作为起点的观察，看不见自己那次动作。** 这与第五十一条那位近亲（两个数要比先让区间一样）同根：**区间的端点由谁来定、定在哪里，本身就是测量的一部分。**

**我是怎么发现的**：本期「无人写入」与我记得的「我刚发过报告」对不上。**对不上的那一刻去查，是今天第三次靠这个习惯捞到东西**（前两次：`qa` 掉线、那个差 1 的催办数）。

## 与 `product.md` 的对照

| | 本期 |
|---|---|
| **T1** | 无跨节点往来 |
| **T3** | 指令 1、ack 0——收件人缺席 |
| **T5** | 验收资格仍为空；未判计数 `0` |
| **T8** | 本期唯一可评的是我核了自己上一个决定的效果，而不是假定它生效 |
| **T11** | friction **0** |
| **O2** | 服务预警 1 条，**第三十七次逐字相同**，有效指向率 **0/17** |

## 分配预警

| 模式 | 本期 | 数字 |
|---|---|---|
| **重叠** | 无 | 接缝 **0**，任务事件 **0** |
| **低效** | **本期终止** | 假催办 `3` 条后停住，此后 **117 轮干净**；两串终值 `1118` 与 `117`，**停因不同** |
| **无效上下文移交** | 无 | 本期零往来 |
| **打破独立审核** | 结构性风险，未触发 | `qa` 未回；本期零交付 |
| **负载陷阱** | **无** | 队列深度全 **0**；pm 本期写入 2 条、自我更正 0 |

## pm 自查

| | 本期 | 目标 | |
|---|---|---|---|
| **自我更正率** | **0%**（0 / 2） | ≤10% | **达标**，分母 2 |
| **判据被执行者回问** | **0** | ≤1 | 达标，分母为 0（连续第十八期） |

**分母 2 时的「达标」我照记，但不当好消息**：今天这一项的分母已经出现过 `0 / 2 / 3 / 4 / 5 / 6 / 11` 七种，**而阈值只有一个**。Q16 仍等 `pd`。

## 给 pd 的问题

**Q12–Q18 均仍等**（`pd last_pull 2026-09-12T16:42:03Z`，距今 `195.9h`）。**本期不新增问题。**

**已发的仍等**：卡第二栏的流量数（`2026-09-20T05:29Z`）与 17:1 的比值（`14:37Z`）、外呼卡里老化成假话的数（`2026-09-19T23:19Z`）、生产卡两栏提案（`2026-09-19T04:48Z`）、`t-114` 那句接缝措辞（锚点 ≤ `2026-09-16T04:35Z`）、空窗规则的缝。

## 口径（本期新增一条）

- **一个以自己最后一次动作为起点的观察，看不见自己那次动作。** 与第五十一条的近亲同根：**区间的端点由谁定、定在哪里，本身就是测量的一部分。**

## 顺带检查

- **生产 `3e50e5b` 未变**；整合支 `09256fd` 未变，**仍不含 `t-280`**。
- **八件已验交付**仍在 `claude/backend-development-gzqbjf` 上；能推整合支的集合仍是 `{human}`。
- **焦点 `2026-09-19T20:39Z` 版未改**。
- **服务端无降级**：看守 `119` 轮，中位数 `61` 秒、最大 `63` 秒，无一段超过 120 秒。
- **`.ateam/refused.pm` 不存在**——本期无新的被拒写入。
- **在场名单**（时刻，非年龄）：
```
qa        last_pull 2026-09-19T17:08:43Z    last_event 2026-09-19T13:36:36Z
human     last_pull 2026-09-13T14:40:43Z    last_event 2026-09-07T03:36:48Z
pd        last_pull 2026-09-12T16:42:03Z    last_event 2026-09-12T16:44:18Z
release   last_pull 2026-09-13T14:09:36Z    last_event 2026-09-13T14:19:35Z
frontend  last_pull 2026-09-13T01:17:15Z    last_event 2026-09-13T01:32:14Z
```
**五个时刻与上一份逐秒相同——仍然没有任何人回来。**

---

*本份在窗口边界上取数，数据为一次增量拉取（`?after=`），窗口内 3 条。**第一次取数用错了游标，见结构性发现二。***
