# 协作顺滑度报告 2026-09-07 18:35 UTC（第十三份）

**窗口** 16:36 → 18:35。上一份 16:35（e1b2dcf）。正文数据截至 18:12，末尾补记 18:12–18:35。

---

## 一句话

**第一个客户今天开始搬家，而我们在准备他的过程中，发现了三条从没走过的路和一条我亲手拆掉的上限。**

human 17:19 说「主体开发 hold 一下，准备迎接第一个客户」，17:3x 补「是一个 S9 流程的客户」。**为客户做准备这件事本身，比过去两小时的任何一件开发都更能找出问题**：回滚从没走过（20 次上线 0 次回滚，第一次去走发现路没铺）、两条逃生路在生产上从没承载过人（untell 0 次、disown 0 次）、牌桌没有体积上限（生产 391KB，判据写的是 60KB）。**这三件都不是今天写坏的，是今天第一次去用才发现的。**

---

## 数字

| | 本期 | 怎么量的 |
|---|---|---|
| 事件 | 283 | `at ≥ 16:36` |
| 各人 | qa 97 · dev 80 · pm 65 · release 35 | 按 actor |
| 日志字节占比 | qa 37% · dev 24% · **pm 23%** · release 15% | 事件 JSON 字节数 |
| 新建任务 | **4 件**（t-220…t-223），全部 pm | 上期 9 件、上上期 12 件 |
| 判决 | 10 通过 / **5 不通过** | `op=verify` |
| 在生产表面判过 | 5 件 | `surface=production` |
| 交活时说得出对人的影响 | **14 / 14** | shows 12 + no_human_impact 2 |
| 上线 | 1 次（第 20 批 d57acbc），**之后生产冻结** | release 17:44 |
| 交付后追加判据 | **3 条**（t-216、t-215、t-070，全在 fail 之后） | 第 4 条 t-220 加在交活之前 |

---

## 四件请 pd 与 human 看的

### 一、我二十小时前拆掉了牌桌的体积上限，今天客户来了

t-070 判据 3 的字面是「默认 board 小于 60KB」。**我 21:32 用一条指令把它改成「默认板 < 完整板 12%」，没落成判据**——dev 照新口径做并如实写进证据，qa 照旧字面判。今天 qa 量：**生产瘦身板 391KB，超 6.5 倍**；不带客户端头那一路 5MB。

qa 给了最短的证明：**同一份生产数据，比例口径 7.78% 达标，绝对口径 391KB 不达标——一次测量两个相反结论。**

**那次更正本身是错的**：完整板含判据、证据、note 正文，随日志无限长；12% 就是无上限的 12%。**我把一个会逼出产品决定的绝对上限，换成了一个永远满足的比例**——而绝对上限之所以难，正因为它逼人回答「日志长到某个程度，人那一页必须不显示什么」。我避开了那个问题，代价是今天没有上限。

**给 pd**：那个「不显示什么」的决定归你。**给 human**：这直接决定客户第一天打开牌桌等多久。

### 二、三条从没走过的路，都是为客户做准备才发现的

| | 发现 | 谁 |
|---|---|---|
| **回滚** | 20 次上线、0 次回滚。真跑一遍：推新 sha 再推旧 sha ⇒ non-fast-forward 被拒。**不是配置，是 `release.ts` 的 Git 接口 fast-forward only** | release 17:25 |
| **untell / disown** | 全日志 6777 条里各 0 次。**而今天两次真的需要**：release 撤那张过期卡被一条假 ack 拦住；qa 03:36 那条误署 human 的 ack 本该被 disown——**误署成 human 的事件只有 human 能撤** | qa 16:55 |
| **牌桌算得动多长的日志** | 门槛 1 万–1.5 万条。1 万约 2.4 秒、2 万约 5.6 秒，**而这些是安静时的数**；六个自己人并发就能把 1.3 秒变成 12–47 秒。`reduce` 线性、**`board` 不是**（事件 ×4.4、board ×25） | release 17:22 |

**共同点**：三条都在 repo 上「验过」，**没有一条被真的走过**。所以我给 pd 提一条口径：**逃生路只在 repo 验过等于没验，要在生产上被真走一次。**

### 三、「一句话不管用」的四种形状

今天反复撞见，凑齐了：

1. **对最需要的人沉默** — t-211 对从不 fetch 的节点整句不说；t-212 的账漏掉「人点按钮」那条路。
2. **内容过期** — t-210 卡是快照；t-215 人写的卡；`unknownSpanReason` 写时为真、今天成了错的指路。
3. **永远出现且永远做不到** — t-211 那句提醒永远为假（历史里两条 `deployed.sha` 不是提交），**下一步就是被当噪音跳过**。
4. **送到了但在没人读的位置** — `basis` 行印着测量时刻，而人读的是旁边那个 `0`。

**四种都不是话写错了，错在话与读它的人之间的关系。**

### 四、工具第一次互相取证

我报「我那两条 `criteria moved` 被拒了」，**qa 没信我，去 t-212 那本刚上线五分钟的拒绝账里换源核到 `rule=criteria n=2 who=pm`**。昨天那两次拒绝只会活在我的终端里。

这也顺带证了一件反例：**t-166 上线后 0 次使用，不是没人用，是用不了**——`rules.ts:521` 把 `moved` 和 `add` 一起挡了，而它要标的正是已验任务。**「验过」与「用过」是两个词**，今天两件同日给出了两个方向的样本。

---

## 生产冻结（release 17:44，我认）

理由不是谨慎，是**可归因**：导入是一次批量写，中途出事时如果我们同时换了代码，没人分得清是导入的问题还是这一版的问题。**一次只动一个变量。**

**解冻条件我改成可核的**：核对卡已答（有 id，日志里看得见），或 human／pd 明说。「搬家告一段落」不作数——没人说得出它何时成立。

**一个已知冲突**：t-215 已验未上线而冻结挡着它，**Q25 那张卡 09-08 00:28:59 会按默认 A 结掉，而 A 说的「明早开工时」已经过去**。我不建议为它破冻，也不建议让它按假默认结掉——**最省事且最正当的是人答一下那张卡**（本来就是发给他的）。已请 release 转达。

## 观察客户项目的边界（human 许可「diagnose 和观察，不要动手，除非真的卡住」）

release 的边界我一个字没改，**这句该被引用**：「许可的是看，不是碰，而这两件事之间的距离，在『想帮忙』的时候只有一步。」四条「卡住」判法我改三处，理由都是同一问——**这条我们看得见吗**：① 批量导入本来就安静，加「且牌桌的数没动」；② **「同一条写入被拒三次」我们从外面看不见**（拒绝不落成事件），不删但明写「无法从外部观察」；③ 补第五条：**人自己说卡住了**。

---

## 待补验收：一个数拆成四行（qa 17:46/17:55）

我前两份报告用的 117 作废。真数按四行记：

| | 件数 | 说明 |
|---|---|---|
| **待补（真有生产表面）** | 约 46 | **按「声明的触点」筛出来的，而触点今天被证明会陈旧**（t-034 声明 html.ts 却一个文件没改）。**更可能偏大，但方向我没量，不替它说** |
| 无生产表面 | 77（命令行 41 + 构建期 36） | 不是欠账，是我 15:15 裁的第三种判决形状 |
| **有表面但状态不对** | 1 已确认（t-079），5 待逐件判 | qa 找到的第四种：有表面，但要观察它必须先改生产状态——**冻结期不为验一件事去改生产** |
| 未上线 | 3 + 证据无效 2 | t-150／t-183 从「量不出」变成「证据无效」，unmeasured 归 0 |

---

## pm 自查

| 指标 | 目标 | 本期 | 达标 |
|---|---|---|---|
| 自我更正率 | ≤10% | **9 / 65 ≈ 13.8%** | **不达标** |
| 判据被执行者回问次数 | ≤1 | **4** | **不达标** |

**四次回问，全部是我的判据有问题**：t-216 判据 3 与 5 互相矛盾（我要一个机制做不到的区分）；t-215 判据 6 我拿钟点过期的卡当事实过期的样本；t-070 判据 3 我改了口径没落成判据；t-220 判据 1 我把范围说小了（`disowned` 在 server/src 与 cli/src 里 0 次，那是一整块从没有过的显示，不是补一句话）。

**九次自我更正里有三次值得单说**：① 我 17:11 夸「区间口径比单点 sha 对」，**同一分钟 release 正在报它误判 13 条**；② 我 17:22 一边说「不开新的」一边把回滚排进必做，**自相矛盾，三个人闲着**，17:43 改成可核的一句「客户进门之后就做不起了的现在做」；③ 我 17:08 `git show` 得 0 差点断言那个提交里没有 premise——**本地没那个对象而已，正是 t-219 三种「说不出」的第三种，我自己刚写完那条判据。**

---

## 口径

窗口 `16:36 ≤ at < 18:35`，数据截至 18:12。手数的地方逐条列出可复核。**本期新立／采纳三条**：改口径必须当场落成判据（我的硬规矩，t-070 的账）；筛法是性质的近似、近似不是性质（qa 自驳时说的，同样适用于我那个 46）；验页面前先核 http 码、字节数、开头是不是 doctype（`GET /` 默认是 7KB 说明书不是牌桌）。

---

## 补记 18:12 → 18:35

**一、拒绝账交出了第一份真数（t-212 判据 5，qa 18:26 落生产判决）**：上线头一小时 **total=10**，减掉 qa 自己那 1 条探针，**组织自发 9 条**——instruction 4／ack 2／criteria 2／done 2。**那 2 条 criteria 就是我的**，也就是 t-166 那个洞的独立证据：**一件工具上线一小时内替另一件作了证**。这个数比「它在记」有力得多，而昨天这 9 条会全部只活在各人终端里。

**二、Q25 的处置提前写好了，不等到点再发明**（还剩约 6 小时）。会发生的是：00:28:59 那张卡按默认 A 结掉，而 A 说的「明早开工时」是 09-07 早上。拦不住的原因是 `premise` 字段不在生产（t-215 已验未上线），而生产冻结的理由正当——**我不为它破冻**。最省事且最正当的仍是人答一下那张卡（已转达一次，**不再重复催**：重复催就是把一张卡变成三条噪音）。

**若它真的落了那条事件，照 pd 10:40 的通则执行，谁在场谁做**：不算数 → 恢复待答 → 期限从人再看到那刻重算 → 卡上落 note 说明默认支指向一个已过去的时刻 → **不许悄悄改回**。

**三、qa 把自己从事后验收里摘出来，我补了一句免得它成死结**：它提出「若那半是我做的，事后就不该由我验」——对，与它今天在 t-216 上的做法同一个理由。**但那半是补救不是任务判决，没有判据要判，所以不需要验收资格**：任何在场角色读日志核那四件事、署名记一笔即可，不必发 `verify`。既不自证，也不用推给不在的人。**说给人听的那一句仍归 pd，谁都不代拟。**

**四、本期最后的在场情况**：pd 缺席 409 分钟（14 条没送到）、frontend 295 分钟；human 在读（最近一次拉取距今 13 分钟）**但今天 0 条事件**——五张卡一张没答，其中两张我们已知是旧的（Q25 的默认支过期、release 那张「37 件已验的没上线」早已失效）。**这两张此刻都改不动**：untell 被一条假 ack 拦住（t-216 已验未上线），premise 不在生产。**我没有再加一张卡去解释它们**，理由与 16:07 拒绝补取消卡时相同——那是拿两张旧卡换三张卡，正撞 O2。
