# 协作顺滑度报告 · 第一百九十三份

窗口 `2026-09-24T18:35Z → 20:35Z`。上一份：`docs/collab/2026-09-24-1835.md`（`8042c9d`）。

## 一句话

**那行「`>90s`」第一次不空了——而它抓到的是我自己：170 秒的缺口，是我把看守挂死的那两分钟；同一窗里服务的两次真实拉取失败，仍然一次都没进那行。**

## 指标

| | 本期 | 上期 |
|---|---|---|
| **节奏** | **6 条**（pm 5、服务 1） | 3 条 |
| **指令与确认** | 发 2（都给 `pd`）、ack 0（收件人缺席 291.8h） | 发 1、ack 0 |
| **任务与验收** | **建 0、交 0、判 0——已连续 `135h01m`** | 已连续 `133h01m` |
| **摩擦** | 0 条 `friction:`（口径 100 是我自己的挂法出错，不是 `ateam` 的摩擦，不冠前缀） | 0 |
| **人的负担** | 卡 6 张，一张没点 | 同 |
| **在场** | **1 人**（只剩 pm） | 1 人 |
| **本期发现关于谁** | **向内 1、向外 1** ← 连续九期纯向内的链断了 | 向内 |

## 与上一份的变化

```
任务停了多久      133h01m  →  135h01m       锚点 2026-09-19T05:34:01Z
除我之外的写入者   0  →  0                   窗内写入者 = {pm, ateam}，其余五角色 0
看守（本窗）      118 轮/最大 75s  →  115 轮/最大 170s（距阈值 +80s）
本窗真实故障       2 次  →  3 次              我的看守断档 1、拉取失败 2
今日拉取失败       5  →  7                   20:31:04、20:32:05 连着两次
重启通知的账      10 次里 1 次真  →  同        本窗零通知；btime 未动，容器已跑 55.8h
不明缺口的率       3576 轮 1 次  →  3691 轮 1 次
痕迹合计          17037  →  17153            差 116 = 窗内 115 + 边界那条 ✓
服务预警          连续相同 64  →  65 条       跨 130 小时（5.4 天）
```

## 结构性发现

### 一、向外：分配预警「低效」冻结 130 小时的机制，已完整复现（口径 101）

低效模式里有两个候选，**一个有窗，一个没有**：

```
ack 延迟候选     allocation.ts:126  instructions 先按 inWindow 过滤，窗 = ALLOCATION_WINDOW_MS = 4 小时（:56）
等验队列候选     allocation.ts:149  直接扫全表 status === "done"，无任何时间窗
:151             worst(ineff) 只留严重度最高的一个
```

日志一静，有窗的候选消失，无窗的候选留下并**永久锁死**。模式不会沉默，它**退化成自己唯一那个不看时间的成员**——而牌桌上看起来跟「分配器仍在盯着」一模一样。

离线重算，与服务两个时点实际发射的正文**逐字对上**：

```
09-19T09:20   窗内 9 条指令   dev n=3 p90=0.4分、qa n=3 p90=35.4分
              → ack 延迟触发，severity 2.36，胜过等验队列的 1.00
              服务当时发的正是「qa 的 ack 延迟 p90 35 分钟，其他角色中位数 0 分钟」
09-19T11:20   窗内剩 2 条     slow 数 0 < 2 ⇒ 候选不再生成
              → 等验队列（severity 1.00）接管，此后 65 次发射逐字未变
```

两个顺带的硬数字：**全表 223 次发射、64 种不同正文**——前 158 次里它变了 63 回，平均每 2.5 次一变。**它不是设计成一行不动的，它是卡住了。** 发射间隔稳定 2 小时。

### 二、向内：那行 `>90s` 第一次抓到东西，抓到的是我的仪器

```
18:39:15 → 18:42:05    170s   距阈值 +80s   ← 我把看守挂死的两分钟
19:11:47 → 19:12:13     26s                 ← 我重挂（新周期立刻起，压掉一个间隔）
19:41:55 → 19:42:19     24s                 ← 同
20:11:57 → 20:12:28     31s                 ← 同
```

**本窗四个离群间隔，四个都是我这台看守自己的生命周期造成的，没有一个来自服务。** 采样仪器自身的起停，主宰了它自己的离群分布。

断档的成因：`newtells.sh` 第 29–31 行自己就用 fd 9 对 `/tmp/pm-cli/watch.lock` 自锁；我在外面又套了一层 `flock -n` **同一个锁文件**，外层持锁 ⇒ 脚本自检判定「已有看守在跑」⇒ 立刻退出。全队唯一在听的节点空了约 2 分钟。

**它的外观是 `completed`、`stream ended`，与正常收尾同形**，只有正文第一行能区分。口径 100 因此加了一步：每次重挂后必须读一次 output 首行确认不是 `WATCH-ERR`，不能只看 Monitor 起没起来。本窗其后三次重挂都跑了这一步，都通过，处理耗时 7s／8s／8s。

### 三、口径 98 今日全账：失败可见与否，只取决于它拖了下一次采样多久

```
失败时刻                所在周期间隔   距门槛(90s)   可见?
2026-09-24T13:58:33Z      105s          +15s        可见
2026-09-24T14:15:43Z       61s          -29s        不可见
2026-09-24T16:11:30Z       92s           +2s        可见
2026-09-24T16:31:30Z       62s          -28s        不可见
2026-09-24T17:53:50Z       62s          -28s        不可见
2026-09-24T20:31:04Z       61s          -29s        不可见
2026-09-24T20:32:05Z       61s          -29s        不可见
                                    今日 7 次：可见 2、不可见 5
```

本窗那两次是**连着的两轮**（相隔 61 秒），拉取失败而 `/health` 在 7 秒后 200、0.23s，`/log?after=` 200、0.40s——**服务没有倒**。两次都把延迟加在 `get` 上，而痕迹的时间戳写在每轮**开头**，所以延迟落不进间隔：这正是口径 98 说的那件事，同一类故障可见与否与它的类别无关。

`watcherr.log` 至今 23 条，**全部是 `fetch-fail`，一条 `parse-fail` 都没有**。也就是说服务从没返回过「200 但不是 JSON」。

## 与 product.md 的对照

| | 本期证据 |
|---|---|
| **T13**（职责分配不合理时有预警） | **第一次有机制级证据说它没在服务**：声称五种模式，实际 130 小时只发得出一种，且那一种是它唯一不看时间的成员。见发现一。 |
| **T7**（知道谁在听、谁缺） | 机制在工作，报出的内容是「没有人」：`COVERAGE` 列出 R1/R2/R3/R5/R6/R9/R11/R12 **八项职责无人**。 |
| **T10**（不被自己淹没） | **此刻反了**：牌桌把同一条冻结正文显示 6 次（1 条活的 + 5 条被取代的），逐字相同。唯一在动的行，是重复最多的行。 |
| **O2**（牌桌上每件事都真的需要他） | `NEEDS HUMAN` 6 条，其中 5 条是服务问「起一个 X？」，已挂 270–292 小时无人答。 |
| **M4**（被拒绝时说出规则名） | 本期 0 次拒绝，无新证据。 |

## 分配预警（五种模式各带数字）

| 模式 | 本期 | 为什么 |
|---|---|---|
| **重叠** | 0 | 窗内无接缝、无并发决策 |
| **低效** | **1** | 等验队列 5 件（`t-025`、`t-094`、`t-133`、`t-219`、`t-226`），severity 1.00，冻结 65 次 / 130h |
| **无效上下文移交** | 0 | 窗内 2 条指令，达不到任何门槛 |
| **打破独立审核** | 0 | 同上 |
| **负载陷阱** | 0 | **不是没触发条件，是闸在前面**：`:195` 要求 `sent.length >= 10`、`:202` 要求 `c.sent >= 10`。窗内 2 条指令 100% 给 `pd`，份额 1.0 远超 `:199` 的 0.5，但连闸都到不了。 |

读完 `:190-206` 补一条：**每个 runtime 模式都有一个基于 4 小时窗的最小量闸**。唯一例外是等验队列——它既不看时间，又只需要一个**永不下降**的计数。所以整个 runtime 分配器在每 4 小时不足约 10 条指令时全体沉默，而活下来的恰好是那个时间盲的。

## pm 自查

| | 本期 | 目标 |
|---|---|---|
| **自我更正率** | **1/2 = 50%** | ≤10% |
| **判据被执行者回问** | 0 | ≤1 |

发的 2 条指令都给 `pd`，其中第二条（`01M3ADYT097WK9P0QYRJ4FGS6R`）是**撤回我自己给 pd 的上一条更正**。超标 5 倍，如实记。

**今天第二次「更正本身有缺陷」**（第一次是口径 96 的带宽按秩定义）。这次的形状是：`#162` 我说冻结是因为「最近 4 小时」窗跑干了；我读到 `:149` 无窗后发更正说「根本没有窗」，并把原说法判为**错**。现在看，两句**各对一半**——有窗的那半说的是 ack 候选，无窗的那半说的是等验候选，而冻结恰恰是这两件事**接力**造成的。**我把一个不完整的说法判成了错的，用的是另一个同样不完整的事实。**

成因是可复述的：`:138` 读起来像「无窗」，因为窗绑定在 `:126`，不在使用处。**只读使用那一行、不读它的绑定**——与当初 `tail -3` 当全文是同一个毛病。

还有一次**没出手的**：本轮我的第一直觉是「等验队列把其他模式挤掉了」，读 `:212-219` 发现 `allocation()` 对五种模式各留一条、有几条报几条，`count=1` 的意思是另外四种确实什么都没算出来。这一条在发出去之前作废了，记在这里是因为**在途作废与已发更正的比例，本身是个该看的数**，而我至今没在量它。

## 给 pd 的问题

1. **Q19（新）**：`T13` 的验收该按什么算？此刻的实现满足「有预警」的字面，但 130 小时只发得出一种、且是唯一时间盲的那种。是「预警机制存在」就算达成，还是要求「五种模式各自可被触发过至少一次」？——这会决定它是一条已交付的需求，还是一条待修的缺陷。
2. Q12–Q18 七条仍未答。
3. `pd` 已缺席 **291.8h**，`last_pull 2026-09-12T16:42:03Z`；欠它的条目累计 **十项**（本期新增口径 101 一项）。

## 顺带

- **部署**：`production 3e50e5b`，`GET /health` 报 `3e50e5bb79cc9d2fb0419f790d6e028092de9416`，一致。qa 上次核对在 140.4h 前。
- **焦点**：`65.9h` 前那版，未改。其 ⑤ 条（「我是唯一在听的节点，若我的看守再失效一次，队里就是零」）**本期实测发生了一次**，持续约 2 分钟，由我自己造成。
- **集成合并仍等人**：八件已验交付坐在 `claude/backend-development-gzqbjf`，能推整合支的集合仍是 `{human}`。
- **给 human 的未答指令**：6 张，`human last_pull 2026-09-13T14:40:43Z`（269.9h）。其中 `pm: 能给一个 https 的外呼地址吗？` 仍无回音。

## 口径

- **口径 100**：看守重挂必须裸跑 `./newtells.sh`；套同锁的 `flock` 会让它自检退出。重挂后读 output 首行确认无 `WATCH-ERR`，不能只看 Monitor 起没起来。
- **口径 101**：低效模式两个候选，ack 延迟有 4 小时窗（`:126`），等验队列无窗（`:149`），冻结是两者接力。更正一个说法之前，先确认新证据覆盖的是不是同一个对象；读一行的语义要连它的绑定一起读。
