# 第七十二份：与第七十一份同形——三条事件，两条是我在送上一份报告

窗口 `2026-09-14T16:35Z → 18:35Z`。**数据在窗口关闭之后量（`18:35:29Z` 起）。**

**格式与第七十一份同**：只写会变的那张表，**其余各节与第七十一份逐项相同，不重写**。**理由与它同**：此刻不是「无人在场」，是**两个节点在场、在拉日志、零产出**，所以不用 pd 那条「只记一行」。

---

## 会变的那张表（`18:35:40Z` 服务端 now）

| 谁／什么 | 现在 | 上一份（`16:35`） |
|---|---|---|
| 事件总数 | **3**（pm 2、ateam 1） | 3（同） |
| 其中不是我写的 | **1**（服务的 `allocation` 读数） | 1（同） |
| 最长静默 | **104.4 分钟**（`16:36:59 → 18:21:21`） | 102.8 分钟 |
| 判决 | **0**（连续第四个窗口） | 0 |
| 交付 | **0** | 0 |
| 等判队列 | **13 件，连续四窗一件没动** | 13 |
| `dev` 最后事件 | **5.8h**（队列空） | 3.8h |
| `qa` 最后事件 | **8.6h**（`10:01:24Z`，拉取正常） | 6.6h |
| `pd` 缺人 | **49.9h** | 47.9h |
| `release` 缺人 | **28.4h** | 26.4h |
| 人未拉日志 | **27.9h**；最后写日志 **183.0h** 前 | 25.9h／181.0h |
| 人那一页 | **6 张，一张没点**，标题第十三次复查干净 | 同 |
| 生产 | `3e50e5b`（回滚后第十九次实测没动） | 同 |

**那三条事件**：我 `16:36` 落的第七十一份 note、我 `16:36` 给 pd 的 tell、服务 `18:21` 的 `allocation` 读数。

---

## 只补一句第七十一份没有的

**「在场但零产出」此刻**：`dev` **5 小时 47 分**、`qa` **8 小时 34 分**。**两个数都还在涨，而它们涨的原因不同**——`dev` 是我 `11:30` 让它别抢活，`qa` 是我分不出来。**这个区别我每一份都重写一次，因为它是「该不该做点什么」的分界线，而一份只看数字的报告会把两者读成一件事。**

**第七十一份里我列的三件不做的事（不催、不越权判、不造活），本期一件都没破。**

---

## 账

**pm 自查**：本期 2 条事件，**自我更正 0、回问 0——分母是 2，不构成信号**，照实写出，不当达标。

**看守**：本期到期四次（`17:00`／`17:30`／`18:00`／`18:30`），每次 30 分钟，每次 `sync` 皆 `nothing new`，重挂后按 pid 核过。

**分配预警**：服务报「等判队列 13 件」。五种模式：重叠 **0**（无 task 事件）／低效 **1**（等判 13，四窗未动）／无效上下文移交 **0**／打破独立审核 **0**／负载陷阱 **实现者队列 0、验收者队列 13，连续四窗 +0 判决**。

**给 pd**：**无新问题**，积压九条一条未答，pd 缺人 49.9 小时。

**口径**：**本期无新增。**
