# 协作顺滑度报告 2026-09-11 08:35 UTC（第三十一份）

**截止时刻：2026-09-11T07:32:38.342Z（快照末条 `01M27P3YT629WGEN86NTFF50GT`，是服务自己写的一条 `project:allocation`）。** 窗口 `06:35:00Z ≤ at < 08:35:00Z`。**末条事件距窗口结束 62 分钟**，其中最后一条人写的事件在 07:19。口径见文末。

## 一句话结论

**这一窗只做了一件事，而那件事值得：我因为报告里一句自我批评而改了动作，第一次去列「我到底欠了多少 ack」——46 条，最老三天半，其中两条是宣布生产冻结和观察边界的那两条。查完之后 qa 两次更正我、我又自己抓出两次，本窗我的 10 条实质发言里 4 条在更正自己。**

## 指标

| | 本期（06:35→08:35） | 上一期（04:35→06:35） |
|---|---|---|
| 事件数 | **18** | 17 |
| 实际活动 | 06:40:13 → 07:19（**39 分钟**，人写的部分），服务 07:32 又写了一条；**此后 62 分钟静默** | 54.4 分钟 |
| 说话的人 | pm 12、qa 5、ateam 1 | pm 10、qa 6、ateam 1 |
| 指令 | 5（pm→qa 3、qa→pm 2） | 6 |
| ack | 4 | 6 |
| note | 4（`--decision` 1） | 5 |
| reading | 5（**pm 写了 4 条，其中 3 条是同一个键的三个版本**） | 1 |
| **task 事件** | **0（连续第四窗）** | 0 |
| 被拒 | 累计 26（**本期 0**） | 累计 26 |
| 给 human 的卡 | 5 | 5 |
| 生产 | `d57acbce`，**已 87 小时未换** | 85 小时 |
| 外呼 | `misconfigured`（**已 105 小时**） | 同 |

## 与上一份的变化

1. **本期被拒 0 条**——上一期我第三次 `ack` 拿错 id 被拒，本期我改了动作（写了个列未 ack 指令的脚本），**没再拿错过**。
2. **`project:ack.owed.pm = 46` 是新的一条事实**，牌桌上以前没有这个数。
3. **在场仍是两个**：pm、qa listening；pd、dev、frontend、release、human 全部 missing。**qa 自 06:51 起静默 105 分钟。**

## 结构性发现：「办了没有」，三处各给一个答案，没有一处对得上

本窗查 ack 欠账时，撞出了三个数，**它们都在回答同一个问题，而三个数互不相等**：

| 问的是什么 | 谁在答 | 此刻的数 |
|---|---|---|
| 「谁欠我一个回执」 | 真正没有 ack 事件指向它的指令 | **47**（pm 46 ＋ qa 1） |
| 「谁读到了还没动」 | `owedTo`（`d57acbc` `board.ts:1163-1169`，筛 `reach !== "acted"`） | **830**，且 pm 与 qa **合成一个数** |
| 「这件晚了」 | `board.overdue`（t-147 重定义为「读到了、带选项、到期仍没答案」，而带选项只能给 human） | **0** |

**差 17.6 倍的那一对（830 对 47）尤其要紧**：我在场、每轮 `sync`、欠着 46 条过期三天半的回执，**牌桌确实说了话，说的是另一件事，而且没说是我**。

**第三个数为什么是 0，是按设计的**：`overdue` 现在是人的决策卡清单，不是「谁欠 ack」的清单。**所以逾期升级只对 human 做了，对在场的角色没有。** 这直接打在 **T3**「必须 ack、**逾期升级**」上。

**同一天第三次撞在同一个洞上**（前两次见第二十九份补章）：
1. 读数不带 `refs`（0/283，因为 `reading` 那一行的选项清单里不列 `--refs`）；
2. **办完一件事的动作是读数或 `git push` 时，牌桌看不出它办了**——本窗真实发生了一次：我办完 qa 06:49 那条，牌桌仍对我说「你读过还没动的有 1 条」，因为 `reach → acted` 要有事件 `--refs` 指着它，而**读数与 push 都不带 refs**；
3. 而牌桌催我的那句话，数的是第三种东西。

**这是外部报告 F5（「指令回执只认 refs」）在我们自己身上的一次**，形状比外部那次难看：**外部是节点不知道要 refs；我是知道、当天引用过十几次，只是办这一件时手上做的是读数和 push。**

**要指给人看需要什么**：三个数同时可读即可。**此刻成立。常驻。**

## 与 `docs/product.md` 的对照

| | 本期证据 | 条件 |
|---|---|---|
| **T3**「送到了、被看见了、延迟可见；必须 ack、**逾期升级**」 | **本窗的主发现，反例**：47 条真正欠 ack 的回执，无一处升级给欠账的本人 | 常驻 |
| **T5**「三方分离」 | 仍在起作用：qa 本窗两次更正我，两次我全收 | 已成事实 |
| **T6**「决定不反复，改了能追到」 | **正面**：`ack.owed.all` 三个版本（146→137→130）在牌桌上并排躺着，前两条 `valid: false` 且各自写明为什么被取代。**台账机制是有效的，哪怕被取代的是我自己两分钟前写的数** | 已成事实 |
| **T8**「全队一起死时人在一小时内知道」 | **仍 FAILED**，外呼 105 小时 misconfigured | 常驻 |
| **M4**「被拒时说出规则名」 | **本期 0 次被拒**。上一期那次（第三次拿错 id）之后我改了动作 | 已成事实 |

## 分配预警（五种模式，各带数字）

| 模式 | 本期 |
|---|---|
| 重叠 | **0** |
| 打破独立审核 | **0** |
| 负载陷阱 | **0**。本窗 5 条指令，pm 收到 2（40%），未触发 |
| **低效** | **1，与上一期同一条，未变**：服务算 qa 的 ack 延迟 p90 **22** 分钟（取法 `floor(n*0.9)`；按 `int(0.9*(n-1))` 是 6.9）。**成因仍是 05:02 与 05:32 那两条——它当时在逐秒核我的表。本窗 qa 只 ack 了 2 条，都在分钟级** |
| 无效上下文移交 | **0** |

## pm 自查

- **自我更正率**：本窗我的实质发言 **10 条**（读数 4＋note 3＋指令 3；ack 不计），其中 **4 条是对我自己先前说法的更正** → **40%**。阈值 ≤10%，**严重超标，连续第二窗**。
  四条是：① 把 human 算进「欠 ack」表（人答卡用 `decide` 不用 `ack`，是用错动词，qa 06:49 指出）；② 把 `46+41+35+5+3` 加成 **137**，正确 **130**，**我自己抓的**；③「对在场的角色一句话都没有」不成立，实际是「有一句，但它数的是另一件事」（qa 06:50 指出）；④ 第三十份里我写「一条写进报告的自我批评没有改变我的动作」——**这一次它改变了**。
  **我不修饰这个数字，但要说清它的形状**：本窗我几乎没写新判断，写的是一条新事实（46）和对它的四次修正。**一条从没人数过的数，第一次数出来要改四遍，这件事本身就是那条发现的证据。**
- **判据被执行者回问**：**0**（连续第四窗 0 条 task 事件）。
- **qa 对我的回推 2 次，2 次全收**；**我自己抓出 2 次**——**这是这几窗里第一次，更正不全是被按出来的。**
- **一笔要记的**：②那条加错发生在一条**讲「数要能复算」的读数**里。**我把方法写进了读数，却没把加法算对。**

## 给 pd 的问题

无新增。**前六条仍未答**（pd 此刻 30+ 条未读，且从 09-07 起没 `sync` 过）。第六条最新：「起一个 X？」这张卡对一个「写了但从不拉取」的角色该说什么。

## 顺带检查

- **部署队列**：t-215、t-216、t-222、t-223 仍堵在叫停后面。**生产 87 小时未换。**
- **焦点**：未改。本窗没有足以改变排序的新事实——**46 条欠 ack 是一笔账，不是一件要现在做的事**。
- **给 human 的未答指令**：5 张，最老一张 100 小时。**其中 4 张是服务自动生成的「起一个 X？」，唯一实事（换外呼地址）排第 5。**
- **解冻**：**human 09-07 的叫停要他自己解除。**

## 口径

- 截止时刻由数据自证。**本份末条是服务写的读数，人写的最后一条在 07:19**，两者都写出来，因为「最后一条事件」与「最后一次有人说话」不是同一件事。
- 每条发现写「要指给人看需要什么条件」，分常驻／瞬时。
- 行号一律带树名（本份全部 `d57acbc`）。
- **百分位写取法**；**相加的数要能复算**（本份的 130 = 46+41+35+5+3，已用 python 核过——上一份我就是在这里加错的）。
- 「欠 ack」的算法：`to=X` 且不存在 `actor=X` 且 `of` 指向它的 ack；`decides` 算已处理；`untell` 的排除；**human 不计**（他用 `decide`）。
