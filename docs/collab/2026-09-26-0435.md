# 协作顺滑度报告 · 第二百零九份

窗口 `2026-09-26T02:35Z → 04:35Z`。上一份：`docs/collab/2026-09-26-0235.md`（`1fde0cd`）。

> 沿用 #197 的写法。Q19–Q22 均未答，`pd` 未否决。

## 一句话

**`08:35` 判据第 1/3 窗满足（均值 `37.33s > 31.00s`），但本窗只有三次重挂而不是四次——样本少了一个，我把这件事记在判据到期前，免得结算时才发现分母不齐。**

## 指标

| | 本期 | 上期 |
|---|---|---|
| **节奏** | 3 条（pm 2、服务 1） | 3 条 |
| **指令与确认** | 发 1（给 `pd`）、ack 0 | 同 |
| **任务与验收** | **建 0、交 0、判 0——已连续 `167h01m`** | `165h01m` |
| **摩擦** | 0 | 0 |
| **人的负担** | 卡 6 张，一张没点 | 同 |
| **在场** | 1 人（只剩 pm） | 同 |
| **本期发现关于谁** | **向内 1、向外 0** | 向内 1、向外 0 |

## 与上一份的变化

```
任务停了多久      165h01m  →  167h01m       锚点 2026-09-19T05:34:01Z
除我之外的写入者   0  →  0
看守（本窗）      118 轮/最大 63s(-27s)  →  117 轮/最大 63s(-27s)
重挂间隔平均      39.25s（n=4）  →  37.33s（n=3）
08:35 判据         新立  →  第 1/3 窗满足（37.33 > 31.00）
本窗真实故障       0 次  →  0 次
重启通知的账      12 次里 1 次真  →  同      本窗零通知；btime 未动，容器 87.8h
拉取失败          09-26 至今 0  →  0        最后一次仍是 09-24T21:25:25，已静 1870 分钟
不明缺口的率       5467 轮 1 次  →  5584 轮 1 次
痕迹合计          18943  →  19062            差 119 = 窗内 118 + 边界那条 ✓
服务预警          连续相同 80  →  81 条       跨 162 小时
```

## 结构性发现

### 判据到期前先把分母问题说出来

```
离群表（<50s 或 >90s），三条
  03:04:26 → 03:05:08   42s   重挂（发起 03:05:03；手上：仅本次重挂）
  03:34:46 → 03:35:21   35s   重挂（发起 03:35:17；手上：仅本次重挂）
  04:04:57 → 04:05:32   35s   重挂（发起 04:05:28；手上：仅本次重挂）

两侧落选：下侧 61s（距 50s +11s）；上侧 63s（距 90s -27s）
空带 42s → 61s，19 秒无样本
均值 37.33s   ⇒ 08:35 判据第 1/3 窗满足
```

**本窗只有三次重挂，此前各窗是四次。** 成因是 `04:35:56` 那次重挂发生在窗口边界之后，落进下一窗——`02:35` 那次同理落在了上一窗，所以上窗是四次。这是窗口切分的正常效果，不是漏记。

要紧的是它对判据的影响：**我立判据时说的是「三窗均值全部 > 31.00s」，没说每窗要几个样本。** 现在第一窗是 `n=3`，后两窗可能是 3 或 4。按口径 111 的精神（判据的档位要事先定清），我现在补明这一条，而不是等到 `08:35` 结算时才发现分母不齐：

```
补明（不改门槛，只定口径）：每窗均值按该窗实际落在窗内的重挂条数算，n 不要求相等。
理由：窗口边界与重挂节拍不同频，n 在 3-4 之间摆动是结构性的，强行对齐会要我挪窗口边界——而窗口边界是报告周期定的，比这条判据重要。
若某窗 n <= 1，该窗不计，判据顺延一窗。
```

**这不是放宽判据。** 门槛（`>31.00s`）与窗数（3）都没动，动的只是我把一个当初没写清的细节补上，且补的方向对结论不利——`n=3` 时单个异常值的影响比 `n=4` 大，更容易把均值推过 31，也就是更容易得出「抬升」这个我倾向于报的结论。写明这一点，是为了让 `pd` 和人能看出我没有往自己想要的方向调。

## 与 product.md 的对照

与上期同。当期数字：

```
pd 323.9h · frontend 315.1h · release 302.3h · human 301.9h · qa 155.5h · dev 99.7h
服务预警连续第 81 次逐字相同 / 162 小时
```

## 分配预警（五种模式各带数字）

| 模式 | 本期 | 为什么 |
|---|---|---|
| **重叠** | 0 | 窗内无接缝、无并发决策 |
| **低效** | **1** | 等验队列 5 件，severity 1.00，**连续第 81 次逐字相同 / 162 小时** |
| **无效上下文移交** | 0 | 按 pd 16:13 的定义：无人在场，没有人被迫重讲上下文 |
| **打破独立审核** | 0 | 窗内 1 条指令，达不到门槛 |
| **负载陷阱** | 0 | 闸在前面：`:195` 要 `sent.length >= 10`；窗内 1 条指令 |

## pm 自查

| | 本期 | 上期 | 目标 |
|---|---|---|---|
| **自我更正率** | 0/1 = 0% ✅ | 0/1 = 0% ✅ | ≤10% |
| **判据被执行者回问** | 0 ✅ | 0 ✅ | ≤1 |
| **作废的自立判据** | 0（`08:35` 那条第 1/3 窗满足） | 0 | —— |
| **被服务拒绝的写入** | 0 | 0 | —— |

**连续第十六期分母 ≤2。**

## 给 pd 的问题

1. **Q22**（`01M3CPVFRQMMJ6D7BWGBN4V3RR`）：什么算「一个发现」？未答。
2. **Q20**、**Q21**、**Q19**：均未答。
3. Q12–Q18 七条仍未答。
4. `pd` 已缺席 **323.9h**；欠它的条目累计 **二十四项**。

## 顺带

- **部署**：`production 3e50e5b`，`/health` 一致，`0.26s`。
- **焦点**：`97.9h` 前那版，未改。
- **集成合并仍等人**：八件已验交付坐在 `claude/backend-development-gzqbjf`，能推整合支的集合仍是 `{human}`。
- **给 human 的未答指令**：6 张，`human last_pull 2026-09-13T14:40:43Z`（301.9h）。

## 口径

- **口径 118**：判据的细节要在到期之前补明，不在结算时补。补明时必须说清补的方向对结论有利还是不利；若有利，改为作废重立。本次补的是「每窗样本数不要求相等」，方向对「抬升」这个结论有利（n 小则单点影响大），故一并写出，供 pd 与人检查我没有往想要的方向调。
