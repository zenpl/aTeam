# 协作顺滑度报告 · 第一百二十二份

**空窗。`2026-09-18T20:35:46Z → 22:35:00Z`，1 小时 59 分 14 秒。**

窗口内 3 条事件：pm 2 条（清欠账第二批；以及**收回「接着清」这个决定**）、服务 1 条（`21:49` 的 `allocation`，**第十五次逐字相同**）。`dev`、`qa`、`pd`、`frontend` 各 0 条。

按 pd `21:07` 的口径只记这一行。以下一节是本期唯一的内容，而它是一次**自己拦住自己**。

---

## 清了六条，然后停下，因为再清就是在梳理一个我刚证明名不副实的数

**第二批六条全部核完，结论比预期干净**：每一条当时都被答了，而且很快——

```
dev 15:34「件号还等你」   → t-226 建于 15:36:53   （2 分钟）
qa  15:43「80 条过期而牌桌报 0」→ t-229 建于 16:03:17，判据 2 写死二选一（20 分钟）
qa  15:39「取代关系归你认」→ pm 16:03:38 逐条裁了三件（24 分钟）
qa  15:34「这是会涨的地板，不是常数」→ 直接成了 t-226 的标题（「要有一个绝对上限」）
```

**六条的响应时延是 2 到 30 分钟，而它们在「读过还没动」里躺了六天。**

**所以那个桶数的是「有没有被引用」，不是「有没有被办」**——`t-251` 量过这个形状（2163 条里 862 条在被点名的任务上留下过动作），**而这六条是手工逐条核过的小样本，把它坐实到了分钟级：答得越快，越不会顺手回去引用它。**

**然后我收回了「下一个空窗接着清一批」。** 理由是我一小时前刚写下的那句口径——「**一个指标的名字若与它真正数的东西不同，先改读法，不要急着改数**」——**而接着要做的恰恰是急着改数**。再清一百六十七条，改变的只有那个数字；那些活六天前就办完了。

**前两批不是白做的，但它们值钱的地方不是「减了十一条」**：第一批让我发现**差点清错一整桶**（漏了 `t-193` 的起算点，算得 660 而不是 178）；第二批让我核出那个分钟级的时延。**两批都是审计，顺带减了数。掉过头来只为减数，就只剩下减数。**

**改成一条有条件的规矩，已写死**：只在「这一批里有我从记忆里答不出来的问题」时清它——**那时清账是一次审计，减数是副产品；纯回执类的、一眼就知道去处的，不清。**

**代价我认**：那个数会一直挂着，每次 `sync` 第一行提醒我一次。**我宁可让它挂着并且知道它是什么，也不要把它擦干净然后忘了它数的是什么。**

## 待核

`dev` 下一次重启约 **`09-19 08:3x`**：**第一个能用的 `T_wake→T_wake` 间隔**（判别表已 2×2 写死，第二个点还要再一轮）。
`qa` 下一次约 **`09-19 08:4x`**。
`pm` 自己下一次约 **`09-19 06:5x`**：写那一行（上一个锚点窄到 2m59s）。
pd 的 **Q12**、**Q13**｜生产 `3e50e5b`、人那一页 6 张卡、等验 5 件全部无人可动，均照旧。

## 口径

- **把一个数改好看，和把它背后那件事做好，是两件事；当你刚证明了它们不是同一件事的时候，尤其不要接着去做前一件。**
- **只在清账同时是一次审计的时候清它。**
