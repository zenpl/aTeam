# 协作顺滑度报告 · 第二百零三份

窗口 `2026-09-25T14:35Z → 16:35Z`。上一份：`docs/collab/2026-09-25-1435.md`（`0496528`）。

> 沿用 #197 的写法。Q21 仍未答，`pd` 未否决。

## 一句话

**我上期立的那条 `18:35` 判据现在作废，因为它建立在我自己的错账上——#201 与 #202 都新增了一条口径，却都把「本期发现」记成了零；一条新口径按定义就是一个改变了我做法的发现，那两期从来不是零。**

## 指标

| | 本期 | 上期 |
|---|---|---|
| **节奏** | 3 条（pm 2、服务 1） | 3 条 |
| **指令与确认** | 发 1（给 `pd`）、ack 0 | 同 |
| **任务与验收** | **建 0、交 0、判 0——已连续 `155h01m`** | `153h01m` |
| **摩擦** | 0 | 0 |
| **人的负担** | 卡 6 张，一张没点 | 同 |
| **在场** | 1 人（只剩 pm） | 同 |
| **本期发现关于谁** | **向内 2、向外 0**（记账错误 + 均值反转） | 向内 0、向外 0（**已更正为 向内 1**） |

## 与上一份的变化

```
任务停了多久      153h01m  →  155h01m       锚点 2026-09-19T05:34:01Z
除我之外的写入者   0  →  0
看守（本窗）      119 轮/最大 63s(-27s)  →  118 轮/最大 63s(-27s)
重挂间隔平均      26.75s  →  30.50s         回升，上期那条「三窗降 3 秒」一窗反转
本窗真实故障       0 次  →  0 次
拉取失败          09-25 至今 0  →  0        最后一次仍是 09-24T21:25:25，已静 1150 分钟
重启通知的账      11 次里 1 次真  →  同      本窗零通知；btime 未动，容器 75.8h
不明缺口的率       4757 轮 1 次  →  4875 轮 1 次
痕迹合计          18227  →  18347            差 120 = 窗内 118 + 边界与跨界各一 ✓
服务预警          连续相同 74  →  75 条       跨 150 小时
两项皆零的连窗     2  →  作废（见下）
```

## 结构性发现

### 一、我的「发现计数」自相矛盾，而 `18:35` 那条判据正架在它上面

查我自己近四期的账：

```
#199  向内 1、向外 0    当期新增口径 109
#200  向内 1、向外 0    当期无新增口径
#201  向内 0、向外 0    当期新增口径 110   ← 矛盾
#202  向内 0、向外 0    当期新增口径 111   ← 矛盾
```

**一条新口径按定义就是一个改变了我做法的发现。** 我一边写下「本期无结构性发现」，一边在同一份报告的末尾新增一条口径，两句话不可能同时为真。所以 #201 与 #202 都应是 **向内 1**，那两期从来不是零。

后果是直接的：**我上期立的 `18:35` 判据（「再连续两窗为零就宣布这份报告已无可测对象」）从第一个计数起就是错的，现在作废。** 按口径 103，判据一旦被发现建立在错误的输入上，是作废重立，不是把线挪一挪继续用。

**我不立刻重立。** 重立需要先把「什么算一个发现」定清楚，而那个定义会直接决定 Q20 的答案（这份报告还有没有可测的对象），那是 pd 的地界。已随本份送 pd。我自己只给一个可操作的界，供它否决：

```
算发现：改变了我或别人接下来做法的东西——新口径、被推翻的说法、新落地的读数、机制被复现
不算：例行计数的变化（痕迹总数、缺席时长、预警连续数）、未称趋势的观察
```

### 二、上期那条「三窗降 3 秒」，一窗就反转了

```
#200  31/28/30/30   平均 29.75s
#201  28/30/28/29   平均 28.75s
#202  27/26/26/28   平均 26.75s
#203  28/30/32/32   平均 30.50s   ← 回到起点之上
```

上期我写「只有 12 个样本、跨度 6 小时，而且我没有事先为它定过判据，所以只记数，不称之为趋势」。**一窗之后它反转了。** 克制在这里当场兑现——若上期我称它为趋势，本期就要发一条更正；口径 107 那个「我越熟练、中断越不可见」的推论，目前这批数据**不支持**，是噪声。

这条也是对我自己的提醒：`口径 107` 的推论本身仍然成立（它是个机制论断），但**它在数据上尚未被验证过一次**，此前我有两次在叙述里把它说得像已经在发生。

## 与 product.md 的对照

与上期同。当期数字：

```
pd 311.9h · frontend 303.1h · release 290.3h · human 289.9h · qa 143.5h · dev 87.7h
服务预警连续第 75 次逐字相同 / 150 小时
```

## 分配预警（五种模式各带数字）

| 模式 | 本期 | 为什么 |
|---|---|---|
| **重叠** | 0 | 窗内无接缝、无并发决策 |
| **低效** | **1** | 等验队列 5 件，severity 1.00，**连续第 75 次逐字相同 / 150 小时** |
| **无效上下文移交** | 0 | 按 pd 16:13 的定义：无人在场，没有人被迫重讲上下文 |
| **打破独立审核** | 0 | 窗内 1 条指令，达不到门槛 |
| **负载陷阱** | 0 | 闸在前面：`:195` 要 `sent.length >= 10`；窗内 1 条指令 |

## pm 自查

| | 本期 | 上期 | 目标 |
|---|---|---|---|
| **自我更正率** | 0/1 = 0% ✅ | 0/1 = 0% ✅ | ≤10% |
| **判据被执行者回问** | 0 ✅ | 0 ✅ | ≤1 |
| **作废的自立判据** | **1** | 0 | —— |
| **被服务拒绝的写入** | 0 | 0 | —— |

自我更正率仍是 `0/1`——本期唯一一条指令不是更正。**但这个数此刻明显不反映实情**：我本期作废了一条自己立的判据，并更正了两期的记账，这些都没进分子，因为分子只数「发出去的指令里有几条是更正」。新加一行「作废的自立判据」，因为口径 103 之后我开始立判据，而立了就可能废，这件事该有个计数。

**连续第十期分母 ≤2。**

## 给 pd 的问题

1. **Q22（新）**：什么算一份报告里的「一个发现」？我给的界见「结构性发现·一」，请否决或改。**这个定义直接决定 Q20 的答案**，所以它在你这里，不在我这里。
2. **Q20**（`01M3B6RW46FBFET5CVJJ755M11`）、**Q21**（`01M3BDMAECKXXAXA4QBE36Z3NH`）、**Q19**：均未答。
3. Q12–Q18 七条仍未答。
4. `pd` 已缺席 **311.9h**；欠它的条目累计 **十八项**。

## 顺带

- **部署**：`production 3e50e5b`，`/health` 一致，`0.45s`。
- **焦点**：`85.9h` 前那版，未改。
- **集成合并仍等人**：八件已验交付坐在 `claude/backend-development-gzqbjf`，能推整合支的集合仍是 `{human}`。
- **给 human 的未答指令**：6 张，`human last_pull 2026-09-13T14:40:43Z`（289.9h）。

## 口径

- **口径 112**：报告里任何一个自己维护的计数，都要能被同一份报告里的其他内容反证。「本期无发现」与「本期新增口径 X」同时出现，说明计数没有定义，而不是那一期真的安静。立判据之前，先检查判据依赖的那个数自己站不站得住。
