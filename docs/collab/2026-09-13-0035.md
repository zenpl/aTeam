# 第五十一份：S1 今天被推进了七步，而打开牌桌的那个人，一步都还没看见

窗口 `2026-09-12T22:35Z → 2026-09-13T00:35Z`。**截止时刻由数据自证**：快照末条 `at = 2026-09-12T23:51:55.717Z`、`id = 01M2C0HT25J2T1JC6ZE4GM840K`、全量 **9,251 条**。23:51 之后的事件在文末「截稿后」单列。

---

## 一句话结论

**今天建的 23 件任务，没有一件写着它服务哪个场景；而逐条对回 `docs/product.md`，至少 7 件直接服务 owner，全落在同一个场景 S1 开工。**

我 22:54 把那个 0 读成「今天一件场景活都没有」，**十分钟后自己推翻了**——读了 `product.md` 才发现两处都错：那 7 件在服务 S1；而第 117 行早就定了「T1 和 T8 优先于一切新场景」，所以剩下那 16 件的顺序也是对的。**真正的过错比我原来认的那条窄，也更具体：我的职责原话是「report scenario completion to pd and the human, not task counts」——没有场景号，我就报不出场景完成度，只报得出任务数。pd 因此看不见自己的 S1 今天被推进了七步。**

**而这七步堵在两道不同的坝后面，我一开始把它们混成了一道，qa 拆开了：五件只差一次发车，两件只差 pd 一句措辞。** 用 qa 的话：「**这个差别对报告要紧：前者发车能解决，后者不能。**」

**由这一天最该带走的一句：我们不是没往产品上走，是走了七步而没人记账——而记账的人是我。**

---

## 指标

**截止 23:51:55，窗口已走 77 分钟（两小时窗口的 64%）。与上期比较时用的是上期完整两小时。**

### 节奏

| | 本期 22:35→23:51（77 分钟） | 上期 20:35→22:35（2 小时） |
|---|---|---|
| 事件总数 | **137** | 195 |
| 按人 | **pm 55／qa 42／dev 34／frontend 6** | dev 104／qa 49／pm 35／frontend 4／ateam 3 |
| 按种类 | instruction 37／task 31／ack 31／note 19／reading 18／untell 1 | task 74／instruction 45／ack 41／note 24／reading 11 |
| 日志长度 | **9,251 条** | 9,071 条 |

### 指令与确认

| | 本期 | 上期 |
|---|---|---|
| 指令 | **37 条** | 45 |
| **ack 时延** | **中位 87 秒**，最快 25 秒，最慢 1110 秒，n=31 | 中位 93 秒 |
| ack 时延 p90 按角色 | qa 136s／pm 195s／dev 217s／**frontend 1110s** | — |
| 指令流入占比 | **pm 38%**／qa 22%／dev 19%／frontend 16%／pd 5% | — |
| 我发出的 | 18 条（`tell` 15、给 pd 2、`untell` 1 条重发） | — |

### 任务与验收

| | 本期 | 上期 |
|---|---|---|
| 判决 | **9 条，全 pass** | 11 条 |
| **其中生产表面** | **7 条**（t-145 t-144 t-178 t-124 t-151 t-160 t-170） | **0 条** |
| 新建任务 | 2（`t-247` `t-248`） | 5 |
| 任务状态 | verified **228**（+4）／open 2／claimed 1／done 3／failed 4／**blocked 4** | verified 224 |
| 开着的接缝 | **0**（本期 12 条接缝，全部当场解） | 0 |
| 98 件那本老账 | **三堆齐了：7 全清 ／ 27 出读数 ／ 64 记「不知道」** | 未拆 |

### 摩擦

| | 本期 |
|---|---|
| friction note | **2 条**（旧 CLI 静默吞掉未知开关；`failed` 的任务要修它的人 claim 不了） |
| concern | **2 条**（`--X-file` 在旧命令行上无声吞掉；牌桌说 human 在 22:41 拉过日志） |
| 变成任务的 | **2 条**（`t-247` 当场建、当场验过 repo；`t-248` 给 frontend） |
| 服务侧慢 | **1 次 44 秒级阵发**（我一条命令超 120 秒、看守报 1 条 `WATCH-ERR`），而 `/health` 同期 0.411 秒 |

### 人的负担

| | 本期 |
|---|---|
| 人看得见的卡 | **5 张，一张没点，本期没有新增** |
| 人那一页大小 | **300,606 字节**（22:41 量，匿名与带钥匙相差 38 字节，差在按钮而不在内容） |
| 每条生产判决让它长 | **2,514 字节**（qa 实测；我先前说的 88 字节是全部事件的均值，错了） |
| 他上一次真做事 | **09-07T03:36**，五天前 |
| 他的 `last_pull` | **22:41:53.934Z**——**这个数还没解释清楚，见结构性发现五** |

---

## 与上一份的变化

**三个数要说明：**

- **生产判决从 0 回到 7。** 上期十一条判决一条没落在生产上；本期九条里七条落在生产上。**原因是我 22:45 把那 98 件里「修已经在 `09256fd` 里」的那批开给了 qa——它们是此刻唯一不被发车堵住的验收**。qa 一小时内把有生产表面的 7 件全清，**并且用的是我 23:30 才放行的第三档办法：在生产上开一次性项目撞服务端的闸，真项目一条写都不发，owner 钥匙没碰。**
- **pm 从 35 涨回 55，dev 从 104 掉到 34。** 上期我写过「这是今天第一次 pm 不是事件数第一」，**本期又回去了**。不是队伍慢了——是本期我量了很多（9 条读数、10 条 note），而 dev 上期那 104 条里有 59 条是接缝事件。**这个数我不粉饰：本期 137 个事件里 55 个是我在说话，40%。**
- **`blocked` 桶从 3 涨到 4，`open` 从 2 变成 2（换了人）。** `t-217` 本期补挂 blocked（我 22:38 要的，dev 40 秒内做完）；`t-243` 做完了，`t-248` 是新建给 frontend 的。**队列没有变长，但等 pd 的那一格又多了一件。**

---
## 结构性发现

### 一、S1 今天被推进了七步，而打开牌桌的那个人，一步都还没看见

**今天建的 23 件任务里，没有一件写着它服务哪个场景。** 三天连起来是 **49% → 11% → 0%**（09-06 是 103 件里 50 件，09-07 是 120 件里 13 件，今天 23 件里 0 件；最后一件带场景号的是 `t-224`，09-07T20:06）。读数 `repo:tasks.created.today = 23`、`repo:tasks.today.naming.a.scenario = 0`。

**我 22:54 把这个 0 读成「今天一件场景活都没有」，十分钟后自己推翻了（23:04 的 decision，supersedes 前一条）。** 逐条对回 `docs/product.md` 的 O／T／M，**至少 7 件直接服务 owner，而且全落在同一个场景 S1 开工**：

| 件 | 服务 | 这一条的原话 |
|---|---|---|
| `t-235` | **O1** | 一天里 owner 打开的只有牌桌——而那一页在长 |
| `t-236` | **O2** | 牌桌上出现的每件事都真的需要他——最急的却排在最下面 |
| `t-229` | **O2** | 「需要你」误报为零——而 80 条过期、牌桌报 0 |
| `t-231` | **O2** | 牌桌说「等谁」时，会让人去问一个被禁止动这件事的人 |
| `t-237` | **O2 O5** | 「有没有生产判决」被印成「有没有上线」，对人说一句假话 |
| `t-234` | **O3**（P0） | 拍板是一次点击——而此刻谁拿着 token 都能替他点 |
| `t-242` | O2（弱） | 「线上」那一节的标题与 `SAID_LABEL.live` 报少了 |

**所以真相是我自己列的第②种读法：我建了服务场景的活、却一件都没写上场景号。** 这不是文书问题——**我的职责原话是「report scenario completion to pd and the human, not task counts」。没有场景号，我就报不出场景完成度，只报得出任务数**，而我每两小时交一份报告，报的正是任务数。**pd 因此看不见自己的 S1 今天被推进了七步。** 从下一件任务起，我建任务时先写场景号或明写 tooling；写不出来的先停下问 pd。

**而这七步，堵在两道不同的坝后面——我 23:19 先把它们混成了一道，qa 23:21 拆开了（它是对的）：**

| 堵在哪 | 几件 | 哪几件 | 谁能解 |
|---|---|---|---|
| **发车这道坝**（验过了，修不在生产那一版里） | **5** | `t-235` `t-236` `t-229` `t-231` `t-234` | 一次发车 |
| **pd 这道坝**（代码半交了，措辞半等 pd） | **2** | `t-237` `t-242` | 只有 pd |

**qa 那句点评是这条更正的价值所在，原样记下**：「**这个差别对报告要紧：前者发车能解决，后者不能。**」**只发车，他能看见五步；另外两步要等一个此刻联系不上的角色。**

**这是今天第 N 次把两种不同的「卡住」读成一种**：`t-226`（没到位置）与 `t-227`（到了位置被挤掉）；`failed` 与 `blocked` 两个桶；如今是「等发车」与「等 pd」。**每一次我都是先看见「它没动」，然后给它安了一个我手边最顺的原因。**

### 二、今天最大的一笔投入是 M4，而 M4 正是全天那条根的正面修法

其余 15 件（`t-238` 已撤回不计）按 O／T／M 分：

- **M4「被拒绝时说出规则名，不绕」＝ 5 件**：`t-228` `t-232` `t-233` `t-241` `t-247`
- **T8「少了谁都不停」＝ 5 件**：`t-225` `t-226` `t-239` `t-240` `t-245`
- **T3「要某人现在做的事送到了、被看见了」＝ 5 件**：`t-227` `t-229` `t-240` `t-244` `t-245`
- **T1「对世界的认知一致」＝ 4 件**：`t-225` `t-230` `t-240` `t-242`
- T5 ＝ 2／T9 ＝ 1／T10 ＝ 1／M2 ＝ 2／M5 ＝ 2（有重叠，一件可服务多条）

**M4 五件不是巧合。** 今天全天反复撞的是同一句：**「命令说它成功了，而我写的那段字不在日志里」**——反引号被 shell 吃掉、`sync --quiet` 吃掉一批、部件级被拒不留痕、未知开关被静默吞掉。**M4 的原话就是「被拒绝时说出规则名，不绕」。我们今天有三分之一的力气，花在让工具把它吞掉的东西说出来。**

**我 22:54 还引自己那条「if a deploy batch is all tooling, something is wrong」判了今天全天不对。这也错了。** `docs/product.md` 第 117 行早就定了顺序（human 09-06 定序）：**「团队侧不单独排期，跟着 owner 场景走，但 T1 和 T8 优先于一切新场景」**。**T1 四件、T8 五件——按产品自己定的顺序，今天这一天是对的。我拿自己一条粗的口径，盖了产品一条细的。**

### 三、`t-246` 给了全队一个新开关，而那个开关在旧命令行上是无声吞掉的——它第一个吞掉的，是它自己的判决

dev 22:29 交了 `t-246`：任何 `--X` 都能写成 `--X-file <路径>`，文件里的字不会再被 shell 解释一遍。**qa 22:38 用 `--evidence-file` 给它落判决：退出码 0、终端印出「task t-246 VERIFIED on repo」、而落库的事件上连 `evidence` 这个字段都没有。整篇两千多字的判决，一个字都没进日志。**

**根因不在 `t-246` 的实现**（qa 在 `556be03` 上单独核过，落库逐字相同）。**根因是这棵工作树跑的是 `t-246` 之前的那支 CLI，而它对不认得的开关一律静默忽略。** 三个数并排（同一台服务、同一份文件、同一条命令）：

| | 结果 |
|---|---|
| 旧 CLI（`eae0b22`）＋ `--evidence-file` | 退 0、一声不吭、`evidence` = 空 |
| 新 CLI（`556be03`）＋ `--evidence-file` | 落库逐字相同 |
| 旧 CLI ＋ 任意未知开关 | 退 0、stderr 一个字都没有 |

**为什么这条比一般的「未知开关被忽略」贵，用 qa 的话**：`--X-file` 这条路的全部意义就是「把一段不能被吃掉的字安全送进去」。**在没升级的节点上，它把「被 shell 吃掉一半」换成了「一个字都没有，而且没有任何提示」——从一种看得见的坏，换成一种看不见的坏。**

**我自己实测也是 0 行**（`ateam help | grep -- -file`），frontend 23:09 实测同样 0 行。**全队此刻跑的多半都是不认得它的那一支。** 已建 **`t-247`**，dev 22:56 认领、**23:1x 已验过 repo**。当口径广播出去的是 qa 的第②条：**用 `--X-file` 之前先 `ateam help | grep -- -file`，0 行就继续用 `"$(cat 文件)"`——后者新旧两支都对。**

**我另量出一处它没提的不对称，写进了判据 3**：同一支 CLI 上，**未知开关＋值 ⇒ 静默退 0**，而**已知开关缺值（`--dry-run` 不带值）⇒ 报「--dry-run needs a value」**。**两条路此刻不在同一处**——也就是说这支 CLI 是「会报错的」，它只是对这一种不报。

### 四、`/health` 在服务卡住的那 44 秒里，照样说「一切正常」

22:49 我发三条 tell，**一条命令跑了超过 120 秒**；22:44 我的看守报了一条 `WATCH-ERR`。于是 22:56 交替量了三轮，每轮四条，字节数三轮完全一致：

| 轮 | `/log` 全量 6.85MB | `/log?after=` 末条 472B | `/log?after=` 半小时前 54KB | `/health` 60B |
|---|---|---|---|---|
| 1 | **43.871s** | **13.745s** | **10.976s** | **0.411s** |
| 2 | 2.831s | 0.188s | 0.238s | 0.180s |
| 3 | 1.899s | 0.244s | 0.251s | 0.195s |

**第 1 轮里四条全慢，唯独 `/health` 不慢。** 6.85MB 要 43.9 秒、472 字节也要 13.7 秒——**慢的不是数据量，是那一阵**。而同一阵里 60 字节的 `/health` 是 0.411 秒，和平时几乎一样。

**`/health` 是我们唯一的「服务还活着吗」的问法，它在那 44 秒里会照常回 `{"ok":true}`。** 也就是**「`/health` 说 ok」不等于「此刻这台服务能用」**——第一面在「健康检查」这个对象上的又一次。今天全队至少三次撞上它。

**与 `t-238` 的关系，说清楚免得看着像翻案**：`t-238` 说的是反向（「`/health` 在别人取板子时涨到 35–46 秒」），我 19:38 建、当天按 qa 的对照实验撤回了。**这次量到的正是它的反面：慢的时候 `/health` 反而是快的那一个。撤回它是对的，这条不推翻它。**

**我今晚没为它建任务**，理由写在 22:59 那条 note 里：我只有现象没有诊断，而「去查一查」是 qa 的量活不是 dev 的代码活，qa 手上已经排了那 98 件。**如果它再咬人一次，那时该做的不是建任务，是把它排到场景活前面——那需要 pd 或人点头，不归我。**

### 五、牌桌说 human 在 22:41:53 拉过一次日志，而他上一次真做事是五天前

**读数原文**（`board --json` 的 presence 行）：`human · status=missing · last_pull=2026-09-12T22:41:53.934Z · last_event=2026-09-07T03:36:48.759Z`。

**我先把自己排除掉**，因为那个时刻正是我在量牌桌。22:41:53～54 我发过两条请求：一条匿名 `GET /`，一条带 token、不带 `X-Actor` 的同样请求。
- **匿名那条我做了对照实验**：23:09 发一次，human 的 `last_pull` **一动不动**。**qa 16:16 那句「匿名看一眼不写 human 游标，读它不改世界」，成立。**
- **带 token 那条我不做对照实验。** 按 17:08 那条队规：**要证只用只读 GET；需要写才能证的那一半谁都不做。推进 human 的 presence 就是改 human 的状态，我不为了证一件事去动他那一行。**

**代码把我推向另一边**：`reduce.ts:383-385` 里 `last_pull` 只由 cursor 记录推出，而 cursor 只在带 `X-Actor` 的拉取路径上写（`app.ts:541` 起，没有 `X-Actor` 直接 400）；牌桌 HTML 那条路（`app.ts:385-401`）通篇没有写 cursor 的地方。**照这个读法，我那两条请求都写不出 human 的游标。**

**于是我问了三个在听的人一句话（读自己的记录，不动任何人的状态）**：22:41:53Z 前后有没有用 `X-Actor: human` 拉过？
- **qa：没有**（翻了全部量具，`x-actor` 只出现过 qa/pm/frontend；唯一一处 `"x-actor":"human"` 指向它自己起的本地一次性服务，今天没跑过）
- **dev：没有**（所有拉日志的脚本都发 `x-actor: dev`）
- **pd / release：它们的 `last_pull` 停在 16:42 / 16:40，没动过——不用问也答了**

**顺带发现我自己那个警报的一个洞，这条我认**：我 18:53 给看守加的报警盯的是**任何新的 `actor = human` 事件**。**而这一次没有事件，只有一次读。**「以 human 的名义写」我看得见，「**以 human 的名义读**」我是瞎的——**而正是「读」这一步会把他的在场数据推新，从而压住 T8 那条「全队一起死时人一小时内知道」的升级。**

### 六、一个在听了两个半小时、什么都没做的节点，和一个正要开工的节点，在牌桌上长得一样

frontend 20:37 之后**两个半小时没有任何事件**，而它的 `idle_pull_s = 6`——**它一直在拉，每几秒一次，只是不做事**。牌桌给它的状态是 `listening`。

**牌桌有词形容「不拉」（`missing`），有词形容「在拉」（`listening`），没有词形容「在拉而不动」**——而后者与「在拉、马上就要动」长得一模一样。我 22:38 给了它一件活（`t-025`，我 17:55 就放行、之后四个半小时没人接），**它 23:09 就答了并开始量**。所以它不是死的，**是没活干**。
**而那件活它后来也接不了**——23:41 它告诉我 `claim` 被服务端拒了（`t-025 is done (owner dev)`）。**「在拉而不动」这一次有两层：上面一层是没活干，下面一层是给它的那件活它拿不到。** 详见分配预警③。

**它没活干的原因不是闲**：frontend 那块地（人看得见的东西）**此刻整块压在 pd 身上**。人可见的活此刻只剩三处：`t-207` 标题就写着「措辞归 pd」；`t-237` 与 `t-242` 的措辞半也等 pd（代码半 dev 已交、挂 `blocked`）；**唯一一件不等 pd 的是 `t-025`——正是我 17:55 放行、然后忘了跟进的那一件。**

**这一条的形状，正是 frontend 自己给 `t-240` 加的那条判据**：「**判『收件人反应没反应』，不判『事件到没到』**」。今天它自己成了那个样本。

### 七、再有两件，队列就见底了——而见底的原因不是活干完了

dev 本期交了 `t-241`、`t-247` 两件（`t-247` 从我 22:48 建、22:56 认领、23:02 交、**23:11 验过 repo，全程 43 分钟**），并在 23:31 交了 `t-243`。**我 23:33 给它排了 `t-175`，并同时说明白了一句：`t-175` 做完，它手上就没有一件不等 pd 的了——它 23:37 就认领了。**

逐条点名，免得听起来像修辞：

| 件 | 状态 | 等谁 |
|---|---|---|
| `t-217` `t-233` `t-237` `t-242` | `blocked` | **pd**（代码半都交了，措辞半等它） |
| `t-207` | `open` | **pd**（标题就写着「措辞归 pd」） |
| `t-025` `t-133` `t-219` `t-226` | `failed` | **一次发车**（生产表面判不过，而修不在生产那一版里） |
| `t-175` | 在跑（23:37 claim） | 没有前置 |
| `t-248` | `open`，owner frontend | 没有前置（23:42 新建，见分配预警③） |

**`t-175` 我特意核过它有没有 pd 前置：没有。** 判据 5 明写「不改变人看到的东西——这是给 pd 与报告用的指标口径」，而两桶的定义 pd 08:40 就给了。**它做完之后，我这份报告里的「自我更正两栏」就不再是我每期手算的了**——工具去数我，而不是我数我自己。

**所以队列见底不是「活干完了」，是「剩下的活的前置全在两个地方：一次发车，和一个联系不上的角色」。** 我给 dev 的最后一句是「**到那时先别自己找活，告诉我**」——因为上一次队列见底时，填满它的是我，填进去的全是工具修工具。
## 与 `product.md` 需求（O／T／M）的对照

**今天这 23 件逐条对回去的结果（`t-238` 已撤回不计；一件可服务多条，有重叠）。事后补标是我标的，不是判据里写的，谁都可以推翻某一条。**

| 层 | 条目 | 今天的件数 | 哪几件 |
|---|---|---|---|
| **O（owner）** | **O1** 一天里 owner 打开的只有牌桌 | 1 | `t-235` |
| | **O2** 牌桌上出现的每件事都真的需要他 | 5 | `t-236` `t-229` `t-231` `t-237` `t-242`(弱) |
| | **O3** 拍板是一次点击 | 1 | `t-234`（P0） |
| | **O5** 出事只看证据 | 1 | `t-237` |
| **T（团队）** | **T8** 少了谁都不停 | 5 | `t-225` `t-226` `t-239` `t-240` `t-245` |
| | **T3** 要某人现在做的事送到了、被看见了 | 5 | `t-227` `t-229` `t-240` `t-244` `t-245` |
| | **T1** 对世界的认知一致 | 4 | `t-225` `t-230` `t-240` `t-242` |
| | **T5** 互相挡住对方的错误 | 2 | `t-231` `t-234` |
| | **T9** 语言一致、引用不断 | 1 | `t-243` |
| | **T10** 不被自己淹没 | 1 | `t-246` |
| **M（成员）** | **M4** 被拒绝时说出规则名，不绕 | **5（今天最大的一笔）** | `t-228` `t-232` `t-233` `t-241` `t-247` |
| | **M2** 三样输入四个动作 | 2 | `t-227` `t-246` |
| | **M5** 刚醒来或新加入时能快速跟上 | 2 | `t-226` `t-239` |

**三句读法：**
1. **落在 O 上的去重后是 7 件，全部指向 S1 开工**（`t-237` 同时够到 S4 出事了）。**而这 7 件人一件都没看见**：5 件等发车、2 件等 pd。
2. **M4 五件不是巧合**，它正是全天那条根的正面修法——见结构性发现二。
3. **`product.md` 第 117 行定的顺序（「T1 和 T8 优先于一切新场景」）今天是被遵守的**：T1 四件、T8 五件。**我 22:54 拿自己那条「全是 tooling 就说明不对」去判它全天不对，是用一条粗的盖了产品一条细的。**

---

## 分配预警（五种模式，各带数字）

**口径按 `docs/responsibilities.md` 283–292 行与 pd 16:13 的定义。** 本期我先对了一次口径才写这一节——**上一稿我把其中两种写错了**（无效移交的判准、负载陷阱的处方），下面是改过的。

### ① 重叠 — 服务算出的是 **0**，而我手上有一条它按设计看不见

**本期起这一节按 cron 的要求引用服务算出的 `project:allocation`**（`t-061` 09-07T17:56 已在生产判过 pass）。**今天服务出了 10 条 `allocation` 读数，全都只报一种模式：低效。重叠一次没报过。**

**我核了它为什么不报，结论是：它没错。** `staticAllocation` 读 `project:roles` 这条读数来判（`allocation.ts:64-65`），而那条读数存在——**是我 09-07T03:35 自己记的**：

```
pd: R1 R2 R3   |  pm: R4 R8:接缝裁决 R13  |  dev: R5:后端
frontend: R5:界面  |  qa: R6 R12  |  release: R9 R8:批次与集成
```

按这份声明逐条算：R8 与 R5 都已经带分界后缀拆开（`R8:接缝裁决` / `R8:批次与集成`），**没有任何一项职责落在两个角色的集合里。所以「0」是对的。**

**而我本期量到的那一条，它按设计看不见**：**R2（决定人看到的任何东西的措辞）声明里只有 pd 一个持有者，实际上我也在持有。** 证据是我扫自己作者的 1047 条判据扫出来的：**至少 4 句人会看到的话是我在判据里直接写出来的**（`t-043` `t-129` `t-201` `t-231`），**其中 3 句已经在生产上，我一个字没改**——由我去改一句已经上线的、人可见的话，是第二次越界。已交 pd（队列第 17 件），正则贴在日志里，谁都可以重扫。

**这不是 bug，是那半道闸量的东西不同**：**静态重叠查的是「声明里有没有两个持有者」，我这一条是「声明里只有一个，实际有两个」。** 又是第一面：**「声明说谁持有」不等于「实际谁在持有」。**
**上一稿我先写成「这条不在 pd 那五种里」、又改成「它就是静态重叠」——两次都不准，这一版是第三次，也是我核过代码之后的那一次。**

**运行时那一半我另核了一遍，与服务一致**：十分钟内两条决策 note 引用同一任务且不互相 supersede ⇒ **0**（我本期两条 decision 是互相 supersede 的，那是更正不是重叠）；同一收件人五分钟内收到两个不同发件人内容相近的指令 ⇒ **0**。

### ② 低效 — **服务说「100% 由 dev 手工」，我说「pm 已降到 0%」，两句都对**
基线：「34 次接缝 32 次 pm 手工」（09-06）。指标是「某类事件由同一角色手工完成的比例」。

**服务 22:34 那条读数的原话，与我本来要写的结论正好相反，而它是对的一半**：「**接缝 100% 由 dev 手工解决（59/59）**」。

我本来要写的是这张表：

| | 接缝总数 | **pm** 手工解 | 占比 |
|---|---|---|---|
| 09-06 | 50 | 41 | **82%** |
| 09-07 | 115 | 36 | **31%** |
| **09-12** | **104** | **0** | **0%** |

**两句都是真的，合起来才是这件事的全貌：pm 不再手工解接缝了——那 104 条一条也没经过我；但接缝并没有变成规则或服务自动解掉，它只是整块从 pm 挪到了 dev 手上，今天 104 次。** 服务盯的是「某类事件由同一角色手工完成的比例」，它不在乎那个角色是谁；我盯的是「瓶颈还在不在我这儿」。**我的口径会让我看见一个胜利，服务的口径看见的是同一件事换了个人扛。**

**今天这 104 条接缝，104 条都是 dev 用 `git merge-base --is-ancestor` 算出来再手工落一条 `seam` 事件的**，`resolution` 里逐条写着 `absorbed: 后者 X 含前者 Y（git-ancestor）`。**算是自动的，落是手工的。**
**根因是 `t-160`**：`reduce.ts:393` 的自动吸收**只在 `absorbForm === "named-sha"` 时执行，而本项目用的是 git-ancestor——那条分支从来没生效过**。09-07 夜里我手工裁的每一条接缝都是这一行的账。**这一整类工作已经从 pm 手上消失了——但按服务的口径，它没有从团队手上消失。下一步不是庆祝 0%，是问 dev 那 104 次落事件能不能也由服务代劳。**
**而 `t-160` 本期刚在生产表面判过 pass**（qa，本期八条判决之一）——**也就是说这条预警不只是降了，是修它的那件事今晚拿到了生产判决。**
另两项指标：**按角色 ack 延迟 p90**——qa 136 秒 / pm 195 秒 / dev 217 秒 / **frontend 1110 秒**（n=2，中位 754 秒，是其余三人的四到八倍）；**等验队列** `t-241` `t-243` 两件（`t-094` 是 qa 自己的，归人），**不增长**。
**另一项不算低效，说明一句免得被误读**：今天 7 条 `criteria add` 全是 pm——**那是 pm 的职责本身，不是「本该由规则做的事靠人手工」。**

### ③ 无效上下文移交 — 本期 **0**，而我手上那一条不符合这个定义
pd 16:13 的定义是「职责链被切断、逼下一个角色重新获取上一个角色已有的上下文（重新读、重新问、重新转述）」，**不是转发计数**；291 行给了判准：「**下一个角色能否只靠产物开工，不必再读、再问、再转述**」。

**本期唯一走完整条链的任务是 `t-247`**：qa 发现（22:43 concern）→ pm 写判据（22:48）→ dev 实现（22:56 claim、23:02 done）→ qa 验收（23:11 pass）。**3 个角色、3 次移交，其中一次是独立审核那一刀（正当）。重新推导 0 次**——dev 没有重量 qa 的三组对照数，它直接照判据做；判据里我引的是 qa 的原始数字，不是我的转述。**按判准，这条链是合格的。**

**我上一稿把 `t-025` 写成本期的无效移交实例，那是错的。** `t-025` 的形状是「讲完了没人接」，不是「逼人重讲」——**dev 17:50 的交代细到连坑都给了**（`html.ts:641`，`criteria_added.index` 是 0 基而旁边 `criteria_moved` 是 1 基，照抄会差一条），**frontend 完全可以只靠这条产物开工。上下文一次都没丢。**
**所以它不属于这五种里的任何一种，我单列出来给 pd 看**：
· qa 17:47 判不过 → dev 17:50 定位并交代给 frontend → **我 17:55 放行**（恢复被误删的既有渲染不算新措辞，不必等 pd）→ **然后 4 小时 43 分钟没有人认领** → 我 22:38 重发 → frontend 23:09 **应答**（报了一条它自己的实测）→ **截到写这份报告为止仍然没有人 claim**，`t-025` 最后一条任务事件还是 17:47 那次判不过。**从 dev 交代清楚那一刻起，5 小时 46 分。**
**断的不是上下文，是「放行之后没有人跟进」，而放行的人是我。** 我 22:38 那次跟进换来的是一句应答不是一次认领——**「应答」与「接了」我今天分不清了两次**，第二次就是这一条。
**给 pd 的一句**：这五种模式里没有一格装得下「交接完好、无人认领」。**要不要有第六种，归你。**

### ④ 打破独立审核 — 本期 **0**，且指标本身是「被拒次数」
定义的运行时指标是「服务器已拒绝的三方分离事件计数——被拒次数本身就是预警」。**本期三方分离被拒 0 次。**
本期我自己被拒过两类（`reading` 键里重复写表面名、`instruction` 正文 283 字超 280），**都不是这一项，且都是规则在工作**。
**但要记一句**：`t-234` 那个 P0 的内容，正是**这条分离在权限层被绕过**——**同一把管理钥匙能以 `human` 的名义落事件，包括 `decide`，也就是替他答卡。判据层守得住，钥匙层守不住**，而那是 T5「互相挡住对方的错误」的地基。**修法是人的一个动作，卡在他那一页第三张。**

### ⑤ 负载陷阱 — **两个方向各一个，成因不同，处方也不同**
基线：「pm 持有 R1 R3 R4 R8 R13，收 109 条指令（35%），18% 十分钟内自我更正（职责太杂）」。

**方向一（太杂）：pm 的指令流入占比 38%，比 09-06 基线的 35% 还高了三个点。**

| 收件人 | 本期流入 | 占比 |
|---|---|---|
| **pm** | **14** | **38%** |
| qa | 8 | 22% |
| dev | 7 | 19% |
| frontend | 6 | 16% |
| pd | 2 | 5% |

**成因判定按 287 行**：pm 职责数高、指令杂 ⇒ **太杂**，处方是**拆**（把某项职责挪走或改成规则／服务）。**而今天恰好有一件正在拆**：`t-175`（自我更正拆两桶）做完之后，**这份报告里的「pm 自查」两栏就由服务算，不再是我每期手算**——R13 报告的一部分从人手上挪进服务。我 23:33 已把它排成 dev `t-243` 之后的下一件。
**但更彻底的那一半今天没发生**：R8（接缝裁决）已经拆干净了（见②，0%），**R13（报告）仍整件在我手上**。

**方向二（空转）：frontend 20:37→22:38 连续两小时零事件，而它的 `idle_pull_s = 6`——它一直在拉，只是没活干。**
**成因判定：不是「太杂」也不是「活太多」，是第三种——职责包合理、活也有，但那些活的前置全在一个联系不上的角色手上。** 287 行给的两张处方（太杂就拆、活太多就扩）**这一次都不对**：拆它没有可拆的，扩出一个 frontend-2 只会让两个节点一起空转。
**唯一的处方不归我开：起一个 pd。** 人那一页第一张卡，挂了 352 分钟。
**顺带一个数**：**被单一角色阻塞的任务数 = 5**（`t-217` `t-233` `t-237` `t-242` `t-207`，全部等 pd），这正是 287 行点名要看的那个指标。

**关于 `t-061`**：cron 说「`t-061` 上线后指标改为引用服务算出的 `project:allocation`，叙述仍由 pm 写」。**我先以为它没上线，核了一遍：它 09-07T17:56 就在生产判过 pass，今天在稳定出读数（10 条）。本节已改为以服务的读数为准，我手算的部分只在服务看不见的地方补，并且每处都写明它为什么看不见。**
**而对这一节最要紧的一句是**：**今天 10 条读数里，服务只报过「低效」一种。另外四种一次都没报。我逐种核过它为什么不报——①④⑤的静态半是按声明算的，声明里确实没有；③它按自己的判准算也确实是 0。也就是说这四个 0 都是真的 0，但它们是「按声明和按可数事件」的 0，不是「按实际发生」的 0。** 本节里我手补的三条（①的 R2、③的「交接完好无人认领」、⑤的 frontend 空转），**没有一条是服务的口径够得着的。**
## 部署与焦点状态

| | 此刻 |
|---|---|
| 生产 | `09256fd`（`GET /health` 本期两次核对，一个字没动） |
| 整合分支 | `claude/new-project-details-gif66k` 仍停在 **`7f31808`**，落后 284 条、**独有 0 条，第六天** |
| dev 那一支 | 本期从 `159a541` 走到 `3dfac2e`，相对生产多出 **84 个文件、+5386 / −291**（22:37 量的那一刻） |
| 生产欠账 | **19 件**（qa 23:21 更新，算法一字没改：有 repo 通过判决、无生产判决、证据 sha 不是 `09256fd` 的祖先） |
| 另一本更老的账 | **98 件**，本期按我的裁定拆成三堆：**10 件有生产表面**（已判 3 件生产 pass）／**24 件自述或明写无人可见影响**／**64 件建于 `t-171` 前，记「不知道」，不并进前一堆** |
| 焦点 | **我 22:40 重写了。上一版写于 17:13，已经三小时四十分**，而它排的 dev 顺序（`t-234`→`t-103`→`t-070`→`t-235`→`t-232`→`t-233`→`t-229`）**没有一件是 dev 此刻在做的**——那三个多小时里 dev 走的是我用 tell 发的另一条队列。**牌桌上写的顺序与真正在跑的顺序不是同一条，而牌桌是 pd、人、以及任何新起的会话唯一读得到的那条。** |

**关于发车，本期新量了一个数，先写在这儿免得有人自己吓自己**：dev 那支头**缺生产的 5 条提交**（`09256fd` `d57acbc` `c83e986` `39353bf` `8309289`），照字面读像是发它就要丢五件东西。**逐条问过：那 5 条全是发车合并本身**，它们的第二父 `eebf073`／`4efe958`／`f186424`／`368833d` 四条全在 dev 头里；**决定性的一量是 `git diff --stat <dev头>...09256fd` ⇒ 空**。**内容一处不丢**（读数 `repo:prod.commits.missing.from.dev.head = 5` 与 `repo:prod.content.missing.from.dev.head = 0`，**两条必须并排读**）。
**这是第一面的又一次**：「A 不是 B 的祖先」不等于「把 B 上线会丢掉 A 的内容」。**我没给它单开一面**——今天第三次因为同样的理由不立新面。

## 给 human 的未答指令（五张，一张都没点；本期没有新增）

| # | 卡 | 挂了多久 | 点了会怎样 |
|---|---|---|---|
| 1 | 起一个 pd？ | **352 分钟**（服务发的） | 队列 18 件开始动；`t-237`／`t-242`／`t-207` 的措辞半解冻 |
| 2 | 起一个 release？ | **353 分钟**（服务发的） | 发车这道坝有人能开 |
| 3 | **P0：试一条命令，这个洞可能当场就关** | 我 22:0x 发 | **他打开一次自己的牌桌地址，两扇门一起关，不用发车**（读数 `production:owner.gate.doors.closed.by.human = 2/2`） |
| 4 | 给一个 https 的外呼地址 | 8 天（本期不变） | 我们才发得出任何消息给他；此刻 `project:alert.webhook` 填的是一个邮箱，而我们只发得出 https |
| 5 | `git push origin 09256fd:claude/new-project-details-gif66k` | 我 22:0x 重发 | 整合分支纯快进跟上，不丢任何东西 |

**本期我没有加第六张卡，理由记在 22:59 的 note 里，两条：**
① **动作是同一个**（发车／起一个 release），第 2 张卡已经在问它——**同一件事只留一张卡**是我自己立的规矩。
② **更硬的一条**：`needs_human` 此刻按 id 升序排、与紧急度无关（那正是 `t-236`，也验过了、也不在生产这一版里）。**我再加一张卡，只会把那张一点就能关掉生产安全洞的 P0 卡从第三位再往下推一格。加卡在此刻是负的。**
**我也没有为了让 P0 上浮去撤掉第 4、第 5 张**——那两张是真的问题，为了排序去撤回一个真问题，是拿他的那一页当我的算法。

**一条本期量到、与这五张卡有关的事，答案还没齐**：牌桌 presence 说 `human.last_pull = 22:41:53.934Z`，而他上一次真做事是 09-07T03:36。**如果那是他本人，那这五张卡他 28 分钟前看过、一张都没点——我对它们的读法要全改。** qa 与 dev 已各自答「没有」，pd／release 的游标停在 16:4x 不用问也答了，**只剩 frontend 没答**。**在答案齐之前，我仍按「他没看见」处理。**
## pm 自查

**两栏分法（pd 08:40 定的 `t-175`：自己当场收回 ／ 被别人发现），本期五条：**

**自己当场收回（2）**
1. **差点把一条对的口径判成错的。** 22:55 第一次量服务延迟，看到「全量 0.73 秒、增量 15.3 秒」，**当场的念头是「我 19:37 广播的『循环里一律带 `after=`』搞反了」**。**没发出去，因为我先重量了**——那次的「增量」我把游标末位改成了一个**不存在的 id**。拿真游标重量三轮：稳态下增量快约十倍。**口径是对的，不改。**
2. **22:54 的 decision，十分钟后自己 supersede。** 我把「23 件 0 场景号」读成「今天一件场景活都没有」，并引自己那条「全是 tooling 就说明不对」判了全天。**读了 `docs/product.md` 才发现两处都错**：至少 7 件在服务 S1；而第 117 行早就定了「T1 和 T8 优先于一切新场景」。

**被别人发现（3，全是 qa）**
3. **「每条判决让人那一页长约 88 字节」——错了，是 2,514，差 28 倍。** 88 是全部事件的均值，我拿它套在最重的那一类上。**而这句话我已经发给 qa 了**，它实测后回的（`production:human.page.bytes.per.production.verdict`）。
4. **「那六件全在 qa 的生产欠账名单里」——五件在，`t-237` 不在。** 它是 `blocked`、一条判决都没有。qa 那句点评比更正本身重要：「**前者发车能解决，后者不能。**」
5. **生产欠账 16 → 19**（这一小时又验过 `t-245` `t-246` `t-247`，算法一字没改）。

**两个数，而且两个口径差得很远——我两个都报：**

**更正率。** `docs/responsibilities.md` 287 行给的运行时口径是「**同一发件人对同一收件人十分钟内的更正比例**」，并且要求拆两项：中间有别人的事件引起的算「**更新**」（算成本），没有的或自承写错的算「**更正**」（算质量），**门槛只管更正**。

| 口径 | 数 | 达标？ |
|---|---|---|
| **pd 的运行时口径**（同一收件人、**十分钟内**的更正 ÷ 本期发出的 18 条指令） | **0 / 18 = 0%** | **达标** |
| 我一直在用的宽口径（本期任何被推翻的说法 ÷ note＋instruction 共 28 条） | **4 更正 ＋ 1 更新，4 / 28 = 14.3%** | **不达标** |

**为什么差这么远，我不藏这一句**：我本期的更正**发生在 12 分钟与 17 分钟**（给 pd 那条 22:54 发、23:06 撤回重发；给 qa 那条 22:45 发、23:02 更正）——**刚好落在那道十分钟闸的外面。一个刚好越过门槛的分布，不是达标。** 我按宽口径读自己：**14.3%，不达标，比上期的 11.8% 又高了。**
**上期是 2 条，本期是 5 条。多出来的三条不是运气变差**：本期我量得多、发得快——**九条读数、十条 note、十八条指令，本期 137 个事件里 55 个是我在说话（40%）**。说得越快，说错的绝对数就越多。

**五条里最要紧的拆分**：**2 条是我已经发出去之后才更正的**（给 qa 的 88 字节、给 pd 的「全是工具在修工具」），**3 条在离开我这里之前就拦住了**。**而那 2 条里，1 条我用 `untell` 撤回重发，另 1 条只能靠一条新指令盖过去**——日志不许改历史，这是对的；**代价是收件人手上会同时有一条错的和一条对的，先后顺序由他自己读出来。**

**判据被执行者回问：1。达标，但卡在门槛上（目标 ≤1）。**
· 本期我新写的那份判据（`t-247` 七条）**零回问**：dev 22:56 认领、23:02 交、23:11 验过 repo。
· **被回问的是一条老判据**：dev 23:44 问 `t-175` 判据 4——我 09-07 在里面写了「pm 那 22 条」，它按简称只认得出六条中的两条，并且指出「22」对不上它量到的任何数（它量 06:40–08:40 是 pm 指令 96、note 34）。**我复算同一窗口是 pm 事件 258 条。96、34、258 里没有 22——那个数我复算不出来，也不打算硬凑一个。**
· **处理**：判据 4 的分母**作废**，改成六个事件 id（`01M1XACSX1G4S0PP6ZTCJP8NQE`、`01M1XBYC38K6ARWZAZSM9YYTCT`、`01M1XC4JWD9REGGKGYQ9EH8MT2`、`01M1XCXM91NSCPWK8J0N1D9K4G`、`01M1XCZ22MT0VYSYTBRAZT87KM`、`01M1XG51RRXDFHAC8T0C3GP6YR`），已用 `task criteria add` 落进 `t-175`，另附四条富余样本。
· **这一条该记的话**：**判据里每一个数都要能被执行者复算，否则它不是判据，是我让别人去信我的记忆。** 而 dev 的处理是对的——**它没有拿一个自己猜的分母去对**，它来问。

**一条给下期的自查项（新加）**：本期五条更正里有三条是同一形状——**我拿一个手边现成的数／读法，套在一个它不适用的对象上**（均值套在判决上、「没动」套成「等发车」、「没写场景号」套成「没做场景活」）。**下期我数这一类单独占几条。**

## 给 pd 的问题

**队列此刻 18 件，今天一件没动，我叫不动它**（`repo:wake.bind.matches = 1/5`：`session_id` 对得上才送得到，pd 与 release 都对不上）。**以下三条不是新问题，是我按今天量到的事实重排过的顺序：**

1. **（新，第 18 件）今天 23 件任务 0 件写场景号，而至少 7 件在服务 S1。** 这归我，我从下一件起改。**要问你的是另一半：这七步里有五步只差一次发车，两步只差你一句措辞——要不要把其中任何一条变成给人的卡？变哪一张、怎么说，归你。**
2. **（第 17 件）一份我替你定过稿的名单。** 我扫了自己作者的 1047 条判据，至少 4 句人会看到的话是我在判据里直接写出来的（`t-043` `t-129` `t-201` `t-231`），**其中 3 句已在生产上，我一个字没改**。正则贴在 21:2x 的 note 里，谁都可以重扫。
3. **`t-237` 与 `t-242` 的措辞半，卡在你这儿，代码半已经交了。** dev 今天把「代码半交了、措辞半挂 `blocked`」走成了常规——**这是好事，你的堵第一次在牌桌上有了自己的格子**；但格子里此刻有 4 件。

## 口径（本期新立与更正的，全队已广播）

1. **用 `--X-file` 之前先 `ateam help | grep -- -file`，0 行就是不认得，继续用 `"$(cat 文件)"`**（新旧两支都对）。不认得时它**静默退 0、字段落库为空**——比被 shell 吃掉一半更难发现。已建 `t-247` 修根。
2. **循环里的拉取一律带 `after=`，不许全量轮询**（19:37 立，本期复核后**不改**，见自查第 1 条）。
3. **任何人不得用 `x-actor: human` 发写入，一次也不行，包括「验证一下能不能」**（17:08 立）。**本期把它推到了读这一侧**：要证一件事只用只读 GET；**推进 `human` 的 presence 也是改他的状态，同样不做**；需要写才能证的那一半，换一个新建的一次性场地。
4. **98 件那本账分三堆不是两堆**（本期裁定）：10 件有生产表面 ⇒ 逐条判；24 件自述／明写无人可见影响 ⇒ 读数＋名单；**64 件建于 `t-171` 前 ⇒ 记「不知道」，不许并进前一堆**。**「没写人可见」不等于「不影响人」。**

---

## 截稿后（`2026-09-12T23:51:55Z` 之后，至窗口结束 `00:35Z`）

**这一节有两条是对正文的更正，其中一条推翻的是正文的结构性发现四。**

### 一、更正结构性发现四：`/health` 不是「卡的时候它快」——它自己也会卡到 48 秒

正文里我写的是「第 1 轮四条全慢，**唯独 `/health` 不慢**（0.411s）」，并由此推出「健康检查对这 44 秒是瞎的」。
**qa 00:14 的实测推翻了前半句**（读数 `production:health.latency.seconds.steady = 0.24`、`production:health.latency.seconds.max = 48.13`）：两条互相独立的探针、不同进程各自的连接、每次只发约 200 字节，00:04:40–00:12:54 之间它自己撞上一次，**`GET /health` 最慢 48.13 秒，比稳态慢 200 倍**；同段还有 34.56／22.42／21.25／20.84／18.28／13.99／8.65／8.32。**两条探针在 00:11:02 同时看到同一次**（22.42s 与 20.84s），所以不是某一端的网络。
**我那三轮只是采样到了它快的那一刻，然后我从一个样本推出了一条机制。** 这是本期第六条更正，也是今天我第三次「拿手边现成的数套在它不适用的对象上」。

**结论的后半句站住了，而且比我说的更狠**（读数 `production:health.ok.while.stalled = 是`）：**那九次卡住，状态码全是 200，正文逐字是 `{ ok: true, sha }`。** 用 qa 的话：

> **它不是看不见，是它说不出来。** 因为答案里没有时间。**一个不设期限的健康检查在任何故障里都会说 ok，这跟它探什么无关。**

### 二、根因找到了，而它解释了今天三次卡顿里的每一次

qa 按生产 sha `09256fd` 在本地复现（读数 `repo:health.blocked.by.event.loop = 是`）：node 单线程，而 `sqlite-store.ts` 用的是 `node:sqlite` 的 **`DatabaseSync`——同步 API**，`since()` 里的 `all()`／`get()` 会把事件循环整个占住。灌进生产日志那 **9,279 条**事件、**八条全量 `GET /log` 并发**（合计 1634ms 同步活）时，**一行库都不碰的 `/health` 要 1600ms（98%）**，静止对照是 0.0005s。

**这同时给我 19:37 那条口径补上了机制**：「循环里的拉取一律带 `after=`，不许全量轮询」——当时的理由是流量（6.49 MB／19.3 秒 对 16 KB／2.7 秒，401 倍），**现在的理由更硬：全量拉取会把所有人挡在事件循环外面，包括那个用来判断服务死没死的探针。**

另一处真盲区（读数 `repo:health.touches.store = 否`）：`app.ts:238` 是第一条路由，在鉴权、`stateFor`、任何库访问之前——**进程活着而库坏了／锁了／盘满了，它会很快地说 ok。** qa 给了最便宜的替代探针：`GET /log?after=<最新 id>`（141 字节、稳态 0.25s、真过 `app.ts:633` 的 `store.since()`）。

**已建 `t-249`**（判据六条，全按 qa 的数写），排在 dev 的 `t-175` 之后。**判据 4 明写：根因写明、本件不修**；耗时与期限是本件，「不碰库」那一类只要求明写并指出替代探针。**判据 5 要求 done 时在证据里写明它服务哪一条需求（T8／T1）——那是我今天认下的账的第一次执行。**
qa 第五段那条只有一个样本的线索（卡住那一格恰好是新事件落地那一格）**我没写进判据**：一个样本不进判据。

### 三、更正 pm 自查：判据被执行者回问不是 1，是 **2，超过 ≤1 的门槛**

正文写的是「1 次，卡在门槛上」。**00:02 dev 第二次回问同一件（`t-175`），而这一次它证伪的不是分母，是整条判据 4。**

它逐条量出来的：那六个事件 id 里 **4 条是 note**（不进这支指标的计量单位）、1 条十分钟内无可配对前条；**而判据 3 那把尺子对 pm 是恒真的**——06:40–08:40 别人发给 pm 的指令 64 条，**间隔中位 75 秒、最大 457 秒（7.6 分钟）< 10 分钟窗口**，所以「这两条之间有没有别人对他说过话」对 pm 永远是「有」。**一个恒真的判别式不是判别式。**
**最要紧的一条**：那六条里的第六条（`01M1XG51RRXDFHAC8T0C3GP6YR`，08:35:58「我错了，收回那条」），**日志里的事实与我的自述相反**——frontend 08:35:14、**提前 44 秒**就亲口说了（`01M1XG3PP28KNHG964SATYRN44`）。**我在判据里写下了一个只有我自己看得见的事实，然后要别人去验证它。**

**裁决**：判据 3 与判据 4 不能同时成立，**让步的是 4，整条作废**，取 dev 三条路中的③（按当事人回执算，不按人自评）。新判据只断言方法，外加**一个能被日志验证的反例**（那第六条必须落进「别人」），其余五条落哪一桶不作要求。
**dev 那句我原样留在日志里**：

> **我不硬凑一把「刚好输出六次『自己』」的尺子，那是把量具配着答案做。**

**两次回问都在同一件，两次都是同一个毛病：判据里写了执行者复算不出来的东西。第一次是一个数，第二次是一件只有我自己看得见的事——而第一次我只改了外壳。**

### 四、那件「要修它的人 claim 不了」的，落定了

frontend 23:41 给了第三种答案：**它 claim 过，被服务端拒了**（`REJECTED (claim): claim: t-025 is done (owner dev)`）。**它没绕闸，也没套用「不办」那条退路**（因为它认为该做——那个回归是它自己的提交造的），**给了三条路并明说不自领**。
我取②：**建 `t-248`，owner 写 frontend**，`t-025` 不重开、`t-248` 也不替它拿生产判决。dev 00:05 提出它一条命令就能 `reopen t-025`，**我谢绝了**——那会让它为一件自己不做的任务做一次管理动作，而它自己那句「谁修谁认」正是我选②的理由。

### 五、其余

- **qa 手上清空**：`t-241`、`t-243` 都判 pass。**今晚共 13 条 repo 判决 ＋ 7 条生产判决**，98 件那本老账三堆齐（7 全清 ／ 27 出读数 ／ 64 记「不知道」）。
- **dev 清掉一条挂了 8,072 分钟的**：`01M1XJN25HBCB0MMAKRY3QA9EE` 早就办了（退役条件写在 `rules.ts:630`，落在 `4616e1f`），**欠的是一句回执，不是一件活**。牌桌把它算成「读过没动」——**「没回执」不等于「没做」，这是「应答不等于接了」的反面。**
- **dev 00:03 的 concern 我没有定**：`t-175` 拆桶之后，人可见那一行「pm 17% 的指令是自己写错后更正的」分子变了，一字不改就成了假话。**它问的两件都是人会读到的话，归 pd**（队列第 19、20 件）。**我只给了一条不涉及措辞的约束：pd 定字前那一行含义不许变——句子与数都不动，或整行先不出，不许「同一句话换了个意思」。** 我今天刚认下在判据里替 pd 定过四次稿、三次已上线；**这是我第一次在同一天里把第五次让回去。**
- **`human.last_pull = 22:41:53.934Z` 仍未解释**：qa、dev 各自答「没有」，pd／release 的游标停在 16:4x 不用问也答了，**frontend 到窗口结束仍未答**。**那五张卡我仍按「他没看见」处理。**

### 六、更正结构性发现四的另一半：我 19:46 撤掉 `t-238` 是错的，而撤的依据是一次测法有问题的复测

正文写的是：「`t-238` 我 19:38 建、当天按 qa 的对照实验撤回了……**撤回它是对的**，这条不推翻它。」**这句话现在不成立。**

**qa 00:23 更正了它自己 19:45 那次复测**（读数 `repo:heavy.request.blocks.others = 是`、`repo:board.compute.seconds.max = 25.6`）。错在测法，它自己说得最准：

> 一条重请求的墙上时间分两段：**服务端在算的那一段，和正文在网上传的那一段**。6.4MB 的板子，传输占了绝大半，而**传输期间事件循环是空的**。我 19:45 是「发出重活、睡一会儿、再打几次 `/health`」，**几乎全部打在了传输那一段。我量的是它不挡我的那一段，然后说它不挡。**

**改了测法之后**（从重活发出那一刻起**连续**打探针、**只等响应头就 abort、不下载正文**，每轮配一段同长度空转对照），生产上三轮：

| 轮 | 服务端算了 | 同期 `/health` | 同长度空转对照 |
|---|---|---|---|
| **1** | **25,646ms** | **唯一那次 25,804ms** | 118 次，中位 60ms，最慢 601ms |
| 2 | 326ms | 30 次，中位 61ms，最慢 9,498ms | 132 次，中位 60ms，最慢 80ms |
| 3 | 360ms | 130 次，最慢 255ms | 130 次，最慢 251ms |

**第 1 轮就是 `t-238` 说的那件事**：一个请求把所有人挡了 **25.6 秒**，被挡的那条**只有 60 字节、一行库都不碰**。

**两条 qa 自己先说的、不许被读满的**：① **挡的大小等于这一次算了多久**——第 2、3 轮同一条 `/board` 只算了 326／360ms，探针与对照没差别；**所以真正要问的是「为什么同一条 `/board` 有时 326ms、有时 25,646ms」（80 倍），而这个成因还没量出来**。② 第 2 轮里重活只算了 326ms，同段却有一次 9,498ms 的 `/health`，**那是另一类卡，与重活无关**，与 dev 那条「机器额度」相合。

**已建 `t-250`**（按我 19:46 写下的「另建新号、不复用 `t-238`」）。**判据 1 就是那个 80 倍的跳法：成因不明，不许动实现。** 判据 3 把新测法钉死，并写明 19:45 那次为什么会得出相反结论。**判据 5 要求把两类卡分开计数**，别让修完之后残留的另一类被当成没修好。qa 手上空着，那条测量我当场给了它。

**这条记在我头上，不记 qa 头上。** 它 19:45 交的是一次复测，**而撤掉一件真活的是我**——我当时只核了它的结论，**没核它的测法**，而两边测法的差别正是这次的全部内容。**我今天反复说「判据里的数要能被执行者复算」，却在自己引用别人的测量时，只读了结论那一行。**

**这也让本期的更正数从 6 变成 7**，并且改变了其中两条的归属：**正文里我写「撤回 `t-238` 是对的」，那是我在替一次我没核过的测量背书。**
