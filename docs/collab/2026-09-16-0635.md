# 第九十份：`qa` 判完四件之后又静了三小时，而专为这一格造的 `t-279` 正等着它判

窗口 `2026-09-16T04:35Z → 06:35Z`。**数据在窗口关闭之后量（`06:35:24Z`）。**

**一句话**：**本期 12 条事件、0 判决、0 交付——`dev` 两件交完之后手上再没有能推得动的活，`qa` 自 `03:19` 判完四件后再没写过东西（窗口关闭时 3.26 小时），而等它判的正是那四件里最要紧的四件（`t-257`／`t-279`／`t-280`／`t-278`）。**

---

## 一、指标

| 指标 | 本期（`04:35→06:35`） | 上一份（`02:35→04:35`） |
|---|---|---|
| 事件总数 | **12** | 96 |
| 按角色 | pm 6／dev 5／ateam 1／**qa 0** | dev 44／pm 35／qa 15／ateam 2 |
| 按种类 | note 4／instruction 4／ack 3／reading 1 | task 31／ack 20／instruction 19／note 16／reading 10 |
| **判决** | **0** | 4（全 pass） |
| 交付（`done`） | **0** | 2 |
| 新建任务 | **0** | 1 |
| 接缝 | **0** | 裁 2 条（第七、第八条同源） |
| **等验队列** | **9 件，一件没动**（`repo` 档 4：`t-257`／`t-278`／`t-279`／`t-280`；生产档 5） | 9 |
| 摩擦 | 落 0 条 | 落 0 条 |
| 人那一页 | **6 张，一张没点**，标题第三十一次复查干净 | 同 |
| 生产 | `3e50e5b`（回滚后第三十七次实测没动） | 同 |
| **焦点** | `03:15Z` 重写，窗口关闭时 **3.3 小时**；本期未改，状态无变化需要它改 | 1.3 小时 |

**在场**（`06:35:24Z`）：`pm`／`dev`／`qa` 拉取都在一分钟内；**`qa` 最后一次写入 `03:19:38`，沉默 3.26 小时**；`pd` 缺 **85.9h**、`frontend` **77.3h**、`release` **64.4h**、**人未拉日志 63.9h**。

---

## 二、那一族的第三例，也是最干净的一例

`dev` `06:01` 报了一个数（不催，把判断交回给我），带出了这一族的第三例：

| 那件活修的是 | 挡住它的是 |
|---|---|
| `t-274`：咬 `qa` 的那个洞 | **唯一能验它的就是被咬的 `qa`** |
| `t-280`：接缝税 | **它自己被这条税收了一次**（第七条） |
| **`t-279`：牌桌看不出「有人在听却很久没动、别人全卡在他身上」** | **此刻正需要它说这句话，而它上不了任何人的牌桌——服务端跑生产 `3e50e5b`** |

> **一个系统在修自己的时候，会被自己还没修好的那部分挡住。** 前两例记于 `03:49`，**第三例把它坐实成规律而不是巧合**：修补从内部发起，而内部还在旧规则底下；**修好与生效之间隔着发车，而发车卡在人那一页。**

---

## 三、我没有提前动手，理由与上一次拒绝重写卡片是同一条

`qa` 沉默 3.26 小时、四件等判、我 `04:24` 那条也未 ack。`dev` 把数摆出来时是 162 分钟。

**我按 `05:10` 定的门槛走，不提前**：写入沉默超过 **4 小时**（约 `07:20`）仍无判决，才复查并记一条。
> **`dev` 报的那个数没有改变任何事实，只是把它摆到了台面上——而「代价更清楚了」不是提前动手的理由。** 这条标准我 `09-15 21:13` 是为「要不要重写人那一页的卡」定的；**同一条标准也管我自己什么时候开始催人。**

**而且这一次的底比昨天厚，两头都堵上了**：
· `qa` 自己立了规矩——**重启第一件事不是 `sync`，是列未 ack**；
· **四件我都发给它了**（`t-280` `03:48`、`t-278` `04:24`），**不像昨天那样因为「不打扰」扣着不发**。
**昨天丢十二小时的那个缺口是我造的，今天它不在。** 所以即便它掉了，回来一捞就全在。

**不发第二条指令给 `qa`。**

---

## 四、`dev` 又一次无活可做，而这一次的等是有尽头的

`t-280` 与 `t-278` 交完之后，`dev` 手上一件能推得动的都没有：5 件 blocked 等 `pd` 或 `frontend`，`t-207` 措辞归 `pd`，`t-258` 要生产，`t-266` 另一半归 `pd`。**我照旧不给它找活。**

**与昨晚不同的只有一点，但这一点是全部**：昨晚队列冻住是因为**没有人能判**；此刻四件在判的路上，**所以这次的「没活可做」是一段等待，不是一个死锁。**

**`dev` 的报法本身值得记**：它 `06:01` 那条写着「不是催……怎么处置归你」——**把数摆出来、把判断交回来，而不是自己去催，也不是自己去动。** 这是它今天第二次在该停的地方停住（第一次是 `03:22` 量完两个方向而不自己选）。

---

## 五、与 `docs/product.md` 的对照

- **T7（知道谁在听、谁在做什么）——本期被打穿得最彻底**：`qa` 三小时没写字、循环每分钟在拉，牌桌报 `listening`、`COVERAGE` 一格不报。**补这一格的 `t-279` 已交、等判，而它等的就是此刻不写字的那个人。**
- **T5（互相挡住对方的错误）——本期 1 次，方向反了过来**：`dev` 挡的不是我的错，是**提醒我一个我可能忽略的数**，并明确不替我决定。
- **T12（每项职责都有人担着）——`R14`（验收）此刻声明有人、实际不产出。**
- **T11（团队能改进自己）——本期 0 件交付。** 今天累计八件全是 tooling、零件服务场景。
- **T3（指令送到了、被看见了）——我 `04:24` 给 `qa` 那条未 ack。**

---

## 六、分配预警，五种模式各带数字

| 模式 | 本期 | 说明 |
|---|---|---|
| **重叠** | **1（牌桌自动报，仍是上期那条）** | 关于我 `t-280` 上两条互补决策的**误报**；条件已写死，未达标不立任务。 |
| **低效** | **1（牌桌自动报）** | 等验 **9** 件，**与上期同数、一件没动**。 |
| **无效上下文移交** | **0** | 本期无交付无移交。 |
| **打破独立审核** | **0** | 本期无判决可破；上期那次加固（拒绝授权 `dev` 自裁）仍然有效。 |
| **负载陷阱** | 样本 12 条 < 30，**按第八十五份的门槛不报比率**：绝对数 `pm` 6 条、`dev` 5 条、`qa` **0** 条。 |

---

## 七、pm 自查

**事件数 12 < 30，不报比率。自我更正 0 次；判据被执行者回问 0 次（达标）。**

**本期我做的六件事全是记账与裁断，没有一件是产出**：ack、裁第八条接缝、拒绝授权 `dev` 自裁、记第三例、告诉 `dev` 停手、落第八十九份。**这与第八十六份记过的那个门槛是同一件事**——队伍停住时，牌桌上动得最多的是负责记录的那个。**区别是这次 `dev` 也在场且有判断力，所以 6/12 不是 4/5。**

---

## 八、给 `pd` 的问题

**积压十一条一条未答，`pd` 缺人 85.9 小时。本期不新增。**

---

## 九、口径

**本期无新增。** 本期真正被用到的是两条旧的，各用了一次：
- **「代价更清楚了」不是提前动手的理由**（`09-15 21:13` 为卡片定的，本期用在「什么时候开始催人」上）。
- **把数摆出来、把判断交回来，不自己去催**（`dev` 本期示范，值得写成全队的报法）。
