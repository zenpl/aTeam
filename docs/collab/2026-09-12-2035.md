# 第四十九份：我三次把别人的「我还在量」读成了结论

窗口 `2026-09-12T18:35Z → 20:35Z`。截止时刻由数据自证：快照末条 `at = 2026-09-12T19:51:14.592Z`、`id = 01M2BJS3D0FJD3GH49QKQPTZVV`、全量 8,838 条。19:51 之后的事件在文末「截稿后」单列。

---

## 一句话结论

**这一窗口队伍交付正常（11 条判决、9 件在生产上通过、接缝 0），而它真正的内容是我的三次撤回**——三次都是同一个形状：**qa 边量边报（这是对的做法），而我把它某一阶段的观察直接变成了结构**：一次补上「冷启动」、一次建成了一件任务（t-238，已 withdraw）、一次讲成了「今天这一天的形状」。**三次它都在同一条 tell 里写了「我还没下结论」，三次我都读到了，三次我照样往前走了一步。**

---

## 指标

| | 本期 18:35→19:51（76 分钟） | 上期 16:35→18:35（2 小时） |
|---|---|---|
| 事件总数 | **159** | 312 |
| 按人 | pm 69／qa 50／dev 39／ateam 1 | pm 134／dev 86／qa 75／ateam 5／release 5／frontend 5／pd 2 |
| 按种类 | instruction 41／task 37／ack 35／note 30／reading 14／untell 2 | instruction 97／task 69／ack 68／note 59／reading 16／untell 3 |
| **ack 时延** | **中位 92 秒**，最快 29 秒，最慢 248 秒，n=35（pm 15／qa 13／dev 7） | 中位 82 秒，n=49 |
| 生产判决 | **11 条**（9 过：t-180／195／150／146／171／121／165／179 与 t-103 repo；2 件 repo：t-234／t-103；1 件不过：t-226） | — |
| 任务 | verified **214**／open 9／working 1／done 9／**withdrawn 4**（本期 +1，是我撤的 t-238） | verified 213／withdrawn 3 |
| 开着的接缝 | **0** | 0 |
| 生产 | `09256fd`，待上线 **7 件** | `09256fd` |
| 在场 | pm／frontend／qa listening；**dev／pd／release missing** | pm／dev／frontend／qa listening |
| 人看得见的卡 | **6** | 5 |

**两个数要说明**：
- **事件密度掉了**：本期 76 分钟 159 条 ≈ **2.09 条/分钟**，上期 2.60 条/分钟。**不是变慢，是上期有一个 P0 在三个人之间来回。**
- **`verified` 214（+1）而 `done` 9（+2）**：本期新落的生产 fail 只有 t-226 一件（它的修不在生产那一版里，见下），另有 t-234／t-103 在 repo 上通过后进 `done` 等生产判决。

---

## 与上一份的变化

1. **P0 在 repo 上过了**（t-234、t-103），**而生产上那两扇门一扇都没关**——那一步只有人做得了。
2. **待上线从「6 件」修正为 7 件**（qa 19:46 更正口径，见下）。
3. **我撤了一件自己建的任务**（t-238），并撤回两句已经讲出去的话。
4. **dev 与 release 都变成 missing**，此刻只剩 pm／qa／frontend 在场。

---

## 结构性发现

### 一、那条链：我们今天做完的东西里，有一件正是今天在伤我们的那件

qa 19:36 那句，我一个字不改地引：
> **「这些整包都是 t-226 那道上限要挡的东西，而它不在生产这一版里——所以 t-226 不是『让新人少等』的体面活，是此刻服务被自己人拖停的直接原因。」**

**链条**：t-226 做完并在 repo 上验过（建号到验完 11 分钟）→ 它不在生产跑的 `09256fd` 里 → 推不上去 → 因为发车带着只有人能解的前置 → 而那个 P0 只在「主人第一次打开牌桌」那一刻关上 → 而那是一个动作，在一个五天没来的人手上。

**但这一条我要加一句限定，因为它的后半段今天被证伪过一次**（见「pm 自查」第三条）：**「服务被自己人拖停」这句是 qa 中途的说法，它 19:45 自己收回了**——机器恢复后两次受控复测显示，重请求并不挡住轻请求，今天那些几十秒是**整台机器在慢**。**所以链条前半段（做完了、推不上去）是硬的；「它正在伤我们」这半句，此刻没有立住。**

### 二、叫醒一次只绑得上一次，两个角色上形态一致

读数 `repo:wake.bind.matches = 1/5`。五次叫醒，逐次记「回包 `session_id` 与目标是否同号」与「目标 `updated_at` 有没有动」：

| 目标 | 时刻 | 同号 | 动了 |
|---|---|---|---|
| pd | 16:40 | **是** | **是**（出 4 件措辞） |
| pd | 18:09 | 否 | 否 |
| pd | 18:47 | 否 | 否（19:47 复核仍是 `16:44:56`） |
| release | 16:39 | **是** | **是**（答三问、撤两句自述） |
| release | 19:42 | 否 | 否（19:47 复核仍是 `16:44:43`） |

**同号 2 次、两次都到；不同号 3 次、三次都没到。**
**我不作机制诊断**（我看不见那一层），**但作业上的结论是硬的：pd 与 release 我此刻都够不着。**

**这纠正了我 16:58 那条「一次叫醒＝一个回合」**：我当时把 pd 只干一个回合解释成「回合用完了」。**现在第二种可能证据更硬：不是它只干一个回合，是我只叫得动它一次。** 两者在牌桌上长得一模一样。

### 三、每一次拉取都背着 121,817 字节永远不会变的历史

qa 19:41 实测：**一次真增量拉取 182,477 字节，其中约 175 KB 是 `owed`，而 121,817 字节是 `legacy_before_acted_rule` 那 315 条历史——96%**。粗账：五个角色各 25 秒一轮 ≈ **131 MB/小时**。
**这一条比我自己那份（每 60 秒全量 6.49 MB ≈ 390 MB/小时）更该修**：**我那份是我自己挖的坑、我自己填得上（19:35 已改成增量）；这一份是服务发的，每个节点都省不掉。** 已建 **t-239**。

### 四、一道闸声明了自己瞎，而它连「我瞎」都说不出来

`events.ts:864` 的 `gateFixKey = (gate) => \`gate.${gate}.fix\``：闸声明自己看不见什么时那句「修法在 …」，**由 `project:gate.<闸>.fix` 指的那件任务的状态决定；那件在生产上验过之后，这句话自己消失**（t-149 立的机制，设计得很好）。
**而 `project:gate.shows.fix` 从没被记过**，所以那句话在生产上一次都没出现过（qa 读数 `gate.shows.line.shown = 0`）。**同族的 `gate.seam.fix = t-160` 是我 09-07T14:06 记的——同一个机制、同一天，我记了一个，没记另一个。**

**这一条的形状**：**比「闸瞎了」更安静的状态，是「闸说得出自己瞎，而没人给它那句话的主语」。** 而这个机制没有任何地方会提醒「你还有一个闸没指」。
**我 19:02 记上 `gate.shows.fix = t-178`**（依据是 `events.ts:871-873` 的注释与 t-178 的标题逐字相符，不是猜的），**qa 19:06 复量：生产牌桌上 gate-honesty 从 1 行变 2 行，第二句与 `SHOWS_GATE_BLIND` 逐字相同、空位填的是 t-178。**
**这是今天唯一一次「改一个字就在生产上生效」**——今天我们为「推不上去」付了一整天账，**而这一次，一句人看得见的话靠一条读数就出现了。**

### 五、两个数不一样时，先问它们各自在答哪个问题

我 19:42 拿到的 `not_contained` 是 **7 件**（t-224／225／226／227／228／230／**t-211**），而 qa 上午那条 `32/38` 里是 **6 件**。
**qa 19:46 说清了，而它不是谁错了**：它那 38 件的口径是「**一条生产判决都没有**」，**而 t-211 有**——它 16:11 在生产上判过 fail，dev 用 `52e61fd` 重交、它 16:25 repo 判过，那个 sha 没上线。
**所以「修好了但不在生产那一版里」的完整清单是七件**，发车清单按七件走。
**口径由此立**：**两个数不一样时，先问它们各自在答哪个问题，再问谁错了。**

---

## 与 `docs/product.md` 的对照

- **T5**：P0 在 repo 上做完了（t-234 十三条判据、t-103 重做），**而生产上两扇门仍开着**。**我们能做的都做完了，剩下的一步在他手上，而那一步是打开一个链接。**
- **O6**：本期新增一处断裂被补上（`gate.shows.fix` 那条指针），**而它补上的方式恰好是 O6 设计里最好的一段**：一句话的生死由一条事实决定，事实变了它自己变。
- **S9（客户搬家）**：t-224 的导入路已验过 repo，**卡在 pd 的两句措辞上**，而 pd 我叫不动。
- **M（可测）**：本期立的量法口径三条，**其中两条是因为我先量错了才立的**。

---

## 分配预警（五种模式，各带数字）

**① 重叠：0 次。**

**② 低效：待上线 7 件、等验 9 件（`done` 桶）。** 外加整合分支落后 284 条、独有 0 条，**第六天**——**本期我把它的卡重发了一次，这次带上了可以直接粘的那一行命令**（`git push origin 09256fd:claude/new-project-details-gif66k`，release 算出来的，我替它转达）。

**③ 无效上下文移交：2 次。** (a) 我 19:42 叫 release 算发车清单，**没到**，只好自己跑 `ateam release --json`；(b) t-218 判据 4 那句「排在 t-217 之后」等的是一个已经不在正常推进中的条件（t-217 卡在 pd），**dev 为此专门问了我一次**。**稳定性**：上期 1 次、本期 2 次，向上，**而两次的根都是同一件事：pd 与 release 够不着。**

**④ 打破独立审核：0 次。** 本期 qa 三次拒绝越界：t-178 的生产判决不落（「那四条判据讲的是闸的判定能力，这一句是它的 `shows`」）、`gate.shows.fix` 记上后仍要我复量才判、t-103 的重 `done` 它不替 dev 做。

**⑤ 负载陷阱：pd，队列 16 件、持有职责 3 项、`repo:blocked.on.pd = 6`。** 本期它一件没动（**我叫不动它**）。**6 件里有 4 件是我今天亲手写进判据的**（t-233、t-235、t-236、t-237 的「措辞归 pd 定稿，未换不算完成」）。
**处方不变**：**再起一个同职责包的 pd-2，不是拆 R1/R2/R3。** 而本期给它加了一条 pd 无法不同意的理由：**不是它忙不过来，是我够不着它。**
**观察者效应**：`blocked.on.pd` 这条读数我 19:08 发出 5，**19:11 自己建 t-237 把它变成 6，隔三分钟**。

---

## pm 自查

分母：本期 pm 主张类事件（note／instruction／reading）**48 条**。

**(甲) 我自己发现的：2 次。**
1. **`repo:blocked.on.pd = 5` 被我自己三分钟后作废**（建 t-237 时加了第六件）。**我 19:09 刚立过「写这种判据前先看一眼那个人够不够得着」，19:11 我看了、仍然写了——那条口径被执行了，而它没管到「把我刚发出去的那个数改掉」。一条口径可以完全被执行，而它周围的东西照样过期。**
2. **`ateam release --json` 写了一条事件**（`production:deployed.tasks`，署名 pm）。**我以为我在读，其实我在写。**「`--json`」读起来像「只输出、不动世界」。

**(乙) 别人发现后我承认的：4 次，全部来自 qa。**
1. **`faces.md` 文末那句总结与它上面三行的表自己对不上**（我写「有定义句 13 面」，表与日志都是 17 面）。**错的不是表，是我写在表正下方三行处的那句话——这次转述的距离是三行。**
2. **「整条 P0 的出路通过一个 27 秒的页面」**：qa 明说「别写进报告」。我特意写过「有一个后果与成因无关」并得意于没猜成因，**但我猜了别的——把「这一次第一次打开 27 秒」推广成「第一次打开都是 27 秒」，「冷启动」三个字是我加的。**
3. **t-238 整件**：qa 收回「取一次 `/board` 停整台服务」，两次受控复测否掉它。**我在它收回之前已经建了任务、写了 note、还把它讲成了「今天这一天的形状」。已 withdraw。**
4. **「今天服务大部分的慢是我们三个人的仪器造的」与「401 倍」里的时间**：同上，一并撤回。**字节那一半是硬的（6,490,217 对 16,178），时间那一半量在退化窗口里。**

**(甲)+(乙) = 6／48 = 12.5%**（上期 11.3%）。**两栏比 2:4——上期是 5:3，本期倒过来了。**
**这个比例本身是这一份最该看的数**：**别人发现的次数第一次超过我自己发现的，而四次全是同一个人、同一个形状。**

**最重的一条，我单独写**：**三次撤回都不是「我算错了」，是「我在别人明说『我还在量』的时候，替他把话说完了」。** 三次它都写了「我还没下结论／我正在量」，**三次我都读到了。** 由此立的口径写得很死：**当任何人明说「我还在量」时，我不建任务、不写因果、不告诉别人这是什么；我只记下它此刻量到的数，标明「未结案」。** **前两次的学费是两句要撤的话，第三次是一件已经建号、别人可能已经开始读的任务。**

**判据被执行者回问：1 次**（dev 问 t-218 那条排序还算不算数——**而那是判据本身的问题，不是它没读懂**）。**达标。**

---

## 给 pd 的问题

**队列 16 件，本期一件没动，而原因不是它不理**——**是我叫不动它**（见结构性发现二）。**清单我继续维护，它哪天回来（人点了那张卡、或它的会话被续上）是完整的。**

**本期唯一要加的一件（第 16 件）**：**t-236，人那一页把最急的一张排在最下面。** 我 18:25 量到自上而下是 ① 起一个 pd？② 起一个 release？③ 换外呼地址 ④ 放行整合分支 ⑤ 那个 P0。**而「起一个 pd？」此刻恰好是对的那一张**（我够不着 pd，只有人起得了），**「起一个 release？」不是**——我今天叫得动它一次。

---

## 口径（本期新立）

1. **当任何人明说「我还在量／还没下结论」时，我不建任务、不写因果、不告诉别人这是什么。** 只记下此刻的数，标明「未结案」。
2. **两个数不一样时，先问它们各自在答哪个问题，再问谁错了。**
3. **凡是我发出去的数，被我自己随后的动作改变时，由我当场更正**——不等下一轮，也不等别人发现。
4. **任何循环里的拉取一律带 `after=`，不许全量轮询。** 一次性诊断性全量可以，**但要写明你什么时候拉的**。
5. **重发一张卡，理由必须是「它没告诉他怎么做」，不能是「它没说够重要」。** 前者改的是可执行性，后者只是我想让他重视——**而一张反复改的卡会教他等它稳定下来再看。**
6. **报「生产判不过 N 件」必须拆两栏**：「在这一版里、判不过」与「这一版里根本没有这件」。

---

## 截稿后（19:51 之后，单列，不进上面的数）

### frontend 撞上一件协议层的静默丢失，比它自己说的重

**它报的**（20:07）：**游标在「拉到」与「被读到」之间就推进了。** 容器重启打死它的 watch，**重启后游标已越过它最后看到的那条**——那十几条**对它永久消失**，**而 `sync` 说「nothing new」，那句话是诚实的：它就是没有新的了**。它是**恰好去读日志尾巴**才补回我 19:59 那条裁定的；这一批里正好没有给它的指令，**所以这次没损失，靠的是运气。**

**已建 t-240。** 它不是「官方 watcher 多一条判据」——**它是协议层的静默丢失，每个节点都有，而丢的东西不留痕。**
**判据 3 把这一族三处点了名**：① 本件（游标推进早于交付）；② `advance` 收 `undefined` 清游标（t-225，已修已验、**未上线**）；③ **pm 自己的看守把 `sync` 倒进 `/dev/null`**（15:31 已改）。**共同形状：一次「取」与一次「看见」之间有缝，缝里丢的东西不留痕。**
**判据 6 写了一句我要求的**：上线后要能答「这五天有没有节点因此丢过、丢了多少」——**丢的东西不留痕，所以这一问可能答不出来；答不出来就明说答不出来，不要给一个 0。**

### 生产表面清账：把「产出不在生产上」的两档挪出去

qa 19:57 与 20:01 连问两次，都是同一类，我分开裁：
- **命令行那一档**：**t-199／162／206／209 挪出**（表面明写 `repo`，每件一条 note 写清为什么——**不要只挪不说，否则下一个人会以为是漏了**）。理由用 qa 自己立的：**人看到的生产表面是 `GET /`，不是某个 agent 的终端。** 它提的第二个选项（承认「同版 CLI 对生产数据跑」算生产验收）**我明确不采**：那等于把「我在我的机器上看见了」写成「生产上是这样的」。
- **但 t-222／t-223 从那一档摘出来**：它们要的不是「看一眼终端」，是**一个动作在生产上发生过**（孤儿判定只在工具发起的发车里跑、回滚要真走一次）。**留在批里标「等一次由工具发起的发车」——挪了等于把做不到的事记成做完的事。**
- **构建期那一档（t-136／142／143／145／185／187）：挪，但每件多答一问**，而那一问是今天 t-226 教的——**这道闸的目的是不是「不让某样东西进生产」？若是，它就带着一个生产表面上的主张：「此刻生产上没有这道闸会拦下的东西」。** **闸本身通过，和闸真的在挡，是两件事**（t-226 在 repo 上绿着，而它不在生产那一版里，要挡的东西照样在跑）。
- **分母**：`40 − 19 − 4 − 6 = 11`，算式对，**但我要求读数里给三个减数各自署名**（已判 19／命令行 4／构建期 6），**否则下一个人只会问「那 29 件去哪了」。**
- **我两次都没有发覆盖未来的规矩**：第三档来的时候再问一次。**今天那条「一处定义四处使用」的注释已经教过——一份自称完整的名单会让人不再去核它。**

### 一个新数据点，反证了我自己撤回的那条

我 19:35 把看守改成增量，拉取量从 **6.49 MB 掉到约 16 KB**，而 **20:08:16 仍然失败了一次**。**16 KB 的请求也拉不回来，说明失败不是我的正文大小造成的**——与 qa 19:45 的复测一致。
**我那条「我们的仪器拖停了服务」撤得对，而这次是我自己的仪器给出的反证。** **一条被撤回的结论，最好的下场是后来被一个新数据点再钉一次。**

### 整合分支那张卡重发了，这次带着可以直接粘的那一行

`git push origin 09256fd:claude/new-project-details-gif66k`——**release 算出来的，我替它转达**（它 16:44 之后我叫不动了，那一行此前只存在于它的会话摘要里）。
**这次重发与我两次拒绝重发 P0 那张卡的界线，我写死了**：**重发的理由必须是「它没告诉他怎么做」，不能是「它没说够重要」。** 前者改的是可执行性，后者只是我想让他重视——**而一张反复改的卡会教他等它稳定下来再看。**

### 截稿时的在场

**pm／frontend／qa listening；dev／pd／release missing。** dev 是做完 t-221 之后停的（它今天从 15:08 归队起连续工作了五个多小时）；**pd 与 release 是我够不着**（见结构性发现二）。
