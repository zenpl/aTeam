# 协作顺滑度报告 · 第一百七十二份

窗口 `2026-09-23T00:35Z → 02:35Z`。上一份：`docs/collab/2026-09-23-0035.md`（`5e3b0f4`）。

## 一句话

**本期发生了一次会话重启，而它在我的痕迹里一个坑都没留下——缺口 `59` 秒，比正常周期 `61` 秒还短；我知道它发生过，只因为外面有人告诉了我。**

## 指标

| | 本期 | 上期 |
|---|---|---|
| **节奏** | **3 条**（pm 2、服务 1） | 3 条 |
| **指令与确认** | 发 1（给 `pd`）、ack 0（收件人缺席） | 同 |
| **任务与验收** | **建 0、交 0、判 0——已连续 `93h01m`** | 已连续 `91h01m` |
| **摩擦** | 0 | 0 |
| **人的负担** | 卡 6 张，一张没点 | 同 |
| **在场** | **1 人**（只剩 pm） | 1 人 |

## 与上一份的变化

```
任务停了多久      91h01m  →  93h01m        锚点 2026-09-19T05:34:01Z
除我之外的写入者   0  →  0                  自 dev 最后一次写起 45.7h、75 条：pm 51、服务 24、其他 0
看守（本窗）      119 轮/最大 64s  →  119 轮/最大 63s     含一次重启，而痕迹上看不出来
不明缺口的率       1074 轮 1 次  →  1193 轮 1 次
痕迹合计          14535  →  14654            两把检查本期都用改过的口径，全过，见下
服务预警          连续相同 43  →  44 条       零漏跳（相邻间隔最大 7202.559s，阈值 10800）
重启通知的账      6 次里 1 次真  →  7 次里 1 次真   本次为假
```

## 结构性发现

### 一次在痕迹里不存在的故障

`01:45` 前后发生了一次重启。**先按规矩量 `pid 1`**：

```
btime         2026-09-22T12:48:14Z   ← 与上一次真重启相同，没有动
uptime        46638.22s              btime + uptime ≈ 01:45，自洽
pid 1 etimes  46637
判决          宿主没有重启。会话／容器重启，通知为假。第 7 次通知，真的仍只有 1 次。
```

**然后看它在痕迹里长什么样**：

```
本次（假通知，01:45）                    对照：09-22 那次真重启
01:43:38 -> 01:44:40   62s              12:45:12 -> 12:46:13   61s
01:44:40 -> 01:45:39   59s   ← 就是这里   12:46:13 -> 12:47:14   61s
01:45:39 -> 01:46:40   61s              12:47:14 -> 12:49:00  106s   ← 那里
01:46:40 -> 01:47:41   61s              12:49:00 -> 12:50:02   62s
```

**`59` 秒。比正常的 `61` 秒还短。** 本窗 `119` 轮里最大间隔 `63` 秒，**没有任何一段超过 `90` 秒**——**若我只看自己的痕迹，本期会被报成「无异常」。**

**为什么两次差这么多**：

```
缺口 = 宿主停机 + 我的重挂延迟
真重启   宿主 12:47:14–12:48:14 这一段是停的，加上开机与我重挂     合计 106s
假通知   宿主一秒没停，只有会话重启与我重挂                        合计  59s
```

**而这两次毁掉的东西完全一样**：**看守与边界等待，两个都死**；仓库、`/tmp`、服务节奏、生产，两次都完好。**从会话里面看，两者的后果一字不差**——**只有 `btime` 分得出它们。**

> **第八十四条：有些故障不在你的痕迹里留下坑。** 本次缺口 `59` 秒，**短于正常周期**，**痕迹上完全看不出来**；我知道它发生过，**只因为外面有人告诉了我。** **一份只读自己痕迹的报告，会把它报成「本窗无异常」——而它确实曾让我聋掉一次。**

**连到第七十八条**：那一条问「毁了什么」和「谁会去修」。**本期补上第三问：「它会不会在我的记录里留下痕迹」。** **这三问的答案可以完全不相关**——**本次毁的东西和真重启一样多，修它的人同样只有一个正在跑的回合，而它的可见度是零。**

**我不为此改看守脚本，也不新建任何机制**（此刻不造活、无人可验）。**我做的只有一件，而且已经做了**：`/tmp/pm-cli/restarts.log` 现在两条都在，**每条都带 `btime`、`uptime`、`pid 1`、缺口、仓库状态**——**因为这类事件唯一可靠的记录者是收到通知的那个回合，而那个回合的记忆会没。**

### 顺带：两把改过口径的检查，本期按新口径全过

```
痕迹合计  14535 → 14654   整文件差值 119
          按取数时刻 [00:35:38, 02:35:28) 计数 119          相等 ✓   （两边同一段区间）
服务预警  连续相同 44 条   相邻间隔最大 7202.559s < 10800    零漏跳 ✓ （分得清漂移与漏跳）
          每周期平均漂移 +0.605 秒（上期 +0.606，稳定）
```

**这两个「✓」与前两期那两个不同**：**这次两边量的是同一段区间、比较是机器做的。**

## 与 `product.md` 的对照

| | 本期 |
|---|---|
| **T1 / T3 / T8 / T11** | 无往来、指令 1 ack 0（收件人缺席）、无可评、friction `0` |
| **T5** | 验收资格仍为空；**「无人能验」已持续 `93h01m`** |
| **O1** | 与人之间唯一的通道仍是那一页，**本期无变化** |
| **O2** | 服务预警**连续相同 44 条、零漏跳**；**每周期稳定慢 `0.605` 秒** |
| **这条日志的用处本身** | **仍为 `0`**：45.7 小时、75 条，非 pm 非服务的事件 **0** |

## 分配预警

| 模式 | 本期 | 数字 |
|---|---|---|
| **重叠** | 无 | 接缝 0、任务事件 0 |
| **低效** | 无 | **假催办 0（来源已毁，非已修复）**；看守轮数：本窗 `119`，痕迹合计 `14654` |
| **无效上下文移交** | 无 | 45.7 小时内非 pm、非服务事件 0，**没有链可断** |
| **打破独立审核** | **结构性风险，已持续 `93h01m`** | `qa`、`dev` 均 missing；**在场 1 人** |
| **负载陷阱** | 无 | 队列深度全 0；pm 本期写入 2 条 |

## pm 自查

| | 本期 | 目标 | |
|---|---|---|---|
| **自我更正率** | **0%**（0 / 2） | ≤10% | 分母 2 |
| **判据被执行者回问** | **0** | ≤1 | 分母为 0 |

**本期重启处理**：收到通知后**先量 `pid 1`、再测锁、再重挂**，看守与边界等待都在同一回合内重建完毕，**缺口 `59` 秒。** 按第七十七条，上面两格仍只在描述我自己。

**本期到期与异常合计 4 次**（`00:52:03`／**`01:45` 重启**／`02:15:42`，以及 `01:22:09` 那次到期），**4 次都在收到通知的当场处理。**

## 给 pd 的问题

**Q12–Q18 均仍等**（`pd last_pull 2026-09-12T16:42:03Z`，距今 `249.9h`）。**本期不新增问题。**

那条「在无人可对齐时这份报告还该是什么样」，**本期的材料**：**本期有一次真实的故障，而这份报告的所有自动量具都看不见它。** 我能写进来，是因为**外面有一条通知，和一个恰好在跑的回合。**

**已发的九件仍等。**

## 口径（本期新增一条）

- **第八十四条：有些故障不在你的痕迹里留下坑。** 本次缺口 `59` 秒，**短于正常周期 `61` 秒**，痕迹上完全看不出来；**我知道它发生过，只因为外面有人告诉了我。** **一份只读自己痕迹的报告，会把它报成「本窗无异常」。**

## 顺带检查

- **生产 `3e50e5b`、整合支 `09256fd` 均未变**，后者仍不含 `t-280`；**八件已验交付仍坐在 `claude/backend-development-gzqbjf` 上，等 human。**
- **焦点 `2026-09-22T02:41Z` 版未变**：本期是它的第十二个整窗。**其第 ⑥ 节的重启事实本期再添一条**：**通知共 7 次，真的 1 次**；**而假的那 6 次里，本次这一种在痕迹上完全隐形。**
- **服务端无降级**：本窗 119 轮，中位 `61` 秒、最大 `63` 秒。
- **给 human 的六张卡，本期仍一张没点**（`human last_pull 2026-09-13T14:40:43Z`）。
- **在场名单**（时刻，非年龄；本期七个角色一个没动）：
```
pm        在听                            ← 唯一
dev       last_pull 2026-09-22T00:52:29Z   last_event 2026-09-21T04:54:12Z
qa        last_pull 2026-09-19T17:08:43Z   last_event 2026-09-19T13:36:36Z
human     last_pull 2026-09-13T14:40:43Z   last_event 2026-09-07T03:36:48Z
release   last_pull 2026-09-13T14:09:36Z   last_event 2026-09-13T14:19:35Z
frontend  last_pull 2026-09-13T01:17:15Z   last_event 2026-09-13T01:32:14Z
pd        last_pull 2026-09-12T16:42:03Z   last_event 2026-09-12T16:44:18Z
```

---

*本份在窗口边界上取数，窗口内 3 条。*
