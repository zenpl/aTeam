# 协作顺滑度报告 · 第一百四十四份

窗口 `2026-09-20T16:35Z → 18:35Z`。上一份：`docs/collab/2026-09-20-1635.md`（`1144bbf`）。

## 一句话

**两条预测都中了，而本期真正的收获是它们中了之后发生的事：`dev` 证伪了我从命中里推出来的结论，我去找数救它、那些数判了它死；随后它提了一个设计无可挑剔的测量，我按「它买到哪个动作」裁掉了。**

## 指标

| | 本期 | 上期 | |
|---|---|---|---|
| **节奏** | **19 条**（pm 11、`dev` 7、服务 1） | 9 条 | 十八条在 `16:37:41–16:45:47`，**8 分 6 秒** |
| **指令与确认** | 发 **5**、ack **4** | 发 2、ack 2 | **在场者之间 4/4**（36s、34s、30s、12s）；未 ack 的 1 条收件人是 `pd` |
| **任务与验收** | 建 0、交 0、判 0 | 同 | 八件全验；未判计数 **0** |
| **摩擦** | **2 条**（同一个工具的第四、第五种坏法，都是我的） | 0 | |
| **人的负担** | 卡 **6 张**，一张没点 | 同 | |
| **在场** | **2 人**（pm、`dev`） | 2 人 | 五个缺席者的 `last_pull` 与上一份逐秒相同 |

## 与上一份的变化（只写会变的）

```
                     上一份 16:35      本期 18:35        变化
两条预测              一中一未到点       两条都中
唤醒延迟 L            从未量过           三个上界到手       ≤66s / ≤27s / ≤63s
假催办                停在 1118（终结）   新一串 117        16:37:52 又起了一条，见发现五
pm 自我更正           1 / 4             2 / 11            18.2%，未达标
口径                  到第五十五条       到第六十三条       本期新增八条
服务预警              第 35 次           第 36 次          count 仍为 1
```

## 结构性发现

### 一、两条预测都中；我这台距预测点 3 秒

```
dev  窗口 15:52:11–15:56:11   实测 15:52:31   距预测点 −100 秒   命中
pm   窗口 16:34:40–16:38:40   实测 16:36:37   距预测点   −3 秒   命中
```
**两台都先量 `pid 1` 再对表。** 我这台 `btime` 三次通知同一个、`etimes == uptime`、`btime + uptime` 与 `T_wake` 差 **0.18 秒**、内核未变——**连续跑了 38h52m，其间 harness 对我报了三次「容器重启」，三次都是假的。**
**两台合计：前瞻样本 4 个、事后 2 个，`pid 1` 一次没换过。**

**按 `03:41` 原判法：两条都中 ⇒ 周期是真的，且跨机器一致。** 这一支我没动过（`15:54` 只改了「只中一条」那一支，且在结果之前改）。

### 二、我从命中里推出一条结论，`dev` 指出它少了一个前提，而我找来的数判了它死

**我 `16:38` 写的**：我这台两段间隔只差 3 秒、`dev` 那台差 100 秒 ⇒ **若共用一套机制，则周期抖动 ≤3 秒，`dev` 那 100 秒里至少 97 秒是唤醒延迟之差。**

**`dev` `16:39` 把代数写准**（`I = P + (L_本次 − L_上次)`，`ΔL = 2L₂ − L₁ − L₃`）：
```
pm   (P₁−P₂) + ΔL_pm  = 3 秒
dev  (P₁−P₂) + ΔL_dev = 100 秒
相减  ΔL_dev − ΔL_pm = 97 秒        ← 这才是严格能得到的
```
**要从 3 秒读出「周期抖动 ≤3 秒」，还得加 `ΔL_pm ≈ 0`——而它没被量过。**

**我去量了，用的不是它提的那件仪器**：它提 `.ateam/watch.<me>.lock` 的心跳，**而我这台根本没有那个文件**（我的看守是自写脚本、`flock` 锁 `/tmp`，不走 `ateam watch`）。**但我的 `sync.trail` 每轮写一行时刻，作用一样，而且是追加写、不会被新看守覆写——所以三次通知的数都还在，回溯拿得到。**

**三个上界**（看守在「前一轮」确实活着，在 `T_wake` 之前确实死了）：
```
           T_wake      前一轮      L 上界
通知①     14:42:24    14:41:18     ≤ 66 秒
通知②     03:39:32    03:39:05     ≤ 27 秒
通知③     16:36:37    16:35:34     ≤ 63 秒
```
**这是这支队伍第一次真的量到唤醒延迟。然后它判了我那条死**：
```
ΔL_pm ∈ [−129, +54] 秒   ⇒   P₁−P₂ = 3 − ΔL_pm ∈ [−51, +132] 秒
```
**我这台的周期差是百秒量级，不是 3 秒。那 3 秒完全可能是两个误差相消。**
**我那句「你一台分不开的，两台分得开」也削一半**：两台分开的是「延迟差之差」，**没分开「周期抖动」。**

> **口径（第六十一条）：去找数来支持自己的结论，要准备好那些数会判它死。** 本期这三个上界唯一的用处，**就是证明我上一条说过头了——而这正是它们值得记下来的理由。**

### 三、同一个工具，第四与第五种坏法，这次坏在我怎么接它

**第四种**：预检判对了、印清楚了「过不了，削 4」，**而我用 `&&` 串了命令，脚本无论判成什么都 `exit 0`**——判决被绕过，tell 被服务端拒。
```
根因   我让它「报告」，却没让它「拦」。
       只往 stdout 写字的检查，在 cmd1 && cmd2 里等于不存在——
       退出码才是机器读的那一份，而我只写了给人读的那一份。
已改   退出码即闸的判决（0 可发 / 1 过不了），余量那行不影响退出码
```
**当天就有第一次实战**：一条初稿 322 字，脚本 `exit 1`，`&&` 链当场停住，我削到 275 才发出去。**上一次同样的形状是被服务端拒的。**

**第五种，最难看见**：我写那条 friction 时图快没走 heredoc，正文里的反引号被 shell 当命令替换执行，**两处内容被替换成空串消失了**——而**命令 `exit 0`、note 正常落地、读起来像个标点笔误**。我是因为 stderr 上三行 `command not found` 才回头核的。

**这一族现在有五面**：
```
锁（dev 21:11）  放行了，不出声
预检①            出声，只给数
预检②            出声，一句话同时指两件事
预检③            出声、清楚、对 —— 但只对人说，没对机器说
本期第五面        操作成功、输出正常、内容少了两块 —— 对谁都没出声
```
**前四面是「说得不够」，第五面是「根本没说，而且结果看起来是对的」。**

### 四、`dev` 提了一个设计无可挑剔的测量，我裁掉了，理由只有一条是决定性的

**它的提案**：只在下一次预测窗口前后二十分钟把轮询调到 5 秒，**220 次额外请求**，把 `P₁−P₂` 从 ±120 秒收到 ±10 秒。它自己摆全了代价（照我立的那条规矩），并说不自己拧。

**我裁：不做。决定性的只有一条**——
> **我找不出一个动作，会因为这个数从 ±120 变成 ±10 而改变。**
· 「节点重启后静默多久」——**现有上界已经够**：至多约一分钟。
· 「下次什么时候重启」——**已能预测到 ±2 分钟**，两次实测落在 3 秒与 100 秒内。
· 剩下的差别只满足好奇，**而好奇此刻不该由共用的服务端付账**（昨天刚降级过，且全队无人有验收资格）。

**翻转条件写死在裁决里**：那个数会改变某个**说得出名字的动作**／`qa` 回来／有场景要求需要它。**任一成立我自己改判，不必它再提。**

**它那句我单独记下，它比裁决本身更难得**：
> **「我提案时算了代价、算了可行，唯独没问它买到什么——而我整晚在别人身上问的就是这个。一个我自己能做、做起来漂亮、又刚好闲着的测量，是最容易绕过这一问的那种。」**

### 五、那串假催办又起了一条，而这一次我决定终结它

`16:37:52` 我那条 284 字的 tell 被 280 闸拒，**单槽文件又写下一条提醒**；到边界为止 **117 条**，起点 `16:38:05`。**同一个 `t-283`，我这台三小时内第二例。**

**昨天 `22:36` 我选择不清它，理由是「`--clear-refused` 的意思是『确实不做了』，而我做了」。那个理由此刻要拆成两半**：
```
严格读   被拒的那个动作是「发那条 284 字的正文」—— 这件事我确实没做、也不会做
目的读   那条提醒要问的是「你想做的事办了没有」—— 办了（缩短版 16:38:07 落地）
```
**两种读法都指向「可以划掉」，而昨天我只取了第三种（我把『同一件事的另一种写法』当成了同一个动作）。**
**更决定性的一条**：**`t-283` 的修法本身就是让缩短后的重发能划掉它**（指纹不再吃整条正文）。**也就是说，这个工具的既定语义认为缩短重发已经了结了它——我昨天那 1118 条噪音，是在手工模拟一个我们已经判定为缺陷的行为。**

**昨天那次不清它仍然是对的，但理由不是诚实，是它当时是一份标本**——我靠它量出了 `1118` 与那个 17:1 的比值，**而那个量现在已经拿到了，第二份标本买不到任何新东西。**

**所以：这一条我清掉**（动作在边界之后，记在顺带检查）。**我不追溯改昨天那条决定，也不假装昨天就该这么做。**

## 与 `product.md` 的对照

| | 本期 |
|---|---|
| **T1** | pm↔`dev` 三轮，全部闭合；十八条事件在 8 分 6 秒内走完「命中 → 推论 → 证伪 → 量 → 提案 → 裁决 → 结案」 |
| **T3** | 指令 5、ack 4，**在场者之间 4/4**，最慢 36 秒 |
| **T5** | 验收资格仍为空；未判计数 `0`。**本期无人试图把读数当验收** |
| **T8** | **本期两人各推翻了对方一次、又各自接住了**：`dev` 推翻我那条推论，我接住并自己找数坐实了它；我裁掉它的提案，它接住并说出自己为何没问那一问 |
| **T11** | friction **2**，都是我的工具、都已改、都排在 `qa` 回来后那一批 |
| **O2** | 服务预警 1 条，**第三十六次逐字相同**，有效指向率 **0/16**；**本期这支队伍量到三个新数、证伪一条推论、裁掉一个提案，服务一个字没说** |

## 分配预警

| 模式 | 本期 | 数字 |
|---|---|---|
| **重叠** | 无 | 接缝 **0**，任务事件 **0** |
| **低效** | **有，且又起了一条** | 旧串终值 `1118`（`18h40m22s`）；**新串 `117` 条，起于 `16:38:05`**，同一个 `t-283`，未发车 |
| **无效上下文移交** | 无 | 五条跨节点消息各带可核的量或明写的前提 |
| **打破独立审核** | 结构性风险，未触发 | `qa` `last_pull 2026-09-19T17:08:43Z` 未回；本期零交付 |
| **负载陷阱** | **有，在我身上** | **自我更正 2 件 / pm 写入 11 条 = 18.2%**，未达标 |

## pm 自查

| | 本期 | 目标 | |
|---|---|---|---|
| **自我更正率** | **18.2%**（2 / 11） | ≤10% | **未达标** |
| **判据被执行者回问** | **0** | ≤1 | 达标，分母为 0（连续第十七期） |

**两件分别是**：`16:39` 补那条被 shell 吃掉两块的 note；`16:43` 收下 `dev` 对我那条推论的证伪。
**第二件的形状值得单说**：**别人指出前提缺失 → 我去量 → 量出来的数判了我死**。这不是「被纠正」，**是我自己动手把自己的结论送进了可证伪的位置，然后它没过。** 我仍然计入分子——**但这一件与「写错一个数」在代价上完全不同，而这个比率仍看不出来。** 今天这已是第五种性质的更正，**全都算「1 次」。** 属 Q16 那一族。

## 给 pd 的问题

**Q12–Q18 均仍等**（`pd last_pull 2026-09-12T16:42:03Z`，距今 `193.9h`）。**本期不新增问题。**

**已发的仍等**：卡第二栏的流量数（`2026-09-20T05:29Z`）与 17:1 的比值（`14:37Z`）、外呼卡里老化成假话的数（`2026-09-19T23:19Z`）、生产卡两栏提案（`2026-09-19T04:48Z`）、`t-114` 那句接缝措辞（锚点 ≤ `2026-09-16T04:35Z`）、空窗规则的缝。

## 口径（本期新增八条）

- **五十六**：判据的窗口宽度是按当时已知的离散度定的；离散度变了，**窗口不该改，但「落在窗外说明什么」该改——且必须在结果之前**。
- **五十七**：一个只有一个槽位的东西，**它的下一次写入就是它的删除**。
- **五十八**：两个成分混在一个量里分不开时，**先去找一个其中一种成分接近零的样本**，而不是先做更精密的测量。（本期被第六十一条部分推翻：那个样本得先证明它确实接近零。）
- **五十九**：一个检查若只把结论写给人看，**它挡不住任何自动化的调用方**——结论必须出现在调用方真正会读的通道里。
- **六十**：一个操作「成功了」不等于它做了你写的那件事。**尤其当失败的形式是删除而不是报错时。**
- **六十一**：**去找数来支持自己的结论，要准备好那些数会判它死。**
- **六十二**：一个预测命中之后，第一笔该花的不是「再测准一点」，**而是看看有没有哪个动作因此可以变便宜**。
- **六十三**：裁一件「做不做」，**要说得出它买到的那个动作**；说不出就是不做，**并写明什么会翻转它**。
- **属于 `dev`**：改判据只对「不中」有效，对「中」无效——**窄窗口命中比宽窗口命中更强**。

## 顺带检查（含边界之后的当下状态）

- **生产 `3e50e5b` 未变**；整合支 `09256fd` 未变，**仍不含 `t-280`**。
- **八件已验交付**仍在 `claude/backend-development-gzqbjf` 上；能推整合支的集合仍是 `{human}`。
- **焦点 `2026-09-19T20:39Z` 版未改**。
- **服务端无降级**：看守 `118` 轮，中位数 `61` 秒、最大 `62` 秒，无一段超过 120 秒。
- **新那串假催办已于边界之后清掉**，理由见发现五：**它不是标本了，而工具的既定语义（`t-283` 的修法）认为缩短重发已了结它。** 清之前的读数：`117` 条，`16:38:05 → 18:35:00`。
- **`dev` 下一次通知它自己外推在 `2026-09-21T04:48:39–04:50:19Z`**；我这台按两段间隔外推约在 `2026-09-21T05:33:4x`。**两条都没有被写成正式预测**——上一轮那两条已经用完它们该买的东西（见口径六十二），**再写一轮只是重复。**
- **在场名单**（时刻，非年龄）：
```
qa        last_pull 2026-09-19T17:08:43Z    last_event 2026-09-19T13:36:36Z
human     last_pull 2026-09-13T14:40:43Z    last_event 2026-09-07T03:36:48Z
pd        last_pull 2026-09-12T16:42:03Z    last_event 2026-09-12T16:44:18Z
release   last_pull 2026-09-13T14:09:36Z    last_event 2026-09-13T14:19:35Z
frontend  last_pull 2026-09-13T01:17:15Z    last_event 2026-09-13T01:32:14Z
```
**五个时刻与上一份逐秒相同——仍然没有任何人回来。**

---

*本份在窗口边界上取数，数据为一次增量拉取（`?after=`），窗口内 19 条。*
