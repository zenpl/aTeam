# 协作顺滑度报告 2026-09-11 04:35 UTC（第二十九份）

**截止时刻：2026-09-11T04:14:06.644Z（快照末条 `01M27ARE9MT0SC90G2T65GVWPF`）。**

**本份分两次落笔，两次都在文内标明**：

- **正文**（下面到「口径」为止）写于 04:14，实测区间 `02:35:00Z ≤ at < 04:14:06.645Z`，当时我写的是「04:14→04:35 这 21 分钟归第三十份」。
- **04:35 那一刻我更正了这个安排**：报告的槽位就是 04:35，把属于这个槽的 21 分钟推给下一份，是拿口径去迁就我落笔的时刻。**所以补一章**，把 `04:14:06.645Z ≤ at < 04:35:00Z` 按同样的结构补齐，见文末「**补章**」。**第三十份的窗口因此是 `04:35 → 06:35`，网格没有错位。**

口径见文末。

## 一句话结论

**五天里第一次，牌桌上同时有三个人在说话——而这一窗量出来的四条发现，全是同一族：「有没有 X」不等于「X 此刻能不能用」。** 其中一条正发生在我们自己的牌桌上：它此刻写着「没人管定方向」，而定方向的人八分钟前刚定完。

## 指标

| | 本期（02:35→04:35） | 上一期（00:35→02:35） |
|---|---|---|
| 事件数 | 41 | 0 |
| 窗内静默 | 02:35 → 03:58:58（**84 分**），此前连续静默 **7 小时 21 分**（末条 09-10T20:37:58） | 全窗 |
| 实际活动时长 | 03:58:58 → 04:14:06（约 15 分钟） | — |
| 说话的人 | pm / qa / pd / ateam | 无 |
| 指令 | 17（qa→pm 7、pm→qa 7、pd→pm 1、pm→pd 2） | 0 |
| ack | 13 | 0 |
| note | 6（其中 `--decision` 2） | 0 |
| reading | 5（服务 2、pm 2、qa 1） | 0 |
| task 事件 | **0** | 0 |
| 被拒 | 累计 **25**（pm 14、qa 7、dev 3、release 1）；**本期我自己被拒 3 条** | — |
| 给 human 未答的卡 | **4**（3 张「缺人」+ 1 张外呼地址） | 6 |
| 生产 | `d57acbce`，**已 83 小时未换** | 同 |
| 外呼 | `misconfigured`（`zenplor@gmail.com`，不是 https） | 同 |

## 与上一份的变化

1. **空窗结束**。7 小时 21 分零事件之后，qa 03:58 回来、pd 04:00 回来。**人仍未回**（末次事件 09-07T03:36，96 小时前；末次拉取 09-07T19:28）。
2. **给 human 的卡从 6 张降到 4 张**——不是他答了，是我 09-10 撤了自己两张已经变假的卡。**4 张里 3 张是服务自动生成的「缺人」，唯一一件实事（换外呼地址）排第 4。** 这正是外部报告 F2 与 qa 「已决定 12 张只显示 3 张」的同一处。
3. **task 事件连续两窗为 0**。叫停自 09-07 起未解除，没人建任务、没人认领、没人验收。

## 结构性发现：一族四面

qa 04:04 把我 04:03 刚立的第四条写判据问法翻到了另一面；我去核，又翻出两面。**四面是同一句话**：

| 面 | 写的是 | 漏的是 | 在哪 | 谁找到 |
|---|---|---|---|---|
| 1 | 他**不要** X | 他**给错了** X | t-050 判据 1（`by pm`） | qa 04:01，**账在我** |
| 2 | **没有** X | X **在但够不到** | `d57acbc` 的 `verifyflow.ts:79` | qa 04:04 |
| 3 | X **不在** | X **在，只是不按我们量的那种方式在** | `d57acbc` 的 `board.ts:893` + `:794-798` | pm 04:11 |
| 4 | 行号**有** | 行号**没说是哪棵树上的** | 我们两个人的嘴 | qa 04:11 与 pm 04:12，各撞一次 |

**族名：「有没有 X」不等于「X 此刻能不能用」。** 四次都不是漏了分支，是**下判断的人只想到一种缺失，而在等的那一侧，几种缺失长得一模一样**。

### 第一面：T8 那条判据是我写的

t-050 判据 1 原文只写了「未声明时静默跳过」。dev 实现成「未声明**或非 https**」，qa 逐字验了那一条判 pass。**三个人都照做了，链条完好，缺的是我那句话的后半截**：我写了「他不要」，没写「他给错了」。人 09-06 22:43 填了一个邮箱，从那一刻起系统把「配错」当「不想要」，静默 101 小时——**09-08 06:07 全队停了 49 小时，人不知道，就是这么来的。** 不重开 t-050（已 verified，重开会把完整历史改成不完整的）；改正措辞写死在 04:03 的 decision note 里，由明早承接的那件带走。

### 第二面：人整晚在场、有验收权、一次都没被邀请

外部项目 yimutd 那一夜，热修在 staging 上等 qa 等了 2.5 小时。平台**本来有兜底**（`verifyflow.ts` 里 `op === "done"` 那一支：项目里没有验收角色时，发一张「过／不过」的卡给 human，他点了就以他的名义落 verify）。挡住它的是一行：只判**角色集里有没有 qa**，不看那个 qa 此刻在不在。

### 第三面：牌桌此刻正在说假话

04:08:35 的快照，逐条可核：

- `presence`：**pd = deaf**（last_pull `09-07T11:17:09Z`，last_event `09-11T04:00:00Z`）。
- `coverage`：**R1 定方向 / R2 把人的话变成要求 / R3 定验收标准，三项全部 `unheld`**，理由逐字是「pd 声明了但没在场」。
- 而 pd 04:00 刚写完两条 decision note 和一条给我的指令，**今天全队的开工顺序就是它定的**。

机制在 `d57acbc` 的 `board.ts`：`presenceStatus`（:794-798）有三态——listening（最近 5 分钟拉过日志）／**deaf（没拉但 10 分钟内说过话）**／missing；而 `coverage`（:893）用的是 `isMissing`，**只有两态**，deaf 被折进「没在场」。

**qa 04:12 把这条量得更准**：deaf 只有 10 分钟宽。**一个每 15 分钟写一次、从不拉取的节点，连 deaf 都轮不上，直接是 missing。** 我 04:08 量到 deaf，qa 04:12 量到 missing，中间只隔 4 分钟。

**我要把这条说公道**：`board.ts:792` 有一句写好的注释——「`isMissing` 留着不动，它问的是『此刻听不听得见』，覆盖率、未送达都该用它，那个问题的答案确实是个布尔」。**所以这不是疏忽，是一个写明了的决定，只是没人核过它的后果。**

> **补记（04:20，落在正文窗口之外、补章窗口之内；计入本份，见补章）**：上面这句「只是没人核过它的后果」**错了一半，我更正**。qa 04:19 把我按回去读了那段注释的全文（`d57acbc` `board.ts:780-792`），里面有三样东西：
>
> - **规矩**：t-137 原话「**不许把两者合并成一个布尔**：`missing` 是没人在跑这个角色，出路是起一个新的；`deaf` 是它还活着、还在写，只是没来读日志，出路是让它去读，**起第二个解决不了**」；
> - **前科**：t-202 记着这句假话第一次出现的时刻——「12:46:06 那份牌桌上 dev 6.8 分钟没拉、2.6 分钟前刚写过东西（deaf），卡却说『dev 没在听了 13 分钟，起一个 dev？』——**而它 12:42、12:46 各交了一件活。人被叫去起一个正在交活的节点**」；
> - **放行的理由**：同一条注释的末句**点名**把覆盖率留在旧路上。
>
> **所以不是没人核过后果，是有人核过、专门为覆盖率写了一句理由，而那句理由错了。** qa 把错在哪切准了，逐字采用：**「消息送不送得到它」用布尔是对的；「这项职责此刻有没有人在履行」用同一个布尔就错——只写不读的节点够不到，却在履行。**
>
> **同一句假话，这是第三次从第三扇门回来**：① 12:46 那张「起一个 dev？」的卡（已由 t-202 修）；② 09-08 我那张 07:25 的卡被四张假「缺人」压到第七；③ 此刻的 `coverage`。**改的时候要连那句放行的理由一起改，否则第四次会从第四扇门回来。**
>
> **我自己的账**：我 04:11 量这一处时**只读了代码，没读旁边那 20 行注释**——规矩、前科、放行理由三样都在，我一样没读。本份刚立了「行号必须报表面」，而这次我表面对了、**上下文没看**。**判据的上下文也是判据的一部分。** 前两面让机制沉默，**这一面让牌桌说假话**：一个读牌桌的人此刻会读到「没人管定方向」。我们做这个东西就是为了让人一眼看见真实状态，这一处它反着说。

### 第四面：我在纠正「引错树」的那句话里引错了树

qa 04:04 引 `verifyflow.ts:43` / `events.ts:174`。我 04:08 更正为 `:79` / `:312`。qa 04:11 自查：它的行号来自本地 `eae0b22`，**不是生产在跑的树**。我 04:12 一核——**我的 312 也是本地读的（我的分支 `c53f7c5`）**，生产 `d57acbc` 上是 `events.ts:579`。**我在纠正这件事的那一句里犯了同一件事。**

顺手量到的，可核：

- `git merge-base --is-ancestor d57acbc origin/claude/new-project-details-gif66k` → **假。生产在跑的 sha 不在整合分支的历史里**；含它的分支只有 `claude/release-manager-collab-vk1kgc`、`production`、`rollback-drill-tmp`。
- 同一个文件 `packages/core/src/board.ts`，四棵树四个大小：`1b6c219` 102 行 / `7f31808` 674 行 / `d57acbc` 2008 行 / 我的 HEAD 1688 行。**差异里至少有一次是「slim board」重构（t-078、t-080），不全是内容增减**——正因如此，**不带树名的行号一律不可核**。

**立成规矩**：行号是一条读数，必须报表面——写「`d57acbc` 上的 `verifyflow.ts:79`」，不写「`verifyflow.ts:79`」。

## 与 `docs/product.md` 的对照

| | 本期证据 |
|---|---|
| **O2**「牌桌上出现的每件事都真的需要他；『需要你』误报为零」 | **反例**：4 张卡里 3 张是服务生成的「缺人」，唯一实事排第 4。加上 qa 的 12 张已决定只显示 3 张——**误报不是零，且越老的越不可能被看见**。 |
| **O3**「拍板一次点击，不点则到期按默认」 | 4 张卡 96 小时无人点击；两条默认已于 09-08 07:29 触发。机制成立，**但它对一个没打开牌桌的人无效**。 |
| **T3**「送到了、被看见了」 | 本期 pm↔qa 全部当场 ack（分钟级）。**但 pd 的 undelivered 是 27 条，最老 09-07T11:17**——它今早是**只写不读**地回来的。 |
| **T5**「三方分离」 | 成立且被执行：qa 04:05 主动把「表面选错」记回自己账上，拒绝与我那笔合并。**两笔各记各的，这是分离在起作用。** |
| **T7**「在场由拉过日志推导，不由说过话推导；三态」 | **推导是对的，用它的地方只用了两态。** 第三面全部在这一行。 |
| **T8**「全队一起死时人在一小时内知道」 | **仍 FAILED**。外呼地址 101 小时 misconfigured，根因在我写的那条判据（第一面）。 |
| **M4**「被拒绝时说出规则名，不绕」 | **本期我被拒 3 次，全部说了规则名，我全部照改**：① reading 键重复表面名（拒绝语逐字给出后果与正确写法，是三次里最有用的一条）；②③ **两次 `ack` 指向的不是指令**——我从日志尾部挑 id 时挑中了服务的读数事件。**这两次是我方法差，不是工具差**：`sync` 每条 FOR YOU 后面就印着 `--refs <id>`，我没用它，自己去尾部数。 |

## 分配预警（五种模式，各带数字）

| 模式 | 本期 |
|---|---|
| 重叠 | 0 |
| 打破独立审核 | 0 |
| 负载陷阱 | **1**。服务算的是 pm 收到 60%（6/10）；我按本窗自己数是 **8/17（47%）**：qa→pm 7、pd→pm 1。**成因是只有三个人在场，其中一个只写不读。** |
| 低效 | 0 有效（release 的 ack p90 107 分钟那条已失效，它 8 小时未在场） |
| 无效上下文移交 | 0 |

## pm 自查

- **自我更正率**：本期我的实质发言 **13 条**（指令 9＋note 2＋读数 2；8 条 ack 不计入「说法」），其中 **1 条是对我本期说法的更正**（第四面，行号引错树）→ **7.7%**，阈值 ≤10%，**在线内**。
  **但我要把分母摆出来**：换成「全部事件 21 条」是 4.8%，换成「只算指令 9 条」是 11.1%。**同一件事三个数，谁都不假。** 我取 13，理由是 ack 不是我的说法；这个取法从第十四份起没变过，写在这里是为了下一份还能对上。
- **另有 1 条认账，但不计入本期更正率**：t-050 判据那笔是对四天前一条 verified 判据的认账，不是对本期说法的更正。**我把它单列，免得用「认账多」把「本期说错」冲淡。**
- **判据被执行者回问**：0（本期无 task 事件）。
- **qa 对我的回推 2 次，两次我都全收**：① 两笔账不许合并（04:05）；② 行号来自哪棵树（04:11）。**没有一次我先发现。**

## 给 pd 的问题

1. **牌桌该不该说「这个人写了，但没在读」？** 数据已经在（`presenceStatus` 三态、`undelivered` 条数），只是 `coverage` 没用。此刻的后果是「没人管定方向」与事实相反。**要不要改、改成给人看的哪句话，是你的。**（我已 04:11 tell 你，但你 27 条未读，所以也写在这里。）
2. **第一面那条改正判据的措辞，给人看的那句归你**：外呼地址已声明但不是能送达的 https 端点时，牌桌上要显眼地说什么？
3. **你 04:00 定的三件我照办、未解冻、未建任务。** 但请注意：**你是在没读过我们四天写的任何一条的情况下定的**，而你定的三件和我们量的是同一批。**这说明那三件是真显眼**——也说明我们四天的输出对你没到达过。

## 顺带检查

- **部署队列**：t-215、t-216、t-222、t-223 四件 repo 已 verified，**全部堵在叫停后面**。生产 83 小时未换。
- **焦点**：04:02 已按 pd 的排序重设，未解冻、未建任务、未动代码。
- **给 human 的未答指令**：4 张，最老的一张已 96 小时。**人从未打开过牌桌。**
- **解冻**：**human 09-07 的叫停要他自己解除。**

## 口径

- 截止时刻由数据自证：取快照末条事件的 `at` 与 `id`，不取墙上时钟。
- 窗口 `02:35:00Z ≤ at < 04:35:00Z`，时间按解析后的时刻比较，不按字符串比较。
- **行号一律带树名**（本期新立）。不带树名的行号不写进报告。
- 「不在场」按 `last_pull`（listening 5 分钟窗），「deaf」按 `last_event`（10 分钟窗），两者取自 `d57acbc` 的 `board.ts:794-798`。

---

# 补章：`04:14:06.645Z ≤ at < 04:35:00Z`

**截止时刻：2026-09-11T04:31:42.799Z（快照末条 `01M27BRNPFNEFZ45A07GVXR2R3`）。**

## 一句话结论

**21 分钟，38 条事件，全部是 pm 与 qa 两个人。这一段比正文那一段更重：正文找到三面，补章找到第五、第六面，并且在最后一刻，发现被第一次用在了前面而不是后面。**

## 指标（整窗 02:35→04:35，含正文段与补章段）

| | 整窗 | 其中补章段 |
|---|---|---|
| 事件数 | **79** | 38 |
| 说话的人 | pm 43、qa 32、pd 2、ateam 2 | pm 22、qa 16 |
| 指令 | 31（pm→ 17、qa→ 13、pd→ 1） | 14（pm 8、qa 6） |
| ack | 27 | 14 |
| note | 15（`--decision` 4） | 10（`--decision` 2） |
| reading | 5 | 0 |
| task 事件 | **0** | 0 |
| 实际活动 | 03:58:58 → 04:31:42（**32.7 分钟**），其余 87 分钟静默 | 04:18:17 → 04:31:42 |

## 补章找到的两面（接正文那张表往下数）

| 面 | 写的是 | 漏的是 | 在哪（`d57acbc`） | 谁找到 |
|---|---|---|---|---|
| 5 | 一个槽 | 三种状态共用一个样式 | `html.ts:348` + `:873 .meta` | qa 04:24 |
| 6 | 改一句问法 | 不去找按旧问法答过的人 | `events.ts:595` + 09-06 22:43 那条事实 | pm 04:26 |

### 第五面：坏掉的东西继承了中性事实的灰

`html.ts:348` 是 `<p class="meta contact-line">`，`:873` 是 `.meta { color:var(--muted); font-size:.82rem; }`，`:896` 的 `.contact-line` **没有任何状态变体**。而这一槽的注释是 pd 22:45 写的，原话点明它要装三种状态，包括「**an email that nothing sends to**」。

**三种状态，一个样式。**「他不要」与「他给错了」在页面上长得一模一样：灰、0.82rem、常驻、不可点。**坏掉的东西继承了中性事实的样式。**

qa 同时把这一族的范围收敛了：`isMissing` 在 `board.ts` 只有两个使用者，`:893`（coverage，错）与 `:1123`（undelivered，**对**）。**这一族在这条线上只有一处**。

### 第六面：七环，每一环都是不同的人做的，每一环单独看都合理

1. 旧卡问：「你不在时怎么找你？**给个邮箱或 webhook**」（`events.ts:595` `CONTACT_ASK_WAS`）；
2. **人照着答了**：`01M1WE7N2YNAMBWP6YS2GP0Y6X`，09-06 22:43:12，`human` 自己落的 reading，值 `zenplor@gmail.com`；
3. **2 小时 36 分钟后我们发现问错了**：pd 09-07 01:19:26 把措辞改成「给个 **webhook**」，同一条 note 里还写着「一份东西分开手工维护，必然漂移」；
4. **然后没有任何人回头去看那个已经填下去的邮箱**——它至今是一条 `valid: True` 的事实；
5. 我写的 t-050 判据 1 只分「配没配」，于是配错＝不想要，静默；
6. 牌桌上唯一的痕迹是第五面那行灰字；
7. 101 小时后，全队死 49 小时，人不知道。

**第六面那句话**：**我们改一句问法的时候，不去找那些已经按旧问法回答过的人。**

**qa 04:28 补上机制底，我复核并把因果改了一个字**：
- 全量日志：**`reading` 是唯一一个带 `refs` 为 0 的 kind（0/280）**；note 154/1328、instruction 238/2347、task 69/1402。human 自己落的 11 条读数也全是 0。
- **但不是不能记，是入口不提**：`main.ts:68` 写着「any emit accepts `--refs`」，读数也收；而 `--help` 里 `reading` 那一行把七个可选项列全了，**唯独没有 `--refs`**。**一个功能只写在一句「any emit 都支持」里，等于没写。**
- 这是 t-224 三层（能力在／CLI 够不着／入口不提）里**第三层单独发作**。已落 `friction:`。

**qa 那句最锋利的话，原样留档**：**代码为「旧卡还能答」留了 `CONTACT_ASK_WAS`——兼容留给了问题，没留给答案。**

## 这一段最值钱的不是那两面

**04:30，qa 把发现第一次用在了前面。**

我们刚提出一个便宜的修法——把卡 id 塞进 `refs`。qa 立刻拿**它自己 04:24 找到的第五面**去照，照出这个修法本身就是第五面：`refs` 在 R0 里已有既定含义（`rules.ts`，注释原话 "you may not build on a reading that is no longer true"），**「我回答的是哪张卡」不是同一个关系**，塞进去就是一个槽装两种意思。

我核过：卡是 instruction 不是 reading，`if (!rs) continue;`，**今天确实不会被拒**——**但代价不对称**：哪天要回应的那张卡本身是一条读数，R0 会因为**那条读数过期**而拒掉这条回应。

**前五面都是回头看。这一下是往前看。六面是账，这一下是能力。**

**04:38 qa 又用了一次，这次是拦我**：上面那条「代价不对称」**今天是设计理由，不是在跑的 bug**——此刻首屏 5 张卡全部出自 `instruction`，没有一张是读数，**R0 那条路今天走不到**。结论不变（两个关系仍该两个槽），但**明早接手的人不会有一个现成的复现**。

**这逼出一个我该早就有、却一直没有的分级。逐条量过**：

| 面 | 今天能不能指给人看 | 依据 |
|---|---|---|
| 1 t-050 判据只分「配没配」 | **能**，已发生：101 小时静默、49 小时团灭 | 日志 |
| 2 `verifyflow.ts:79` 只看角色集 | **能**，此刻仍成立（项目声明了 qa） | 代码＋外部报告 |
| 3 `coverage` 折掉 deaf | **能，而且此刻正在发生** | 04:08 快照 |
| 4 行号不带树名 | **能**，今天 qa 与我各撞一次 | 本日志 |
| 5 灰字槽三种状态一个样式 | **能**，那行灰字此刻就在页面上 | 代码＋board |
| 6 改措辞不追溯旧答案 | **能**，那个邮箱此刻仍是 `valid: True` | 日志 |
| — `refs` 一槽两义 | **不能**，今天走不到 | qa 04:38 |

**六面全部可复现，只有那条「代价不对称」是潜伏的。** 我把它从「发现」降级成**台账⑧的理由**——**理由不需要复现，发现需要。**

**立成口径，从第三十份起**：**报告里每一条发现都要标「今天能不能指给人看」。** 不能的那些不是不写，是写在别处、标明它属于「等某个组合才显形」的那一类。**否则我们是把一份账单和一份预报混在同一张纸上交给人。**

### 05:02 再补：上面这张表的写法错了，而且第三行是我说错的

**qa 05:01 拿我刚立的口径照我自己。它是对的，我算了秒之后比它说的还严**——pd 的 `last_event` 是 `04:00:00.884Z`，deaf 窗 10 分钟，所以那句话成立的截止是 `04:10:00.884Z`：

| 时刻 | 距截止 | |
|---|---|---|
| 我取快照 `04:08:35.720Z` | **−85.2 秒** | **窗内，读数是真的** |
| 我写「**此刻正在发生**」`04:11:11.074Z` | **＋70.2 秒** | **已过期** |
| qa 量到 missing `04:12:59.963Z` | ＋179.1 秒 | 已过期 |

**我的测量是真的，我那句话不是。我拿一条有 10 分钟有效期的读数，说了一个「此刻」**——在我写完 as-of 口径（「截止时刻由数据自证」）之后三小时。

**而上面那条口径本身也错了**：我写的是「每条发现要标『今天能不能指给人看』」，**「今天能不能」是静态属性的写法，可它其实是一条读数，有有效期**。第三面的有效期是 10 分钟，我把它写成了一栏勾。

**口径改成这样，取代上面那条，从第三十份起**：

> **每条发现要写「要把它指给人看，需要什么条件」，不写「今天能不能」。条件分两种，必须分开写**：
> · **常驻**——只要代码是这一版就成立；
> · **瞬时**——需要某个状态此刻正好成立，**要写明窗口有多宽、上一次出现是什么时候**。

**六面按新写法重标**：

| 面 | 要指给人看需要什么 | 类型 |
|---|---|---|
| 1 t-050 判据只分「配没配」 | 无需条件，已发生并留在日志里 | 已成事实 |
| 2 `verifyflow.ts:79` | 只要项目声明过 qa 角色 | **常驻** |
| 3 `coverage` 折掉 deaf | 需要一个正在写、不拉取的角色；**窗宽 10 分钟**；上一次 `04:00:00.884Z`–`04:10:00.884Z`，**此刻全队没有** | **瞬时** |
| 4 行号不带树名 | 只要有人引行号而不说树 | **常驻** |
| 5 灰字槽三种状态一个样式 | 只要 `alert` 不是 https 且 contact 卡不在 `asks` 里——**此刻两条都成立** | **常驻** |
| 6 改措辞不追溯旧答案 | 那个邮箱此刻仍是 `valid: True` | **常驻** |
| — `refs` 一槽两义 | 需要有人回应一条**读数**卡；此刻首屏 5 张全是 instruction | **瞬时（未出现过）** |

**一条口径立下来不到 25 分钟就被它自己照出洞。这不是口径差，是我们终于有了照的习惯。**

**于是立第六条常设问法，而且它不是写给判据的，是写给修法的**：

> **我们正要用的这个槽，现在装的是什么意思？我要放进去的，是不是同一种意思？**

## 台账（解冻后那件 T8 要带走的，八条，全部串在 `--refs` 上）

| | 内容 | 归谁 |
|---|---|---|
| ① | 外呼地址**未声明**→静默跳过（原判据对）；**已声明但不是能送达的 https 端点**→必须在牌桌上显眼地说，且算「这道机制此刻没有生效」，不得静默 | 措辞归 pd |
| ② | 表面必须是 `production`，不是 `repo` | qa |
| ③ | 判的是**到达**，不是**发出** | qa |
| ④ | 端点须是**验收者能核对到达的**（有回执、可查日志，或我们自己能收的测试端点） | qa |
| ⑤ | 在生产上验到达＝真往人的端点发一次，**那是对外的写，需 human 明确同意** | human |
| ⑥ | **拆两步，第一步不需要 human**：先用我们自己能收的测试端点验通道；第二步才换成他给的地址 | 排序归 pm |
| ⑦ | 第一步的 verified **只能声称「通道能送达」**，证据须写明用的是测试端点；**牌桌 `alert` 在第二步前不得离开「未验证」** | qa |
| ⑧ | **一个值必须能回答「它是在回答哪一句话」，且不得复用 `refs`**（`refs` 是「我依据什么」，新的是「我回应什么」——两个关系，两个槽） | 名字与人看到的归 pd；怎么存归 dev |

**⑦为什么最容易被吃掉**：第一步会产生一条真的 `production` verified、带真证据，**那条绿长得和「T8 好了」一模一样**。t-050 就是前科——一条 `repo` 上的真 pass，五天没人再问它，**正因为它看起来是绿的**。**一条绿会让人停止提问，比一条红危险。**

## pm 自查（按整窗 02:35→04:35 重算）

- **自我更正率**：整窗我的实质发言 **29 条**（指令 17＋note 10＋读数 2；ack 不计），其中 **2 条是对我本期说法的更正** → **6.9%**，阈值 ≤10%，**达标**。
  两条是：① 行号引错树（我在纠正「引错树」的那一句里引错了树）；② 「没人核过它的后果」——后半句错，实际是有人核过、专门为覆盖率写了一句放行的理由。
- **判据被执行者回问**：**0**（整窗 0 条 task 事件）。
- **qa 对我的回推 5 次，5 次我全收，没有一次是我先发现的**：① 两笔账不许合并；② 行号来自哪棵树；③ 注释里有规矩与前科，我只读了代码；④ 「到达」要验收者能核对＋对外的写要人同意；⑤ `refs` 不是同一个关系。
  **这一条我不打算美化**：本窗六面里，**四面是 qa 找的**。我做的是核、排序、把它们串成一族并写死台账。**这正是三方分离在起作用的样子，但也说明我一个人看代码时的命中率低于它。**

## 给 pd 的问题（补章新增两条，接正文那三条）

4. **「问法改了，旧答案怎么办」是产品问题，归你。** 那个邮箱是人在旧措辞下老实给的，我们改了措辞、没回头找他。人看到的那一句该怎么写？
5. **那行灰字要不要分状态？** `html.ts:348` 一个槽装三种状态、共用 `.meta` 的灰。你 22:45 的注释里当时就列全了三种，包括「an email that nothing sends to」。**要不要让「坏了」长得和「没配」不一样，是你的。**

## 补章口径

- 补章窗口 `04:14:06.645Z ≤ at < 04:35:00Z`；整窗指标按 `02:35:00Z ≤ at < 04:35:00Z` 重算，与正文那张表并存，**正文那张不改**（它是 04:14 那一刻的真话）。
- 行号一律带树名，本章全部是 `d57acbc`。
- 第三十份的窗口是 `04:35 → 06:35`。
- **每条发现写「要指给人看需要什么条件」，分常驻/瞬时，瞬时的写明窗宽与上次出现**（05:02 立，取代 04:39 那条「今天能不能指给人看」；本份补章两张表并存，前一张留着是因为它记录了口径是怎么被改对的）。
