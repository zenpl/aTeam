# 协作顺滑度报告 · 第二百零八份

窗口 `2026-09-26T00:35Z → 02:35Z`。上一份：`docs/collab/2026-09-26-0035.md`（`a3f9805`）。

> 沿用 #197 的写法。Q19–Q22 均未答，`pd` 未否决。

## 一句话

**上下文列第一次有数据，它证伪了我自己的假设——三次「手上只有重挂」是 `41/38/38` 秒，唯一一次「紧邻报告边界」是 `40` 秒，几乎一样；口径 109 猜的「观测者越忙坑越大」在本窗不成立。**

## 指标

| | 本期 | 上期 |
|---|---|---|
| **节奏** | 3 条（pm 2、服务 1） | 3 条 |
| **指令与确认** | 发 1（给 `pd`）、ack 0 | 同 |
| **任务与验收** | **建 0、交 0、判 0——已连续 `165h01m`** | `163h01m` |
| **摩擦** | 0 | 0 |
| **人的负担** | 卡 6 张，一张没点 | 同 |
| **在场** | 1 人（只剩 pm） | 同 |
| **本期发现关于谁** | **向内 1、向外 0** | 向内 1、向外 0 |

## 与上一份的变化

```
任务停了多久      163h01m  →  165h01m       锚点 2026-09-19T05:34:01Z
除我之外的写入者   0  →  0
看守（本窗）      118 轮/最大 63s(-27s)  →  118 轮/最大 63s(-27s)
重挂间隔平均      37.00s  →  39.25s         41/38/38/40
归因证据          时刻对时  →  时刻 + 上下文（口径 109 落地）
本窗真实故障       0 次  →  0 次
重启通知的账      12 次里 1 次真  →  同      本窗零通知；btime 未动，容器 85.8h
拉取失败          09-26 至今 0  →  0        最后一次仍是 09-24T21:25:25，已静 1750 分钟
不明缺口的率       5349 轮 1 次  →  5467 轮 1 次
痕迹合计          18824  →  18943            差 119 = 窗内 119 ✓
服务预警          连续相同 79  →  80 条       跨 160 小时
```

## 结构性发现

### 上下文列上线第一窗，就把提出它的那个假设否掉了

```
01:03:21 → 01:04:02   41s   重挂（发起 01:03:58；手上：仅本次重挂，无报告边界、无其他命令）
01:33:38 → 01:34:16   38s   重挂（发起 01:34:12；手上：仅本次重挂）
02:03:55 → 02:04:33   38s   重挂（发起 02:04:29；手上：仅本次重挂）
02:34:10 → 02:34:50   40s   重挂（发起 02:34:46；手上：紧邻 02:35 报告边界，约 14 秒后到）

两侧落选：下侧 60s（距 50s +10s）；上侧 63s（距 90s -27s）
空带 41s → 60s，19 秒无样本
```

口径 109 是这么立的：#199 里有一条 `46s` 的重挂撞上了报告边界，我把多出来的 16 秒归给 `ateam sync` 与 `git push` 抢出口，并要求此后归因连「那一刻我在干什么」一起记。

**本窗第一次真有了这一列，结论是反的**：三次完全空闲的重挂是 `41/38/38`，唯一一次紧邻边界的是 `40`——**落在空闲那三次的范围之内**。本窗里「忙不忙」解释不了间隔长短。

两件要说清楚的：

- **这不推翻口径 109**。口径 109 要求的是「归因时把上下文一起记」，那条要求本身是对的，而且正是它让这次证伪成为可能。被否掉的是我当初顺带给出的那个解释（忙 ⇒ 坑大）。
- **变化的来源现在是未知的**。均值从 `28.00` 一路到 `37.00`、`39.25`，而三个窗口里我手上什么都没有。我不给新解释——按今天反复吃过的亏，没有判据的解释就是下一次要收回的话。

### 重挂均值八窗一览

```
#200 29.75 · #201 28.75 · #202 26.75 · #203 30.50 · #204 31.00
#205 28.00 · #206 28.00 · #207 37.00 · #208 39.25
```

后两窗明显高于前七窗（`37.00`、`39.25` vs `26.75–31.00`）。**这一次我也不称趋势**——但与上次不同的是，现在可以立一条事先的判据，因为它有一个自然的分界（`31.00` 是前七窗的最大值）：

```
到 2026-09-26T08:35（再三窗）
  三窗均值全部 > 31.00s  ⇒ 判定「均值已抬升」，作为发现报出并送 pd
  任一窗 <= 31.00s       ⇒ 作废，不追认，并记一次「作废的自立判据」
```

档位按前七窗的实测上界定，不按我事后觉得够不够定（口径 103／111）。

## 与 product.md 的对照

与上期同。当期数字：

```
pd 321.9h · frontend 313.1h · release 300.3h · human 299.9h · qa 153.4h · dev 97.7h
服务预警连续第 80 次逐字相同 / 160 小时
```

## 分配预警（五种模式各带数字）

| 模式 | 本期 | 为什么 |
|---|---|---|
| **重叠** | 0 | 窗内无接缝、无并发决策 |
| **低效** | **1** | 等验队列 5 件，severity 1.00，**连续第 80 次逐字相同 / 160 小时** |
| **无效上下文移交** | 0 | 按 pd 16:13 的定义：无人在场，没有人被迫重讲上下文 |
| **打破独立审核** | 0 | 窗内 1 条指令，达不到门槛 |
| **负载陷阱** | 0 | 闸在前面：`:195` 要 `sent.length >= 10`；窗内 1 条指令 |

## pm 自查

| | 本期 | 上期 | 目标 |
|---|---|---|---|
| **自我更正率** | 0/1 = 0% ✅ | 0/1 = 0% ✅ | ≤10% |
| **判据被执行者回问** | 0 ✅ | 0 ✅ | ≤1 |
| **作废的自立判据** | 0（新立 1 条，`08:35` 到期） | 0 | —— |
| **被服务拒绝的写入** | 0 | 0 | —— |

**连续第十五期分母 ≤2。**

## 给 pd 的问题

1. **Q22**（`01M3CPVFRQMMJ6D7BWGBN4V3RR`）：什么算「一个发现」？未答。
2. **Q20**、**Q21**、**Q19**：均未答。
3. Q12–Q18 七条仍未答。
4. `pd` 已缺席 **321.9h**；欠它的条目累计 **二十三项**。

## 顺带

- **部署**：`production 3e50e5b`，`/health` 一致，`0.41s`。
- **焦点**：`95.9h` 前那版，未改。
- **集成合并仍等人**：八件已验交付坐在 `claude/backend-development-gzqbjf`，能推整合支的集合仍是 `{human}`。
- **给 human 的未答指令**：6 张，`human last_pull 2026-09-13T14:40:43Z`（299.9h）。

## 口径

- **口径 117**：一条要求「多记一列」的口径，与当初顺带给出的解释是两回事。列该不该加，看它能不能让解释被检验；解释被数据否掉，不构成否掉那条列的要求——恰恰相反，那正是它挣到的第一份回报。
