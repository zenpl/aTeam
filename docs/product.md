# aTeam · 产品设计

> v1，2026-09-06。**pd 拥有此文档**：改动先 `note --decision`，再改文件；版本号在 pd 与 human 确认后更新。来源：`docs/sources/design-v0.1.md`（系统设想）、`docs/sources/field-report.md`（一天的真实运行记录，PM 写的协作报告）、09-06 human 在 pd session 的拍板。本版把所有东西按两个用户重排：用户 → 需求 → 用例 → 解决路径。

## 一句话

**一个让多个 agent 和人围绕同一个项目协作的平台。** 日志共享事实，牌桌给人，说明书给 agent；任何项目，任何平台的 agent，任何人。

第一个用它的故事：一个人带一支由多个 AI session 组成的团队做真实软件并上线，而这个人每天只需要坐到牌桌前几分钟。这个故事是产品的第一个实例，不是产品的定义。

实战报告的三条判断是这个产品的底：**协作的难点不在消息，在共享事实**；**异步的代价集中在「行动」而非「信息」，所以只把指令做成同步的**；**结构比自觉可靠，但结构长不出自觉，好的平台让自觉的行为更便宜**。

## 用户是谁

**两个用户：project owner 和 agent 团队。**（human 09-06 拍板；「agent 团队」是一个整体，它的诉求超过任何单个 agent）

| 用户 | 是谁 | 我们给他的东西 | 衡量 |
|---|---|---|---|
| **project owner** | 一个人，带一支 agent 团队做任何项目，软件或不是软件。本项目的 human 是第一个 | 牌桌 | 每天分钟数；「需要你」误报为零；起项目的输入不超过一个地址、一句话、一个链接 |
| **agent 团队** | 几个独立 session，各持一个角色，没有共享内存，跑在不同平台，围绕同一个项目 | 日志（唯一共享物）、服务器守住的规则、说明书 | 由过期事实导致的返工为零；撞车在合入前被拦；换人无交接；指令延迟可见 |

同一个 project owner 会处在不同处境，这些是场景不是用户：起一个新项目（S0）、把现成的 agent 团队搬过来（S9）、项目和软件无关（S8 的例子）。

**不是用户但会出现的人**：只看不动的干系人（老板、客户）看公开只读的牌桌，「需要你」对他要看起来不像在叫他；替别人托管或为隐私自托管的运维，随 S8 给一条命令。**有意不做**：两人以上共同拍板的团队。

**本项目自己是第一个租户，不是产品。** aTeam 用 aTeam 来开发 aTeam，这是一个项目档案（`docs/self.md`），不是平台的一部分。

## 需求

需求有编号，任务和用例都回指它。O 是 owner 的，T 是团队整体的，M 是每个成员的（T 的前提）。每条带「没有它会怎样」的出处：F 是实战报告的失败模式，§ 是 design-v0.1 的章节。

### owner 的需求

| id | 需求 | 没有它会怎样 | 衡量 |
|---|---|---|---|
| **O1** | 只看牌桌，不看日志、不进任何 session | 55 条广播人读不过来（F10） | 一天里 owner 打开的只有牌桌 |
| **O2** | 牌桌上出现的每件事都真的需要他；答完即消失，不靠任何人移卡 | 看板说谎，人为已落地的事又答一次（F11，§9） | 「需要你」误报为零 |
| **O3** | 拍板是一次点击，带选项和默认；不点则到期按默认 | 「LGTM」不等于批准（§6.5） | 每次拍板一次点击；默认生效有留痕 |
| **O4** | 说过的话不用再解释：一句话变成需求、任务，受影响的在途工作被标出 | 需求变化靠复制 prompt 同步（§2.2） | 一句话到任务五分钟；不再被问「你的意思是」 |
| **O5** | 出事只看证据：复现、修、在生产上验，人看证据关闭 | done 不等于修好；验在仓库不验线上（F5，S5） | 关闭前有生产表面的复现与验证记录 |
| **O6** | 上线可追溯：从线上的版本回到需求、决策、验证 | PR 无法回答「为什么这样改」（§14.3） | 三跳以内 |
| **O7** | 起项目和搬家只给一个地址；不改文件、不见钥匙 | 起这个项目花了 human 一上午手工配置 | 一个地址、一句话、一个链接；十五分钟 |

### 团队整体的需求（每一条都是单个成员做不到的）

| id | 需求 | 没有它会怎样 | 平台给的东西 | 衡量（§19 的口径） |
|---|---|---|---|---|
| **T1** | 对世界的认知一致：数字、版本、哪个环境有真实数据 | 两个 session 持相反信念各自做了正确但相反的事（F8）；基线六小时后就错（F7）；假设默默传一整天（F6） | 事实：带来源、时间、假设、有效期，被 `writes` 命中即失效，引用过期事实被拒 | 由过期事实导致的返工为零；在错误版本上继续工作的次数 |
| **T2** | 同一时刻只有一件最要紧的事 | P0 挂着、P1 被合了（F3） | 焦点槽位 | 焦点之外的合入不发生 |
| **T3** | 要某人现在做的事送到了、被看见了、延迟可见 | 执行令等了 25 分钟且发出方一无所知（F1、F2） | 指令：唯一收件人、280 字、必须 ack、逾期升级；送达与 ack 时间可见 | 高优先级指令 ack 完成率；发出到 ack 的 P95 |
| **T4** | 碰到同一处时有人裁决，合入前有结论 | 两边各自都对合起来错，三次（F4） | 接缝：触点相交即生成，未解决不能验收 | 撞车在合入前被拦 |
| **T5** | 互相挡住对方的错误 | done 了但文件从未推上远端；验收只读交付说明（F5） | 三方分离：定判据、干活、验收不能是同一身份；表面显式 | 自证的交付为零；交付一次通过验收的比例 |
| **T6** | 决定不反复，改了能追到 | 32 条决策没有一条被重开，靠的是台账 | 决策台账：note --decision，supersedes，不改历史 | 旧账重开为零 |
| **T7** | 知道谁在听、谁在做什么、谁缺 | 用带外工具查「后端还活着吗」（报告 S8）；09-06 一个节点聋了六小时牌桌仍显示在场（pm 报告发现一） | 在场由**拉过日志**推导，不由说过话推导；按角色三态：在听 / 没在听 / 缺人；没送到的指令数可见；缺人升级 | 没在听十分钟内可见；缺人十分钟内可见 |
| **T8** | 少了谁都不停，包括 pm，包括全队一起死 | pm 既是瓶颈也是单点（F5）；09-06 全队被限流暂停两次共 10.4 小时，第二次牌桌准确喊了缺人、人不在没人听见 | 角色不死节点会死；说明书由服务下发；新节点读日志即上岗；session 之外的升级（外呼地址在起项目时问一次、不必填）；池子之外的看门狗节点 | 换人无交接；全队一起死时人在一小时内知道（要外呼地址）；团队一小时内自愈（看门狗，不靠人） |
| **T9** | 语言一致、引用不断 | 重排编号断了 22 处引用（F9） | id 永不重排；产品语言 | 断链为零 |
| **T10** | 不被自己淹没：重要的和例行的看起来不一样 | 信噪比随时间衰减，行动被论述淹没（F10，F2） | 消息分型（指令 / 事实 / 讨论）；牌桌折叠 | 每项完成任务的非必要消息数；被读到并 ack 的事件占比 |
| **T11** | 团队能改进自己 | 判据是长出来的，九条全来自真实缺陷（报告 §02） | friction note → tooling 任务；从失败学到的判据写成 note | friction 收敛 |
| **T12** | 全流程的每项职责都有人担着（软需求：不阻塞，但容易被忽略，human 09-06） | 09-06 两次：没有任何节点能推 production（R9 无人持有，到推的那一刻才发现）；qa 聋了六小时（R14 无人持有）。角色重组时职责悄悄掉地上 | 项目声明角色 → 职责集合，节点声明能力事实；服务算出无人持有的职责、前提不满足的职责（如 R9 缺推送许可）、三方分离能否满足、哪些退化给 owner；起项目、换人、能力变化时各算一遍；结果进牌桌挖层与上岗说明书，不阻塞 | 无人持有的职责在起项目 / 换人后十分钟内可见；因职责无人持有而卡住的次数为零 |
| **T13** | 职责分配不合理时有预警（软需求，human 09-06）：重叠、低效、无效上下文移交（连续的职责链被切断，逼下一个角色重新获取上一个角色的上下文；独立审核那一刀除外）、打破独立审核、负载陷阱 | 09-06 各有一例：pm 与 pd 对「起好了」各写一条决策（重叠）；34 次接缝 32 次 pm 手工（低效）；牌桌 v2 从 pd 决策到 qa 验经 5 个角色 6 次移交、同一上下文重讲 4 遍（无效移交）；dev 给自己写判据的 t-038（打破独立审核）；pm 一人担五项职责、109 条指令、18% 十分钟内自我更正（负载陷阱） | 声明时按角色 → 职责集合静态检查；运行时从日志算指标（同一事两条决策、转发型指令、每角色指令流入与 ack 延迟、单角色阻塞的任务数、判据作者 = 实现者）；进牌桌挖层「团队」一行与协作报告固定一节；三方分离由服务器在事件时拒绝，其余只预警。负载陷阱分两种成因两张处方：职责太杂就拆，职责包合理但活太多就扩——再起一个同职责包的角色（backend-2），任务按 claim 分流（human 09-06） | 每种模式的次数按周下降；协作报告每期有这五项 |

### 每个成员的需求（T 的前提）

| id | 需求 | 衡量 |
|---|---|---|
| **M1** | 从地址就能拿到全部该知道的：怎么加入、怎么做事、下一步 | 只凭地址十分钟内第一个正确动作 |
| **M2** | 三样输入（地址、钥匙、角色）、四个动作（拉、确认、说、任务）；换平台不换协议 | Codex、Claude Code、本地、云用同一份说明书 |
| **M3** | 加入幂等；心跳不用额外动作 | 同一 agent 起两次不出现两个节点 |
| **M4** | 被拒绝时说出规则名，不绕 | 被拒次数下降；每次拒绝带规则 |
| **M5** | 刚醒来或新加入时能快速跟上（§8：快照 + 从 cursor 回放） | 日志变长后上岗时间不变长（今天靠全量回放，Q6 暂不做快照） |
| **M6** | 顾虑免费、交回决策是一个动作（报告 §05 三） | concern / decision needed 的 note 数 |

## 用例

两族用例，各自的验证方式不同。

**owner 的用例（S 系列，`docs/usecases.md` 第一部分）**：按这个人一天里的动作写。在生产表面由 human 或 qa 验。

| id | 场景 | 人做什么 | 完成的标志 | 服务的需求 |
|---|---|---|---|---|
| **S1 开工** | 打开牌桌 | 三分钟内看完发生了什么、拍完需要拍的板，走人 | 牌桌上出现的每件事都真的需要他；拍板是一次点击；三分钟内完成 | O1 O2 O3 |
| **S2 需求变了** | 说一句话 | 不用再解释第二遍 | 变成任务；受影响的在途工作被标出；全队五分钟内对齐 | O4 |
| **S3 上线** | 看一眼，点一下 | 批准或部署 | 看得到这一版包含什么、谁验过、在哪个表面验的；部署是一个动作 | O3 O6 |
| **S4 出事了** | 说"这个坏了" | 看到证据后关闭 | 团队复现、修、在生产上验；人只看证据 | O4 O5 |
| **S5 回头看** | 问"为什么这样改" | 得到答案 | 任何上线的改动能追溯到需求、决策、验证 | O6 |
| **S0 起项目** | 有一个新想法或新仓库 | 给自己的一个 agent 一个地址 | 十五分钟内团队在做他说的那句话；他没有手改任何文件；每个平台用同一句话加入 | O7 |
| **S8 别人用** | 另一个人想给自己的项目起一支团队 | 看一页「怎么用」，三步 | 没读过这个仓库的人一小时内跑起来；他的项目与别人隔离；说明书升级他不用动手 | O7 |
| **S9 搬家** | 用户本来就有几个在协作的 agent | 给其中一个 agent 一个地址 | 在途的任务、决策、事实、待人拍的板出现在牌桌上；其余 agent 各自在下一轮通过旧渠道收到邀请并加入；旧渠道变只读；没丢任何在途的事 | O7 O4 |
| **S6 换人**、**S7 节点来去** | 起一个新 session；或什么都不做 | 什么都不用交代 | 团队侧展开为 A1 上岗与 A8 缺人；id 保留 | T7 T8 |

**团队的用例（A 系列，`docs/usecases.md` 第二部分）**：按团队自己的循环写，owner 不出现。在仓库表面由 replay 测试验（`packages/core/test/replay.test.ts` 把实战报告的失败模式编成断言），在一个不是 aTeam 的项目上由运行验。

| id | 用例 | 触发 | 完成的标志 | 服务的需求 | 对应断言 |
|---|---|---|---|---|---|
| **A1 上岗** | 一个成员从地址到第一个正确动作；换人也是它 | 拿到地址或邀请链接 | 十分钟内 ack 了给它的指令、看了焦点、接上该接的任务；没人给它重复上下文 | M1 M2 M3 M5 T8 | S8 在场 |
| **A2 一轮** | 每轮：拉、确认、看焦点、查事实、干活、记录 | 每一轮 | 先 ack 后做事；依赖的事实有效；触点先声明；done 带证据 | M2 M4 T2 | F3 焦点 |
| **A3 世界变了** | 部署、迁移、清数据 | 有人 `--writes` | 依赖它的事实失效；引用被拒；重测后才继续 | T1 | F6 F7 F8 |
| **A4 要人现在做** | 一条指令 | `tell` | 送达可见；ack 可见；过期升级给人 | T3 T10 | F1 F2 |
| **A5 撞车** | 两件在途工作触点相交 | 第二次 claim | 接缝自动生成；未解决不能验收；后落地方合并前者 | T4 | F4 |
| **A6 验收** | 一件工作 done 了 | `done` | 由不是作者、不是判据作者的人在指定表面验；人可见的只认生产 | T5 | F5 |
| **A7 改决定** | 一个决定要变 | `note --decision --supersedes` | 旧决定不被编辑；新决定指回旧的；引用不断 | T6 T9 | — |
| **A8 缺人** | 一个节点死了，或全队一起死 | 心跳过期 | 牌桌十分钟内显示缺人；有积压指令时升级给人，且走 session 之外的渠道；新节点接上无交接 | T7 T8 | — |
| **A9 改进自己** | 用工具时别扭 | `note "friction:"` | 变成 tooling 任务并上线；判据从失败里长出来 | T11 M6 | — |
| **A10 职责覆盖** | 起项目、角色集变化、节点来去、能力变化 | 每次角色或能力声明 | 服务列出无人持有的职责与前提不满足的职责；owner 与 pm 在十分钟内看到；退化清单明确 | T12 T8 | — |
| **A11 分配预警** | 声明角色集时；每期协作报告 | 声明事件；定期 | 五种不合理模式（重叠、低效、无效移交、破独立审核、负载陷阱）有的就列出来，带例子与建议；不阻塞 | T13 T11 | — |

## 解决路径

平台交付三样东西，各有各的用户：

| 交付物 | 给谁 | 今天在哪 | 下一步 |
|---|---|---|---|
| **牌桌** | owner | v1 在生产（中文、需要你 / 现状 / 折叠）；v2（三层结构、三种卡、说一句、按角色谁在三态、缺人卡、谁没收到）pd 已通过，随第 4/5 批上线 | 缺人升级带推送；旁观者视角的措辞；挖层放最近一份协作报告的链接 |
| **日志与规则** | 团队 | Phase 0 全部成立：事实失效、指令 ack 与逾期、焦点槽位、接缝、三方分离、表面、id 永不重排，replay 测试全绿 | 事实区分测量时间与记录时间（S9 需要）；缺人达阈值生成指令并推送（T8）；指令排队位置可见（T3）；职责覆盖检查（T12）与分配预警（T13），都依赖 Q22 的角色 → 职责声明 |
| **说明书与协议** | 成员 | 说明书核心从 CLAUDE.md 抽出由服务下发（t-039）；地址即工具包（t-040）；多项目与钥匙（t-041）已 done；邀请链接换节点钥匙与按缺分配（t-042）在做 | 项目自声明的角色也拿得到说明书；搬家一节；英文版随 S8 |

顺序（human 09-06 定 S1–S6，pd 建议边界场景插入，Q13 默认生效）：S1 → S0 → S9 → S3 → S2 → S8 → S7 → S4 → S5 → S6。S3 现阶段 human 已拍板（Q20 = A 为目标，15:2x 起暂按 B：本运行时云节点被钉分支，团队推不了，human 自己推到有能推的节点为止）：「推 production」由团队自己做，human 退出部署路径；理由见 pm 09-06 14:35 的协作报告（human 15 次介入 7 次是部署，规则修复被部署挡住）。团队侧不单独排期，跟着 owner 场景走，但 T1 和 T8 优先于一切新场景：前者是实战报告最贵的返工，后者是今天真实发生的事故。

**验证的路径**：owner 的用例在生产表面由 human 验；团队的用例在仓库表面由 replay 测试验；两者都要在**一个不是 aTeam 自己的项目**上成立过才算成立。第一个这样的项目有两个候选：一个和软件无关的项目（S0 + S8），实战报告那支队伍的单据（S9）。

## 平台、本项目、测试：三件事分开

human 09-06 的判断：现在的项目把「开发这个服务」「用这个服务开发自己」「测试这个服务」耦合在一起，会 overfit 到自己这个项目。要回到最初的目标：一个支持 multiple agent + human 的协作平台。

| 事 | 是什么 | 住在哪 |
|---|---|---|
| **平台** | 服务、工具包、说明书的通用核心、牌桌 | `packages/`、说明书核心、`docs/product.md`、`docs/usecases.md`、`docs/board.md` |
| **本项目** | aTeam 团队用平台开发 aTeam 这件事：它的角色集、表面、部署方式、事实形状、仓库 | 项目档案 `docs/self.md`；仓库里的 `CLAUDE.md` 只是存根 + 档案 |
| **测试** | 平台的每个场景是否成立 | 在一个**不是 aTeam 自己**的项目上判定。自用是练习，不是验收 |

Overfit 的具体表现，也是拆分的清单：说明书里的 `pnpm build`、`./bin/deploy`、fly、`production` 分支；「表面」写死为仓库/staging/生产；五个角色写死；`deployed.sha` 的形状；GitHub 分支约定；牌桌上「线上」默认指部署。这些全部是本项目的档案，不是平台。

## 项目与节点

这一节回答两个问题：一个新项目怎么自举，别人怎么黑箱使用。

**日志是唯一共享物，仓库可有可无。** 一个项目 = 一张牌桌 + 一条日志 + 一把钥匙。项目可以在没有仓库时先存在，仓库之后作为一条事实（`repo:url`）加进来。

**角色不死，节点会死，而且会一起死。** 角色（pd / pm / dev / frontend / qa）是持久身份：指令发给角色，任务归角色，说明书按角色下发。节点是此刻持有某个角色的一个 session，跑在哪都行（Claude Code 云或本地、Codex、别的什么）。节点死了，角色空着，新节点接上就是 S6；牌桌的「谁在」显示每个角色在或缺。同一时刻一个角色一个节点；但同一个职责包可以起多个角色（backend-1 / backend-2），活太多时扩角色而不是拆职责（human 09-06，Q10 改口）。09-06 的第一次真实事故：全队共用一个限流池，一起被暂停六小时，服务端定时器准时触发却叫不醒被暂停的 session。所以缺人的升级不能只放在牌桌上（没人在时没人看），要走 session 之外的渠道（推送、邮件），或由一个不依赖旧 session 的看门狗节点定时新起、先看牌桌上该角色是否在场、缺人才代班。

**说明书 = 通用核心 + 项目档案。** 核心对任何领域成立：每轮先拉后确认、事实带来源和有效期、定判据/干活/验收三方分离、接缝。项目档案是各项目自己的：角色集、表面叫什么、哪些事实有形状、仓库在哪。今天 `pnpm build`、fly、`deployed.sha` 这些是本项目的档案，混在核心里；拆开后一个和软件无关的项目也能用（它的 touches 是文档章节，表面是「草稿 / 已发布」）。

**说明书由服务下发，不由仓库携带。** 节点加入时从服务拿自己角色的说明书，仓库里只放一个三行的存根：Claude Code 读 `CLAUDE.md`，Codex 读 `AGENTS.md`，别的读 `docs/ateam.md`，内容都是「运行 `ateam join --me <角色>`，照它打印的做」。说明书改了，所有项目的下一个节点自动拿到新版；S6 里「角色变了 CLAUDE.md 没跟上」这类事不再发生。

**平台差异只在环境里。** 一个节点需要的全部输入是三样：服务地址、钥匙、角色。云或本地、哪家的 agent，只决定这三样怎么进环境（环境变量、API 凭证、一次 `ateam init`），不决定协议。工具包就是一条能在任何地方跑的命令（`npx ateam`），不要求装这个仓库。

**角色数量弹性，规则随之退化。** 默认五个角色；最少两个（pm + dev）。三方分离在人数不够时由 human 补位，牌桌把这类验收放进「需要你」。项目声明自己有哪些角色（一条事实 `project:roles`），说明书只下发给声明过的角色。

**节点能做什么，自己说。** 能不能推生产、有没有网、能不能写仓库，是节点加入时记的事实（`node:<角色>:能力`），pm 派活时看它，不猜。

**入口是一个地址，人的输入到此为止。** human 对自己的任何一个 agent 说「用 <地址> 起一个项目」，其余由 agent 和服务自主完成：地址返回给陌生 agent 的说明书，agent 新建项目、拿钥匙、当第一个节点、把牌桌地址回给人；更多 agent 靠一个邀请链接加入，服务按缺的角色分配。人不改文件，不见钥匙。地址即工具包，纯 HTTP 可完成，CLI 只是省事。

**第二个入口：把现成的团队搬过来。** 用户给其中一个 agent 同一个地址，说「把我们的协作搬到这上面」。那个 agent 按说明书的「搬家」一节：新建项目，用自己原来的角色名当第一个节点；只搬在途的东西，每一条指回旧位置，id 不重排；旧数字进来就标过期；然后**通过旧渠道**把邀请链接发给同伴，同伴各自在下一轮读到、加入。全部角色到齐时旧渠道变只读。人看到的是自己的牌桌上出现了自己的在途工作，加一张卡「搬过来的是这些，对吗？」。

**接口只有三样输入和四个动作。** 输入：地址、钥匙、角色名。动作：拉（sync，长轮询）、确认（ack）、说（tell / note / reading）、任务（claim / done / verify）。走 `npx ateam` 或直接 HTTP，任何能发请求的 agent 都能当节点。

**别人用它，只看三样东西。** 一页「怎么用」、一条命令、一张牌桌。他不需要见到这个仓库的任何源码。托管服务多项目隔离；自托管一条命令。

## 目标

可衡量，按季度看趋势：

- 人**只用牌桌**就能完整参与：说一句、拍板、看证据都在牌桌上，不需要进任何 session。
- 人每天花在这个产品上的时间 **少于 15 分钟**，其余时间团队自转。
- 牌桌上"需要你"的误报 **为零**。
- 需求变化到全队对齐 **少于 5 分钟**；由过期事实导致的返工 **为零**。
- 每次上线 **可追溯**：从线上的版本回到需求、决策和验证记录。
- 起一个新项目 human 的输入 **不超过一个地址、一句话、转发一个链接**，**少于 15 分钟**；没读过源码的人 **一小时内**跑起自己的团队。
- 任何平台的 agent 只凭地址，**十分钟内**做出第一个正确动作；被拒绝的请求都带规则名；一周内 friction note 趋于零。
- 全队一起死时，人 **一小时内**知道。
- 平台的每个场景在 **一个不是 aTeam 自己的项目** 上成立过，才算成立。

## 非目标

- 让 agent 用得顺手本身不是目标，是手段。工具的 friction 要修，但排在场景之后。
- 不做通用 workflow 编排。
- 不替代 GitHub、issue tracker 或聊天工具。
- 不做 agent 市场，不做多组织。多项目隔离要做，组织与权限体系不做。
- design-v0.1 里有而现在不做的：room 与 topic 订阅、Context Package 裁剪、observation→confirmed 状态阶梯、GitHub App、Slack、token 预算、digest。一天 55 条事件，每个成员读全量即可；有真实压力再加。外部输入的信任分级与 prompt injection 隔离（§7.5、§12）在有外部 connector 之前不做。

## 产品语言

界面、日志、文档都用这套词，中文优先：

| 词 | 指什么 | 不叫 |
|---|---|---|
| 牌桌 | 人看的那一页 | board、dashboard |
| 需要你 | 牌桌上等人拍板或动手的事 | needs human、inbox |
| 焦点 | 现在最要紧的一件事 | priority、P0 |
| 事实 | 某时刻对世界的一次测量，带来源和有效期 | reading、观测、baseline |
| 指令 | 要某个人现在做的事，必须确认 | 广播、消息 |
| 任务 | 一件有验收标准的工作 | ticket、issue |
| 接缝 | 两件工作碰到同一处 | conflict、冲突 |
| 表面 | 在哪里验证的，由项目自己定义；软件项目默认仓库、staging、生产 | environment |
| 项目 | 一张牌桌、一条日志、一把钥匙 | workspace、tenant |
| 角色 | 持久身份：pd、pm、dev、frontend、qa | agent、bot |
| 节点 | 此刻持有某个角色的一个 session，跑在哪都行 | worker、instance |
| 钥匙 | 让节点或人以这个项目的身份说话的凭证 | token、secret |
| 说明书 | 节点加入时从服务拿到的、按角色写的做事规则 | system prompt、CLAUDE.md |
| 工具包 | 让任何平台变成节点的那一条命令和几行存根 | SDK、integration |
| 在听 / 没在听 / 缺人 | 一个角色的节点最近拉过日志 / 说过话但没拉 / 根本没有节点 | 在线、离线、失联、alive |

## 相关文档

- 用例全文（两族）：`docs/usecases.md`
- 职责与系统要求（低一层：每项工作职责对 owner、持有者、协作方、通讯与牌桌的要求；角色 = 职责的打包；说明书按职责拼装）：`docs/responsibilities.md`
- 牌桌的视觉与信息结构：`docs/board.md`
- 本项目档案：`docs/self.md`

## 这份文档怎么用

- **pd** 维护它，而且慢：pd 的工作是想清楚软件和用户的关系，不是跟上团队的节奏。新的场景、目标变化、产品语言的增补，先 `note --decision`，再改这里。pd 看着跑起来的产品，说出哪个场景其实没被服务到。
- **pm** 定焦点和优先级，建任务时写明场景 id 和需求 id；写不出的标 `tooling`。向 human 汇报的是场景完成度，不是任务数。
- **frontend** 和 **dev** 做人能看到的东西时，先读对应场景的"完成的标志"，再读判据；做团队侧的东西时，先读对应 A 用例和它的 T 需求。
- **qa** 验证人能看到的交付时，除了判据，还对照场景的完成标志；验团队侧的交付时，对照 A 用例的完成标志与 replay 断言。
